# 现在

这份文件只讲**现在**：什么状态、我接下来做什么、什么在等你。
只有这份文件可以声明事项的**当前状态**；其他文件分别记录长期原则、你的决定和已经发生的事。

最近发生了什么 → 《人话进度报告》。技术细节 → `WORK_LOG.md`。
最后更新：2026-07-31

---

## ⏳ 等你决定的（0 件）

目前没有必须立即由你拍板、否则系统就无法继续的事项。个人买卖提醒可以长期保持未配置，不影响市场分析。

（2026-07-31 你拍板两件：证据菜单走 B——只改登记表里写错的键名，不把下载链接这类元数据摆上菜单，理由是 token 不该浪费在无用上下文上；以及确立"**结构用来定位，不许用来拒收**"为常驻边界，配套"轨道 > 导出 > 规矩"三层判据，已写进 `CLAUDE.md` 与 `ARCHITECTURE.md` 3.1b/3.1c。）

（"判意思的规则该往哪放"这条已于 2026-07-30 拍板：三条"必须怎么说"的闸门直接删除，保证移交报告渲染；剩下的禁止型规则保留，因为代码替代不了、且实测零成本。）

---

## ▶️ 我接下来要做的

**下一件：T20（证据菜单里写错的键名）。** 按你拍板的 B：只把六个数据源登记表里写错的键名改成真实字段名，菜单保持精选，不把下载链接、源文件名这类东西摆上去。不花钱。

做完之后是**卷宗审计报告**：出一份"从来没被任何一站引用过的证据条目"清单，**带多空分布**（只出报告不动刀）。这是那 88.6% 输入花销的第一步，也是你说的"以后可能要大幅精简"的可测量入口。之所以必须带多空分布：按引用率剪枝会系统性砍掉唱空的材料——反方证据的价值在于"被看见并被驳回"，不在于被引用，而菜单不许先天偏科是常驻边界。

**刚做完的三件**：① 编号改选单 + 容器可空，已通过真实跑验收（`20260731_002156`），分辨实验结论是"真货"；② 证据编号的合法性判据已按你批准的方案拆开，合法编号 104 → 459 条、零丢失；③ 事件总结那一站现在**永不因形式失败**——编号改由代码从正文标记里扫，解析不了就把正文捞出来照常渲染并留降级痕迹，同时把被误摘掉的语法锁装了回去。细节都在 `WORK_LOG.md` 2026-07-31。

你不同意随时喊停，不用等我来问。

---

## 📋 全部未完成（6 件）

| 编号 | 一句话 | 状态 | 怎么算做完 | 细节在哪 |
|---|---|---|---|---|
| T20 | 证据编号错位：登记表键名 ≠ 真实字段名（T35 修完后剩下的一半） | 可以做 | T35 已把 **bridge 段**修好（合法性改判真实字段名）。但 thesis / final / reviser 三站另有一道闸门，白名单来自 `synthesis_packet.evidence_index`，其 `#field` 条目仍只由登记表构造（`orchestrator.py:4415-4418`），模型在这三站写真实字段名仍被拒。**两条路二选一**：(a) 把 `evidence_index` 的 `#field` 也按同一并集逻辑扩——一致，但那份索引是**给模型看的证据菜单**，实测会从 104 条涨到 459 条且混入 `#download_url` 这类元数据键，等于邀请模型引用它们；(b) 把登记表键名改成真实字段名——菜单保持精选，改动小，但触及数据层且需按新指标治理清单同步登记。**你已拍板 (b)**（理由：不该把无用上下文塞进菜单浪费 token）。实测错位清单（跨 L1/L2/L4，非仅 L4）：`get_fed_funds_rate_path`(3)、`get_vix_term_structure`(1)、`get_crowdedness_dashboard`(1)、`get_m7_capex_cycle`(2)、`get_m7_earnings_blackout_calendar`(1)、`get_m7_buyback_flow`(2) | `src/agent_analysis/orchestrator.py:4415`；`src/tools_L1.py` / `tools_L2.py` / `tools_L4.py` 的 metric_authority 构造 |
| T16 | Forward PE 主值/分位用哪个源（悬而未决） | 后排 | 定下 forward PE 的主值与百分位来源：确认是否把 Trendonify 从审计附件正式升为主源（含取数是否改走直连接口而非整只浏览器、与 HoM 序列做分位交叉验证、Cloudflare 可用性降级、正式治理登记），或维持仅审计；未决前 forward 水平以全成分自算为可复算锚 | `investigation_reports/20260723_l4_earnings_audit/DECISION.md` |
| T04 | 可选的个人买卖提醒阈值 | 后排 | 只有你明确想启用自动个人提醒时才配置；每条阈值带 `threshold_status=confirmed` 和正确单位，不配置也不影响市场分析 | `investigation_reports/20260718_first_principles_debate/WORK_ORDERS.md` W3 |
| T06 | 最重要 10 个指标的透明化审计 | 可以做 | 10 个核心指标说清数据源、单位、算法和局限；复杂计算抽 3 项独立复核；系统自定规则明确标为待真实成绩验证 | `investigation_reports/20260720_report_quality/WORK_ORDERS.md` Q2 |
| T09 | 第一批判断的真实成绩单与复盘 | 等条件 | 至少一个 run 拿到完整 T+20 交易日价格窗口后被真实打分，再走完"错在哪一类 → 人工裁决 → 改了哪个方法"登记一轮 | `RUN_REVIEW_CHECKLIST.md` 评分复盘流程 |
| T10 | 五处真实运行抽样验收 | 等条件 | 下次完整正式跑一遍时，长期评估、事件总结、利率数据与期货持仓接口各留下一次真实成功或如实“取不到”的记录；补采清单要么没有低质量占位，要么可执行项多于占位项 | `investigation_reports/20260718_first_principles_debate/WORK_ORDERS.md` W3/W4/W7；`investigation_reports/20260720_report_quality/WORK_ORDERS.md` Q3/Q6 |

---

## 🩺 系统现状

- **自动检测 1047 项全绿**（另有 58 条既有第三方弃用/精度提示，非新问题）。
- **发布闸门正常**：产物被判"不可发布"时不得当结论用；同一次运行的数据与新闻必须同一天，对不上自动降级。
- **数据通道**：SEC 官方通道已恢复，七巨头资本开支/回购走官方源（回购 1 家缺字段，如实留空）。Wind 需本机终端登录，未登录时估值指标会缺。Invesco 持仓接口经 Clash 分流修复（仅 Invesco 域名走 YKK 节点，2026-07-23 实测 200、108 条持仓）；若代理规则变动会回落到静态兜底并留痕。
- **HoM 第三方估值源**：接口实连可用，但自报覆盖率超过 100%；forward PE 的月度序列停更在 2026-05-18（trailing 仍每日更新，2026-07-24 实测尾部到当日）。新门槛已把 trailing 和 forward 一起降为仅审计参考，不会把 5 月的陈值当"当前"发布。它始终只是"第三方自述归因、未独立核验"。**forward PE 主源因此有真实缺口——接替方案见 T16。**
- **校准闭环**：判断打分器已就位。7 月 27 日只是满 20 个自然日的入场检查点；完整成绩要等 T+20 个交易日价格齐全，预计在 8 月初。

---

## 🚫 明确不做的事

写在这里是为了让新对话不要重新提议，不是待办。

- **Wind 美股一致预期**：已实测判死（有港A股权限、无美股权限），别再试。
- **前 15 权重股自算 forward PE 水平**：指数点位不能直接除以少数成分股每股盈利。原重估条件"HoM 断源"已于 2026-07 触发，重估结论（`investigation_reports/20260723_l4_earnings_audit/DECISION.md`）：改做**全成分**正式链（T14），前 15 算水平仍然禁止；前 15 官方权重只允许当修正**动量**代理（带标签，T15 范围）。
- **Finviz 校验源**：不接。无分位、无 vintage、无上修/下修家数，覆盖不了真实缺口，边际价值低于维护成本（2026-07-23 终审裁决）。
- **FMP/Finnhub 历史一致预期**：在不做前 15 自算之后，追多年期免费一致预期的性价比不足。要做深回测时再单独立项。
- **浏览器采集（Trendonify）升级为正式源**：当前只作审计附件，不得进入五层主证据链。（forward PE 场景下是否破例升为主源，正在 T16 复议；未决前边界照旧。）
- **正式前端框架**：报告与控制台的信息架构稳定前，继续用单文件 HTML。
- **泛新闻情绪回流数据链**：只做官方事件底账与时间邻近连接。
- **交易执行、组合建议、自动下单**：不属于本系统范围。
- **让第三层接管报告首页**：它的价值是解释数据与事件如何对质，不是暗中改写第一层判断；继续保持第一层坐首页。
- **把"纳指集中度书面论证"当成必交作业**：若未来真要改变个人政策上限，按政策书正常复审；不再作为系统待办催促。
- **单独拆分巨型代码文件**：没有用户可见收益且回归风险高；以后有功能必须触及相关区域时就地拆。
- **在干净历史不足时建"历史上像今天"引擎**：继续冻结；不把一个已经决定不做的东西继续算作未完成。
- **OpenBB 平台接入**：只吸收数据治理思路，不接平台。
- **把其余历史 `docs/` 再做一轮大扫除**：不单独立项。它们不在当前阅读路线里；以后若发现活入口误指或审计证据打不开，就在相关任务中就地修复。
