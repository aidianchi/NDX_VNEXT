# 项目总纲

## 北极星

`ndx_vnext` 的目的：**做出可靠 NDX 判断，覆盖短、中、长期，帮所有者做出比"长期持有"更好的买卖决策**。可审计推理链是手段不是目的。三根支柱：**事实扎实**（数字可独立重算、来源可追溯）、**证据面均衡**（多空两侧同一份菜单，不得先天偏科）、**校准闭环后置**（判断可评分、被事后评分、评分修正方法）。

每个重要结论须能答六问：判断对象 → 哪些证据有发言权 → 哪些确认 → 哪些反驳 → 何时改判 → 事后对错与方法修正。

核心架构原则：**Context-first, role-second**——Agent 拆分的理由是上下文隔离和认知变换边界，不是模仿人类职位。

## 沟通方式（用户没有软件工程背景）

本节只有一条根本目的：**让老板看懂**。其余全是手段。

先判这是什么场合再张嘴。随口一问就聊天答；要拍板就给选项 + 代价 + 建议，别替老板做决定；探讨就亮思路骨架自由展开。

**看懂的本质是上下文，不是名词。** 任何新概念进对话，必须带前因后果一起来——它从哪来、解决什么问题、在这件事里扮演什么角色。孤立的名词都看不懂：光给黑话是黑话，光给外号是暗号，黑话和外号必须当面对应（"经费卡 = 每次调研的花费上限，代码里叫 budget_cap"）。

比喻是主力工具：从生活里取材，把陌生概念挂到熟悉的因果链上；用完要能走回数字和文件名，别跑野了回不来。一条消息里新概念别堆超过三个，多了就拆。

**写给老板的文字，唯一基准是他此刻脑子里有什么**——他不在你的对话时间线里，几天没跟，前面说过的话对他等于没说过。所以前因后果不只带给"新概念"：每个代号（任务编号、子项号、宪法、原则）每次出现在文里都要自带来历（它是什么、从哪来、解决什么问题），哪怕本对话讲过。写完自检一遍：假想一个几天没跟、手里只有这份文件的人从头读，哪里断了就重写哪里。

结构按金字塔原理：结论先行（塔尖），往下每一层都支撑上一层（塔身），同层不重复不遗漏。先答问题，再讲理由；理由要落到具体的人、事、数字，别只堆论断。

技术细节不堆在对话里，但提到的概念必须能对应到真名（文件名、字段名）；核查线索写进 `WORK_LOG.md` / 工单，核实者自己去文档里翻。

## 常驻边界

不可被普通待办覆盖，除非用户明确要求改变系统原则。

- **层间隔离**：L1-L5 运行时上下文不得互通；各层只知静态五层本体、投资对象定义和本层指标法典。
- **主链纯度**：主要分析必须来自 vNext artifacts；legacy adapter 只做兼容导出和兜底映射。
- **冲突是资产**：不得为报告顺滑抹平跨层冲突、反证和未解决张力。
- **指标不得越权**：代理指标不冒充官方事实；技术指标不证明估值便宜；复合指标子项不绕过总分语义升格。
- **弱来源不进主链**：新闻、浏览器采集、登录态工具、sidecar 结果默认只是隔离观察；未正式升级不得成为 `evidence_ref`。
- **时点纪律**：回测与快照尊重有效日期；晚于回测日 / `effective_date` 的材料不得进上下文；缺口写入数据边界，不得用当前网页伪装历史。
- **不得编造**：胜率、回测收益、样本期、概率、点位阈值——evidence refs 没给的，只能表达为待验证假设或风险观察。
- **形式不得拒收内容**：结构用来定位，不许用来拒收——宁要没索引的真思考，不要空白。但收下 ≠ 认可：缺索引的产出接进发布闸门标"不可作发布依据"，不得静默通过。
- **机械字段不出答卷**：凡代码能确定性生成或回填的字段（身份名、枚举值、编号、时间戳、引用键），一律由代码装配，不让模型填；模型只回答需要判断的问题。闸门只守代码装配不到的地方。
- **闸门六原则**（2026-08-31 老板批准入宪；施行细则与出处见 `investigation_reports/20260828_run_performance/WORK_ORDERS.md` T69 节）：①架构一致——代码与文档描述的架构必须一致，架构错了局部修补无效；②注意力优先——每次 AI 调用按充分必要上下文优雅投喂，输出只干本职深度分析；③不埋没人才——不让分析师干脏活（背字段名、复读固定句、填编号、凑字数、管标点）；④仆从——代码为 Agent 服务（递材料、簿记、排版），材料组装对了闸门自然失业；⑤报刊——文章让专家写，代码只排版装配；⑥第一性判定——限制的存废只问"不这样，深度分析还能被信任地交付吗"，能答"能"的默认可删。
- **发布闸门**：标记 blocked / unpublishable 的产物不得当可发布结论。
- **历史材料不是现状**：`docs/archive/**` 与带日期的旧审计文件只说明"当时怎么判断"。
- **年度复盘**：每年复盘本清单——能被机器闸门完全接管的条目，删字留闸门，防只增不减。

## 文档路由

按任务读最小必要文档。

| 你要找什么 | 去哪 |
|---|---|
| 现在什么状态、下一件、等用户什么 | `现在.md` —— **唯一可声明事项状态的文件** |
| 用户否决过什么、在乎什么 | `人话进度报告.md` —— 涉及方向与取舍时**必读** |
| 已完成的技术记录 | `WORK_LOG.md`（最近三个月；更早在 `docs/archive/`） |
| 某件事的调查细节、验收口径 | `investigation_reports/*/WORK_ORDERS.md` |
| 系统全景、逐站点说明、长期原则 | `系统说明书.md` —— **先读它建立全局**，再按需读其他 |
| 指标判读、跨层推理标准 | `RESEARCH_CANON.md`（含数据源分工与使用边界） |
| 跑后复盘、回测专项检查 | `体检方法.md` 三·五节 |
| 回测第一性原理 | `回测原则.md` |
| 怎么成批发现系统的毛病（体检方法论） | `体检方法.md` |
| 历史材料（不是现状） | `docs/archive/INDEX.md` |
| 多 agent 委派规则（explorer/worker/reviewer 分工与成本控制） | `.codex/agents/README.md` |

冲突时：状态以 `现在.md` 为准；系统全景与长期原则以 `系统说明书.md` 为准；完成记录以 `WORK_LOG.md` 为准；本文件常驻边界优先于全部。

## 执行纪律

- 先判断、再最小修改、后验证。不确定先说清假设与取舍；假设有明显风险先问用户。多路可行选最简单、最贴近现有架构的。
- 改动外科手术式收敛：只改必需位置，不顺手重构；本次制造的残留同步清理，既有无关死代码不动。
- 每个非平凡任务要有可验证的成功标准（修 bug 能复现并确认消失；改合约有测试；改 UI 能检查页面）。
- **约束住哪：轨道 > 导出 > 规矩**——schema 能锁的进 schema；代码能导出的别让模型填；剩下的才进提示词，且只约束"必须交代什么"。依据与实测见 `系统说明书.md` 4.1。
- **文档维护纪律**：`系统说明书.md`、`现在.md`、`RESEARCH_CANON.md`、`体检方法.md` 等被本路由表索引的文档，凡所描述的系统行为发生重大变化（新增站点、改隔离边界、换数据主锚、动发布闸门），必须同一批次更新对应文档；不更新视为未完成。此纪律由所有者 2026-08-08 明确要求。
- **闸门不判意思**：校验器只判形状与身份。凡要比对两段模型散文才能判定的规则都是设计错误，正解是插编号变回身份比对。
- **状态只写一处**：当前状态只在 `现在.md`。收工同一提交内：`现在.md` 删行 + `WORK_LOG.md` 写 `【关闭 T##】` + 工单追加关单记录。编号永不复用。里程碑另加一封信。
- **信必须够格**：每封至少含"否决/未做及理由"或"等用户决定"之一；纯翻译技术变更的信应删。
- **WORK_LOG 写决策不写过程**：改了什么、为什么、红灯测试在哪、否决了什么。
- **模型失败先怪结构**：先怀疑结构与上下文冗余，不怀疑模型能力；flash 一次跑通全链是架构合格的实测标准（2026-08-17 所有者裁决。配套口径：日常真实跑启动名单只挂 flash；同一处 flash 错两次则当次 run 中止、留精确诊断、先修结构再断点续跑；pro 保留在代码与配置里，仅在老板明确批准的例外场合启用）。
- 跑 `pytest tests/test_docs_consistency.py` 自查文档纪律，不靠记性。

## 常见误判（都不是 bug，别修）

- 通则：标 unavailable / 太年轻 / blocked 的，先查是不是设计如此，别当 bug 修。带日期的具体病例移存在 `体检方法.md` 第七节。


## 验证原则

按改动范围选，交付前说明验证结果；未验证的说明原因与剩余风险。

- 代码合约与核心逻辑：相关单测，必要时全量（`.venv/bin/python -m pytest --cache-clear -q`）。
- 数据 / 回测 / 快照：DataIntegrity、`run_summary.json`、`backtest_data_boundaries`、数据日期与发布状态。
- vNext artifact / 报告：最新合适 run 生成 native `brief`，查 evidence refs、冲突保留、审计区。
- workbench / console / UI：生成目标页面，桌面与移动布局检查。
