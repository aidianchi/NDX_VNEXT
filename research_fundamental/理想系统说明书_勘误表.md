# 《理想系统说明书》勘误表

> 核验员：研究战役核验员。核验日期：2026-09-20。
> 核验对象：`research_fundamental/理想系统说明书.md`（五篇，约 7900 字）。
> 核验方法：把全文拆成 75 条事实性论断（数字、文件名与行号引用、日期、机制描述、历史案例数据、"实测证明"类主张），逐条回到一手来源对账——源码类打开对应文件核对，数字类回到 `research_fundamental/` 下证据文件核对，答卷类回到 `answers/` 答卷核对。遵守 BRIEF.md 铁律：未读 `investigation_reports/`，未改任何代码。

**总结：共核 75 条事实性论断——准确 70 条，有偏差 4 条，查无实据 1 条。**

---

## 一、查无实据（1 条）

| # | 说明书原文（逐字） | 位置 | 核验结果 | 正确表述与依据 |
|---|---|---|---|---|
| W1 | "反向回路把事件层的题变成数据层的家庭作业：事件层在新闻主线发现'这事数据层该回答'的问题（由 `event_narrative_ledger.py` 的纯代码出题工厂产出，带 `event_to_data` 标记），**经问询路由器（`inquiry_router.py`）路由成数据层的受控调查任务书**，沿用既有纪律——调查报告不得回写五张层卡。" | 第三篇"双向提问回路"段 | 出题工厂与 `event_to_data` 标记真实存在（`src/event_narrative_ledger.py:1108-1110`），但"经问询路由器路由成数据层任务书"这一截在源码中不存在：`event_to_data` 只出现在生产者 `event_narrative_ledger.py` 与渲染器 `vnext_reporter.py` 两个文件里，没有任何代码把这类题接进 `inquiry_router.py`（该路由器现只服务编排器的 feedback_inquiries，`orchestrator.py:704-705`）。蓝图自己写明这是半成品：`ideal_blueprint.md` 2.3 节"反向（事件层 → 数据层）是半成品……'事件层的题进入数据层的后续采集与分析议程'这一截不存在……（这是按现成挂点推导的规格决定，不是已建成事实）"。说明书第五篇末段虽有"以验收卡二逐站实测为准"的总交代，但第三篇正文以既成事实口吻陈述。 | 正确表述：反向回路中"出题工厂产出带 event_to_data 标记的题"已建成；"经问询路由器路由成数据层家庭作业"这一段是蓝图规格、尚未接线。依据：`ideal_blueprint.md` 2.3 节诚实声明；全仓 `event_to_data` 检索仅命中两个文件。 |

## 二、有偏差（4 条）

| # | 说明书原文（逐字） | 位置 | 核验结果 | 正确表述与依据 |
|---|---|---|---|---|
| D1 | "重算带（`recompute_belt.py` 的 **17 个函数**用原始序列独立重算分位与比率并与管道比对……）" | 第四篇"闸门只留四种"段 | `src/recompute_belt.py` 实际有 **16 个** `check_*` 重算检查函数（`run()` 注册表逐一点名，文件 1789-1817 行），外加 `run` 与 `critical_deviation_findings` 两个入口函数；全文件含私有助手共 46 个 def。"17"在任何口径下都对不上。 | 正确表述：16 个重算检查函数。依据：`src/recompute_belt.py` 函数清单与 `run()` 注册表（1789-1817 行）。 |
| D2 | "五层之间唯一的汇合通道是跨层桥接岗位：**它只读五张层卡的结论字段和候选跨层关系清单，不读指标级逐字叙事。**" | 第三篇"数据层：五层互相隔离"段 | 这句是蓝图规格，不是现役实况。Q6 实测：bridge 的 payload 是五张层卡**整包**约 13.8 万字符，含每个指标的逐字叙事与推理过程全文，裁决为"喂多了"（`research_fundamental/Q6/context_audit.md:131-133`）；蓝图 2.1 把"只读结论字段"列为理想菜单并注明这是现状教训。说明书第五篇有"按建成后的状态陈述系统"的总交代，但此句描述的机制方向与实测相反，属于该交代表盖不住的实点。 | 正确表述：现役 bridge 实测拿到层卡整包（含指标级逐字叙事），"只读结论字段"是 `ideal_blueprint.md` 2.1 的理想菜单。依据：`Q6/context_audit.md:131-133`。 |
| D3 | "打分器积累至今的分数分布没有人统计过——不知道，验证方法是读 `output/state_ledger/claim_outcome_ledger.jsonl` 里**已落的分数记录**做分布统计。" | 第五篇"局限与诚实声明"其三 | 该路径确实是代码登记的落盘位置（`src/agent_analysis/outcome_scoring_runner.py:52` `DEFAULT_LEDGER_PATH`），但核验日盘上不存在此文件：`output/state_ledger/` 只有 `event_agenda.jsonl`、`method_revision_ledger.jsonl`、`state_ledger.jsonl`、`term_candidates.jsonl` 四个文件。"已落的分数记录"目前无从读起。 | 正确表述：落盘路径真实登记在代码里，但截至核验日该文件尚未生成，即没有已落盘的分数记录可统计。依据：`outcome_scoring_runner.py:52`；`ls output/state_ledger/` 实测。 |
| D4 | "所以每个层站最多配四条范例**且经选型审查**（`few_shot.py`）" | 第四篇"意思层的道住在哪里"段 | "最多四条"真实：`src/agent_analysis/few_shot.py:65` `max_total_examples: int = 4`。"经选型审查"是流程纪律，出自 `answers/Q6.md:29`（"必须经选型审查"），`few_shot.py` 全文没有审查机制。引注只撑得住半句。 | 正确表述：四条上限见 `few_shot.py:65`；选型审查纪律见 `answers/Q6.md:29`，不是代码机制。 |

## 三、准确条目清单（70 条，不展开）

**第一篇（哲学映射指向的机制，全部真实存在）**
1. 数据层真名 `src/tools_L1.py` 至 `src/tools_L5.py` 五个采集层。
2. 事件层真名 `src/event_research/`。
3. 第三层综合裁决（IA）真名 `src/integrated_synthesis_report.py`（用 `IntegratedAdjudication` 契约与 `integrated_adjudicator.md` 提示词）。
4. 断言台账真名 `final_claim_ledger.json`（`src/state_ledger.py:163`；各 run 目录实测存在）。
5. 反方假说岗位真名 `prompts/counter_thesis.md`（`src/agent_analysis/prompts/` 下存在）。
6. 反方与正方互相看不见对方产物（`orchestrator.py:3309-3340` 禁读审计 + 7842 行 drop 字段）。
7. 冲突矩阵真名 `conflict_matrix`（`contracts.py`、`integrated_synthesis_report.py` 均用此名）。
8. 研报三层楼形制规格真名 `ideal_blueprint.md` 第一节（"一、形制层"）。

**第二篇（金融原理与历史数字）**
9. 截至 2025 年底前十大成分股权重占 52%，引 `RESEARCH_CANON.md:259`——逐字相符（"截至 2025-12-31，纳指100前十大证券占比为 52%"）。
10. 七类驱动力推导与"五层不重不漏"结论见 `answers/Q4.md`（第 7、17 行）。
11. 证据分七档、真名 `src/data_evidence.py` 的 `source_tier`（262 行起，official→unknown 恰七档）。
12. "波动率期限结构平静时不得引用为看多证据"引 `src/tools_L2.py:575-580`——该区间正是 contango `not_bullish_evidence` 条款。
13. NDX 2000 年顶后跌 83%（`QH/evidence_boundary.md:16`，83.0%）。
14. 含分红再投资 14 年 11 个月回本（同上行，逐字相符）。
15. 2007 年顶后跌约 53%（同上）。
16. 2021 年顶后跌 35.1%（同上，及第 89 行）。
17. 三个顶一个坑的取证与出处标注（`evidence_boundary.md` 第二节）。
18. 信用利差触底回升是四次案例唯一全勤信号、领先 9 天到 4 个月（`evidence_boundary.md:111,149`，9 天到 120 天）。
19. 2000 年估值极端持续一年多（`evidence_boundary.md:59`）。
20. 2007 年估值历史正常区间、全程沉默（`evidence_boundary.md:69,74`）。
21. 2020 年高收益债利差约 1087 个基点（`evidence_boundary.md:82`，03-23 约 1087bp）。
22. 黄金坑要件修订为"外生一次性或政策兜底"（`evidence_boundary.md:85`）。
23. 字段级牌照与越权拦截机制（`data_evidence.py` license_note / `_license_note`）。

**第三篇（架构与事件层机制）**
24. 五个层分析师互相看不见对方材料、只喂本层（`Q6/context_audit.md:127` 实测"物理上不存在他层干扰项"）。
25. "像答案的错材料比完全无关的材料更能把 AI 带偏"，依据 `answers/Q6.md:9`（C3 实测）。
26. DSH = DeepSeek Harness 自造框架（`event_research/__init__.py:4`）。
27. 经费卡卡面默认 3000 万 token（`src/event_research/budget.py:4`，老板 2026-08-24 拍板）。
28. 日常巡逻档 300 万（`src/event_research/sync_patrol.py:59-61`，`SYNC_PATROL_BUDGET_CAP = 3_000_000`，老板 2026-08-25）。
29. 经费卡真名 `budget.json`（`hooks/budget_gate.py:8`）。
30. `hooks/budget_gate.py` 每次模型请求前查账、超额请求发不出去（文件 docstring 第 2-6 行逐字支撑）。
31. `hooks/fetch_gate.py` 域名白名单外直接拒抓（27-28 行）。
32. 对账器 `reconcile.py` 逐条核对、引文对不上降级（8-14 行）。
33. 相似度阈值 0.8 是老板 2026-08-26 拍板（`reconcile.py:84-86` 逐字相符）。
34. 红线"永远不进数据主链"写在 `src/event_research/__init__.py`（第 6 行逐字相符）。
35. 抗诉红灯为常设检查 PC-28、真名 `persistent_checks_b.py`（1184-1257 行，抗诉三条件；`vnext_reporter.py:2605-2642` 横幅置顶渲染，"报告第一屏"成立）。
36. 常设检查 28 项（A 包 PC-01~10 注册 10 项 + B 包注册 18 项 = 28 项在跑；PC-27 已退役、编号永不复用，`persistent_checks_b.py:1184-1188`）。
37. 出题官 `topic_composer.py` 读六站对抗残局（桥接未解问题+跨层开放题+终审主要矛盾+家里能力清单）、写 0-2 份任务书（`topic_composer.py:2,13-16`）。
38. 缺口桥 `gap_bridge.py` 把任务书转成候选议程（docstring 逐句相符）。
39. 议程账本 `event_agenda.jsonl` 只追加不改写、编号永不复用、老板关闭的题不复活（`agenda.py:4-6`、`gap_bridge.py` docstring）。
40. 事件层出题工厂产 `event_to_data` 标记问题（`event_narrative_ledger.py:1108-1110`）。
41. 问询路由器 `inquiry_router.py` 真实存在且能把题路由成受控调查任务书（`orchestrator.py:704-705`）——存在性无误，偏差仅在反向接线状态（见 W1）。

**第四篇（代码与 AI 分工）**
42. 需要 AI 出判断的岗位共十四类（`ideal_blueprint.md` 2.1 菜单表恰 14 行，与说明书列表逐一对上）。
43. 逐站菜单表真名 `ideal_blueprint.md` 2.1 节。
44. 反方假说岗位曾被灌 32.4 万字符、其中约 15.4 万是证据审计底账（`Q6/context_audit.md:143`：总提示词 323,892 字符、evidence_index 约 15.4 万；`answers/Q6.md:36`）。
45. "AI 读长材料丢中段、材料变长伤性能"两条实测特性（`answers/Q6.md:7`；`context_audit.md` C1/C2）。
46. 受控调查材料有 1.2 万字符代码硬顶且按相关性筛过（`orchestrator.py:2633-2687`，12000 字符硬顶；`answers/Q6.md:36`）。
47. 机械字段不出答卷、模型报序号代码回填编号、"抄写笔误物理不可能"（`orchestrator.py:186,852,4988`，原话逐字在）。
48. 表单剪枝登记真名 `llm_engine.py`（223-257 行 prune 机制）。
49. 引用白名单"许可集合 ⊆ 实发集合"恒真（`integrated_synthesis_report.py:360-366`，注释逐字支撑）。
50. 形状闸契约校验 `contracts.py`（2807 行，覆盖各站输出形状）。
51. 判决数字须在代码预生成数字菜单内（终审数字逐字核对硬闸门，`orchestrator.py:7034-7046`，经 `context_audit.md:185` 转引核对）。
52. 时点闸：证据发布日期不得晚于判断日期（`data_evidence.py:644` `data_date_after_effective_date` 硬阻断）。
53. 资格闸四类硬阻断（`data_evidence.py:637-653`，hard_block 恰四种）。
54. "示例只教腔调不教内容、示例里出现一个立场输出就染一分"（`answers/Q6.md:10` 逐字相符）。
55. 范例库亲自示范"引力拉满""空心胜利"且与研报缺陷逐字同构，验尸记录真名 `research_fundamental/Q8_whitelist/content_autopsy.md`（84、96-97、198 行）。
56. 卡二"六个核对点都不需要读懂内容"（`ideal_blueprint.md` 5.2 节，恰六点）。
57. 卡三闸门五问（`ideal_blueprint.md` 5.3 节，五问逐条在）。
58. 提示词逐字落盘目录真名 `prompt_audit`（`orchestrator.py:660`）。

**第五篇（工程纪律）**
59. 价格层快照哈希指纹真名 `tools_L5.py` 的 `ohlcv_sha256` 字段（131、235 行）。
60. `tools_L3.py` 幸存者防线：拿不到历史成分股名单硬失败、绝不落回当前名单（21、27、48 行逐字支撑）。
61. 只在实时可用的数据源进回测直接拒答（`data_evidence.py:646` `latest_only_source_used_in_backtest` 硬阻断）。
62. 数据可用性登记处真名 `data_availability.py`（文件存在，登记不可用状态与边界）。
63. 方法修正台账真名 `method_revision_ledger.jsonl`，每条要求老板批准、挂真实提交号、带复查日期（`state_ledger.py:240-300`，`review_after` 必填、`change_ref` 必须 `git cat-file -e` 通过）。
64. 查提交号真伪的闸门在 `state_ledger.py`，且全仓唯一（全仓仅此一处 `git cat-file`）。
65. 打分器满 20 个自然日才打分（`outcome_scoring_runner.py:57` `MIN_SCORABLE_AGE_DAYS = 20`）。
66. 三个时间窗口 T+20/60/120（`outcome_review.py:25-27`）；三档"兑现、打脸、没法判"对应代码 verdict `consistent/falsifier_triggered/not_scorable`（317-324 行），属人话翻译，语义相符。
67. 打分器靠关键词判方向、含"进攻、买入"判看多（`outcome_review.py:218,230`）。
68. 坦白句"本分数优先作为数据问题探测器使用，不代表对判断质量的最终结论"逐字在（`outcome_scoring_runner.py:60-61`）。
69. 校准闭环第三段按老板 2026-09-20 裁决整体延后（`answers/QF.md:3` 逐字相符）；"分数进提示词扩大时点泄露面"的理由与 `QF.md:24` 一致。
70. 三处已知缺口（FRED 的 ICE/BofA 利差自 2026 年 4 月起只保留三年观测、拥挤度无分位刻度、广度四指标共用单一数据源）与"机制只增不减、年检对抗"，全部见于 `QH/evidence_boundary.md:126,132-138` 与 `answers/Q7.md:37`、`ideal_blueprint.md:122`；反向回路诚实声明引 `ideal_blueprint.md` 2.3 节属实（该节确有此声明）。

---

*文件路径：`research_fundamental/理想系统说明书_勘误表.md`。核验遵守 BRIEF.md 三条铁律：系统行为论断只信源码并给出 `文件:行号`；未读 `investigation_reports/`；未改任何代码。*
