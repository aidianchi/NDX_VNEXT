# T47：上下文根本性审查

> 这份文档是 T47 的**唯一权威表述**。`现在.md` 的 T47 行只存状态，意义与做法以本文为准。
> 写这份专门文档的原因：T47 原表述把真正重要的东西埋住了，导致执行方（包括 AI 顾问）把主次读反——把"机械缺料检查"当成主菜，把"AI 通读找结构病"当成收尾。为防止再次读偏，目的、边界、方向单独成文。
> 2026-08-06 由所有者拍板重定义。编号保留 T47（项目纪律：编号永不复用），内容彻底重写。

---

## 一、这件事的意义：为什么它值得当主方向

这套系统一切判断质量的上限，不取决于模型多聪明，而取决于**每一站实际看到了什么**。而这个最关键的问题，此前没有任何机制在回答：

- 现有体检器只查"不该进来的东西有没有溜进来"，几乎不查"该给的给了没有、给的方式对不对"；25 个站里 13 个连检查规则都没有，出事的两站它连提示词都读不到。
- 系统运行至今最值钱的发现——治理四站正反偏科（T46）、体检器只问一半问题、终审站数值供给 0/26——**全部来自人或 AI 带判断力的通读，没有一条来自机械检查**。
- 机械判据在结构问题面前天然失明：代码只能比对"在不在、像不像"，判不出"这站拿着这份材料这件事本身合不合理"。

更现实的一层：T38（上下文改渐进供给）就在眼前。**不先看清每一站现在真实吃什么、吃得对不对，改供给就是蒙着眼换引擎**——裁掉的可能是命脉，留下的可能是赘肉。T47 是 T38 的前置，不是可选项。

## 二、目的：一句话，加一份防误读清单

**目的：用"每一站真实看到了什么"这面镜子，对整个架构做一次根本性的结构审查——发现的是病的类型，不是病的个例。**

它**是**：

- 一次性的、根本性的架构审查（审查的是结构，不是修个别 bug）；
- 让 AI 带判断力通读全部 25 站的真实上下文，回答"哪一站拿到的上下文结构本身就是错的"。

它**不是**（每一条都有真实误读发生过的）：

- **不是机械缺料扫描。** "许可了没发"这类检查可以是发现的副产品，不是目标。若有人把 T47 做成"写一堆机械断言"，就是把主菜换成了配菜。（真实事故：2026-08-06，AI 顾问给出的开工方案把机械检查排成第二段主菜、AI 通读压到第三段，被所有者当场纠正。）
- **通读阶段不开处方，但确认后的小修可以当场修。** 先发现、先复核，再谈改不改。复核确认后的发现分两级：**证据硬、改动小的小修**（补一次比对、修一条路径之类）可以当场修——外科手术式、配红灯测试、记入工单；**凡是涉及供给设计、架构方向、"该给什么"的应然判断的**，不开药方，留给 T38 或所有者拍板。"顺带发现顺带改"允许的是前者，不允许的是后者。
- **不是常设检查。** 它是一次性根本审查。确认过的高价值类型另落成常设机械检查防复发——那是收割后的保鲜，不是本任务的主目标。
- **不是抽样。** 抽样看不到结构，个例看不出类型。25 站，一站都不许缺。

## 三、大体怎么做（只定方向，不写技术细节）

**第一步：造摊开器。** 把每一次跑里 25 个站**实际收到的全部上下文**——真实发出去的字节，不是设计文档说该给什么——完整导出、摊开。包括现在连体检器都读不到的那两站，一站都不能少。字符量太大就做投影，但必须证明投影没丢关键结构（参考 `体检方法.md` 第六节的实测口径），抽样比对留证。

**第二步：AI 通读（主菜）。** 逐站通读真实上下文，核心问题只有一个：**这一站拿到的材料结构本身对不对？** 四类已知的结构病是引子，不是封闭清单：

- **职责与材料错配**——只该看估值的站，手里是否攥着会诱导它的东西；
- **结构性冗余**——两站吞掉全程 43% 的 token，是不是材料组织方式本身有病；
- **结构性缺失**——从没人声明该给的东西，机器永远查不出来，只能靠通读发现；
- **结构性偏科**——正反双方拿到的材料是否真的对称（T46 是已确诊的活病例，同类可能还有）。

通读必须遵守 `体检方法.md` 第五节的三条铁约束：每条发现带证据（哪个文件哪一行、什么命令、什么输出）；严重度放全链路上评定，不许单站定级；复核通过前不动手修任何东西。分片按"失败方式"拆，不按代码模块拆；每一片如实交代自己看不到什么。

**第三步：收口。** 通读发现逐条确认后，每条三态归一：

1. 值得防复发的 → 落成一条常设机械检查（成本近零，此后自动复查）；
2. 不能机械化的 → 写明为什么不能，那个"为什么"是下一轮方法的线索；
3. 涉及"该给什么"的应然判断 → 留给所有者拍板，不外包给 AI。

**当场修的小修**（防误读清单第二条的"前者"）修完仍要回到三态归一留痕，不许修完就跑。

## 三之一、多代理执行的方式约束（所有者 2026-08-06 拍板：T47 用多 subagent 执行）

- **根线程（主对话）只做四件事**：理解需求、按"失败方式"分片、整合发现、做最终判断。**最终判断不许下放给任何 subagent。**
- **通读片（subagent）只读、只交证据**：每条发现带文件/行号/命令/输出，并如实交代自己看不到什么；不许递归再派 agent，不许扩大范围，不许覆盖已有未提交改动。
- **复核通过前，任何 subagent 不动手修**；允许当场修的小修，也只在复核后由被明确指派的执行者做，做完配红灯测试。
- **严重度只能由根线程在全链路上评定**——subagent 报的级别只算线索（七路审计三路严重度报高被汇总层改判的教训）。
- **可以用不同模型跑不同通读片**：不同模型的盲区不相关，同一条发现被两个模型家族独立命中，可信度更高；但验收标准不变（证据 + 复核），模型不同不降低证据要求。

## 四、与相邻工单的关系

- **T38（渐进供给）**：T47 是它的前置。先看清现状的结构病，供给设计才知道该改成什么样。
- **T46（正反偏科）**：是本次通读已确诊的第一个病例，修复照原工单走，不并入 T47。
- **T09（打分）**：不冲突，穿插进行。
- **历史依据**：立案证据在 `investigation_reports/20260805_audit_verification/REPORT.md` 三之二节（历史材料，说明"当时怎么判断"）；方法论在 `体检方法.md`。表述冲突时以本文为准。

## 五、怎么算做完

1. 25 站全部摊开可读（含此前读不到的两站），投影无损有比对留证；
2. AI 通读覆盖全部 25 站，发现清单带证据、经复核；
3. 每条确认发现已按三态之一归位（常设检查 / 写明理由 / 留所有者拍板），未归位的不许算完。
