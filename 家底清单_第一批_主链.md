# 家底清单 · 第一批（主链）

> **这是什么**：把这套系统里每个功能、每个机制清点成一张人话卡片。每张卡写清五件事——它是什么、哪来的；它想解决什么问题；它现在怎么运作；它实际干成了什么、坏在哪；我的初判建议。**你是裁判：逐条盖章"留 / 杀 / 重做 / 待定"。**盖章结果就是第二幕（杀哪些、重做哪些）和第三幕（分片重写）的图纸。
>
> **第一批范围（主链）**：五层站点、桥接与调查员、治理链七站、新闻事件支线、第三层综合裁决、出题官巡逻、全部 29 项常设检查 + 14 处代码校验器、五层数据管道与地基机制。共 **76 张卡**。
>
> 第二批（事件链二档细节、报告门面、图表工作台、打分台账、引擎与装配）还没做，第一批盖完章再开。本批由 4 路 agent 并行读代码挖掘，没跑流水线，花的是读文本的用量。

## 第 0 章 · 先看整体（哲学基座与架构地图）

### 0.1 一句话总览

这套系统只做一件事：把纳指相关的**数据**和外部世界的**事件**，变成一份每个数字可追溯、每个判断事后能被打分的投资判断书。骨架是两个大层加一个对质点（76 张卡全部挂在这张图上）：

```
数据层（由分到总）：L1–L5 五层互盲 → 桥接·受控调查 → 治理链六站（反方→论点→批评/风险→修订→终审）→ 数据判决 ──┐
事件层（两档）：　　常规底账（采集→事件卡→外部世界总结）　＋　专题档（出题官→老板圈题→巡逻→研究架） ──────────────┤
                                                                                                                    ↓
                                                                          第三层 · 综合裁决 IA（唯一对质点）→ 最终研报
```

四区卡片的位置：A 区=数据层各站点；D 区=数据层脚下的数据管道；B 区=治理链六站、事件层、第三层；C 区=闸门（挂在各站身上的机械检查）。

### 0.2 宪法序言（草案 v1 · 待老板逐句圈改）

**这台机器为什么存在。** 它要产出的从来不是一份"读起来像研报"的文本，而是一个**可辩护的判断**：纳指现在处于什么状态、这个价格里已经包含了什么、各种持仓的人分别该怎么做、出现什么证据就证明判断错了。判断覆盖短、中、长期，目的是帮所有者做出比"长期持有"更好的买卖决策。凡是不能让所有者复述、不能事后打分的结论，都不算这台机器的产出。

**我们相信什么。**

**一、判断与废话的分界是可证伪（波普尔）。** 说不清"什么证据出现我就错了"的判断，不是判断，是姿态。所以本系统每个重要结论必须自带认错条件；每张事件卡必须写明"哪条数据能确认我"；经核实的外部事实可以抗诉数据判决（亮红灯，改判权在所有者）；每次判断登记在册，等市场阅卷。

**二、证据的作用不是支持谁，是排除谁（情报分析·竞争假说）。** 不比谁的故事好听，比谁的故事最后被证据留下。所以反方必须先于主论点出场；每个候选假说必须被逐一点名——接受、部分吸收、或带着具体反证驳回——不许用"证据不足"四个字让假说无声消失。而对质的前提是真正的独立：五层互盲、反方先于论点、风险哨兵拿不到论点——**看得见答案的反对不是反对，是复读；互相看得见的"五重验证"，只是同一结论复述五遍。**

**三、价值与趋势是两派真功夫，本系统不皈依任何一派，让它们对质。** 从巴菲特处学来：价格相对盈利与现金流有没有吸引力、安全边际多厚、能力圈之外不装懂（这是 L4 估值层的全部职责）；不取他的集中下注，因为本系统不做组合建议。从利弗莫尔处学来：市场永远是对的、趋势是否有效、失效点在哪、不与市场争吵（这是 L5 趋势层的全部职责）；不取他的杠杆孤注。两派各自在互相看不见的房间里测量，然后到桥接与终审面前对质——**所有者不需要一个永远正确的学派，需要一座每次都能告诉他"现在哪派占理"的法庭。**所有者政策书里的"价值买入、趋势卖出"，只登记为待检验假说，同样要过打分这一关。

**四、抓主要矛盾（辩证法）。** 判断不能平均用力：十个小矛盾并排摆放的报告，等于没有判断。每次必须点出当前最决定收益与风险的那一对张力，并尽量说清"被顶住的那一环"；不许为显审慎把弱矛盾升格，不许为求顺滑把真冲突降级。数据判决是正题，事件与反方是反题，第三层综合裁决是合题——合题不是折中和稀泥，是把两边站得住的部分都保住之后升上去的裁决。

**五、语境使数据活（所有者，2026-09-16）。** 数据无语境则盲，语境无数据则空。VIX 处于低位，在 2000 年是崩盘前的自满，在降息预期里是"有人兜底"的安逸——同一个读数，不同的判断。数据层回答"是什么、是多少"，事件层回答"这是哪个故事、为什么"；两层各自跑完，只在第三层对质——数据才不是僵死的数字，故事才不是无根的叙事。

**六、实践是检验判断的唯一标准。** 每个数字必须能指回原料、能由独立代码重算；来源分等级，代理指标不许冒充官方事实；每个判断事后要被打分，打分结果回来修正方法；编不出来的不许说，说不清来源的不许进正文。

**我们不相信什么。** 不相信"听起来像"——五遍复读听起来像五次验证，顺滑听起来像可靠，后见之明听起来像先见之明，精确的小数听起来像事实。本系统的全部机械纪律，都是在和"听起来像"作战。也不相信任何机制有免审权——包括本序言没有点名的机制。

**出生证。** 本系统的每个机制——每个岗位、每道闸门、每张卡片——必须能指认自己从本序言的哪一句长出来。指认不出的：是新机制，不许出生；是旧机制，进家底清单过堂，由所有者逐条裁决留、杀、重做。

### 0.3 思想武装对照表（序言每句的系统落点，可核查）

| 思想来源 | 一句话信念 | 系统落点（真名，供 AI 核查） |
|---|---|---|
| 波普尔·可证伪 | 没有认错条件的不是判断 | 失效条件（thesis/final 合约字段）、事件卡 needs_data_confirmation 钩子、抗诉通道 PC-28、事后打分闭环（暂缓中，T09） |
| 情报分析·竞争假说 | 证据用于排除，不用于支持 | 反方先于论点（counter_thesis.md）、假说逐一回应（orchestrator 校验器）、事件卡 supports/refutes、批评者对称攻击（critic.md） |
| 巴菲特·价值 | 价格 vs 价值、安全边际、能力圈 | L4 估值层（l4_analyst.md、Wind 主锚、来源等级阶梯） |
| 利弗莫尔·趋势 | 市场永远对、失效点、不争吵 | L5 趋势层（l5_analyst.md、确定性快照守门） |
| 辩证法·主要矛盾 | 不平均用力，点出被顶住的一环 | 桥接冲突清单与主要矛盾字段（A-6、A-补卡）；正反合=三层架构（说明书 §4.7） |
| 语境论（老板 09-16） | 数据无语境则盲 | 事件层两档（B-8、B-11）、第三层 IA 对质（B-9）；"历史上像今天"引擎（冻结中，analog_history_audit.py） |
| 实践论 | 可重算、可追溯、被打分 | 重算带（recompute_belt.py，D-10）、证据护照（data_evidence.py，D-11）、来源等级、打分闭环（暂缓中） |

### 0.4 五条战术原则（序言的操作化——判卡的总尺子）

这五条是序言六条信念在机制层的操作化。**拿起任何一张卡，先问它从序言哪句长出来，再问它从哪条原则长出来；答得出→留；答不出或长歪了→杀/重做候选。**

1. **矛盾显真（辩证法）**——判断不来自汇总平均，来自矛盾的对质；冲突是资产，不许为了顺滑抹平。（出处：常驻边界"冲突是资产"。）从它长出来的卡：A-6 桥接、A-补 抓主要矛盾、B-1 反方、B-3 批评者、B-4 风险哨兵、B-9 综合裁决、B-10 裁决批评者。
2. **实践检验（实事求是）**——每个数字可追溯、可重算；每个判断事后要被打分；算不出来的不许说。（出处：北极星"事实扎实"、重算带、校准闭环。）从它长出来的卡：A-7 调查员、B-5 结构检查、B-8 事件支线、B-11 出题官巡逻、D 区全部、C 区绝大多数闸门。
3. **金字塔（结论先行）**——塔尖是判断，塔身是证据链；每一层都必须答得出"你凭什么"。（出处：门面体例 T70/T72、判断书结构。）从它长出来的卡：B-2 论点建构、B-6 修订者、B-7 终审。
4. **独立测量（防复读）**——五个互相看不见的判断才叫交叉验证；互相看得见，就退化成同一结论复读五遍。（出处：常驻边界"层间隔离"。）从它长出来的卡：A-1～A-5 五层、A-8 层间隔离、B-4 的论证盲设计、C 区的 PC-01/06/07/16。
5. **注意力经济（充分必要）**——每个岗位只收充分必要的材料；机械字段不出答卷；能代码装配的绝不让模型填。（出处：§4.1"轨道>导出>规矩"、T54。）从它长出来的卡：C 区 PC-21/22（去重）、D-8 缓存；B-补 数字菜单本意也是它，但实测做成了冗余→待定。

### 0.5 三处真乱（待裁，图上标红的三处）

- **乱点①·治理链展开段**：修订者与终审职责重叠。分工本意是"裁判不亲自改文章"，原则站得住；但修订者的真实贡献从未实测——判据：对比最近几次 run 的"修订稿 vs 原稿差异率、批评意见落实率"，一测便知是脊梁还是赘肉。（实测候选，未做，做前要批预算。）
- **乱点②·实现形态**：orchestrator 一个文件 9,954 行。这是实现的烂，不是架构的烂。第三幕按"一卡一模块"切开，卡就是未来模块的户口本。
- **乱点③·出题官锚点**：现状锚在"数据链残局"（桥接没答完的疑点），本质是数据层的下游附件；老板的定位是"抓市场主线、让事件层有的放矢"。B-11 盖章时可直接改判"重做——换锚到市场主线"。

### 0.6 证据等级口径（先声明，免得误读）

- **粪土判定全部基于代码铁证**（逐行读过，行号在存档行）。
- **"珠玉"是设计珠玉**——这机制该不该存在、设计对不对——**不等于"实测质量珠玉"**。判后者要拿真实 run 的产物逐站体检，属另一道工序，做前会请示预算。
- IA 卡（B-9）的装配代码未读、仅据说明书与提示词，是全清单证据最薄的一张，卡上已标注。

---

## 先说三句最重要的结论

1. **坏消息比想象少，而且集中**：76 张卡里初判"粪土"只有 3 张、"待定"3 张——你真正需要过堂的就这 6 张（下面有直达清单）。其余 69 张初判"珠玉"，扫一眼标题批量确认即可。
2. **你的两个预感都被证实**：①闸门里确实有垃圾——终审那道"数字必须逐字出现在输入里"的闸门（C-35），实测把"相减、比率、分位差"三类合法推算全拦，和你说的一模一样；②隔离确实是珠玉——它不是纸面承诺，是代码强制 + 实测核验过的（A-8）。
3. **腐烂的主因不在"机制全是垃圾"，在形态和无户口**：绝大多数闸门是"身份比对类"（只查编号在不在、形状对不对），便宜零误伤，该留。真正的问题是三件：少数几条"语义代理类"闸门拦错了对象（本清单已逐条标出）；orchestrator 一个文件 9,954 行的形态臃肿；机制没有"出生证"导致只增不减。这三件分别由"杀/重做清单、分片重写、出生证制度"解决。

## 怎么盖章（两种都行）

- 每张卡末尾有「**老板盖章：**＿＿」一行。你可以直接在这个文件里写（留 / 杀 / 重做 / 待定 + 一句话），也可以在对话里报卡号给我，我来填。
- 建议顺序：先盖下面 6 张争议卡；其余珠玉卡可以按区批量盖，比如说一句"A 区全留、D 区全留"。

## 必须你过堂的 6 张（初判粪土/待定）——2026-09-17 已全部盖章

盖章结果：**C-31 杀｜C-35 改（③降级留痕不拦）｜C-39 保持降级｜C-26 改（删二字硬约束）｜C-10 留（归身份比对）｜B-补 fact_card 杀·缓执行（与 C-35 重算制同批）**。执行移交 T77（杀粪土小战役），交接文件《交接_杀粪土清腐烂.md》。其余 69 张珠玉卡仍未盖，可继续按区批量盖。

| 卡号 | 一句话案情 | 我的初判建议 |
|---|---|---|
| C-31 调查 limits 措辞闸门 | 用正则读 AI 写的"否认数字"措辞判自相矛盾，既会误伤也会漏判 | 杀，或收窄成"limits 点名的数字须在材料里找得到" |
| C-35 终审数字逐字闸门 | 判决正文里的百分数和小数必须逐字出现在输入材料里，派生数字（相减/比率/分位差）全被拦 | 引用格式与在册两项留；数字逐字这项杀，或改成"可追溯+可重算"（正道已有雏形：重算带 D-10） |
| C-39 事实/解读逐字相同 | 拿"两字段逐字相同"代理"没分离"，8 月底已自我降级为只留痕不拦 | 保持降级；谁想恢复成硬拦就先杀 |
| C-26 子项 yield gap 的 reason 必须逐字含"诊断/辅助"二字 | 换个同样合规的写法就会被误伤 | 保留 usage 枚举判定；二字硬约束改成"reason 非空且说明用途" |
| C-10 事件站措辞口径 | 逐字查提示词模板（查的不是 AI 产出），误伤面低 | 待定：你判它算身份比对还是语义代理 |
| B-补 终审的数字菜单（fact_card） | 同样的数据喂终审放了两份，菜单不进报告、不参与校验，还弄丢了数字的推理过程 | 大概率杀；牵连"数字怎么保证不写错"的整体设计，建议和 C-35 一起裁 |

（PC-27 已退役，不用盖。）

## 卡片总目

- **A 区 · 数据链站点（9 张）**：L1-L5 五个分析员、桥接、受控调查员、层间隔离，外加补卡"抓主要矛盾"
- **B 区 · 治理与裁决站点（12 张）**：反方、论点建构、批评者、风险哨兵、结构检查、修订者、终审、事件支线、第三层 IA、IA 批评者、出题官巡逻，外加补卡"终审的数字菜单"
- **C 区 · 闸门（43 张）**：PC-01~PC-29 常设检查 + 14 处代码校验器；附录 6 条管道外校验器不计入
- **D 区 · 数据管道（12 张）**：五层数据工具、采集器、完整性检查、缓存、时点归档、重算带、证据登记、证据家族与可用性

评级口径：珠玉=体现系统原则且实现健康；粪土=违背原则或必然误伤（附证据）；待定=信息不足或利弊相抵。**初判是我和挖掘 agent 的建议，你的盖章才是终判。**注意：卡片评的是"机制该不该存在、设计对不对"；代码形态问题（万行单文件、缝合臃肿）是另一根轴，归第三幕重写处理。

---


## A 区 · 数据链站点（9 张）

五层分析员各看一摊数据、互不见面，是这个系统的地基。本区 8 张站点卡全部初判珠玉；另有 1 张补卡（A-补）是你点过名的"抓主要矛盾"，单列出来请你裁要不要升级它。

**卡 1 · 初判【珠玉】：L1 宏观流动性分析员——只管"油价"不管"车往哪开"**

**它是什么、哪来的。** 它是数据链第一棒，五层分析员之一。系统把纳指（NDX，市值加权的科技龙头指数）判断拆成五摊互不照面的活儿，L1 专门看宏观与货币环境。来历：与 vNext 架构同期设计，五份层提示词是同一模板套五遍，最早可追到 2026 年 4 月的提示词重写实验（doc 存档里有对应文件），精确初版日期待确认。

**它现在怎么运作。** 它只收 L1 自己的原始数据（政策利率、实际利率、期限利差、净流动性、M2、通胀预期等）和本层事实摘要，外加一份"静态五层说明书"——那说明书只告诉它"别的层各自负责什么"，不夹带任何当前数据。代码里有一份白纸黑字的禁入清单，L1 看不到其他四层的任何数据或结论，也看不到事件新闻。它产出一份本层卡，给出"宏观在给估值加油还是抽油"的判断，并把需要别的层验证的问题写成"待验证提问"（而不是现成结论）。

**它的实际效果/问题。** 好处是职责干净：它明确禁止自己给买卖建议（那是后面论点建构和终审的活），也禁止从本层数据反推别的层在想什么，从源头杜绝"同一结论被复述五遍"。值得注意的机制：层卡里的层级号、指标展示名、六个法典字段（发言权类型、指标正问、误读防线等）都由代码装配，模型只填需要判断的内容，对法典有异议走专门的异议通道，不静默丢弃。已知边界（非缺陷）：它挡不住模型脑子里自带的训练知识，系统已明文承认这点、不为它新增机制。

**我的建议：留（隔离原则落地干净，脑内先验的代价已诚实标注）。** 需要老板拍板：是否要为"脑内先验"另立一道对策（如让每层声明哪些判断来自本次数据、哪些是常识默认）？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：L1 `src/agent_analysis/prompts/l1_analyst.md`；禁入清单 `orchestrator.py:1332-1372 _build_layer_input_policy`（forbidden_runtime_inputs）；静态本体注入 `orchestrator.py:8069-8071`；机械字段装配 `系统说明书.md:118（T54）`；脑内先验边界 `系统说明书.md:364 §4.3`。读了系统说明书 §一/§2.1/§4.3、l1_analyst.md、_build_layer_input_policy 实测。

---

**卡 2 · 初判【珠玉】：L2 风险偏好与信用分析员——专看"市场敢不敢冒险"**

**它是什么、哪来的。** 数据链第二棒，五层之一。它回答一个别人答不了的问题：市场当下愿不愿意承担风险，这种冒险是健康、恐慌、贪婪、自满，还是信用市场已经不配合。来历与 L1 同批，是 vNext 五层模板套用的产物，精确初版日期待确认。

**它现在怎么运作。** 只收 L2 本层数据：VIX、纳指波动率 VXN、高收益债利差（信用市场给风险定价的硬指标）、恐贪指数、仓位拥挤度等。同样靠代码禁入清单，看不到其他层数据、桥接、论点、事件新闻。它产出本层卡，并明确禁止三件易混的事：不能把"低波动"直接等同"低风险"，不能把"风险偏好强"直接等同"市场健康"，极端恐惧和极端贪婪都必须说明反向条件。它把问题写成"待验证提问"交给别的层。

**它的实际效果/问题。** 设计亮点在"防误读"：它硬要求区分"表面情绪"和"信用实质"，逼模型把低波动、情绪贪婪、信用利差分开看。一个细节值得老板知道：它要求引用信用利差数值时必须写清单位（百分比还是基点），因为函数名虽带"_bp"，字段值实际是百分比——这是防模型把单位标错的实招。代价同 L1：脑内先验挡不住，已诚实标注。整体看它把"风险偏好的真假"这个最难量化的事，拆成了可逐条核查的清单。

**我的建议：留（职责边界与防误读规则清晰，无证据显示误伤）。** 需要老板拍板：信用利差单位标注这类细节规则，是否要推广到所有层统一执行？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：L2 `src/agent_analysis/prompts/l2_analyst.md`；禁入清单 `orchestrator.py:1332-1372`；单位标注规则 `l2_analyst.md:108`；防误读三禁令 `l2_analyst.md:105-107`。读了系统说明书 §2.1、l2_analyst.md、_build_layer_input_policy 实测。

---

**卡 3 · 初判【珠玉】：L3 市场内部结构分析员——数一数"到底多少只股票在涨"**

**它是什么、哪来的。** 数据链第三棒，五层之一。它回答一个朴素但关键的问题：指数上涨是靠大部队（多数成分股）推进，还是被少数龙头硬撑。来历同批，是五层模板套用产物，精确初版日期待确认。

**它现在怎么运作。** 只收 L3 本层数据：腾落线（累计涨跌家数，判断参与度最直接）、高于均线的成分股比例、NDX 相对等权基准的强弱、Top10/M7 权重集中度、新高新低、广度动能。靠代码禁入清单看不到其他层与事件。它产出本层卡，并把"趋势是否获得广度确认""高集中度是否有盈利支撑"写成待验证提问交给 L5、L4。

**它的实际效果/问题。** 最值得老板留意的是它的"分寸感"机制：L3 被反复叮嘱，只能确认集中度这个事实，绝不能替 L4 判断"少数龙头是不是优质胜出"——它若看到头部权重高，必须同时写出"头部支撑"和"参与不足的结构脆弱性"，两者不能合并成一句含糊话。数据缺失时它必须写"缺什么"而不是假装恶化或当证据。这类规则让 L3 真正成为独立的"结构体检"，而不是估值或趋势的附庸。代价仍是脑内先验挡不住（已标注）。

**我的建议：留（把"事实确认"与"价值判断"严格切开的机制很成熟）。** 需要老板拍板：当 L3 因数据缺失只能降置信时，是否要系统显式提醒读者"结构结论置信度下降"？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：L3 `src/agent_analysis/prompts/l3_analyst.md`；禁入清单 `orchestrator.py:1332-1372`；分寸规则 `l3_analyst.md:60-64、101-102`；数据缺失纪律 `l3_analyst.md:28-29、103`。读了系统说明书 §2.1、l3_analyst.md、_build_layer_input_policy 实测。

---

**卡 4 · 初判【珠玉】：L4 估值分析员——全系统最"较真"的一层**

**它是什么、哪来的。** 数据链第四棒，五层之一。它回答：当前价格相对盈利、现金流、无风险替代收益和风险补偿，到底有没有吸引力。它是五层里规则最厚、最"斤斤计较"的一层，来历同批。

**它现在怎么运作。** 只收 L4 本层数据：NDX 的 PE/前瞻 PE、盈利收益率、Wind 指数级快照、M7 资本开支与回购现金流、简式收益差距等。靠禁入清单看不到其他层与事件。它产出本层卡，明确禁止因"趋势强"或"情绪高"就合理化估值，也禁止给买卖建议。

**它的实际效果/问题。** 这一层集中体现了系统的"数据发言权"原则（指标分事实型/代理型/合成型等五类，防止越权推理）。它要求每个估值指标老老实实报出数据来源等级、数据日期、新鲜度；Wind 高信任数据可用时优先，不可用就降级，绝不许把第三方或旧值伪装成 Wind。最硬的规矩是关于"字段权限"：一个数据包裹里若字段权限不一致，引用时必须在函数名后加"#字段名"精确指向；父级引用只能代表混合容器，不能单独支撑"贵/便宜"的强结论。这些规则把"模型嘴快"的误读从根上卡住。代价同前：脑内先验挡不住（已标注）。

**我的建议：留（数据发言权与字段权限纪律是全系统的标杆）。** 需要老板拍板：L4 这套权限纪律是否要作为范本，强制其他层（尤其 L5 精确数值）照抄？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：L4 `src/agent_analysis/prompts/l4_analyst.md`；禁入清单 `orchestrator.py:1332-1372`；数据发言权 `l4_analyst.md:50-82`；字段权限纪律 `l4_analyst.md:70-74`；防合理化 `l4_analyst.md:160-161`。读了系统说明书 §2.1/§4.4/§4.5、l4_analyst.md、_build_layer_input_policy 实测。

---

**卡 5 · 初判【珠玉】：L5 价格趋势分析员——只判"趋势还在不在"，不判"值不值"**

**它是什么、哪来的。** 数据链第五棒，五层之一。它回答：价格趋势是否仍有效、动量是否确认、有没有过热、触发点和风险边界在哪。来历同批，是五层模板最后一棒。

**它现在怎么运作。** 只收 L5 本层数据：均线排列、RSI、MACD、ADX、ATR、成交量、唐奇安通道等。靠禁入清单看不到其他层与事件。它产出本层卡，把"趋势是否获广度确认""失速是否触发估值压缩"写成待验证提问交 L3、L4。

**它的实际效果/问题。** 它最突出的机制是"精确数值守门"：所有精确价格、均线、RSI、MACD、ATR 必须优先取自一个名为"确定性快照"的固定函数，其他指标只能当同源分项。规则明令禁止模型凭记忆、图像印象或自然语言自己补数字——这是防 LLM（大语言模型）"随手编个价位"的硬闸。它还被要求把"趋势有效"和"短期过热"分开处理，不能互相抵消，也明确禁止因趋势强就给买入建议、或合理化估值（那是 L4 的事）。代价同前：脑内先验挡不住（已标注）。

**我的建议：留（精确数值守门规则直击 LLM 编数痛点）。** 需要老板拍板：当"确定性快照"标为不可用时，系统是否要明确拒绝 L5 跑、而不是让它空着跑？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：L5 `src/agent_analysis/prompts/l5_analyst.md`；禁入清单 `orchestrator.py:1332-1372`；确定性快照守门 `l5_analyst.md:27、108`；趋势/过热分离 `l5_analyst.md:109-111`。读了系统说明书 §2.1、l5_analyst.md、_build_layer_input_policy 实测。

---

**卡 6 · 初判【珠玉】：桥接 Bridge——五层吵完，它来画"谁和谁打架"的地图**

**它是什么、哪来的。** 桥接是五层之后、正反辩论之前的关键一站（系统自称为"vNext 架构的关键创新"）。传统多 Agent 系统让总管自己脑补跨层关系、容易把冲突抹平；桥接的活就是把层与层之间的支撑、冲突、传导关系显式画出来。来历与主线同期，属数据链核心环节。

**它现在怎么运作。** 它能看到全部五份层卡全文（不摘要压缩）和一份预置的"候选跨层关系"清单（哪两层理论上可能相关），就这两样。它看不到论点、批评、风险、终审这些下游产物——因为此时它们还没生成。它产出一份"桥接备忘"：标出每层间是印证还是冲突、冲突严重度、主要矛盾是谁、风险已反映进价格多少。若它自己判断不了某个疑点，就生成"求证问题"交给受控调查员去查。

**它的实际效果/问题。** 最亮的设计是"姿态中立"加"冲突是资产"：它明令不得为了显得审慎而把弱张力升格成高冲突，也不得为了通顺而把真冲突降级；且 conflicts 字段不许交空清单——真有零冲突也得诚实写"印证环境下的监测项"。一处已知短板（非缺陷）：这套"不许交空清单"目前只守在提示词层，没有机器闸门；真有零冲突时代码不会拦，但 2026-08-19 老板已裁定不补闸门、保持诚实标注。另注意桥接拿不到事件材料，若它写出事件引用会被校验打回。

**我的建议：留（中立测绘 + 冲突资产化是系统精髓，诚实标注已足够）。** 需要老板拍板：是否要为桥接补一道"零冲突时强制留痕说明"的机器闸门？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：Bridge `src/agent_analysis/prompts/cross_layer_bridge.md`；输入范围 `系统说明书.md:138`；中立/冲突资产 `cross_layer_bridge.md:12-13、158-163`；无机器闸门 `系统说明书.md:140`；事件禁入 `系统说明书.md:138`。读了系统说明书 §2.2、cross_layer_bridge.md 实测。

---

**卡 7 · 初判【珠玉】：受控调查员——只许按一小包材料"老实答题"的取证员**

**它是什么、哪来的。** 它是桥接的辅助支线（系统称"反馈调查支线"）。桥接或代码自动检出材料缺口、判断不了的疑点时，会写一批"求证问题"交给它。设计意图是：让一个有疑问的环节，去找一个"只准看指定材料、不能自由发挥"的角色取证，而不是自己上网乱搜或脑补。来历与桥接同期。

**它现在怎么运作。** 它只收到一小包被打包好的材料原文，外加要回答的问题。铁律四条：材料里没有的事实，一律写进"无法确认"，不许猜、不许补、不许用常识填空；它不知道、也不需要知道系统当前整体判断是什么，不会揣摩去迎合哪个方向；数字分位日期只能逐字引用，禁止创造新数字新阈值；支持性和挑战性发现分开列，不许合并成"总体来看"式的和稀泥。若材料整体不足，它最有价值的输出就是把"缺什么才能回答"写清楚。

**它的实际效果/问题。** 这是五层隔离精神在"取证"环节的延伸：它不知道系统倾向，所以不会为了讨好结论而编造。它自带兜底——若确实找不到材料或规则不允许开口，会产出一份明确写"无法确认"的报告，而不是硬编一个结论。值得老板记住的一点：2026-08-25 老板裁定拆除了它原本"可读事件层文件回答事件挑战"的例外，事件侧求证已转交二档研究部，调查员回归纯数据侧本分，隔离恢复无例外（PC-06 机器断言它不得引用事件侧产物）。

**我的建议：留（"不知系统倾向 + 无法确认兜底"是干净的抗迎合设计）。** 需要老板拍板：调查员材料包的大小与来源是否要设上限，防止它变相读到不该看的东西？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：受控调查员 `src/agent_analysis/prompts/controlled_investigator.md`；铁律四条 `controlled_investigator.md:3-8`；兜底 `系统说明书.md:142`；拆例外 `系统说明书.md:98（2026-08-25 裁定）`、PC-06。读了系统说明书 §2.2、controlled_investigator.md、§4.7 实测。

---

**卡 8 · 初判【珠玉（候选）】：层间隔离——"五道关各看各的"是整座系统的地基**

**它是什么、哪来的。** 它不是某一站，而是横跨五层的机制：允许每层知道"别人负责什么"（静态五层职责、投资对象定义、本层指标法典），但禁止知道"别人这次看到了什么、判断了什么"（其他层的运行数据、结论、桥接/论点/终审判断）。一句话原则写在 CLAUDE.md 和 AGENTS.md 里："L1-L5 运行时上下文不得互通"。这是"五层交叉验证不是同一结论复读五遍"的根本保证。

**它现在怎么运作。** 落点是代码里的显式禁入清单 `_build_layer_input_policy`（orchestrator.py:1332）：每层只允许收"本层的高亮、事实、原始数据、本层手动覆盖"，外加"ObjectCanon 和本层 IndicatorCanon 作为静态规则"；forbidden 清单列死了其他层 facts/raw、候选跨层关系、桥接/论点/批评/风险/终审产物、调查报告和所有事件新闻产物。静态五层本体注入时明文标注"只用于路由，不代表当前状态"。五层依次执行（不是同时），但顺序不影响隔离——关键是运行时谁也看不到谁的当前结论。

**它的实际效果/问题。** 我给"珠玉"初判的理由：它不止是纸面承诺，有代码强制 + 实证核验。T47（2026-08-06 上下文审查）逐层核对真实提示词，五层"是否出现跨层信号"的字段全部为空数组，跨层函数名只出现在格式示例、从不带数值；测试 `test_vnext_orchestrator.py` 也断言禁入清单确实生效。已知边界（非缺陷）：它挡不住模型脑内自带的训练知识（LLM 读完材料后"凭常识"默认会怎么想），系统 08-15 已裁决"不为它新增机制"，代价作为偏差下界对待。我的判断：这是系统最该保住的设计，重写时任何改动都不能动这条边界。

**我的建议：留（代码强制 + 实证核验，是全系统最健康的地基）。** 需要老板拍板：是否正式把"层间隔离"写入不可动摇的宪法级红线，并明确"换引擎/换编排器时隔离必须整体重造"为硬要求？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：机制 `CLAUDE.md:32`、`AGENTS.md:32`、`系统说明书.md:68 §4.3`；代码落点 `orchestrator.py:1332-1372 _build_layer_input_policy`、静态本体 `orchestrator.py:8069-8071、6910-6942`；实证 T47 `investigation_reports/20260806_t47_context_review/phase1/H3_隔离.md`、测试 `tests/test_vnext_orchestrator.py:630-636`；脑内先验边界 `系统说明书.md:364`。读了系统说明书 §一/§4.3、CLAUDE.md、_build_layer_input_policy 实测、T47 实证报告。

---

**卡 A-补 · 初判【珠玉】：抓主要矛盾**

**它是什么、哪来的。** 五个分析员（利率、信用、资金、估值、趋势）各自独立写完报告之后，有一个岗位专门把五份报告摆在一起对质，叫"桥接"（见 A-6）。桥接的任务之一，是在所有矛盾里点出哪一对是当前最决定收益和风险的——系统内部叫"主要矛盾"。立这道工序的原因：早期的系统会把发现平均罗列，十个小矛盾并排摆放，你读完不知道哪一个真正要紧。你定下的原则是"判断不能平均用力"，所以桥接被明确要求每次必须点出一对主要张力，而且不许为了显得审慎把弱矛盾升格，也不许为了报告顺滑把真冲突降级。

**它现在怎么运作。** 桥接每跑一次就产出一张冲突清单，每条冲突写明涉及哪两层、各自的证据、为什么互相打架；主要矛盾就从这张清单里选出，随后一路传给下游——正反双方辩论围绕它，终审写判断书必须回应它，报告读者第一眼看到的就是它。你之前追问"真实利率升至十年之巅，为什么不能从第一性原理分析"，推的正是这道工序往深处走：不只指出矛盾，还要说清矛盾里"被顶住的那一环"。

**它的实际效果/问题。** 它是系统的脊梁，但有两个软肋：①主要矛盾现在埋在散文里，没有专门的结构化字段，下游要"读懂句子"才能取出来用——渲染端一旦需要读懂句子才能取值，就是设计隐患；②事后打分时，系统现在做不到回头追踪"那一次的主要矛盾抓对了没有"。

**我的建议：留，并在重写时升成一等公民。** 主要矛盾要有专门字段，进打分闭环。需要你拍：升级方向是否就是"专门字段 + 打分追踪"这两条？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：桥接站产物的冲突清单与主要矛盾字段（typed_conflicts / main_tension），说明书 §2.2、§4.2（常备六对张力）；第一性取材教训见 T76 案卷（investigation_reports/20260902_研报语言第一性/）。

## B 区 · 治理与裁决站点（12 张）

从反方到终审的对抗链、新闻事件支线、第三层综合裁决、以及把你本人嵌进链路的出题官巡逻。站点本身全部初判珠玉；但注意 B-补那张"终审的数字菜单"——它和 B-7（终审卡）的看法有出入，两卡对照看，冲突如实保留。

**卡 1 · 初判【珠玉】：反方——在主论点出生前先给它找个对手**

**它是什么、哪来的。** 反方（Counter-Thesis）是治理链的第一道对抗：在正式主论点还没写出来之前，先逼一个"反方角色"构造出当前证据能撑起的最强对立解释。来历查得到——说明书 2.3 与提示词 counter_thesis.md 一致，是为了对抗"辩护式反对走过场"而设。它要的是"理性投资者会真金白银下注"的对立解释，至少一个必须做方向对抗（主线谨慎就构造最强乐观解读，反之亦然）。

**它现在怎么运作。** 编排里它排在桥接第二版之后、论点建构之前。它只看桥接两版的摘要、非兜底调查报告、一份允许引用的证据清单，然后交 1-2 个反方假说（含支持/反证/失效条件）。

**它的实际效果/问题。** 这是全系统表述最硬的一处隔离：它明文禁止读 thesis_draft.json、analysis_revised.json、final_adjudication.json，而且因为"先跑反方、再写主论点"的物理顺序，它根本不可能先看主论点再假装反对。失败时有兜底——LLM 两次不过，代码用固定模板生成降级稿并打 deterministic_fallback 标记，追进终审质量说明，读者看得见这次反方是模板凑数。设计健康，隔离真实。

**我的建议：留（理由：顺序隔离 + 模板兜底，把"冲突是资产"原则落到了物理上）。** 需要老板拍的是：模板兜底只在反方站出现，事件总结另有套话兜底——这种"不同站兜底方式不一"是否要统一口径？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：counter_thesis.md 全文；系统说明书 §2.3（148-160 行）。关键真名：`synthesis_packet_without_self_reference` 输入；禁读 `thesis_draft.json`/`analysis_revised.json`/`final_adjudication.json`（counter_thesis.md:31-36）；降级字段 `deterministic_fallback`、终审质量字段 `counter_thesis_degraded_deterministic_fallback`（§2.3）。

---

**卡 2 · 初判【珠玉】：论点建构——把证据翻译成一句能落地的判断**

**它是什么、哪来的。** 论点建构（Thesis Builder）是主论点站，负责把五层侦察 + 桥接的结论转成一句落地判断：当前状态、价格定价了什么、坏消息反映多少、承担风险补偿是否变厚、核心/战术/等待仓分别怎么做、什么证据推翻判断。来历明确（§2.4、thesis_builder.md）。提示词强调"姿态校准"：证据一边倒时必须敢写进攻或防守，把"谨慎骑墙当默认安全答案"与"证据恶化时喊进攻"列为同等严重的失真。

**它现在怎么运作。** 它只收一份 `synthesis_packet`（桥接整合版：五层摘要、高严重度冲突、证据索引、反方假说清单），并对反方清单每个候选假说逐一回应（接受修正/部分吸收/驳回三选一，驳回必须点名具体反证，不许用"证据不足"带过）。

**它的实际效果/问题。** 它明确被禁止绕过五层和桥接自己重新解读原始数据（"你不是重新分析原始指标的人"），防止它"看穿"底层数据把猜测当证据。失败即整链崩溃退出，无兜底稿——这是链路关键收口站，牺牲软着陆换隔离纯度。好处是姿态失真被双向对称约束，结论不会无脑和稀泥；代价是它一旦两次不过就整跑报废，没有像修订者那样的退路。

**我的建议：留（理由：双向姿态校准 + 不重解原始数据，是系统"不抹平冲突"原则的核心执行者）。** 需要老板拍的是：它失败无兜底、整链崩溃，是否接受这种"宁可全废不可降质"的取舍？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：thesis_builder.md 全文；系统说明书 §2.4（162-173 行）。关键真名：`synthesis_packet`；姿态失真等价条款（thesis_builder.md:31）；竞争假说强制回应（thesis_builder.md:79-81）。

---

**卡 3 · 初判【珠玉】：批评者——专挑主论点的逻辑漏洞**

**它是什么、哪来的。** 批评者（Critic）是治理链里专挑刺的一站，像严格审稿人找主论点的逻辑漏洞：跨层因果跳步、选择性引用（只引对自己有利的）、高严重度冲突被轻描淡写、置信度与证据强弱不匹配。来历明确（§2.5、critic.md）。它特别要求"对称攻击"——既抓过度乐观跳跃，也抓"为了不犯错而过度谨慎、错过赔率"，防批评者自带悲观偏见。

**它现在怎么运作。** 它收一份压缩治理包（`governance_input`：主论点各段落、支撑链、时间尺度、仓位动作、高严重度冲突、关键证据索引），逐项攻击。设下限为"不设数量下限，也不为凑数发明问题"，幸存时还要给出"最强反对意见"。

**它的实际效果/问题。** 2026-08-15 起它与风险哨兵刻意分料（它拿论点论证+压缩证据包，风险哨兵拿论证盲材料），这是设计不是偏科，防两站互相迎合。一处真实空白要诚实标出：说明书明说"批评者与风险哨兵产出里的引用目前没有任何机器校验（已知空白，留档待评估）"——它们的输出引用没被 Schema Guard 这类代码把关。这不是误伤，但是质量盲区，值得老板定夺是否补机器校验。

**我的建议：留（理由：对称攻击 + 分料设计健康；唯一瑕疵是输出引用暂无机器校验，属待补项）。** 需要老板拍的是：是否要像 Schema Guard 那样，给批评者/风险哨兵的输出引用也加一道机器校验？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：critic.md 全文；系统说明书 §2.5（176-187 行）、§2.7（208 行"批评者与风险哨兵产出里的引用目前没有任何机器校验"）。关键真名：`governance_input`；对称攻击条款（critic.md:99-112）。

---

**卡 4 · 初判【珠玉】：风险哨兵——只列风险边界，不替论点说话**

**它是什么、哪来的。** 风险哨兵（Risk Sentinel）负责监控失效条件、风险边界和双向权衡。它列出所有可能让判断失效的条件，评估风险边界（估值压缩/盈利 miss/流动性冲击/集中度崩塌等）当前是安全/警告/已触发，并同时列反方向风险（过度谨慎的机会成本、等待确认的代价、假安全风险）。来历明确（§2.6、risk_sentinel.md），原话："这不是要求系统冒进，而是要求风险报告完整。"

**它现在怎么运作。** 它走"论证盲"分料：收不含任何论点论证的治理包（五层摘要、高严重度冲突、主要矛盾候选、未解决问题、从冲突与层摘要重建的关键证据索引）。提示词明文"你拿不到论点论证，不得脑补一个论点出来攻击"。

**它的实际效果/问题。** 论证盲是 2026-08-15 刻意施工，目的就是让它独立盯风险、不被论点带偏。统计约束很硬：明文禁止编造历史胜率、回测收益、样本区间、点位阈值或任何具体概率数字，除非输入明确给，只能用"若……则……"条件语言——防"假精确"。失败即整链崩溃无兜底。一处可商榷：它必须检查 13 种冲突矩阵（A-M），清单很长，提示词要求显式检查，成本不低但属一次性。

**我的建议：留（理由：论证盲 + 禁假精确，把"风险报告完整"原则落到实处）。** 需要老板拍的是：13 种冲突矩阵是否都需要每次显式跑，还是可只跑关键几种省 token？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：risk_sentinel.md 全文；系统说明书 §2.6（190-201 行）。关键真名：`governance_input`（论证盲）；统计约束（risk_sentinel.md:12-15、194-208）；13 种冲突矩阵 A-M（risk_sentinel.md:179-192）。

---

**卡 5 · 初判【珠玉】：结构检查 Schema Guard——全系统唯一不调 AI 的一站**

**它是什么、哪来的。** 结构检查（Schema Guard）是唯一不是 AI 的一站，纯 Python 代码（orchestrator.py 的 `_run_schema_guard`）。它回答机械问题：桥接产物里引用的证据编号是不是真存在、格式合不合规、桥接标的高严重度冲突是否被论点建构悄悄弄丢（逐条"认亲"）、复合指标（如恐贪指数这种"总分+子项"）有没有被子项越权升格成强结论。

**它现在怎么运作。** 它直接扫原始数据文件，把所有函数级/字段级合法证据编号列成表，拿桥接实际用的编号逐条比对（orchestrator.py:7288-7305）。它产出 `passed:true/false` 报告，不判断"投资结论对不对"，只判断"引用和格式站不站得住"——把判断权和把关权分开。

**它的失败会怎样（与别站不同）。** 它失败不会让流水线崩溃，只产出报告。若发现结构性缺失或字段缺失（`structural_issues`/`missing_fields` 非空），自动触发批评者和风险哨兵各重跑一次、把问题反馈给它们，重跑后再查（orchestrator.py:780-818）；若只是"一致性问题"（如冲突被轻描淡写），不触发重跑，带问题继续。残留问题落盘（`schema_guard_report.json`、`run_review_report.json`）并注入终审输入——但写不写进判决书靠终审按提示词执行，非代码强制。

**它的实际效果/问题。** 设计健康，是全链路引用合法的硬闸门。但有两处软肋要诚实说：①一致性问题（含高严重度冲突被抹平）不自动重跑，只靠终审自觉；②落盘是兜底的铁证，"一定出现在报告里"非代码担保。这两点同源——把关权交还了提示词。

**我的建议：留（理由：纯代码把关引用合法性，是唯一不依赖 AI 自律的硬墙）。** 需要老板拍的是：一致性问题（冲突被抹平）是否也该升级成强制重跑，而非只靠终审自觉？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：orchestrator.py:7272-7582（`_run_schema_guard`）；调用与重试触发 orchestrator.py:775-818（条件 `schema_report.structural_issues or schema_report.missing_fields`）；系统说明书 §2.7。关键真名：`SchemaGuardReport`（`passed`/`structural_issues`/`consistency_issues`/`missing_fields`）；`_bad_refs`、`_high_severity_conflict_candidates`、`_composite_submetric_issue`。

---

**卡 6 · 初判【珠玉】：修订者——编辑岗，不抹平冲突也不重写**

**它是什么、哪来的。** 修订者（Reviser）吸收批评者、风险哨兵、结构检查的意见，产出修订稿。提示词把角色定得很克制："你是一名编辑，不是重写者"——修复问题、强化论证，但明确禁止为让报告显得完美而把高严重度冲突改没了。来历明确（§2.8、reviser.md）。

**它现在怎么运作。** 它收主论点原文、批评意见、风险清单、结构检查问题清单、证据索引。它对每个非淘汰的竞争假说必须留恰好一条回应（覆盖候选/主线/未解决/分裂四种状态），省略任何一个等于让假说在判断书里消失，属抹平冲突。

**它的失败会怎样（全系统独一份）。** 它有软着陆：失败时退回上游已验证的原稿——把修订前的主论点原稿当结果用（原稿已独立通过同一套合约校验，退回不等于放水），但打 `degraded_fallback` 标记，一路传终审质量说明（`reviser_degraded_unrevised_thesis`），不会被当成正常结果悄悄放行。注意它和"模板兜底"不同：它是退回原稿，全系统只它这么干，因为上游约 25 分钟的采集到风险全推倒重来不划算。

**它的实际效果/问题。** 设计克制且诚实，证据引用纪律和论点建构一致（"看到 m7_quarterly_total 不代表某 ref 是合法"）。唯一要留意的：它"退回原稿"的兜底意味着极端失败时读者拿到的是未修订版——风险清单可能不完整，但被显式标注，未静默放行。

**我的建议：留（理由：编辑定位 + 退回原稿兜底，是全链路唯一不崩也不降质的退路）。** 需要老板拍的是：退回原稿时风险清单可能不全，是否接受这种"标注后放行"的透明度？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：reviser.md 全文；系统说明书 §2.8（218-229 行）。关键真名：`governance_input`；竞争假说回应纪律（reviser.md:15-22）；降级标记 `reviser_degraded_unrevised_thesis`（§2.8）。

---

**卡 7 · 初判【珠玉】：终审——写判决书的最后一棒，但不能给自己放行**

**它是什么、哪来的。** 终审（Final Adjudicator）写最终判决书，同时兼两个身份：`quality_gate`（内部质量闸门：能否发布、证据是否可追溯、风险是否保留）和 `reader_final`（给普通读者的人话结论：状态、价格、赔率、行动、失效条件）。来历明确（§2.9、final_adjudicator.md），提示词强调两个身份不能混写——不能把内部审批话术当读者首屏。

**它现在怎么运作。** 它是整链路看材料最全的一站（修订后主论点+批评+风险+结构检查+修订说明）。数字忠实走 `fact_card`（代码装配的"本段允许出现的数字菜单"），终审只从卡与输入里选用数字，写错数字是装配 bug 由系统修管道而非打回模型。

**它被明确禁止什么。** 终审不能给自己放行：不能跳过批评/风险/结构检查另起炉灶，不能重新调原始源绕过前面判断，不能为了顺滑抹平冲突。它的"终"指链路最后一棒，不是有权推翻前面所有独立判断——这是最易被误解、实则最关键的一条。

**它的失败会怎样。** 最多 2 次尝试；尝试耗尽不再整链崩溃，降级为 `approval_status=rejected` 的兜底裁决并留痕，报告只能作内部审阅材料。

**它的实际效果/问题。** 双身份分离、`fact_card` 防编造数字、降级保留留痕——健康。同源软肋：内部质量闸门"写不写进报告"部分依赖提示词自觉（与 Schema Guard 一致）。整体是系统原则的忠实收口。

**我的建议：留（理由：双身份 + fact_card 数字闸门 + 降级留痕，健康收口）。** 需要老板拍的是：质量闸门结论"是否出现在报告"仍部分靠自觉，是否要像引用校验那样加硬约束？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：final_adjudicator.md 全文；系统说明书 §2.9（232-245 行）。关键真名：`quality_gate`/`reader_final` 双身份；`fact_card`（数字菜单，final_adjudicator.md:119）；`stance_label` 五枚举（:37-39）；降级 `approval_status=rejected`（§2.9）；`claim_ledger` 代码装配（:298-299）。

---

**卡 8 · 初判【珠玉】：新闻事件支线——外部世界的材料，被两道边界卡死不进主链**

**它是什么、哪来的。** 新闻事件支线有两位：事件卡解读员（Event Card Interpreter）把采集到的新闻（标题/来源/日期/正文摘录）加工成带认识论标签的结构化卡片；事件总结员（Event Section Summary）把这批卡收束成一段"外部世界对照"总结，放进报告展示位。来历明确（§2.10、两个 md）。这是第二条完全独立的链，从头到尾不进数据判断链路。

**它现在怎么运作。** 事件卡解读员只看三样：单条新闻材料本身、允许引用的九条金融传导渠道清单、当前竞争假说清单（仅作"这条新闻支持/挑战谁"的引用背景，不当证据）。总结员只看已生成的事件卡。机制只能写假设（"该事件可能通过××渠道影响××"），禁止写成已发生因果；事实与解读必须分开写。

**它被明确禁止什么。** 两道边界卡死：①看不到 L1-L5 任何指标/读数/结论；②产物必须不能变成 L1-L5 证据引用、必须不能反向写入主链（`must_not_feed_back`）。还有严格时间纪律：不引用晚于"有效日期"的信息，防回测场景"事后诸葛亮"穿越。这正是"弱来源不进主链"原则的具体落地。

**它的失败会怎样。** 不影响主链。单张卡失败进失败清单；有效卡不足两张→总结直接跳过留白（`insufficient_cards`/`no_cards`），不用套话硬凑——"宁缺毋滥"的体现。

**它的实际效果/问题。** 双道边界卡死、宁缺毋滥，设计健康。但一处深层差距要诚实标出（说明书 2.11 已知缺口）：事件卡本身没有多空辩论的对抗治理——数据侧是六站对抗的"最终品"，事件侧底账只有单卡解读加汇总，巡逻成果有对账但无多空辩论。这条缺口留给老板结合第三层卡一起看。

**我的建议：留（理由：双边界 + 时间纪律 + 宁缺毋滥，是弱来源隔离的范本；事件卡缺对抗治理是另一笔账）。** 需要老板拍的是：事件卡无多空辩论的对抗治理，要不要像数据侧那样补一道？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：event_card_interpreter.md、event_section_summary.md 全文；系统说明书 §2.10（248-260 行）、§4.7（396-412 行，三层架构与"事件永不进第一层"）。关键真名：`must_not_feed_back`；`needs_data_confirmation` 钩子；`effective_date` 时间纪律；`insufficient_cards`/`no_cards` 留白（event_section_summary.md:18）。

---

**卡 9 · 初判【珠玉】：第三层综合裁决 IA——全系统唯一同时看见两边的站**

**它是什么、哪来的。** 第三层综合裁决（Integrated Adjudicator，简称 IA）是全系统唯一"同时看见数据侧和事件侧"的一站。它不在主编排器里，由管线尾部（src/main.py → integrated_synthesis_report.py）从磁盘读数据判决、事件卡、研究成果架，组装后做一次独立 AI 对质。来历明确（§2.11、integrated_adjudicator.md）。

**它现在怎么运作。** 它把第一层数据判决和第二层事件卡放进同一证据权限框架对质：哪些事件解释了数据、哪些数据检验了事件、哪些两边都够不着；用竞争假说和主要矛盾产出综合裁决正文。它收到：数据判决本体（终审姿态/理由/主要矛盾/风险/失效条件——数据判决是锚，它无权改判）、至多 10 张事件卡、至多 3 份调查报告、至多 8 道跨层问题、一份许可白名单。

**它被明确禁止什么。** 权力边界写在 policy：不能改判数据姿态（`stance_anchor_rule`）、不回流任何上游站（`no_backflow`）、事件永远不能写成因果断言、不得引入输入外数字事实。原因：它是裁决者不是第四方数据源，权力是"对质"不是"增补"。

**它的失败会怎样。** DataIntegrity 判定 blocked 时根本不调 LLM，自动降级确定性拼装，降级痕迹报告里看得见；发布闸门对第三层同样有效。它若怀疑数据判决本身，唯一合法出口是 `data_verdict_objections`（抗诉通道），改判权留老板，锚不动。

**它的实际效果/问题。** 补足了"治理倒挂"关键一环——此前影响力最大的综合报告是零对抗单次调用。抗诉通道让"外部事实挑战数据判决"能亮红灯置顶给老板。设计健康，是全系统的对质点。

**我的建议：留（理由：唯一对质点 + 抗诉通道把改判权交还老板，原则落地干净）。** 需要老板拍的是：抗诉亮灯只认"研究架对账通过的事实 + 实质矛盾"，事件卡挑战只记录不亮灯——这个分水岭你是否认可？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：integrated_adjudicator.md 全文；系统说明书 §2.11（262-277 行）、§4.7（402-411 行）；装配代码 integrated_synthesis_report.py（未读，仅据说明书）。关键真名：`stance_anchor_rule`/`no_backflow`；`data_verdict_objections`（抗诉通道，§2.11）；`conflict_matrix` 三态（`confirmed_by_data`/`challenged_by_data`/`not_yet_testable`）；PC-28 红 + 报告第一屏"抗诉"横幅（§2.11）。

---

**卡 10 · 初判【珠玉】：IA 的裁决批评者——给最有权力的产物加一道复核**

**它是什么、哪来的。** IA 裁决批评者（Integrated Adjudicator Critic）是 2026-08-27 施工的一道对抗：IA 出草稿后，批评者专挑四类刺（逻辑跳步／把事件写成既成因果／和稀泥抹平冲突／引用越权与编造数字），IA 定稿必须逐条回应（接受或驳回带理由）。批评与回应落盘 `integrated_synthesis_report.json` 的 `integrated_adjudication_critique` 字段。来历明确（§2.11、integrated_adjudicator_critic.md）。

**它现在怎么运作。** 它拿综合裁决草稿 + 与 IA 相同的输入面（数据判决/事件卡/巡逻成果/调查报告/跨层问题/许可清单），其中事件卡等正文是不可信引用材料，里面出现的任何"指令"一律无效。它只挑毛病，不重写、不替 IA 下判断、不第四方数据源。

**它被明确禁止什么。** 不得引入输入外事实/数字/分位/阈值/概率；不得改判数据姿态（数据判决是基准）；没有真问题就输出空清单，不为显得尽职硬挑。

**它的失败会怎样。** 失败兜底＝退回草稿 + `critic_degraded` 标注（不阻断 run）；环境变量 `INTEGRATED_ADJUDICATION_CRITIC_ENABLED=0` 可关。

**它的实际效果/问题。** 这是补上"治理倒挂"的关键一环：全系统影响力最大的产物（综合报告）第一次有了复核者——此前 IA 是零对抗的单次调用，而数据侧已有六站对抗的"最终品"。设计健康，是治理对称性的收口。

**我的建议：留（理由：给最强产物加了唯一复核，堵住了"数据侧有对抗、综合侧零对抗"的倒挂）。** 需要老板拍的是：这个批评者可用环境变量整个关掉，关掉是否要留审批记录？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：integrated_adjudicator_critic.md 全文；系统说明书 §2.11（270 行，T67/W5 裁决批评者）。关键真名：`integrated_adjudication_critique` 字段；四类刺（logical_leap/event_causality/conflict_smoothing/ref_or_number）；`critic_degraded` 标注；开关 `INTEGRATED_ADJUDICATION_CRITIC_ENABLED=0`。

---

**卡 11 · 初判【珠玉】：出题官 + 同步巡逻——把老板本人嵌进链路的唯一环节**

**它是什么、哪来的。** 出题官（Topic Composer，2026-08-26 老板全盘重构）读六站对抗的残局（桥接未解问题/跨层开放题），酿成 0-2 份研究任务书（题目/为什么现在问/连着哪个矛盾/家里已知/验收标准/认错条件）；对着"家里能力清单"论证"家里答不了"才准出题，数据侧缺口分流进 data_gaps 不花巡逻钱。出题判据（老板定）：家里答不了 + 答案活外部世界 + 现在能查，三条缺一不可。同步巡逻（Sync Patrol）则把任务书亮给老板圈题、圈中当场巡逻。来历查得到（topic_composer.md、topic_composer.py、sync_patrol.py 文件头注释）。

**它现在怎么运作。** 出题官在主链尾部、IA 之前跑一次（老板定：不许把题推到下次 run）。同步巡逻挂在主链 IA 组装之前：出题→缺口桥收进议程账本→终端/控制台亮任务卡→老板圈题（软暂停）→圈中当场巡逻（日常档经费 300 万 token 封顶，实测一次百万级）→成果两落（run_dir 留痕 + 研究成果架跨 run 累积，IA 按 effective_date 时点过滤取用）。没人理就超时跳过，run 永不等人；回测整体跳过（时点纪律）。

**它的失败会怎样。** 形状校验不过整体降级为空；经费卡封顶、耗尽硬停，半成品如实标注。

**它的实际效果/问题。** 这是把老板本人嵌进链路的唯一环节（圈题软暂停）。"现在能查"作为铁律，防把明天才发生的财报伪装成今天可验收的题；08-26 重构教训是"好题是写出来的不是捡来的"。一处要老板定：回测 run 整体跳过巡逻，意味着历史截面下事件侧无可补——这是有意纪律，但需老板认可。

**我的建议：留（理由：把老板圈题做成硬闸门 + 现在能查铁律，是研究与投资闭环的入口）。** 需要老板拍的是：回测 run 整体跳过巡逻（事件侧历史截面不可补），你是否认可这条时点纪律？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：topic_composer.md、topic_composer.py:1-25（文件头注释）、sync_patrol.py:1-26（文件头注释）；系统说明书 §2.11（268-274 行，出题官/巡逻/抗诉）、§4.7、代码模块地图 event_research 段（460-475 行）。关键真名：ARTIFACT_NAME=`research_topics.json`、MAX_TOPICS=2、SYNC_PATROL_BUDGET_CAP=3_000_000；软暂停 DEFAULT_PAUSE_TIMEOUT_SEC=3600；RESEARCH_SHELF_PATH=`output/event_research/research_shelf.json`。

---

**卡 B-补 · 初判【待定（大概率杀）】：终审的数字菜单（fact_card）**

**它是什么、哪来的。** 今年 9 月 2 日"人话工程"施工时造的。当时的考虑：终审写判决要用数字，怕它写错，于是代码提前把"这段允许使用哪些数字"整理成一份菜单——每个数字带上指标名、读数、权限等级——喂给终审，意思是"你只许从菜单里挑数字"。

**它现在怎么运作。** 终审收到的材料包里，同样的数据放了两份：菜单摘要约一万字，证据原文约九万字。菜单只是原文的防偏科摘要，终审同时也能读到原文。

**它现在的问题。** 实测发现三个毛病。第一，重复：菜单是多余的第二份数据。第二，它不进报告、也不参与任何校验，说好的"防错"没有兑现成任何一道检查。第三，最要命：菜单只收了数字的读数，把数字背后的推理过程全弄丢了，还让系统内外误以为"合法数字只有菜单里这些"——实际上合法范围是终审收到的全部材料。你对它的原话评价："这卡片就是狗屎"。

**注意与 B-7 的出入。** B-7（终审卡）按说明书口径把这份菜单描述为"防写错数字"的健康设计；本卡按 9 月 15 日的实测和你的裁决记录呈现另一面。两卡冲突如实保留，不抹平——这正是这份清单要你裁的地方。

**我的建议：大概率杀。** 但它背后那个真问题——"终审的数字怎么保证不写错"——菜单答错了，问题本身还悬着。所以列入清单由你裁：杀掉之后，数字忠实这件事是交给 C-35 的"重算制"一并解决，还是另立设计。

**老板盖章：【杀·缓执行】**（2026-09-17 盖）——不单杀留新洞；与 C-35 的"可追溯+可重算"改造同批设计，另立重做工单。

> 存档（AI 核查用，不用读）：fact_card，09-02 人话工程造（T70 P-B），混在喂给终审的治理材料包（governance_input）里；实测记录：菜单 9,789 字符 vs 原文 92,285 字符。

## C 区 · 闸门（43 张）

总览：身份比对类约 37 条（初判珠玉，该留）；语义代理类 3 条（初判粪土：C-31、C-35 的数字子项、C-39）；待定 2 条（C-10、C-26 的 reason 子项）；PC-27 已退役不分类。每张卡都写了它查什么、怎么查、会不会误伤。附录里 6 条管道外子系统校验器（CLI、控制台等）均为身份比对类，不单独成卡。

**卡 1 · 初判【珠玉·身份比对类】：critic/risk 分料身份检查（PC-01）**

**它是什么、哪来的。** 2026 年根本审查总报告 A1 病的 C3 版收口。原病是 critic 站和 risk 站共用一份治理输入、会互相串味。这条检查想防"两站材料没隔离"。来历清晰（03 报告 §3 A1）。

**它现在怎么运作。** 在常设检查 A 包（persistent_checks_a.py）触发，只读 critic/risk 两站的 governance_input。三项判定全是机器能定的：critic 的 thesis_main 非空；risk 包不得残留任何 thesis_* 键（分料泄漏）；两包 governance_input 序列化后不得逐字节相同（必须确实分成两份）。任何一项不过 → 红卡。

**它的实际效果/问题。** 纯身份比对：比较两份产物的序列化是否相同、特定键名是否出现/缺失。不读任何文字意思，零误伤面。属于老板说的"这是谁的/形状对不对"。

**我的建议：留（机械隔离，零误伤，正是该留的一类）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc01 / src/agent_analysis/persistent_checks_a.py:196；依据 persistent_checks_a.py:1-228 与 03 报告 §3 A1。

---

**卡 2 · 初判【珠玉·身份比对类】：治理站引用全集对账（PC-02）**

**它是什么、哪来的。** 03 报告 A2 病（C3 版）。原病是 thesis/synthesis 里点名的证据引用，到了 critic/reviser/final 各治理站却对不上账。想防"引用在总稿有、到分站丢"。

**它现在怎么运作。** 收集 thesis_draft、synthesis_packet 里全部 evidence_refs，与 evidence_index 的键求交集得到"该站必须带"的引用集，再逐站核对 governance_input 的 key_evidence_refs 是否包含。缺哪条就报哪站缺几条。不过 → 红卡。

**它的实际效果/问题。** 这是最典型的"引用的东西在不在账上"：只做集合成员判定（ref ∈ evidence_index 键集），机器确定性成立。零误伤——合法引用天然在账上，丢了的才是真病。

**我的建议：留（引用对账，零误伤，核心身份比对）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc02 / src/agent_analysis/persistent_checks_a.py:331；依据 persistent_checks_a.py:235-397。

---

**卡 3 · 初判【珠玉·身份比对类】：final 只收修订稿+修订说明+机器对账（PC-03）**

**它是什么、哪来的。** A3 病 + 老板 2026-08-16 重裁。原病是终审输入混进 thesis 原稿、且修订说明对不上。想防"原稿回潮"和"改了啥说不清"。

**它现在怎么运作。** 三项机器核对：final 的 thesis_* 字段与 analysis_revised.revised_thesis 对应字段逐字节（canonical JSON）相同；revision_summary 非空；revision_claimed_fields（代码 diff 装配的"改了哪些字段"）与"thesis_draft vs revised_thesis 顶层字段 diff"重算结果逐项一致；且 final 不得再含 thesis_original 键。任一不过 → 红卡。

**它的实际效果/问题。** 全部是逐字节相等、键存在性、集合相等——身份比对，不读意思。零误伤面。08-16 重裁后口径明确（原稿进 final 即供给回潮）。

**我的建议：留（修订稿对账，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc03 / src/agent_analysis/persistent_checks_a.py:461；依据 persistent_checks_a.py:404-560 与 08-16 重裁注释。

---

**卡 4 · 初判【珠玉·身份比对类】：IA ref_authority unknown 占比告警（PC-04）**

**它是什么、哪来的。** 03 报告 A5 病：基线 run 里 34 条引用权限 23 条标 unknown（67.6%），等于许可闸门半瘫。想给"引用权限大面积失联"亮灯。

**它现在怎么运作。** 从 IA 的 prompt payload 读 ref_authority 字典，数 usage=="unknown" 的条数占比。阈值写死 50%（注释说明来自 03 报告基线，可日后按新 run 复核）。占比 >50% → 红卡告警；否则过。

**它的实际效果/问题。** 纯计数/枚举判定（usage 字段值是否等于字符串 unknown）。身份比对，零误伤。它是"告警"性质——亮灯提醒人去看权限登记，不删任何结论。

**我的建议：留（计数告警，零误伤）。** 老板可拍：50% 阈值是否要随新 run 调整？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc04 / src/agent_analysis/persistent_checks_a.py:604；依据 persistent_checks_a.py:29-31,604-625。

---

**卡 5 · 初判【珠玉·身份比对类】：委托调查 vs 进 IA 报告数对账（PC-05）**

**它是什么、哪来的。** 03 报告 A6 病：委托出去的调查数和最终进 IA 的报告数对不上（有静默缺席）。想防"派了活没交差还假装交了"。

**它现在怎么运作。** 读 inquiry_router_output.agent_specs（委托数）、IA payload 的 allowed_investigation_ids（非 stub 数）、investigation_reports（报告数）、investigation_gaps（占位失败数）。判定：委托数 == 允许数 + 占位失败数，且报告数 == 允许数。不等 → 红卡。

**它的实际效果/问题。** 纯整数计数等式，身份比对，零误伤。代码注释也承认"可能是真静默缺席，也可能是占位装配未接线"——它只报数不对，把定位扔给人。

**我的建议：留（计数对账，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc05 / src/agent_analysis/persistent_checks_a.py:632；依据 persistent_checks_a.py:632-671。

---

**卡 6 · 初判【珠玉·身份比对类】：CI 材料闭合性 + 立场字段 + 事件侧隔离（PC-06）**

**它是什么、哪来的。** 03 报告 A7 病 + 老板 2026-08-25 拆洞裁决（调查员材料不得来自事件侧）。想防"调查材料是空壳、或夹带事件侧素材、或偷偷写了仓位立场"。

**它现在怎么运作。** 扫 controlled_investigation 各 inv 的 [M#]...[/M#] 材料块：①块必须能当 JSON 解析（闭合性）；②块头不得写死引用事件侧 artifact（cross_layer_questions/event_* 等 6 个名单）；③块正文不得出现立场/仓位字段 token（dominant_side/action_implication/不宜重仓/触发核心仓）。任一 → 红卡。

**它的实际效果/问题。** 三项全是身份/形状判定：JSON 可解析（结构）、artifact 名是否出现在块头（字符串匹配）、禁令牌是否出现在正文（字符串匹配）。不读意思——它查的是"这个字符串在不在"，不是"这段话什么立场"。零误伤面。

**我的建议：留（结构+禁令牌比对，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc06 / src/agent_analysis/persistent_checks_a.py:678；依据 persistent_checks_a.py:54-66,678-731。

---

**卡 7 · 初判【珠玉·身份比对类】：事件字段恒空（PC-07）**

**它是什么、哪来的。** 03 报告 A9 病（反向断言）。三明治架构要求：事件永不进第一层治理链，bridge/critic/risk/reviser/final 的 event_refs/key_event_refs 必须恒空。想防"事件漏进第一层"。

**它现在怎么运作。** 递归找这五个站 payload 里的 event_refs / key_event_refs 容器，只要任一非空就报"站:路径 非空=值"。全空 → 过；有值 → 红卡。

**它的实际效果/问题。** 纯"容器是否为空"判定，身份比对，零误伤。事件 ref 是结构化键，要么有要么没有，机器确定性。

**我的建议：留（空值判定，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc07 / src/agent_analysis/persistent_checks_a.py:738；依据 persistent_checks_a.py:175-186,738-755。

---

**卡 8 · 初判【珠玉·身份比对类】：同名指标多值检测（PC-08）**

**它是什么、哪来的。** 03 报告 A11 病："一处错处处错"——同名指标在不同位置数值不一致，会污染全链。想防数值矛盾供给。

**它现在怎么运作。** 收集 L1-L5 与 layer_cards 里指定指标键（pe/forwardpe/obv 等 8 个）的数值叶，排除历史月序列（只比"当前值"），同一键出现 >1 个不同数值就报冲突。无冲突 → 过；有 → 红卡。

**它的实际效果/问题。** 比的是数值本身（canonical 字符串相等），身份比对。零误伤——同名同窗出现两个真不一样的当前值，本来就是数据矛盾，值得拦。已排除月序列误报（注释见 persistent_checks_a.py:770）。

**我的建议：留（数值一致性，零误伤，且已排掉月序列假阳）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc08 / src/agent_analysis/persistent_checks_a.py:787；依据 persistent_checks_a.py:38-47,762-818。

---

**卡 9 · 初判【珠玉·身份比对类】：约束/指令引用键存在性（PC-09）**

**它是什么、哪来的。** 03 报告 B1/B10 病：提示词点名要某键（如 raw_data/NO_DATA_AVAILABLE），payload 里却没这键，属提示词与数据对不上。想防"说了要、实际没给"。

**它现在怎么运作。** 只扫提示词的"指令区"（Runtime Input 头之前，剔除 few-shot 示例段），提取点名的键名；若某站提示词点名了 INSTRUCTION_REF_KEYS，就去该站 payload 递归查这个键是否真实存在。点名了却无键 → 红卡。

**它的实际效果/问题。** 字符串在提示词出现 + 键在 payload 存在，是身份比对。注释专门区分了"数据区/模型产物自带的键名不算约束点名"——已做范围隔离，零误伤面。

**我的建议：留（键存在性对账，零误伤，且已做指令区隔离）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc09 / src/agent_analysis/persistent_checks_a.py:843；依据 persistent_checks_a.py:49-52,825-871。

---

**卡 10 · 初判【待定】：事件站措辞口径统一检测（PC-10）**

**它是什么、哪来的。** 03 报告 B2，老板 2026-08-16 裁决：旧 A（规定句首用词）、旧 B（"措辞完全由你决定/不设检查"元话语）都作废，统一为"弱来源用据报道/该媒体称/仅标题说清分寸"这一语义要求。想防旧口径回潮。

**它现在怎么运作。** 用三条正则扫事件站提示词的指令区：①出现"必须以/必须用据报道/该媒体称开头"等硬规定 → 报旧 A；②出现"措辞完全由你决定/不设检查"等元话语 → 报旧 B；③必须出现"用……据报道/该媒体称……这类限定语"的统一口径句。任一不满足 → 红卡。

**它的实际效果/问题。** 这条用"某字符串逐字出现"来判断提示词是否合规——机制上和语义代理类的指纹同源。但它扫的是**提示词模板的指令区**（不是 AI 产出意思），且命中的是明确的旧说法/统一说法，误伤 AI 分析内容的风险低。仍建议老板拍板：它是"格式合规"还是"语义代理"？

**我的建议：待定（机制是逐字短语匹配，但作用在提示词模板、零误伤 AI 产出；倾向留，但请老板确认归类）。** 需要老板拍：PC-10 该算身份比对还是语义代理？

**老板盖章：【留】**（2026-09-17 盖）——归身份比对类（它查的是提示词模板，不是 AI 产出，误伤面≈0）。不动代码。

> 存档：_check_pc10 / src/agent_analysis/persistent_checks_a.py:878；依据 persistent_checks_a.py:890-918 与 B2 裁决注释。

---

**卡 11 · 初判【珠玉·身份比对类】：输出示例指标本层存在性（PC-11）**

**它是什么、哪来的。** 03 报告 B4 病：提示词里的"正确/输出示例"点名的指标，本层数据里其实不存在（示例骗人）。想防示例张冠李戴。

**它现在怎么运作。** 从各层 prompt 的 Example/结构示例段提取点名的 function_id 集合，与本层 payload 的 layer_raw_data 键、key_metrics、core_signals 取差集。示例点名但本层没有 → 红卡。

**它的实际效果/问题。** 集合差集（示例 id ∈ 本层数据键集），身份比对。零误伤——示例点名的指标本就该在本层数据里。

**我的建议：留（示例 id 存在性，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc11 / src/agent_analysis/persistent_checks_b.py:247；依据 persistent_checks_b.py:226-288。

---

**卡 12 · 初判【珠玉·身份比对类】：manual_overrides 陈旧占位日期（PC-12）**

**它是什么、哪来的。** 03 报告 B5 病：manual_overrides 里塞着 2022 年或一年以上的陈旧占位日期，假装是近期数据。想防"假新数据"。

**它现在怎么运作。** 读 run 数据日期（context_brief.data_summary），逐层查 manual_overrides.date：不可解析、或年份==2022、或距 run 日期 >365 天 → 陈旧 → 红卡。

**它的实际效果/问题。** 日期解析 + 数值比较（年份、天数差），身份比对。阈值 365 天是施工任务书收口值（非 03 原文数字，代码注释已声明）。零误伤。

**我的建议：留（日期比对，零误伤）。** 老板可拍：365 天阈值是否合适？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc12 / src/agent_analysis/persistent_checks_b.py:295；依据 persistent_checks_b.py:130-153,295-334。

---

**卡 13 · 初判【珠玉·身份比对类】：percentile 取值域混用（PC-13）**

**它是什么、哪来的。** 03 报告 B7 病：percentile 字段同一批数据里既用 0-1 又用 0-100 口径，没声明单位，会算错。想防口径混用。

**它现在怎么运作。** 收集各层/层卡的 percentile 字段数值；若同时出现 ≤1 和 >1 的值，且这些字段没有带 unit/scale 声明 → 报混用。否则过。

**它的实际效果/问题。** 数值范围判定（≤1 vs >1）+ 兄弟字段是否声明 scale，身份比对。零误伤——真混用且没声明才拦。

**我的建议：留（取值域+声明比对，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc13 / src/agent_analysis/persistent_checks_b.py:373；依据 persistent_checks_b.py:341-407。

---

**卡 14 · 初判【珠玉·身份比对类】：context_brief 日期 vs 指标日期（PC-14）**

**它是什么、哪来的。** 03 报告 B8 病：原口径逼"所有指标日期==brief 日期"，误伤月度滞后指标；2026-08-16 改成"不晚于运行时点即合法"。想防未来数据泄漏。

**它现在怎么运作。** 每层：brief 必须声明"各指标实际数据日期以各自 data_quality 为准"（缺声明 → 红）；再逐指标日期解析，凡晚于 brief 运行时点（未来日期）→ 红卡（未来数据泄漏）。

**它的实际效果/问题。** 日期解析 + 大小比较，身份比对。08-16 改后已允许月度滞后（只拦晚于运行时点），零误伤面。这是"日期对不对"的典型。

**我的建议：留（日期比较，零误伤，且已修掉月度滞后假阳）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc14 / src/agent_analysis/persistent_checks_b.py:464；依据 persistent_checks_b.py:458-500。

---

**卡 15 · 初判【珠玉·身份比对类】：high_severity 容器名与 severity 字段一致性（PC-15）**

**它是什么、哪来的。** 03 报告 B11 病（前半）：high_severity_* 容器不是数组、或里面 severity 填了非法值。想防结构错。

**它现在怎么运作。** 读 synthesis_packet，凡以 high_severity 开头的键必须是 list；每条元素的 severity 必须是 high 或 medium（low/缺失 → 红卡）。

**它的实际效果/问题。** 类型判定（是否 list）+ 枚举值判定（high/medium），身份比对。零误伤。

**我的建议：留（类型+枚举，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc15 / src/agent_analysis/persistent_checks_b.py:507；依据 persistent_checks_b.py:31-32,507-556。

---

**卡 16 · 初判【珠玉·身份比对类】：他站产物 null 占位/禁用标记进输入（PC-16）**

**它是什么、哪来的。** 03 报告 B12 病：别的站的产物键或 _disabled/forbidden_as_core_ref 标记以非空值混进某站输入（供给污染）。想防跨站串味。

**它现在怎么运作。** 递归扫各站 payload：下游站产物键（thesis_draft/critique/final_adjudication 等 12 个名单）以非空值出现、或 _disabled/forbidden_as_core_ref 标记键非空 → 红卡。注释明确排除了合法项（data_quality.forbidden_use 是数据纪律、counter_thesis 的 forbidden_context_refs 是反方边界）。

**它的实际效果/问题。** 键名 + 非空判定，身份比对。已显式排除两类合法同名键，零误伤面。是"形状对不对/这是谁的"典型。

**我的建议：留（键名+非空，零误伤，且已排除合法同名键）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc16 / src/agent_analysis/persistent_checks_b.py:563；依据 persistent_checks_b.py:35-48,563-617。

---

**卡 17 · 初判【珠玉·身份比对类】：bridge 冲突矩阵行完整性（PC-17）**

**它是什么、哪来的。** 03 报告 B13 病：bridge 冲突矩阵缺 A-M 某些行（装配漏行）。想防矩阵残缺。

**它现在怎么运作。** 在 bridge prompt 的"检查冲突矩阵 A-M"段后 40 行内，用正则抽表格行的首字母（| A | 等），与期望集合 A-M 求差。缺哪行 → 红卡。

**它的实际效果/问题。** 正则行字母提取 + 集合差，身份比对。零误伤——矩阵就该有 A-M 全行。注释已说明只取该段后 40 行避免误收正文。

**我的建议：留（行完整性，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc17 / src/agent_analysis/persistent_checks_b.py:624；依据 persistent_checks_b.py:624-657。

---

**卡 18 · 初判【珠玉·身份比对类】：L4 数据陈旧 + 回购逐字重复行（PC-18）**

**它是什么、哪来的。** 03 报告 C3（available 却陈旧）+ C5（回购明细逐字重复行）。想防"标可用实则过期"和"复制粘贴行"。

**它现在怎么运作。** L4：get_m7_buyback_flow 及各指标若 availability==available 但数据日期早于 run 日期 >365 天 → 红卡（C3）；回购明细行做 JSON 序列化逐字去重，重复行 → 红卡（C5）。

**它的实际效果/问题。** 日期比较（C3）+ JSON 逐字节重复（C5），全是身份比对。零误伤——逐字相同的行本来就是重复，过期可用本来就是病。

**我的建议：留（日期+逐字重复，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc18 / src/agent_analysis/persistent_checks_b.py:680；依据 persistent_checks_b.py:664-760。

---

**卡 19 · 初判【珠玉·身份比对类】：canon 名 vs 输入 metric_name（PC-19）**

**它是什么、哪来的。** 03 报告 C12 病：IndicatorCanon 注册名和 payload 实际 metric_name 对不上（改名不同步）。想防名称漂移。

**它现在怎么运作。** 从各层 prompt 的 IndicatorCanon 段解析 function_id→metric_name 映射，与各层 payload layer_raw_data 的 metric_name 逐条字符串比较。不一致 → 红卡。

**它的实际效果/问题。** 字符串相等比较，身份比对。零误伤——注册名和实际名就该一致。

**我的建议：留（名称相等，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc19 / src/agent_analysis/persistent_checks_b.py:789；依据 persistent_checks_b.py:767-823。

---

**卡 20 · 初判【珠玉·身份比对类】：已落地三件复核（PC-20）**

**它是什么、哪来的。** 三件已落地的修复（FIX-1 重试反馈要带真实定位、FIX-2 ESS 卡带 needs_data_confirmation、摊开器测试 ≥15）。想防修复回潮。

**它现在怎么运作。** 三项：①ESS 输入卡必须含 needs_data_confirmation 键（键存在）；②非空 retry_feedback 必须含真实错误定位指纹（正则匹配 行/列号、JSONDecodeError、L1-L5 字段路径、validation error、规则路径——这是代码生成的报错格式）；③tests/test_context_spread.py 存在且收集数 ≥15（文件存在 + AST 计数）。任一不过 → 红卡。

**它的实际效果/问题。** ①键存在、③文件存在+计数，都是身份比对。②用正则查反馈文本是否含定位指纹——但反馈是**代码生成**回喂 AI 的报错信息，不是 AI 自由发挥，命中的是指定格式，误伤 AI 产出风险低。整体零误伤面。

**我的建议：留（键存在+文件计数+代码报错格式，零误伤）。** 老板可拍：②的定位指纹正则是否过严（真报错但换个写法会被漏判）？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc20 / src/agent_analysis/persistent_checks_b.py:880；依据 persistent_checks_b.py:62-70,830-929。

---

**卡 21 · 初判【珠玉·身份比对类】：事件站输出契约去重（PC-21）**

**它是什么、哪来的。** 03 报告 B3 病：output_contract 已从 payload 移除，若回潮属重复供给。想防旧结构回潮。

**它现在怎么运作。** 扫 event_card_interpreter.* 与 event_section_summary 两站的 payload，若携带 output_contract 键 → 红卡（B3 回潮）。

**它的实际效果/问题。** 单键存在性判定，身份比对。零误伤。

**我的建议：留（键存在性，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc21 / src/agent_analysis/persistent_checks_b.py:948；依据 persistent_checks_b.py:948-979。

---

**卡 22 · 初判【珠玉·身份比对类】：指标清单与 Runtime Input 去重（PC-22）**

**它是什么、哪来的。** 03 报告 B6 病：指标清单段重复供给了完整 data_quality 块（应只在 Runtime Input 一份）。想防重复供给。

**它现在怎么运作。** 切出各层 prompt 的"当前层指标清单"段，若段内含字面 '"data_quality"' → 红卡（B6 回潮）。

**它的实际效果/问题。** 字符串在指定段出现判定，身份比对（查的是"这个结构块在不在重复位置"）。已精确切到指标清单段，零误伤面。

**我的建议：留（段内结构块比对，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc22 / src/agent_analysis/persistent_checks_b.py:982；依据 persistent_checks_b.py:982-1007。

---

**卡 23 · 初判【珠玉·身份比对类】：高严重度冲突容器条目集一致（PC-23）**

**它是什么、哪来的。** 03 报告 B11 另一半：high_severity_conflicts 与 high_severity_typed_conflicts 条目集必须相等，且 thesis 必须保留全部编号。想防编号对不上。

**它现在怎么运作。** 读 synthesis_packet 两容器，按 conflict_id 取集合；不等 → 红卡；再核 thesis.retained_conflicts 是否含全部编号，缺 → 红卡。

**它的实际效果/问题。** 集合相等 + 子集判定，身份比对。零误伤——编号本就该一致且被保留。

**我的建议：留（集合对账，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc23 / src/agent_analysis/persistent_checks_b.py:1010；依据 persistent_checks_b.py:1010-1062。

---

**卡 24 · 初判【珠玉·身份比对类】：L3 持仓锚计数与滞后声明（PC-24）**

**它是什么、哪来的。** 03 报告 C1 病：持仓锚必须分名分账（provider 总数/解析数/as-of 与滞后天数），两个裸数字并存或缺时点声明即病。想防持仓数据说不清。

**它现在怎么运作。** 读 L3 的 get_qqq_top10_concentration：value 须含 holdings_as_of/holdings_lag_days/holdings_parsed/total_holdings/holdings_lag_note 等字段；data_quality.coverage 的计数与 value 计数须一致；lag_days 不得为负。缺一/不一/为负 → 红卡。

**它的实际效果/问题。** 字段存在性 + 数值相等 + 符号判定，身份比对。零误伤。

**我的建议：留（字段+计数对账，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc24 / src/agent_analysis/persistent_checks_b.py:1065；依据 persistent_checks_b.py:1065-1106。

---

**卡 25 · 初判【珠玉·身份比对类】：supplier_lookback 待验证仍撑主斜率（PC-25）**

**它是什么、哪来的。** 03 报告 C2 病：supplier_lookback 处于 pending_validation 时仍被当 30d/90d 主斜率唯一材料。老板 2026-08-16 裁决：列待审核项目，审核前不许悄悄转绿。

**它现在怎么运作。** 读 L4 的 get_ndx_earnings_revision_metrics.value.slope_30d/slope_90d：若某斜率 material=="supplier_lookback" 且 verification_status=="pending_validation" → 红卡（持续红，属设计）。指标不可用则跳过通过。

**它的实际效果/问题。** 枚举值相等判定（material/verification_status 是否等于指定字符串），身份比对。零误伤——它只在"待验证还撑主斜率"这个确定状态亮灯。注意：它是**常红待审灯**，不是误伤，是老板要的"审核结论出来前不许转绿"。

**我的建议：留（枚举值判定，零误伤；它是待审常亮灯，符合老板意图）。** 老板可拍：O12/E3 专审结论出来后，这条是否该随标签变更自动转绿？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc25 / src/agent_analysis/persistent_checks_b.py:1109；依据 persistent_checks_b.py:1109-1144 与 O12/E3 注释。

---

**卡 26 · 初判【待定（usage 部分珠玉，reason 部分语义代理）】：yield gap 身份矛盾检测（PC-26）**

**它是什么、哪来的。** 03 报告 C4 病，老板 2026-08-16（O13）裁决：yield gap 只能当诊断性辅助指标（supporting_only），不能当核心证据。想防身份越权。

**它现在怎么运作。** 读 evidence_registry.json 里 L4.get_equity_risk_premium#level 的 passport：①usage 必须 == "supporting_only"（不是则红）；②reason 字段必须**逐字包含"诊断"或"辅助"**二字（不含则红）。

**它的实际效果/问题。** ①是枚举值判定（身份比对，零误伤）。②用"reason 里必须出现'诊断'/'辅助'二字"来代理"已声明诊断身份"——这是**语义代理类的指纹**：拿某字符串逐字出现代理内容有据。若人工把 reason 写得同样合规但没用这两个字，会被误伤。registry 是人审配置，误伤面在配置作者。

**我的建议：待定（留 usage 枚举判定；reason 的逐字措辞要求建议改为"reason 非空且说明用途"，去掉固定二字硬约束）。** 需要老板拍：reason 必须含"诊断/辅助"二字这一子项，杀还是改？

**老板盖章：【改】**（2026-09-17 盖）——usage 枚举判定留；删"reason 必含'诊断/辅助'二字"硬约束，改"reason 非空且说明用途"。T77 执行。

> 存档：_check_pc26 / src/agent_analysis/persistent_checks_b.py:1147；依据 persistent_checks_b.py:1147-1181。

---

**卡 27 · 初判【退役·不分类】：抗诉通道亮灯前身（PC-27，编号永不复用）**

**它是什么、哪来的。** 原 O17（2026-08-19）：本意"事件挑战数据判决才亮灯"，但实现点在 conflict_matrix 的 challenged_by_data——而该枚举真义是"数据削弱事件叙事"，方向装反，自落地起从未按原意生效。

**它现在怎么运作。** 已退役，代码里只留注释（persistent_checks_b.py:1184-1188）。IA 合约不动：conflict_matrix 里 challenged_by_data 行照常落盘，只是不再单独亮灯。抗诉职责由 PC-28 正确承接。

**它的实际效果/问题。** 编号 PC-27 永不复用。不算现行闸门，列此仅为家底完整。

**我的建议：保持退役（方向装反，已确认未生效；职责移交 PC-28）。** 无需老板拍板。

**老板盖章：**＿＿（退役 / 待定 + 一句话）

> 存档：persistent_checks_b.py:1184-1188 注释；PC-28 承接于 _check_pc28 / persistent_checks_b.py:1191。

---

**卡 28 · 初判【珠玉·身份比对类】：抗诉通道亮灯（PC-28）**

**它是什么、哪来的。** T67/W4，老板 2026-08-26 裁定"该抗诉才抗诉"：经核实的外部事实与数据判决正面冲突，才亮灯求人裁决。承接 PC-27 退役后的职责。

**它现在怎么运作。** 读 integrated_synthesis_report 的 data_verdict_objections：每条异议的 source_ref 必须命中 event_research_patrols 里 verified_cards 的 source_url（集合成员判定，身份比对），且 materiality=="material"（枚举）。两者都满足 → 红卡（亮灯，标"需老板人工裁决"，非系统故障）。事件卡未核实挑战只记录不亮。机制未启用则跳过通过。

**它的实际效果/问题。** 集合成员（verified URL）+ 枚举值（material），身份比对。亮灯是**有意为之的人工升级**，不是误伤——它专挑"已核实事实+实质矛盾"。零误伤 AI 产出。

**我的建议：留（核实事实+实质矛盾才亮灯，设计正确，正是该留的升级类闸门）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc28 / src/agent_analysis/persistent_checks_b.py:1191；依据 persistent_checks_b.py:1191-1260 与 O17/PC-27 注释。

---

**卡 29 · 初判【珠玉·身份比对类】：词表活化三账一致（PC-29）**

**它是什么、哪来的。** T67/W7，老板 2026-08-27 批准"词表变更落盘留痕，防静默增删"。想防新闻增量词表被手改绕过圈选。

**它现在怎么运作。** 调 verify_keyword_ledgers 比三本账（overrides ↔ change_log ↔ term_candidates）：overrides 每条生效词须在 change_log 找到同词同用途 adopt 记录；change_log 引用的 candidate_id 须真实存在于候选账；枚举/时间戳形状完整。对不上 → 红卡；三账全缺 → 跳过通过（机制未启用）。

**它的实际效果/问题。** 形状校验 + 跨账身份比对（同词同用途、candidate_id 存在），不读意思。零误伤——静默手改 JSON 绕过流程才会被点名。注释明言"闸门不判意思"。

**我的建议：留（跨账身份比对，零误伤，且未启用不打扰）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_check_pc29 / src/agent_analysis/persistent_checks_b.py:1270；依据 persistent_checks_b.py:1268-1313 与 term_activation.verify_keyword_ledgers。

---

**卡 30 · 初判【珠玉·身份比对类】：受控调查材料引用合规（_validate_investigation_material_citations）**

**它是什么、哪来的。** orchestrator 里受控调查（controlled investigation）产物的合约校验，确保调查结论每条都挂到真实材料块 [M#] 上，不凭空编造。

**它现在怎么运作。** 对 payload 的 finding / claims_supported / claims_challenged / cannot_establish / counter_evidence_refs 各文本字段：①必须至少含一个 [M\d+] 引用（缺失即报错）；②引用的 M 编号必须在 1..material_count 合法区间内（超界即报错）。报错 → 抛 ValueError → 触发该站重试；重试耗尽 → 降级为 rejected 兜底（不再硬崩整跑，见 T69 P3-2）。

**它的实际效果/问题。** 主体是身份比对：引用的 [M#] 编号必须 ∈ 合法材料清单（"引用的东西在不在账上"）。①的"必须带 ≥1 引用"是结构化引用格式的完整性要求，不是读意思。误伤面低——引用是 [M#] 机械格式，合法引用天然在册。属该留的一类。

**我的建议：留（引用编号在册判定，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_investigation_material_citations / src/agent_analysis/orchestrator.py:2768；依据 orchestrator.py:2768-2791。

---

**卡 31 · 初判【粪土·语义代理类】：调查 limits 与材料数字自相矛盾（_validate_investigation_limits_against_materials）**

**它是什么、哪来的。** orchestrator 里对受控调查 limits 字段的校验，想防"材料里明明有数字，limits 却声称没有任何定量数据"的自相矛盾。

**它现在怎么运作。** 两步：①用正则在 materials_text 里找"有没有报道过的数字"（\d+% / 倍 / 分位 / 亿美元 等）；②若材料有数字，再用正则在 limits 文本里找"无/没有/未包含任何 定量/数字数据""未分析具体数字""不含实际市场数据"等否认措辞。命中否认措辞 → 抛 ValueError → 重试（耗尽降级 rejected）。

**它的实际效果/问题。** ①②都用正则去读 AI 写的 limits **自由文本**含义——它靠"某短语逐字出现"来代理"AI 是否在否认数字"，是典型的语义代理类。风险：AI 合法地写了"我们没分析具体数字，因为只拿到标题材料"之类，却碰巧命中否认正则，被误伤；反之换个说法又能绕过。要读懂 limits 真意才能判，机器确定不了。

**我的建议：杀或重做（改成可追溯+可重算：只核对 limits 里点名的数字是否与材料数字一致，不靠否认措辞正则）。** 需要老板拍：这条是否直接杀，还是改为"limits 点名的数字须能在材料找到"的窄身份比对？

**老板盖章：【杀】**（2026-09-17 盖）——从校验链摘除，留退役注释；备选"收窄为窄身份比对"方案留档在卡。T77 执行。

> 存档：_validate_investigation_limits_against_materials / src/agent_analysis/orchestrator.py:2864；依据 orchestrator.py:2864-2894。

---

**卡 32 · 初判【珠玉·身份比对类】：反方草案引用合规（_validate_counter_thesis_draft）**

**它是什么、哪来的。** orchestrator 里对 CounterThesisDraft（反方解释草案）的校验，确保反方只引用账上证据、且结构完整。

**它现在怎么运作。** 逐条 hypothesis：support/counter/diagnostic_evidence_refs 里的每条引用必须 ∈ allowed_refs（evidence_index 键集，不在则报错）；support_evidence_refs、diagnostic_evidence_refs、falsification_conditions 不得为空。报错 → 重试（耗尽降级）。

**它的实际效果/问题。** 引用 ∈ 账上键集（身份比对）+ 结构化字段非空（形状）。零误伤——反方引用本就该在 evidence_index 内。是"引用的东西在不在账上"的典型。

**我的建议：留（引用在册+结构完整，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_counter_thesis_draft / src/agent_analysis/orchestrator.py:3262；依据 orchestrator.py:3262-3278。

---

**卡 33 · 初判【珠玉·身份比对类】：thesis 假说回应合规（_validate_thesis_hypothesis_responses）**

**它是什么、哪来的。** orchestrator 里 Thesis→Final 段的校验（T28 收紧）。原病：越有争议的假说越不被要求回应（candidate 集合归零致合约空转）。想防"该回应的假说被漏掉"。

**它现在怎么运作。** 凡 status≠downgraded 的 competing hypothesis 都必须有 hypothesis_responses 条目（缺/重复即报错）；若某回应 verdict=="reject"，其 evidence_refs 必须非空且 ∈ evidence_index（不在账上即报错）。报错 → 重试（耗尽降级）。

**它的实际效果/问题。** 条目存在性 + 引用 ∈ evidence_index 键集（身份比对）。零误伤——它只查"该回应的有没有回应、reject 有没有挂账上引用"，不判回应写得好不好。

**我的建议：留（回应存在+引用在册，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_thesis_hypothesis_responses / src/agent_analysis/orchestrator.py:5254；依据 orchestrator.py:5254-5311。

---

**卡 34 · 初判【珠玉·身份比对类】：各站 evidence_ref 在册校验（_validate_stage_evidence_refs）**

**它是什么、哪来的。** orchestrator 里 final 等站的通用引用校验（接在 _run_stage 的 validator 链）。想防站产出引用了账上没有的 evidence_ref。

**它现在怎么运作。** 递归遍历候选产物，收集所有 evidence_refs / counterevidence_refs / counter_evidence_refs，每条必须 ∈ allowed_refs（synthesis_packet.evidence_index 键集）。不在 → 报错"refs must come from evidence_index" → 重试（耗尽降级）。

**它的实际效果/问题。** 纯身份比对：引用字符串是否在账上键集。零误伤——这是"引用的东西在不在账上"的最直接实现。注释提及 T35 修复：合法性只看"字段这一轮真实存在"（raw value 顶层键并集），不再只读人工 MetricAuthority 表，已修掉 L4 字段真实存在却被判"不在索引"的误伤事故（run 20260730_114704）。

**我的建议：留（引用在册，零误伤，且已修掉权限表误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_stage_evidence_refs / src/agent_analysis/orchestrator.py:6857；依据 orchestrator.py:6857-6889 与 T35 修复注释（7296-7301）。

---

**卡 35 · 初判【粪土·语义代理类（已知反面教材）】：终审判决正文数字锚定（_validate_reasoned_verdict_refs）**

**它是什么、哪来的。** orchestrator 里 final_adjudicator 的核心校验，把"判决必须带可追溯引用"从软标注升级为真合约。其中第三项（数字锚定）是老板点名的语义代理类反面教材。

**它现在怎么运作。** 对 reasoned_verdict 三段判定：①必须至少带 1 个 [bracket] 引用（零引用→拦）；②引用的 ref 必须能解析进 allowed evidence_index（身份比对，不在→拦）；③若传了 source_text，提取正文里所有百分数/小数 token，凡**不逐字出现在本次输入 payload** 里 → 拦（"数字必须逐字在输入里"）。报错 → 重试（耗尽降级为 rejected 兜底）。

**它的实际效果/问题。** ①②是合法身份比对（引用格式+在册），该留。③是粪土·语义代理类：它用"某数字逐字出现在输入材料"代理"内容有据"，但判决书里大量合法数字是**派生算出来的**——两个数相减、比率、分位差——材料里逐字找不到，就被拦。这正是老板说的典型误伤：相减/比率/分位差三类合法推算全被③卡掉。代码 docstring 自己也写"这是身份比对，不是语义判断"，但"数字须逐字出现"对派生数字并不成立。

**我的建议：杀或重做（①②留；③改造成"可追溯+可重算"——派生数字须能由账上原始数字按公式重算，而非逐字出现）。** 需要老板拍：③是整体杀，还是改为"派生数字须附计算公式且原料在册"的可重算校验？

**老板盖章：【改】**（2026-09-17 盖）——①②（引用格式、在册）留；③数字逐字锚定降级为"留痕不拦"（照 C-39 模式）；重算制改造另立重做工单。T77 执行。

> 存档：_validate_reasoned_verdict_refs / src/agent_analysis/orchestrator.py:6977；依据 orchestrator.py:6977-7047 与 _reasoned_verdict_numeric_tokens:7064-7075；docstring 自陈"数字存在性比对"。

---

**卡 36 · 初判【珠玉·身份比对类】：终审须回应保留的高严重度冲突（_validate_final_conflict_responses）**

**它是什么、哪来的。** orchestrator 里 Thesis→Final 段的编号级校验（T42③）。原病：保留的冲突条目还在结构里，但终审叙事完全绕开它——"冲突没被删，只是没被回答"。

**它现在怎么运作。** 取 thesis 里 retained、severity==high 且带 conflict_id 的冲突编号，逐条检查是否**逐字出现**在 final 的 principal_contradiction.conflict_refs / reasoned_verdict / adjudicator_notes / final_stance / secondary_contradiction 里。编号没出现 → 报错 → 重试（耗尽降级）。

**它的实际效果/问题。** docstring 自陈"只做身份比对"：冲突编号（结构化 token，非自由文本）必须逐字出现在 final 文本。它查的是"这个编号在不在"，不是"回应写得好不好"。零误伤——编号是机器生成的 ID，命中即覆盖，不命中即漏答。属该留的一类。

**我的建议：留（编号逐字出现判定，零误伤，正是身份比对）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_final_conflict_responses / src/agent_analysis/orchestrator.py:7102；依据 orchestrator.py:7102-7148（docstring 明言"只做身份比对"）。

---

**卡 37 · 初判【珠玉·身份比对类】：LayerCard v2 结构完整性（_validate_layer_card_v2）**

**它是什么、哪来的。** orchestrator 里 Bridge 消费前的层卡校验，拒收"薄皮 legacy 式"层卡。想防空壳层卡进桥接。

**它现在怎么运作。** 检查 layer_synthesis / internal_conflict_analysis / quality_self_check 非空；每个 indicator_analysis 的 narrative / reasoning_process / evidence_refs 非空；expected_function_ids 全有对应分析、无幻觉出的未知 function_id（不在输入键集→拒收）；metric 名与输入相等；quality_self_check 覆盖全。任一不过 → 重试（耗尽降级）。

**它的实际效果/问题。** 字段非空（形状）+ function_id ∈ 输入键集（身份比对）+ 字符串相等（metric 名）。零误伤——未知 function_id 是模型幻觉，必须打回。T54 批 5 已把拼写错误在装配层回正，走到这里的都是真配不上的。

**我的建议：留（结构+函数 id 在册，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_layer_card_v2 / src/agent_analysis/orchestrator.py:7150；依据 orchestrator.py:7150-7211 与 T54 批 5 注释。

---

**卡 38 · 初判【珠玉·身份比对类】：BridgeMemo v2 结构完整性（_validate_bridge_memo_v2）**

**它是什么、哪来的。** orchestrator 里桥接备忘录校验，拒收缺证据/机制/反例的"软桥"。想防桥接链空泛。

**它现在怎么运作。** 对 typed_conflicts / resonance_chains / transmission_paths：evidence_refs 非空、mechanism/implication 非空（typed_conflicts 由 legacy 派生时免 mechanism）；transmission_paths 无重复 path_id；evidence_refs 不得含 event: 前缀（事件永不进第一层）；顶层 event_refs 必须空。任一不过 → 重试（耗尽降级）。

**它的实际效果/问题。** 字段非空（形状）+ 前缀/空值判定（身份比对，"event: 不得作 evidence_ref""event_refs 恒空"）。零误伤——都是结构化约束，不读意思。

**我的建议：留（结构+事件隔离，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_bridge_memo_v2 / src/agent_analysis/orchestrator.py:7213；依据 orchestrator.py:7213-7270。

---

**卡 39 · 初判【粪土·语义代理类（已降级为留痕不拦）】：事实与解读逐字相同（_validate_fact_interpretation_separation）**

**它是什么、哪来的。** contracts.py 里事件卡(EventCard)的模型校验。原意：fact_summary 与 interpretation 两字段必须分开写（事实 vs 模型解读），逐字相同 = 没分离。

**它现在怎么运作。** 两字段逐字（casefold）相同 → 不再 raise，只打 logger.warning + 落盘侧记 semantic_warnings（quality gate 备注）。不拦、不重试。

**它的实际效果/问题。** 它用"两字段逐字相同"这个**形状**代理"事实与解读是否混写"这个语义判断——要真判"有没有分离"得读懂内容，超出闸门职权。正是老板说的"形状代理语义一律出局"。2026-08-31 T69 P1-7 已主动把它从 raise 降级为 warning，等于**已自我纠正**。当前不误伤（不拦），但属语义代理类遗痕。

**我的建议：保持降级（只留痕不拦；若有人想恢复成硬拦，必须杀）。** 无需老板拍板。

**老板盖章：【保持降级】**（2026-09-17 盖）——维持只留痕不拦；任何人想恢复硬拦，先杀。不动代码。

> 存档：_validate_fact_interpretation_separation / src/agent_analysis/contracts.py:432；依据 contracts.py:431-444 与 T69 P1-7 降级注释。

---

**卡 40 · 初判【珠玉·身份比对类】：回答-证据联动（_validate_answer_evidence_linkage）**

**它是什么、哪来的。** contracts.py 里 IntegratedQuestionAnswer（第三层跨层问答）的模型校验。想防"说答了却没给证据/说部分答了却没说缺什么"。

**它现在怎么运作。** 枚举条件：answer_status=="answered_by_data" 则 data_refs 或 investigation_refs 至少一项非空；=="partially_answered" 则 missing_evidence 非空。否则 raise → 该结构校验失败、重试。

**它的实际效果/问题。** 枚举值驱动的字段非空要求，身份比对（结构化字段约束，不读意思）。零误伤——答了就该挂证据，部分答了就该说缺口。

**我的建议：留（枚举条件字段约束，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_answer_evidence_linkage / src/agent_analysis/contracts.py:491；依据 contracts.py:491-496。

---

**卡 41 · 初判【珠玉·身份比对类】：数据侧须具体引用（_validate_concrete_data_side）**

**它是什么、哪来的。** contracts.py 里 IntegratedConflictRow（事件-数据对质矩阵行）的模型校验。想防 data_side_refs 用占位词糊弄。

**它现在怎么运作。** data_side_refs 里不得出现纯占位字符串（pure_data_report/data/pure_data，casefold 匹配）→ 否则 raise；relation=="not_yet_testable" 必须填 note；relation∈{confirmed/challenged} 必须带具体 data_side_refs。否则 raise → 重试。

**它的实际效果/问题。** 占位字符串黑名单匹配（身份比对，"这个字符串是不是禁用的占位词"）+ 枚举条件字段约束。零误伤——它查的是"引用是不是具体证据 ref"，不读意思。

**我的建议：留（占位词黑名单+枚举约束，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_concrete_data_side / src/agent_analysis/contracts.py:512；依据 contracts.py:512-520。

---

**卡 42 · 初判【珠玉·身份比对类】：抗诉 material 须点名被挑战数据（_validate_material_refs）**

**它是什么、哪来的。** contracts.py 里 DataVerdictObjection（抗诉通道）的模型校验。想防"标了实质矛盾却不说挑战了哪条数据"。

**它现在怎么运作。** materiality=="material" 则 contradicted_data 必须非空（否则 raise → 校验失败重试）。

**它的实际效果/问题。** 枚举值驱动的字段非空要求，身份比对。零误伤。它与 PC-28（抗诉亮灯）是上下游：本卡管"抗诉结构合法"，PC-28 管"满足条件才亮灯"。

**我的建议：留（枚举条件字段约束，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_material_refs / src/agent_analysis/contracts.py:544；依据 contracts.py:544-547。

---

**卡 43 · 初判【珠玉·身份比对类】：ref-digest 字段与体积合规（_validate_ref_digest）**

**它是什么、哪来的。** vnext_reporter.py 里对"引用摘要"（ref digest，渲染用紧凑证据包）的校验。想防摘要结构错或体积爆表。

**它现在怎么运作。** 每个 metric 的字段集合须 == REF_DIGEST_FIELDS（否则 raise）；单 metric ≤1KB、trends ≤12KB、整 digest ≤50KB（超限 raise）。

**它的实际效果/问题。** 字段集合相等（身份比对）+ 字节体积上限（数值）。零误伤——纯形状/体积约束，不读意思。

**我的建议：留（字段集+体积，零误伤）。** 无需老板拍板。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档：_validate_ref_digest / src/agent_analysis/vnext_reporter.py:2289；依据 vnext_reporter.py:2289-2299 与 REF_DIGEST_FIELDS。

---

## 附录：Grep 命中的 pipeline 外子系统校验器（不计入分析闸门，仅作清查完整）

以下 `def _validate` / `def validate` 不在 25 站分析流水线内，属 CLI/控制/事件研究卡等子系统的输入校验，均为身份比对类、零误伤，未单独成卡：

- `src/main.py:169` validate_date — CLI 日期解析校验
- `src/control_service.py:135` validate_command — 控制命令合法性
- `src/control_service.py:164` validate_env_overrides — 环境变量覆盖形状
- `src/prompt_examples.py:693` validate_prompt_examples — 示例注册表完整性
- `src/state_ledger.py:236` _validate_method_revision_entry — 方法修订账条目形状
- `src/event_research/runner.py:126` validate_narrative_state / `topic_composer.py:123` validate_topics / `card.py:37` validate_research_card — 事件研究卡输入校验

这些与"多 agent 研究纳指"的分析闸门无关，建议家底清单不纳入统计口径。

## D 区 · 数据管道（12 张）

五层数据工具与地基机制。本区全部初判"留（只包不重写）"——这些是带实战伤疤的外部接口代码，重写风险高收益低；但每张卡里都写了一两个真实风险点（如 L5 单源依赖、HoM 第三方线未独立核验）等你拍板。重算带（D-10）是"防编造数字"的正道，值得细读。

**卡 1 · 初判【珠玉】：L1 宏观流动性数据工具（利率/净流动性/商品比）**

**它是什么、哪来的。** 这是五层数据工具的第一层（文件 `src/tools_L1.py`），归 L1 宏观流动性层用。它负责把"当前货币环境在给估值加油还是抽油"这个问题需要的原始数字取回来并预处理好。来历在文件头注释里写明是"第1层数据获取函数"，具体动工日期查不到，按项目惯例是随流水线早期版本一起长出来的（待确认具体版本）。它管这些指标：联邦基金利率、10年-2年利差、10年国债收益率、10年实际利率、10年盈亏平衡通胀、M2 同比、美元净流动性动量（=美联储资产负债表 WALCL − 财政部账户 TGA − 逆回购 RRP）、铜金比、DXY 美元指数、SOFR、WTI 油价、金油比。

**它现在怎么运作。** 数据源主要是 FRED（美联储经济数据库，免费公开，管利率/货币量类）和 yfinance（雅虎财经接口，管期货/ETF 价格类，如铜 GC=F、金 HG=F、QQQ）。每个函数先拉序列，再做预处理：算出"当前值(level)"、相对自身历史的"分位(relativity)"、动量，M2 还会算同比变化率(YoY)。代码里明确写了单位换算补丁——FRED 的 WALCL 原始单位是百万美元，必须除以 1000 才变成十亿美元（见 `_fetch_walcl_history` 注释）。取不到数时返回带 `availability=unavailable` 的"无数据"标记，不会硬塞一个数。

**它的实际效果/问题。** 整体健康：来源清晰、预处理到位（分位和变化率都算了，不是只给裸值）、单位换算这种实战补丁写得很老实。和重算带（卡10）对接良好——净流动性、利率动量这类会进"重算原料"。一处可留意的小风险：它依赖 FRED_API_KEY，缺密钥时净流动性会显式报错而不是假装算出来（这是对的），但老板要知道免费源偶发断供会让这些指标当天"无数据"。我读到的都是正面实现，没发现指标越权或冒充事实。

**我的建议：留（只包一层、不重写）。** 外部接口带实战补丁，重写风险高、收益低。需要老板拍板：FRED 免费源偶发断供时，是否接受 L1 当天的若干指标"无数据"而非降级到别的源？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/tools_L1.py`（`get_fed_funds_rate` L636、`get_m2_yoy` L665、`get_net_liquidity_momentum` L868、`_fetch_walcl_history` L692、`_attach_recompute_value_series` L59）。依据：系统说明书 §2.1/表(124行) L1 职责；tools_L1.py 文件头 L1-16、FRED/yfinance 源引用 L218/L332/L1004、单位换算注释 L695-L726。

---

**卡 2 · 初判【珠玉】：L2 风险偏好/信用数据工具（VIX/信用利差/恐贪/仓位）**

**它是什么、哪来的。** 第二层数据工具（`src/tools_L2.py`），管"市场愿不愿意冒险、这种冒险健不健康"。指标有：VIX、VXN（纳指波动率）、VXN/VIX 比、VIX 期限结构、高收益债利差(HY OAS)、投资级债利差(IG OAS)、HY 质量利差、HYG 动量、可选/必需消费比(XLY/XLP)、拥挤度仪表盘、CFTC 纳指期货持仓、FINRA 融资余额、CNN 恐贪指数。

**它现在怎么运作。** 数据源：yfinance（VIX/^VXN/^VIX3M/^VIX6M、XLY/XLP、^SKEW、期权链）、FRED（HY/IG OAS 利差）、CNN 恐贪指数（网页/接口）、CFTC（期货持仓）、FINRA（融资余额）。VIX/VXN 这类除当前值外，还算出"spot_over_ma20_ratio"（现货相对20日均线的位置，用来降噪），以及基于 yfinance 日收盘的历史分位。

**它的实际效果/问题。** 健康，且有一处特别值得表扬的诚实：VIX 期限结构的历史分位，代码自己写明"基于 yfinance ^VIX/^VIX3M 每日收盘计算，不是 CBOE 官方期限结构分位，仅作确认/预警强度参考"（见 L633 的 `reason` 字段）——它没有把自家算的近似冒充成交易所官方口径。来源等级标注清楚（如"derived_from_yfinance_daily_closes"）。没发现越权或冒充。一个小待确认：CNN 恐贪指数和 CFTC 仓位走的是非 FRED 外部接口，断供时回落逻辑是否和 FRED 一致，我未在本次精读（待确认）。

**我的建议：留（只包不重写）。** 来源分层清楚、自带"非官方分位"免责声明，正是系统要的纪律。需要老板拍板：VIX 期限结构这种"自家算分位、非官方"的指标，报告里是否要始终带"非 CBOE 官方"标注（目前代码里带了，建议固化成硬规矩）？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/tools_L2.py`（`get_vix` L162、`get_vix_term_structure` L409、`get_hy_oas_bp` L664、`get_cnn_fear_greed_index` L1563、`get_cftc_nq_positioning` L1277；非官方分位声明 L633）。依据：系统说明书 L2 职责(125行)；tools_L2.py 文件头 L1-5、yfinance/FRED/CNN 源引用 L74/L435/L668、VIX 期限结构分位声明 L632-633。

---

**卡 3 · 初判【珠玉】：L3 指数内部结构数据工具（腾落/集中度/宽度）**

**它是什么、哪来的。** 第三层数据工具（`src/tools_L3.py`），管"上涨是大部队推进，还是少数龙头孤军深入"。指标：NDX100 成分股清单（带溯源）、NDX/NDXE 比（市值加权 vs 等权）、QQQ/QQEW 比、腾落线(advance-decline)、站上均线比例、新高新低、McClellan 震荡指标、QQQ 前十大权重集中度、M7 基本面、量化护城河评分。

**它现在怎么运作。** 数据源：yfinance（NDX100 成分股日收盘面板，用来派生腾落/宽度四件套）、Invesco 官方 QQQ 持仓接口（前十大集中度）、以及 M7 个股基本面。宽度四件套都从同一份"成分股价格面板"算出来，只是聚合口径不同（见证据家族卡12的映射说明）。集中度这类不能证明回测日可见的，已在采集器里标记回测跳过。

**它的实际效果/问题。** 健康。代码把"同一份面板派生出的四个宽度指标"老老实实归到同一个证据家族（卡12），避免它们被算成四条独立证据而虚高置信度——这条纪律执行得对。没发现指标越权。一处待确认：`get_m7_fundamentals`（M7 个股基本面）函数存在但没接进采集器 L3 名单（L3 只列了宽度/集中度类），它大概被别处（M7 专门分析）调用；它属于"基本面/估值"味道更浓的指标却住在 L3 文件里，老板可留意这是不是轻微的位置错位（待确认，不影响评级）。

**我的建议：留（只包不重写）。** 宽度家族归并正确、来源标注清楚。需要老板拍板：M7 基本面这类"估值味"的函数放在 L3 文件而非 L4，是否是刻意（L3 只负责取数、真正解读在别处），还是该挪窝？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/tools_L3.py`（`get_ndx100_components_with_provenance` L19、`get_advance_decline_line` L953、`get_qqq_top10_concentration` L1620、`get_m7_fundamentals` L1857）；采集器 L3 名单 `collector.py` L168。依据：系统说明书 L3 职责(126行)；证据家族映射 `evidence_families.py` L113-124（ndx_constituent_price_panel 四件套同族）。

---

**卡 4 · 初判【珠玉】：L4 估值数据工具（PE/PB/盈利预期/风险溢价——最该"只包不重写"的一层）**

**它是什么、哪来的。** 第四层数据工具（`src/tools_L4.py`，8268 行，是五层里最大的一层），管"当前价格相对盈利/现金流/无风险收益有没有吸引力"。指标：Wind NDX 估值快照(PE/PB/PS/历史分位/风险溢价)、Wind 时点盈利预期、NDX PE 与盈利收益率、Forward 盈利质量、全成分 Forward PE、盈利修正指标、股权风险溢价(ERP)、M7 资本开支周期、M7 业绩静默期日历、M7 回购现金流、Damodaran 美国隐含 ERP。

**它现在怎么运作。** 数据源优先级最讲究：主锚是 Wind（付费、高信任，NDX 指数级 PE/PB/PS 与风险溢价），其次 yfinance 成分股交叉核验、SEC 官方 XBRL（M7 资本开支/回购抓 filing）、Damodaran 学术 ERP、History of Market(HoM) 第三方估值。代码里写死了来源等级阶梯 `SOURCE_TIER_*`（licensed_provider/Wind → licensed_manual/Wind → official → component_model → third_party_estimate → proxy → unavailable），每个指标带 `fallback_chain`（断源时往下降级）。HoM 这个第三方来源，代码强制在标签里加"Bloomberg BEst 归因、未独立核验"的免责（见 `HISTORY_OF_MARKET_BEST_CAVEAT`）。

**它的实际效果/问题。** 这是整条数据管道里治理最严的一层，几乎就是系统原则的样板：不把 proxy/成分模型冒充官方 Wind、来源降级链显式、第三方归因带未核验声明、回测时 Wind 实时快照和"只返回最新财报"的函数一律跳过并写入边界。我读到的都是正面实现。唯一需老板知道的真实风险：HoM 的"Bloomberg BEst 归因"本身就是供应商自己说的、项目未独立核验（代码如实标注了），所以 HoM 那条估值线本质上是"带质疑地用"，不是铁证。

**我的建议：留（强烈建议只包不重写）。** 这正是老板预感里"带实战伤疤、重写风险高"的典型：Wind 怪癖、SEC XBRL 解析、HoM 免责全靠补丁压着。需要老板拍板：HoM（History of Market）这条"Bloomberg BEst、未独立核验"的第三方估值线，是否长期只作交叉参考、绝不单独当 L4 主锚？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/tools_L4.py`（`SOURCE_TIER_*` L51-57、`HISTORY_OF_MARKET_BEST_CAVEAT` L58、`get_ndx_wind_valuation_snapshot` L956、`get_damodaran_us_implied_erp` L4196、`VALUATION_FALLBACK_CHAIN` L78；SEC XBRL L141-208）。依据：系统说明书 §4.5(L378-388，L4 发言权/来源等级/Wind 主锚/HoM 不冒充 Wind 分位)；tools_L4.py 文件头与源等级声明。

---

**卡 5 · 初判【珠玉】：L5 价格趋势数据工具（均线/RSI/MACD/ATR/量价）**

**它是什么、哪来的。** 第五层数据工具（`src/tools_L5.py`），管"趋势是否有效、有没有过热、失效点在哪"。指标：QQQ 确定性快照、综合技术指标、RSI、ATR、ADX、MACD、OBV(能量潮)、成交量分析、量价质量(VWAP/MFI/CMF)、唐奇安通道、多尺度均线位置。

**它现在怎么运作。** 数据源单一：几乎全部来自 QQQ 的日频 OHLCV（开盘/最高/最低/收盘/成交量）历史，经 yfinance 拉取。11 个技术指标都是对同一份 QQQ 价格历史做不同数学变换（RSI 看超买超卖、MACD 看动量、ATR 看波动、ADX 看趋势强度等）。采集器把它们全归到同一个证据家族（卡12），不会虚增证据多样性。

**它的实际效果/问题。** 健康，且和重算带（卡10）配合最好：重算带会独立用原始 OHLCV 把 SMA/RSI/MACD/ATR/ADX 等重算一遍对账（见 `_l5_recompute_values`），等于给这批指标上了双保险。没发现越权或冒充。一处如实说明的局限：代码自己列了 `L5_UNRECOMPUTABLE_TECHNICAL_KEYS`（部分指标当前不在重算带覆盖内，状态记为"uncovered"而非假装覆盖），这是诚实做法。数据源单一（只靠 yfinance QQQ）是把双刃剑：干净、好对账，但 yfinance 断供时 L5 整层会同时"无数据"。

**我的建议：留（只包不重写）。** 单源、可重算、家族归并正确。需要老板拍板：L5 完全依赖 yfinance 的 QQQ 数据，是否接受"yfinance 断供则 L5 整层降级"这一脆弱点（可考虑备用源，但属重写范畴，建议先留）？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/tools_L5.py`（`get_l5_deterministic_snapshot` L274、`get_rsi_qqq` L738、`get_macd_qqq` L888、`calculate_technical_indicators_yf` L364）；家族映射 `evidence_families.py` L182-192（qqq_ohlcv_technical）。依据：系统说明书 L5 职责(128行)；重算带 `_l5_recompute_values` `recompute_belt.py` L1403、不可重算键 L1362。

---

**卡 6 · 初判【珠玉】：采集器（collector.py——五层数据的"总调度"）**

**它是什么、哪来的。** `src/core/collector.py` 里的 `DataCollector` 类，是数据管道的指挥中心。它在每次运行开头，按 L1–L5 把各层该取的函数一个个调用、收集、打时间戳、归档成 `data_collected_v9_*.json`。来历：文件头说是"从 tools.py 收集、缓存、格式化所有市场数据"，是流水线第一步（步骤1/4）。

**它现在怎么运作。** 核心是一张 `LAYER_FUNCTIONS` 表（L147），写明每层调哪十几个函数（和卡1–5 一一对应）。它做几件关键事：①优先用 `manual_data.py`（老板可手填的数值）覆盖自动源，且会给人工值打"新鲜度"检查（超过120天标 stale）；②回测模式下，凡"只能证明是最新、不能证明是回测日可见"的函数（如 Wind 实时快照、最新持仓）一律跳过并写进 `backtest_data_boundaries`，防前瞻偏差；③把每个指标的"重算原料"(recompute_input) 抽出来单独放顶层，长序列不进 agent 上下文；④把严格回测不变量 `strict_backtest_invariants` 一并产出。

**它的实际效果/问题。** 是整条管道健康的枢纽：手动覆盖、回测跳过、重算原料抽取、不变量声明都在此集中实现，且都"只标注不阻断"。一个设计亮点：Damodaran ERP 的人工覆盖值如果来源不是 Damodaran 官方，会被打上"erp_independence_compromised"（独立性受损）标注——防止人工值悄悄抹掉"独立于 Wind 的第三声部"。没发现问题。小待确认：手动覆盖的"新鲜度阈值120天"是写死的魔法数，是否合理由老板定（待确认，不影响评级）。

**我的建议：留（只包不重写）。** 它是各层与下游之间的接缝，逻辑清楚、纪律到位。需要老板拍板：人工覆盖值"超过120天标 stale"这个阈值，是否要随指标性质分开设（利率和财报显然不该共用一个天数）？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/core/collector.py`（`DataCollector` L143、`LAYER_FUNCTIONS` L147、`BACKTEST_UNSUPPORTED_FUNCTIONS` L256、`_build_strict_backtest_invariants` L291、`run` L450、recompute_inputs 抽取 L633-647、`MANUAL_DATA_STALENESS_THRESHOLD_DAYS` L74）。依据：系统说明书 §五(L422 collector 描述)；collector.py 全文。

---

**卡 7 · 初判【珠玉】：数据完整性检查（checker.py / DataIntegrity——发布闸门）**

**它是什么、哪来的。** `src/core/checker.py` 的 `DataIntegrity` 类，是采集结果的"体检医生"。它在采集后跑一遍，算出一个 `confidence_percent`（数据可信度百分比），并决定报告能不能发（发布闸门）。来历：系统说明书 §五 称其为"数据完整性检查，判定覆盖率与时间一致性"。

**它现在怎么运作。** 它对每个指标检查：①有没有有效值（`has_meaningful_observation_value`）；②覆盖率（成分股缺多少）；③回测模式下有没有"晚于回测日的数据日期"（前瞻偏差硬阻断）；④证据合约问题（见卡11，hard_block 直接阻断）；⑤估值源严重冲突（如 Wind 与 HoM 差太多）；⑥最关键是调用重算带（卡10），把"重算关键偏差"也列为阻断理由。置信度用"证据家族"加权（卡12），不是简单数函数个数。低于60%或整层失败就 `blocked`，不发布。

**它的实际效果/问题。** 这是把"系统原则"变成可机器执行的闸门，非常扎实：回测前瞻日期、来源冲突、重算偏差都能真阻断发布。有个重要设计点：重算带本身崩溃时，它只记录错误、不拦发布（"带的 bug 不是数据被污染的证据"）——这是对的，避免误杀。唯一局限（代码自己承认）：函数级可用性百分比公式保留着，只为透明对照，主评分已改用家族级（更紧）。没发现误伤或越权。

**我的建议：留（只包不重写）。** 它是发布闸门本体，和重算带、证据合约咬合得好。需要老板拍板：当前 `MIN_PUBLISH_CONFIDENCE_PERCENT=60` 和整层失败才阻断的尺度，是否够严（系统说明书提过"只紧不松"，老板可定更高阈值）？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/core/checker.py`（`DataIntegrity` L29、`MIN_PUBLISH_CONFIDENCE_PERCENT` L32、`run` L176、回测前瞻阻断 L224/L331、重算带接入 L216、家族加权 L243-280、阻断理由 L303-355）。依据：系统说明书 §五(L423)、§2.11(L269 降级痕迹可见)；checker.py 全文。

---

**卡 8 · 初判【珠玉】：数据缓存（data_cache.py——避免重复下载的内存缓存）**

**它是什么、哪来的。** `src/data_cache.py` 的 `SharedDataCache` 类，是个纯工具：同一份数据源在一次运行里只下载一次，后面直接取缓存，省时间、防重复打外部接口。来历：文件头说是"共享数据缓存模块——消除重复数据下载"，设计原则写明了线程安全、非侵入、可验证。

**它现在怎么运作。** 它是个带锁的字典：用 `get_or_fetch(key, 取数函数)` 取数，命中就返回缓存，未命中才真下载；对 yfinance 有专门的 `get_or_fetch_yf`。带"在途请求"合并（两个线程同时要一个 key，只有一个去下载，另一个等结果），并提供命中率统计。它跟采集器的"运行级重置"配合——每次运行开头清缓存，所以缓存只在单次运行内有效。

**它的实际效果/问题。** 简单、干净、文档清楚，是健康的纯基础设施，没掺任何数据语义（不判断真假、不改数）。一处老板该知道的真实局限：它**没有过期时间(TTL)**——靠"每次运行重置"来保证不陈旧。只要系统是"一次运行清一次缓存"的模型，这就安全；但若以后有人改成常驻进程跨天复用缓存，就可能返回昨天的数。这是设计前提，不是当前 bug。

**我的建议：留（只包不重写）。** 它不碰数据含义，重写无收益。需要老板拍板：系统以后会不会变成"常驻不重启"的长进程？如果是，缓存必须加过期时间，否则当前"按运行重置"的前提就破了。

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/data_cache.py`（`SharedDataCache` L30、`get_or_fetch` L100、`get_or_fetch_yf` L161、`CacheContext` L228；无 TTL，靠 `clear` 重置）。依据：data_cache.py 文件头 L1-20、设计原则 L7-10；与 collector 运行级重置配合（collector.py run L458）。

---

**卡 9 · 初判【珠玉】：历史时点归档（vintage_archiver.py——自产"当时可见"数据的种子）**

**它是什么、哪来的。** `src/vintage_archiver.py` 是个独立脚本（不接进主流水线），每天把"今天的共识盈利预期"快照存一份，攒成自己的历史档案。来历写得坦白：Wind 没有好用的美股共识 EPS 覆盖（2026-07-12 验证过），免费源也没有多年"时点可见"的盈利修正史，于是用零成本办法——每天存一份 yfinance+FMP 的当前共识，过30–90天就攒出真正"当时那天的数"，以后回测才用得上。

**它现在怎么运作。** 每天收盘后跑一次，把全 NDX/QQQ 有效成分股的 EPS/营收一致预期存进 `output/vintage_archive/<日期>/eps_consensus.json`，同日重跑覆盖不重复。它从 Invesco 持仓接口拿成分股清单，yfinance 取预期、FMP 补刀，单只失败不拖垮整批。

**它的实际效果/问题。** 这条最值得老板注意：文件用大段文字钉死**边界**——它是"隔离观察数据"，不是正式数据源，严禁进 L1–L5 证据链、严禁当 `evidence_ref`，只有一处" scoped promotion"例外（仅供 NDX 盈利修正指标的时点材料，且±2天内优先）。这正是系统"事件/材料不乱进数据链"原则的延伸，执行得很硬。现实风险：它依赖 yfinance/FMP 的"当前共识"本就未必准，攒出来的"时点史"质量上限受源限制——但至少诚实，不冒充官方。

**我的建议：留（只包不重写，且保留它的硬边界）。** 它是为未来回测埋的种子，纪律正确。需要老板拍板：那处"scoped promotion"（盈利修正指标可用）是否要一直限在±2天、且永远不许它扩张成通用数据源？（建议：钉死，谁要扩都得你单独拍板）

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/vintage_archiver.py`（WHY L2-13、BOUNDARY L15-22、ISOLATION_NOTICE L94、scoped promotion L186-195、`build_archive` L365、`write_archive` L475）。依据：系统说明书 §五(L427 历史时点归档)；CLAUDE.md 常驻边界引用（文件 L17/L166）。

---

**卡 10 · 初判【珠玉】：重算带（recompute_belt.py——防编造数字的"第二本账"，老板最关心的正道）**

**它是什么、哪来的。** `src/recompute_belt.py` 是老板很在意的"数字闸门正道"。它叫"独立重算校验带"，外号"第二本账"。来历很硬：一次受控篡改实验证明，原来的数据完整性检查（卡7）在十个快照数值被改的情况下**零反应**——它只查结构完不完整，从不查数值对不对；历史上一个差了一个数量级的净流动性值就这么混过了所有门。于是专门造了这条带子来抓这类 bug。

**它现在怎么运作——独立是关键。** 它立了死规矩：绝不许 import 流水线自己的计算代码（tools_*/collector），只用 Python 标准库重算。因为如果两本账共用同一段代码，那段代码的 bug 两本账一起瞎。它从采集器抽出来的"重算原料"(recompute_input，原始序列) 重新算一遍，和流水线写进快照的数比对，差值在容差内算 match、超了算 deviation。容差都按"为什么是这个大小"命名并记录（如四舍五入2位小数留0.01余量、分位用管线自己的算法重算）。

**它现在能对哪些数字重算（接进的环节）。** 已在 `run()` 里接好的检查共15组：Damodaran ERP 分位、Wind PE 分位、Wind 排名分位自洽、VIX 期限结构分位、联邦基金利率路径（含成交量稀簿判定）、净流动性（及单位量级哨兵，专抓十亿/百万混用）、M7 资本开支量级、M7 业绩静默日历、M7 回购现金流、跨指标比率（VXN/VIX、ERP、PE 等内部勾稽）、利率/波动率偏离百分比、L5 移动均线(SMA/RSI/MACD/ATR/ADX 从原始 OHLCV 独立重算)、多尺度均线位置、动量字段、以及"审计值序列分位"和"未编目分位字段扫描"。它和卡7咬合：重算带跑出的"关键偏差(critical_deviation)"会被 DataIntegrity 列为发布阻断理由之一；且带子自己崩溃也只记录、不拦发布。它诚实声明覆盖缺口——查不到原始料的字段标 `unrecomputable_missing_raw`、还没接的标 `uncovered`，让缺口看得见而非假装覆盖。

**它的实际效果/问题。** 这就是老板要的"防编造数字"的正道：用一本不共享代码、从原料重算的独立账，专补"只查结构不查数值"的洞。实验已经证明原门有此漏洞，这条带正是补丁。现实局限（代码自己认）：不是每个数字都覆盖，部分 L5 技术指标在 `L5_UNRECOMPUTABLE_TECHNICAL_KEYS` 里暂未覆盖；覆盖度靠 `coverage_ratio` 透明展示。

**我的建议：留（强烈建议只包不重写，且优先把覆盖缺口补上）。** 它已是系统最值钱的反造假机制。需要老板拍板：是否批准把"未覆盖字段"逐步补齐（尤其 L5 那几个暂未重算的技术指标），并把它从"应急可关"升格为"默认永不可关"？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/recompute_belt.py`（模块说明 L1-24、INDEPENDENCE RULE L12-18、容差常量 L40-94、`run` L1789 接15组检查、`critical_deviation_findings` L1826 供卡7阻断；覆盖字段含 `check_net_liquidity` L764、`check_l5_moving_averages` L1508、`check_wind_pe_percentile` L298 等；不可重算键 L1362）。依据：系统说明书 §五(L427 重算带)；checker.py L216/L350 接入阻断；investigation_reports/20260711_first_principles 工单#3（篡改实验出处，文件 L3-10）。

---

**卡 11 · 初判【珠玉】：证据登记（data_evidence.py——每个数字的"出生证/护照"）**

**它是什么、哪来的。** `src/data_evidence.py` 是给每个数据指标发"证据护照"的登记处。来历：它是系统"数据发言权"原则（系统说明书 §4.5）的代码落点，要求每个 L4 指标带上 data_date、来源等级、公式、覆盖率、异常、降级链等元数据。

**它现在怎么运作。** 它定义了一套"证据护照合约"（`REQUIRED_DATA_QUALITY_FIELDS`，17个必填字段：provider、source_name、source_tier、as_of_date、vintage_date、formula、coverage、anomalies 等）。`normalize_data_evidence` 给每个指标贴护照；`data_evidence_issues` 做体检，分三档严重度：hard_block（硬阻断）、degraded（降级不阻断）、audit_warn（进审计区）。最关键的一条硬阻断是 **`proxy_marked_as_official`**——如果一个本该标"proxy/成分模型"的数被标成了"官方 Wind"，直接硬阻断，这正是老板担心的"来源不明冒充事实"的防火墙。`source_authority_model_for_tier` 把来源等级映射成使用权限（rejected/audit_only/supporting_only/core_allowed），遵循"最小权限"。

**它的实际效果/问题。** 这是把"不许冒充事实"变成机器可查的核心机制，和重算带（卡10）、完整性检查（卡7）三位一体。它明确把"proxy 冒充 official"列为硬阻断，等于在数据入口就焊死了老板最怕的那类造假。我读到的都是正面实现，没发现它自己放水或越权。小待确认：`LATEST_ONLY_FUNCTIONS` 这类"只返回最新、回测不可用"的清单在此登记，和采集器（卡6）的跳过清单是否完全同步，建议老板让人核对一处（待确认，不影响评级）。

**我的建议：留（只包不重写）。** 它是"来源可信"的守门员，原则执行得硬。需要老板拍板：`proxy_marked_as_official` 这类硬阻断，是否要扩到"第三方归因(如 HoM)未带未核验声明"也升为硬阻断（目前 HoM 只带声明、未阻断）？

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/data_evidence.py`（`REQUIRED_DATA_QUALITY_FIELDS` L16、`normalize_data_evidence` L474、`data_evidence_issues` L602、`proxy_marked_as_official` 硬阻断 L653、`source_authority_model_for_tier` L442、`LATEST_ONLY_FUNCTIONS` L81）。依据：系统说明书 §4.5(L382-383 元数据要求)；data_evidence.py 全文。

---

**卡 12 · 初判【珠玉】：证据家族 + 数据可用性（把"独立证据"数对的尺子；以及"无数据"哨兵）**

**它是什么、哪来的。** 这两块合在一张卡，因为它们都是"怎么诚实地数证据、不虚标"的基础件。①证据家族 `src/core/evidence_families.py`：一张"函数→证据家族"映射表；②数据可用性 `src/data_availability.py`：一套"无数据"哨兵机制。来历：证据家族是 2026-07-11 一次根因审查(GOV-04)的产物——发现原来置信度按"函数个数"算，会把"同一份数据算出的11个L5指标"虚高成11条独立证据。

**它现在怎么运作。** 证据家族：把每个采集函数归到"独立底层证据流"。同一份数据不同变换必须同族（如11个L5技术指标都读同一份QQQ OHLCV，同属 `qqq_ohlcv_technical`；L3 宽度四件套同属 `ndx_constituent_price_panel`）；但同一供应商不同经济含义仍分族（Wind 估值快照 vs Wind 盈利修正）。卡7 据此在家族级算置信度，N个函数共享一源最多贡献1分，避免虚高。映射由测试强制"每个函数必须有显式条目"（不许静默兜底）。数据可用性：当取不到数时，`normalize_no_data_payload` 贴 `NO_DATA_AVAILABLE` 哨兵，并写明 `allowed_use="data_boundary_only"`、`forbidden_use="do_not_estimate_or_fabricate"`（只当数据边界用、严禁拿去估算或编造）——`has_meaningful_observation_value` 负责识别"看起来有值其实没有"。

**它的实际效果/问题。** 两块都是"反虚标、反编造"的底层纪律，且都设计成"宁可保守、失败也安全"（映射缺失就当独立单例、不稀释别人；无数据就显式哨兵、禁止冒充观测值）。没发现越权或误伤。小待确认：证据家族是"函数→家族"而非"源序列→家族"，像净流动性把三个FRED序列合成一个函数，会少算它本该多占的权重（文件自己承认这是已知局限、超出本次工单范围），老板可留意。

**我的建议：留（只包不重写）。** 它们是诚实计数的地基，原则正确。需要老板拍板：证据家族要不要从"函数级"升到"源序列级"（如净流动性拆成三个FRED家族），以更准地反映证据多样性？（属增强，非重写，建议排期）

**老板盖章：**＿＿（留 / 杀 / 重做 / 待定 + 一句话）

> 存档（AI 核查用，不用读）：真名 `src/core/evidence_families.py`（`EVIDENCE_FAMILIES` L44、`family_of` L196、GOV-04 背景 L5-42、测试强制完整 L38-41）；`src/data_availability.py`（`NO_DATA_AVAILABLE` L7、`has_meaningful_observation_value` L53、`no_data_reason` L91、`normalize_no_data_payload` L166 含 forbidden_use L174）。依据：系统说明书 §五(L425 证据家族、L427 数据可用性)；两文件全文及 docstring。
