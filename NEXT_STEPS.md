# NEXT_STEPS — 下一步做什么

**这份文件是写给"下一个对话"看的**：如果你是一个刚被打开、没有这段历史记忆的 agent，只要读完这一份，加上 `CLAUDE.md` 的常驻边界，就应该知道现在该往哪个方向使劲、哪些是真正卡住等人拍板的开关、去哪份文档找细节。不需要在这里重新读懂全部历史。

**这份文件是路线图，不是任务清单**：不追求穷尽每一条待办的操作细节——具体怎么做，凭你自己的判断加上下面指的源文档就够了。这份文件负责回答三件事：现在整体状态如何、往哪几个方向推进、哪些是等人拍板的开关。

**更新纪律（这是这份文件不腐烂的唯一保证）**：每次做完一批工单，在写 `WORK_LOG.md` 完成记录的**同一次编辑**里，回来改这份文件——划掉已经完成的方向、更新变化了的现状、补上新发现的缺口。只更新 `WORK_LOG.md` 不更新这里，等于没做完收尾这一步。

最后重写：2026-07-13（这是把旧版 `NEXT_STEPS.md` 和 `人话进度报告.md` 里"重要且未完成"的事项合并后的全新结构；旧版扁平表格已经出现过"标了完成标准却早就做完""方向已经判死却还写着去验证"这类失修问题。旧版全文在 `git log -p -- NEXT_STEPS.md` 里，需要时可以找回，这里不重复存档）。最后更新：2026-07-22（报告质量工单 Q1/Q3-Q6 完工，Q2 用户裁定本批暂缓；三轮提交前复审发现的问题均已逐条修复；测试基线 911。详见 `investigation_reports/20260720_report_quality/WORK_ORDERS.md`）。

---

## 现状快照

- 系统经过一轮大修：北极星从"推理链条可审计"改写为"判断要准，且能被验证越来越准"（可审计降级为手段）。
- 完成过一次完整的 fresh E2E 验收（run `20260712_221916`）：publishable、判断姿态经人工审核合格、两个新指标全链零越权、独立重算校验带零偏差。
- 2026-07-14 完成数据基础三连修与证据菜单再平衡收官（幸存者偏差硬防线、手工槽位边界、fed funds 利率路径、回购/静默期日历、DataIntegrity 家族计分、对称性审计裁决"菜单不再先天偏空"、L3/HoM live 验收），详见 `WORK_LOG.md` 当日条目。
- 测试基线：**911** 个单测全绿（`.venv/bin/python -m pytest --cache-clear -q`，58 条均为既有第三方弃用/数值精度 warning）；报告质量工单 Q1/Q3-Q6 及三轮提交前复审修复均已纳入回归测试。DataIntegrity 现在报两个数字：家族口径 confidence_percent（发布闸门用）与函数口径 function_availability_percent（对照用）。
- 2026-07-16 已验证 SEC 可通过 YKK 节点访问；Clash Verge 只新增 `data.sec.gov → YKK`，FRED 仍走 YKK，其余外部域名仍走 IPRoyal。项目实际 `_fetch_sec_xbrl_summary("AAPL")` 已恢复成功，M7 capex/buyback 的 live/PIT 主路 smoke 也已通过。
- **git 状态**：W1-W7 验收批次已于 2026-07-19 合并并推送；2026-07-22 报告质量工单 Q1/Q3-Q6 的目标是在提交前复审与全量测试验收后，由 `discuss-l4-redesign` 无损快进合入 main。远端推送状态以实际 git 状态为准。
- "已经做完的事"的完整记录看 `WORK_LOG.md`（技术记录，按时间倒序）和 `人话进度报告.md`（给项目主人看的白话版，按"封信"倒序）——这两份不用在这里重复，读它们就知道最近做完了什么。

---

## 现在最值得推进的方向（按重要性排序，不代表工时顺序）

### 0. 报告内容级遗留已收口（2026-07-20；Q2 权威性审计仍按用户裁定暂缓）

- ✅ **L2 利差单位**：采集值保持 FRED 原始百分比口径，unit、canon、提示词和运行时 few-shot 已统一为 `X.XX%（≈XXXbp）`，不再把百分点原值误写成基点。
- ✅ **外部世界 governed 总结**：新增独立合约、事件卡白名单、有效日期与材料质量边界；失败时留空，不允许模板句冒充分析。真实 live 文案验收留到下一次正式 run。
- ✅ **final_stance 短姿态枚举**：新增五档受控标签、同义归一化、非法值与方向冲突清空留痕；旧档案继续兼容。
- ✅ **W2 补采清单质量**：未回答问题不再静默漏采；具体缺口与低质量占位分账，并统计 `low_quality_count`。

2026-07-17 已完成剩余三项：

- 旧快照中“明确失败却标 available”的旧形状会被兼容归一为 unavailable；新合约仍严格阻断空值。`20240805`、`20250409`、`20260509` 三份旧快照复核后仍全部 blocked/unpublishable，未来数据和低层覆盖没有被兼容逻辑洗白。
- 原始长序列改为顶层 `recompute_inputs` 审计附件，不进入 L1-L5 raw prompt；快照回放、手工 payload 和最终 prompt 也有独立清洗闸门。2026-07-17 最新完整 live 采集后，第二本账从 **61 个缺原始数据降到 6 个**，共检查 223 项、217 项吻合、0 偏差、0 未覆盖，覆盖率 97.31%；本轮 6 个剩余项是 Wind 不可用连带 ERP 缺值 1 项及第三方检查字段无原始序列 5 项，继续如实保留。
- VIX、VXN、铜金比、HYG、XLY/XLP、拥挤度、VXN/VIX、CNN 恐贪 8 个弱权限指标统一带 field-level `metric_authority` 和 `downgrade_rules`，并进入 Evidence Passport；安全策略采用“最小权限胜出”，旧 payload 不能把它们抬成核心证据。不可用或没有有效观测值的护照一律 `verified=false`。
- 第二本账同时抓出并修复一个旧错误：带 `date` 列但不是日期索引的月度序列，1 年分位曾误用全部历史。

已完成、不要重做的（详见 WORK_LOG 2026-07-14）：幸存者偏差硬防线（#19）✅；手工槽位边界（#7）✅；DataIntegrity 家族计分双数字（#20）✅；利率预期路径缩水版 + 回购/静默期日历 + 对称性审计（#4 全部收官，裁决：证据菜单不再先天偏空）✅；L3 广度/HoM 撤回 live 验收 ✅。

### 2. 盈利预期这条防线要继续加固

Wind 美股一致预期已经实测判死（有权限查港A股、没权限查美股）。现在的路线是 yfinance `eps_trend`（近路，已可用，能算修正斜率）+ 自建 vintage 档案（`src/vintage_archiver.py`，**已经装成 macOS 定时任务每天 9:30 自动跑，不需要再决定要不要装**）。接下来：

- 当前只继续审 HoM 的获取接口、`current` 与历史尾值一致性、数据更新时间、历史截断和 percentile 计算。HoM 是公开的独立聚合站，不是 Bloomberg 官方接口；其站点说明 forward-PE 文件由人工维护，因此只能作为第三方参考锚，不能把“Bloomberg BEst”标签升级成 Bloomberg 官方事实。若 HoM 断源，优先降级为 trailing PE + 盈利修正/质量代理，不用不完整的成分股自算冒充同口径 forward PE。
- 2026-07-17 已把运行时来源名改成“HoM 公开 API 自述归因 Bloomberg BEst，但归因未独立核验”，避免界面把它看成 Bloomberg 官方源；Top 15 自算、FMP/Finnhub 都未动工。
- HoM 的替代路径按可靠性排序：Bloomberg Terminal/API 或 FactSet、LSEG、S&P Capital IQ 等付费一致预期源；Nasdaq 官方研究材料可作低频人工交叉核验，但不是稳定日更 API。免费源最多只能做完整 NDX 成分股模型，必须同时满足点时权重、价格、全体成分股 forward EPS 和负盈利处理，不能用前 15 只代替全指数。
- FMP/Finnhub 历史一致预期端点暂不评估：在决定不把前 15 自算接入主链后，继续追逐多年期免费一致预期的投入产出比偏低；若以后要做深回测，再单独立项。
- 这条不需要人现在做什么，只需要记住：claim 打分器要等到 **2026-07-27** 之后才会有第一批真正成熟（≥20 天）的历史判断可以打分，之前打回来的"全部太年轻"是设计内的诚实跳过，不是 bug，不用去催、也不用现在去验收它。

### 3. 个人决策接线：工程接线完成，只等用户日后确认阈值

- 已修复 tracked IPS schema 与 `UserDecisionProfile` 字段不匹配却静默变成 0 条纪律的问题：现在只读取明确的 `reader_exit` 白名单，私人金额/持仓不会进入产物；没有用户确认的阈值时，Golden Pit 明确显示“尚未配置/证据不足”，不再生成默认纪律。
- 状态变量已带可在 Evidence Registry 中解析的完整字段路径、单位和允许比较符；Wind PE 分位路径改为真实的 `PEHistoricalPercentile`，派生回撤同时引用 Donchian 上轨与 QQQ 价格。日后填写阈值时必须同时写 `threshold_status=confirmed` 和正确单位，否则 fail-closed。
- 仍需用户日后逐条确认真实阈值；本轮没有猜 forward PE、回撤或任何金额阈值。W3（2026-07-19）已建成"长期资产评估层"（`LongTermAssessment`），未来"买卖纪律阈值"工单归入该框架施工，阈值本身仍 fail-closed 等用户数字。
- ~~早前提过的"三道风险口味问题"~~ 已定案（2026-07-13 主审确认）：被工单 #17 取代——用户明确表示以个人投资政策书为准、前两题外行难以作答，不再用问卷追问；画像唯一权威来源是 IPS 文档。

### 4. 输出体验本轮已完成

- predicate 条件现在只引用自己状态变量对应的精确 evidence ref，并生成自己的失效条件；旧的无 predicate 条件才保留 claim 级兜底。
- HY OAS、ADX、MACD 等常见术语已加入本地静态白话解释，支持鼠标悬浮、键盘聚焦和点击，不依赖外网。
- 报告审计区会显示快照文件名、绝对引用、真实文件 SHA256、采集时间、有效日期和 live/replay 模式；只要 `--data-json` 搭配 `--date`，快照必须带完全相同的明确历史有效日，live/未标日期快照和 event-only 路径同样拒绝重贴历史日期。
- 简洁研究启动器已实现并通过控制台/服务测试；旧路线图条目不再是待办。
- 2026-07-19 已按唯一 style-b demo 返工 brief：章节固定为门脸 + 01..08；外部世界与第三层裁决由结构化参数装配，不再字符串替换；门脸单层卡、判决正文分段、抽屉多引用拆分与伪引用降级均已验收。控制台完成后的链接只保留综合总报告、Workbench 与完整报告；JSON/废弃事件页均下架。随后用户截图复核发现主论证、五层与抽屉仍像后台界面，已完成二次结构返工：正方改为文章式支撑链，五层改为摘要优先 + 两张关键读数，完整指标卡只留独立底稿；抽屉改为生成时明确的短数值 + 辅助说明，并隐藏机器编号。真实 run `20260719_130534` 已重生成为当前 brief。

### 5. 报告与新闻层重构（2026-07-17 立案，当前最大施工方向）

2026-07-17 四问诊断后立工单包 **R1-R8**（细则与提示词在 `investigation_reports/20260717_redesign/WORK_ORDERS.md`）。**2026-07-18 两轮定时验收后的状态**：

- ✅ **已验收通过五单**：R1 模板止血、R2 脊柱重排（brief 从 744KB 瘦到 92KB、B 风格 slate_v3 皮肤已交付并设为默认）、R4 受控调查执行器（降级机制史上首次开火）、R5 新闻官方底账（Fed/BLS/BEA 通了、时间窗/实体门/source_tier 三 bug 修死、标签去 L1-L5 化）、R7 Thesis 强制回应竞争假说。
- ✅ **R3 判决正文**（2026-07-18 充值后关单）：提示词三轮实证定稿（600-1200 字带、五风险点名制、主要理由强制标注、audit-only 数值禁令），1198 字终验样本 Fable 亲读合格。**R1-R7 七单全部关单。**
- ✅ **R6 LLM 事件卡验收通过**（2026-07-18 晚，Codex 施工/Fable 亲验）。
- ✅ **R8 第三层真裁决终稿完成**（2026-07-18 深夜，Fable 亲做+Codex 红队 15 条+逐条裁决，详见工单状态行与 `r8_redteam_report.md`）。**重构工单包 R1-R8 至此全部关单。** 遗留小单：门脸署名切换的语义锚机制（红队 C1 裁决暂缓切换，机制补上后再切）。
- ✅ 两批工作已合并提交 `afc44a6` 并推送 main（用户拍板：全部提交、仓库保持公开）。

### 7. 第一性原理发现施工（2026-07-18 立案，2026-07-19 收官）

对辩终审（`investigation_reports/20260718_first_principles_debate/DEBATE.md`）的八项发现立案为工单包 **W1-W8**（同目录 `WORK_ORDERS.md`）。**2026-07-19 状态：W1-W7 全部完工，Fable 逐单亲验通过，统一提交（868 测试全绿）；W8 事件研究维持冻结，激活条件写在工单里。** 逐单验收细节见工单包"验收记录区"，完成记录见 `WORK_LOG.md` 当日条目。遗留可自然覆盖项：W3/W4 的真实 LLM/FRED 在线抽样、W7 本机 CFTC API 的 TLS 连接（当前诚实 unavailable），下一次完整 live run 顺带验证即可。后续相关：7-27 首批成熟评分到来后，按 `RUN_REVIEW_CHECKLIST.md` 新增的"评分复盘流程"走 error_taxonomy 聚类 → 人工裁决 → method_revision_ledger 登记。

### 6. 工程债，不影响功能，随时可以往后排

本轮评估后不动工：巨型文件拆分需要单独设计与迁移验收，当前改动只会扩大回归面；旧文档归档也不影响发布质量。

- `orchestrator.py` / `tools_L4.py` 两个巨型文件按已有方案拆分模块。
- `docs/` 目录下的旧审计/实验材料归档瘦身（历史材料容易让后续 agent 误读成当前事实）。

---

## 等你拍板（不做决定就会一直卡在原地的开关）

1. **SEC 官方数据通道已恢复，主路验收完成**：Clash Verge 只把 `data.sec.gov` 加到 YKK，FRED 仍走 YKK，其他外部域名不变；M7 capex/buyback 的 live 与带 `effective_date` 的 PIT smoke 已确认 SEC 官方字段重新进入主路。回购仍有 1 家缺官方字段，保持缺失，不用 yfinance 越权补齐。
2. **GitHub 仓库要不要转私有**：现在是公开的，这也是为什么工单 #17 要求真实金额永远不能进 git 跟踪文件的原因。
3. **纳指集中度书面论证**：长期目标是纳指到总资产 45-50%，但政策书里战略权益内的临时上限是纳指/标普各 50%，超过上限需要单独写明长期优势来源和集中风险。这份论证还没写；`config/user_decision_profile.local.json` 里已标记 `concentration_argument_status: not_completed`。
~~4. 三层架构第一性原理审查结论是否立案动工~~ 已拍板（2026-07-18）：**按序全立案**，工单包 W1-W8 见方向 7；协作方式沿用"Fable 写工单与全部提示词、用户手动派 Codex、Fable 验收"。
（2026-07-18 已销案三项：DeepSeek 已充值；两批工作已提交推送 `afc44a6`；仓库定案保持公开、"转私有"不再是待决项。）

---

## 明确暂缓或不做的事（防止后续 agent 白跑方向）

- OpenBB：暂缓平台接入，只吸收 provider metadata 和数据治理思路。
- 前 15 权重股 forward EPS 自算：暂缓，不进入主证据链。原因是 NDX 指数单位不能直接除以少数成分股的每股 EPS；前 15 只覆盖不完整、剩余成分股处理需要额外假设，且 yfinance forward EPS 不是稳定的官方一致预期源。只有 HoM 来源断裂或算法无法解释时，才重新评估是否做成独立交叉验证。
- Trendonify 浏览器 sidecar：只作审计附件，不得进入 L1-L5 raw prompt、主证据、分位选择或当前来源投票。
- 正式前端框架化：`brief`/console/workbench 信息架构稳定前继续用 self-contained HTML，不引入框架。
- 新闻 LLM 解读：只做官方事件底账和事件-数据时间邻近连接；泛新闻情绪与摘要由三层研报结构承接，不回流纯数据链。
- 交易执行、组合建议、自动下单：不属于 vNext 当前范围。

---

## 文档地图（这份文件和其他文档的关系）

- 已经做完的事、验证结果 → `WORK_LOG.md`（技术记录）
- 每件事的调查细节、编号、施工方/验收方 → `investigation_reports/20260711_first_principles/WORK_ORDERS.md`
- 给项目主人看的白话进度信 → `人话进度报告.md`
- 长期架构原则和常驻边界 → `ARCHITECTURE.md` / `CLAUDE.md`
- 数据源覆盖与边界 → `DATA_COVERAGE_REVIEW.md`
- 真实 run/回测复盘流程 → `RUN_REVIEW_CHECKLIST.md`

这份文件不重复以上任何一份的内容，只负责回答"现在往哪走、谁在等谁拍板"。
