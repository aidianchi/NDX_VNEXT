# T49 第一件 · 施工任务书（存档副本，以对话中粘贴的代码块为准）

你是执行者，这份文档是你唯一的任务来源；中途没人可问，拿不准的写进 `BLOCKED.md`，跳过继续做别的，最后随交付提交。
断了或换新会话：先读项目根的 `PROGRESS.md` 接着做，别重做；每做完一项立刻更新它。
这活为什么干：这是一套纳指 100 投研流水线，分数据链和事件链两条独立的链。事件链（第二层）现在质量不明——材料有多少只有标题没有全文、来源靠不靠谱、有没有整个渠道静默漏事件，都没有数字。你的任务是把现状量出来：一张采集源体检表 + 五项质量指标的基线数字。**只测量，不改变系统任何行为。** 这些数字是后续改造工程的设计输入和达标尺子，数字不准，后面全错。
让步顺序：测得真 > 测得全 > 测得快。量不出来的指标如实写"测不了+原因"，严禁填估计值。
「只允许」「不许」是死规矩，违反即失败；「建议」有更好的路可以走，在 `PROGRESS.md` 记一句为什么。

## 我替领导拍的板

- 测量对象 run：默认 `output/analysis/vnext/20260725_145833/`（说明书记载的最近完整成功运行）。若你发现更新的、且同时含 `final_adjudication.json` 与 `event_interpretation_cards.json` 的完整运行，改用它并在报告注明（猜的：领导会同意）｜猜错代价：重跑一遍脚本而已。
- "必要吗"拿不准的源：标"存疑"，不猜｜代价：无。
- 新文件只许建在 `scripts/layer2_baseline/` 与 `investigation_reports/20260811_layer2_research/baseline/` 两个新目录｜代价：无。
- 报告用中文大白话，每个数字后面跟一句它怎么算出来的。

## 界限

- 可写白名单：上述两个新目录 + 项目根 `PROGRESS.md`、`BLOCKED.md`。其余一切只读。
- `src/`、`tests/`、`output/` 一个字符都不许改。`output/` 是测量对象，改了等于篡改证据。
- 看到现成 bug（比如正文被硬截断在 2200 字符）不许顺手修，记 `BLOCKED.md`。
- 不装新依赖，只用项目 `.venv` 里已有的库。

## 现状（2026-08-11 实测）与任务 0

- 项目根：`/Users/aidianchi/Desktop/ndx_mac`；Python 一律用 `.venv/bin/python`。
- 测试基线：`.venv/bin/python -m pytest tests/ -q` → 1084 passed, 2 failed（2 个失败是 console_run_all 既有问题，与采集无关）；`.venv/bin/python -m pytest tests/test_docs_consistency.py -q` → 10 passed。
- git 现状：`git status --porcelain -- src/ tests/` 恰好 5 条（context_spread.py、llm_engine.py、orchestrator.py、test_context_spread.py、test_vnext_orchestrator.py，均为 M）。交付时必须仍是这 5 条，不多不少。
- 采集源注册处：`src/news_event_ledger.py` 顶部的 `OFFICIAL_CALENDAR_SOURCES`、`SOCIAL_RSS_SOURCES`、`WIND_DOC_QUERIES`、`ALPHA_VANTAGE_TICKERS`，以及同文件后续代码里的 SEC EDGAR、Yahoo Finance、BLS Latest 等——自己通读找全，注册的源不止运行里出活的那些。
- 样本 run 实况：`event_source_raw.jsonl` 共 87 条材料，仅 22 条有全文（`raw_text_available=true`）；13 个源出了活；`event_interpretation_cards.json` 选中 9 张卡。
- 任务 0：复跑上面两条测试命令和 git 命令核对数字；确认 run 目录与文件存在。任何一项对不上就停，证据写 `BLOCKED.md` 最上面，只做不受影响的部分。核对无误后，把"理解的目标／顺序／最大风险"≤10 行写进 `PROGRESS.md` 再动工。

## 任务 1：采集源体检表

目标：一张表覆盖代码里注册的**全部**采集源（包括注册了但这次没出活的）。每源三列判定：①靠谱吗（官方／授权／主流／弱）②必要吗（它供给哪类材料——已发生／将发生／被相信／规则语境；与别的源供给重叠的标出来）③能死板拿吗（无需登录、无需浏览器、接口稳定；需要登录或浏览器的标"不能死板拿"）。
产出：`investigation_reports/20260811_layer2_research/baseline/源体检表.md` + 同目录机器可读 `sources_audit.json`。
验收：`.venv/bin/python scripts/layer2_baseline/audit_sources.py --check` 退出码 0，且输出"注册源数 = 表内源数"；每个源都有三性结论或"存疑"。

## 任务 2：五项度量基线

对样本 run 算五项（定义出处 `investigation_reports/20260811_layer2_research/02_第二层形态探讨稿.md` 的 Q5 节）：

1. 全文率：`raw_text_available=false` 的占比。
2. 来源等级分布：按 `source_type` 七档统计条数与占比。
3. 主张对账通过率：用固定种子抽 5 张事件卡，把每张卡 `fact_summary`/`interpretation` 里的具体断言逐条对回 `event_source_raw.jsonl` 原文，判"原文有／原文无／原文不全"。每条判定必须引用原文的 source_id 与字段，不许凭印象。
4. 渠道真空覆盖率：注册源里本次运行有明确"有／无新事件"记录的比例；注册了却静默无记录的逐个列名。
5. IA 引用可用率：IA 实际输入里事件卡关键字段（时间、来源、等级、正文）完整的卡占比；IA 输入看 `synthesis_packet.json` 与 `integrated_synthesis_report.json`，找不到事件卡输入落盘就如实报"测不了+原因"。

第 1、2、4 项必须纯脚本可复跑：`.venv/bin/python scripts/layer2_baseline/baseline_metrics.py --run output/analysis/vnext/20260725_145833` 输出 JSON。
反向验证：把 `--run` 指向一个不存在或残缺的目录，脚本必须报错并非零退出，不许输出漂亮数字——贴一次变红输出，再贴恢复后的正常输出。
报告：`baseline/基线报告.md`，五项各一节；测不了的写原因。
验收：脚本连跑两次输出逐字节一致；报告数字与脚本输出一致；全文无估计值。

## 规矩

- 本任务最大的作弊姿势是**编数字**：任何没有脚本输出或原文引用撑腰的数字都算作弊。第二姿势是偷换测量口径让指标好看（把难判定的卡踢出样本等）——抽样种子固定并写进报告。
- 测试数只许 ≥ 基线，skipped 0；不许用 skip、放宽断言、mock 被测对象、删测试、`|| true`。
- 同一验收连败 3 次换下一项；结果比开工差就回滚如实报告——"没做成但说清了"合格，"做了但更糟"不合格。

## 完成条件

1. 基线报告五项每项有数字或有"测不了+原因"，脚本可复跑（贴两次逐字节一致的输出 + 反向验证红→绿证据）；体检表覆盖全部注册源。
2. `pytest tests/ -q` 仍是 1084 passed / 2 failed，docs_consistency 10 passed；`git status --porcelain -- src/ tests/` 仍是原 5 条。

每条都要在对话里贴实际命令输出，只说做完了不算。`BLOCKED.md` 随交付提交，空的写"无"。
止损：同一验收连败 3 次即换项；整体跑满 2 小时即停，如实汇报卡在哪、还差什么。
