# T49 第二件 · 施工任务书（存档副本，以对话中粘贴的代码块为准）

你是执行者，这份文档是你唯一的任务来源；中途没人可问，拿不准的写进 `BLOCKED.md`，跳过继续做别的，最后随交付提交。
断了或换新会话：先读项目根的 `PROGRESS.md` 接着做，别重做；每做完一项立刻更新它。
这活为什么干：这套纳指 100 投研流水线刚量出事件链的底子——80% 材料只有标题；更疼的是，就算采到了正文，事件卡落盘时正文被丢掉，第三层裁决（IA）一张卡的正文都看不到（实测 0%）。你的任务：把正文接上链——事件卡落盘时带上原文摘录，IA 看到的卡里也要有。全程离线验收，**不许 live 重跑流水线**（那要花 API 钱，留给领导决定）。
让步顺序：不断链 > 逐字保真 > 改动小。
「只允许」「不许」是死规矩，违反即失败；「建议」有更好的路可以走，在 `PROGRESS.md` 记一句为什么。

## 我替领导拍的板

- 正文摘录截断上限 2200 → 8000 字符（猜的：正文宁多勿少，token 成本可忽略）｜猜错代价：成本略涨。
- 新字段名 `evidence_excerpt` 与 `raw_text_available`（猜的，语义直白）｜代价：无。
- IA 侧摘录再截断到 500 字符（够核对、控成本）｜代价：无。
- 三个报废源（nasdaq 超时、alpha_vantage 缺 key、wind 公告实体匹配丢）**不修**：alpha_vantage 要领导的 key，不是你能拍的；三条都记 `BLOCKED.md` 待裁决。
- `synthesis_packet.event_index` 为空的问题**不修**，记 `BLOCKED.md`。

## 界限

- 可改白名单：`src/news_event_ledger.py`、`src/agent_analysis/orchestrator.py`、`src/integrated_synthesis_report.py`、`tests/`（只许新增测试文件或追加用例，**已有断言一个字符不许动**）、`scripts/layer2_baseline/**`、`investigation_reports/20260811_layer2_research/**` 新建文件、项目根 `PROGRESS.md` 与 `BLOCKED.md`。
- 其余一切只读。特别地：`contracts.py` 不许动（证据注入在模型验证之后做，模型契约零改动）；`output/` 只读，它是验收证据。
- 不装新依赖；不许新增任何 LLM 调用（注入是纯代码搬运）。

## 现状（2026-08-11 亲手定位）与任务 0

- 断点①：卡生成的输入里本来有 `raw_text_excerpt`（`orchestrator.py:1500`），但输出契约（:1505-1527）没要它，落盘即丢。
- 断点②：IA 实际看到的卡由 `_compact_card_for_prompt`（`integrated_synthesis_report.py:730`）压缩，只有 fact_summary 等字段，无正文、无 `raw_text_available` 标志。
- 截断字面量 2200 共四处：`news_event_ledger.py:256`（默认参数）、:325、:847、:1074（显式调用）。
- 判卷标准（冻结）：`tests/test_vnext_orchestrator.py` 的事件卡测试群（:203/:272/:326/:374/:408/:438 等）、`tests/test_governance_input.py`、`tests/test_bridge_v2.py` 的隔离断言（event_refs 恒空、must_not_feed_back）。全量基线：1084 passed / 2 failed（console_run_all 既有两败），共 1086 项。
- git 现状：`git status --porcelain -- src/ tests/` 恰 5 条 M（名单见 `investigation_reports/20260811_layer2_research/验收指纹_T49第一件.sha256`）。本次允许 orchestrator.py 内容变化；**其余四个文件交付时指纹必须仍与该文件一致**；名单不许出现第六个文件。
- 任务 0：复跑全量测试与 git 命令核对上面数字；按符号名找到三处断点（行号可能漂移，找不到就停）。核对无误后把"理解的目标／顺序／最大风险"≤10 行写进 `PROGRESS.md` 再动工。

## 任务 1：截断上限 2200→8000

在 `news_event_ledger.py` 顶部定义命名常量（如 `RAW_TEXT_EXCERPT_LIMIT = 8000`），四处全部改为引用它。
验收：新增测试喂 5000 字符正文，断言落盘 excerpt 长度 >2200；`grep -n "2200" src/news_event_ledger.py` 输出为空。

## 任务 2：卡落盘注入证据字段

在卡模型验证**之后**、落盘**之前**，orchestrator 把输入材料的 `raw_text_excerpt` 与 `raw_text_available` 逐字注入卡 dict，字段名 `evidence_excerpt` / `raw_text_available`。模型契约（contracts.py、output_contract 提示）一个字不改——**注入不是让模型回显**：模型回显有改写风险，正文是材料不是创作。空正文纪律：`raw_text_available=false` 时 `evidence_excerpt` 必须为空字符串；limitations 的既有"仅标题"纪律不受影响。
验收：新增测试 mock 卡生成，断言落盘卡的 `evidence_excerpt` 与输入材料逐字一致。

## 任务 3：IA 压缩器带新字段

`_compact_card_for_prompt` 增加 `evidence_excerpt`（截断 500）与 `raw_text_available`。
验收：新增测试断言 IA prompt 负载里的卡带这两个字段；隔离测试群全绿（新字段不外流第一层任何站）。

## 任务 4：离线重装配演示

写 `scripts/layer2_baseline/demo_relay.py`：读旧 run（默认 `output/analysis/vnext/20260731_002156`）的 `event_source_raw.jsonl` + `event_interpretation_cards.json`，输出"注入后 IA 本应看到的卡"，逐字校验每张卡的 `evidence_excerpt` 等于对应源材料的 `raw_text_excerpt`（或为其前缀截断），并按新口径重算 IA 引用可用率（有全文的卡应 >0%）。
反向验证：把一张卡的 `evidence_excerpt` 改一个字，脚本必须报错并非零退出——贴变红输出，再贴恢复后的正常输出。

## 任务 5（小）：渠道真空口径拆三态

`baseline_metrics.py` 第④项改报三态：正常有事件／确认无事件／失败（失败逐个列名），重跑贴新输出——上轮基线已证明"100% 覆盖"里藏着 3 个失败源。

## 规矩

- 防作弊点名：①把 fact_summary 复制进 `evidence_excerpt` 冒充正文——任务 4 的逐字校验必抓；②让模型回显正文代替代码注入；③改旧测试适配新行为；④skip／放宽断言／mock 被测对象／删测试／`|| true`——全算失败。测试总数只许 ≥1086，failed 恰为原 2 个，skipped 0。
- 引文纪律（上轮验收发现的瑕疵，加严）：报告与 `BLOCKED.md` 里引用任何原文必须**逐字复制**并给出文件＋字段位置，不许复述、不许凭印象引句。
- 同一验收连败 3 次换下一项；结果比基线差就回滚如实报告——"没做成但说清了"合格，"做了但更糟"不合格。

## 完成条件

1. 正文上链被离线证明：`demo_relay.py` 输出里凡源材料有全文的卡都带逐字一致的 `evidence_excerpt`，新口径 IA 可用率 >0%（贴输出），反向验证红→绿齐全；五项任务的新增测试全绿。
2. 全量测试总数 ≥1086、failed 恰为原 2 个、skipped 0；`git status --porcelain -- src/ tests/` 仍恰为原 5 个文件，且除 orchestrator.py 外四文件 `shasum -a 256 -c 验收指纹_T49第一件.sha256` 全 OK（贴输出，该文件里 orchestrator.py 那行允许 FAIL）。

每条都要在对话里贴实际命令输出（含反向验证的红→绿证据），只说做完了不算。`BLOCKED.md` 随交付提交（至少含三个报废源与 event_index 两条），空的写"无"。
止损：同一验收连败 3 次即换项；整体跑满 2 小时即停，如实汇报卡在哪、还差什么。
