# 架构迭代时间线：WORK_LOG.md 考古（2026-07-03 之后）

**用途**：架构校准报告素材。把 `docs/2026-07-03_NDX_VNEXT_EPISTEMIC_ARCHITECTURE_REPORT.md`（三层架构：第一层正式观测层 L1-L5 / 第二层外部世界材料层 / 第三层综合裁决层）与之后的实际迭代对账。

**范围与方法**：只读 `WORK_LOG.md`（1593 行，倒序，最新在前），扫描范围为文件开头（2026-08-09）至 2026-07-06 条目为止；2026-07-01 及更早条目超出范围，仅在涉及基线概念（如"事件五段流水线"的定义）时作为背景引用并明确标注。只收"动过架构"的条目：改变站点编排、层间数据流向、站点职责、供给内容、治理链接线。UI/视觉、数据源接入、纯测试修复、纯文档改革类条目原则上不收（个别影响"读者能看到哪层产物"的展示层条目以【次要】标注收录）。日志没写的一律不编。

**行号引用格式**：`L###-L###` 指 WORK_LOG.md 的行号。

**一个日期异常（如实记录）**：WORK_LOG 的 `## 2026-07-29` 节（L297）为空，其下紧跟 `## 2026-07-30`（L299），L301-L435 的所有条目都落在 07-30 节内；但其中 T30/T33 条目内部多处写"2026-07-29 引入"。本时间线按日志节标题记为 07-30，并在相关条目注明内部的 07-29 引用。另外，07-17 与 07-18 没有独立日期节，R1-R8 重构与三层对辩相关条目全部记录在 `## 2026-07-19` 节（L955-L1071）内，条目正文可辨实际工作日期，本时间线按正文标注。

---

## 一、背景基线（2026-07-03 之前，仅供概念对照，不计入时间线）

- **"事件五段流水线"的定义**（2026-07-01 条目，L1562-L1592）：第二层从"新闻/事件标题侧栏"升级为五段式事件研究流水线——**① 采集与时间闸门 → ② 事件聚类与 claim 拆解 → ③ 事件研究包 → ④ 市场验证 → ⑤ 账本/报告/综合层交付**。claim 枚举收敛为 7 类（official_fact / company_disclosure / data_release_claim / interpretation_claim / view_claim / narrative_claim / rumor_claim）。注意：此时全流程**无 LLM**，是规则化研究包（L1592 剩余风险明示"不是 LLM 深度调查"）。
- **三层措辞基线**（2026-07-01 条目，L1511-L1571）：当时提法是"第一层（数据/L1-L5）/ 第二层（新闻事件研报 + 跨层问题交付）/ 第三层（综合报告）"；"外部世界"作为章节名此时尚未出现。

---

## 二、架构迭代时间线（按时间正序）

### 2026-07-06：竞争假说治理链诞生 + Bridge V2 输入收窄 + 统一证据层

1. **阶段 3 最小竞争裁决落地——counter_thesis / hypothesis_competition 两站插入流水线**。新增 `CompetingHypothesis`、`CounterThesisDraft`、`HypothesisCompetition`、`AdjudicationChangeRecord`、`AdjudicationHistory` 合约；在 **Bridge V2 之后、Thesis 之前**生成 `counter_thesis.json`、`hypothesis_competition.json`、`adjudication_history.json`；反方显式禁读 `thesis_draft.json` / `analysis_revised.json` / `final_adjudication.json`；新增非单调重判记录（调查挑战强单一路径裁决时保留旧假说与降级原因）。（L1454-L1481）

2. **审查后更正：Counter-Thesis 改为优先独立 LLM 阶段，确定性构建器降为兜底**。原因：此前记录的"完成"只代表合同与管道完成，不代表行为质量；`InvestigationReport` 增加 `is_deterministic_stub`，确定性调查如实标注"未执行真实调查"，不得作为真实反证触发竞争裁决降级。（L1424-L1450，关键改动见 L1430）

3. **Bridge V2 在 SynthesisPacket 中的贡献收窄**：只贡献反馈摘要和未解决问题，**不再把 Bridge V1 的冲突和主要矛盾重复计入 Thesis 输入**。这是 bridge → thesis 数据流向的一次明确改动。（L1432）

4. **阶段 4 统一证据层落地**：新增 `EvidencePassport` / `EvidenceRegistry` / `ClaimLedger` / `ClaimLedgerEntry`，覆盖数据、事件、受控调查、竞争假说和最终 claim；Integrated Synthesis Report 可读注册表与台账摘要，但**不允许反向污染 L1-L5 或纯数据主链**。（L1487-L1507）

### 2026-07-08：治理链强化 + 架构冻结决定（后被 07-17 打破）

5. **P2 阶段模型路由 + Final/Reviser 证据引用源头校验**：`counter_thesis / thesis / reviser / final` 默认优先 deepseek-v4-pro；reviser/final 生成时递归检查 `evidence_refs`，越界引用在生成阶段打回重试（此前只靠事后 claim 台账降级）。原因：反方阶段常落最弱的 flash 模型；Final 幻觉引用曾把全部 claim 误拉黑。（L1301-L1327，关键见 L1308-L1309）

6. **Counter-Thesis schema 摩擦修复（别名吸收）+ claim 闸门改比例原则**：合约层吸收 `falsifiers` / `explains_poorly` 等已观测字段变体；`_verify_claim_entry` 改为"无法核验的引用点名降级，仅当没有任何可核验引用时才阻断"。原因：两次真实 run 反方 LLM 均死于 schema 摩擦，高质量反方内容被整体丢弃；Final 幻觉出 `L5.get_ta_indicators` 经共享 refs 池把 6 条 claim 全部误标 blocked（"闸门第二次喊狼来了"）。（L1381-L1418）

7. **【治理决定】架构冻结一个季度**：07-07 独立审核宣布"施工收尾、架构冻结一个季度、进入使用期"——冻结期规则为"允许 bug 修复/数据维修/prompt 卫生/呈现打磨，**不允许新增阶段/artifact/改合约**"。（L1264、L1294-L1299）**此决定十天后的 07-17 被 R1-R8 重构工单包实际打破（见第 11 条），是校准报告必须对账的一个拐点。**

8. 【次要】`hypothesis_competition.json` + `counter_thesis.json` 首次进 brief 正文（"谁在竞争解释权"区块）；新闻区读出格新增"第三层综合裁决"一行。展示层变化，不改流水线。（L1280）

### 2026-07-10：L3/L4 职责重划

9. **L3/L4 站点职责调整**：L3 主 prompt 只讨论广度、集中度、Top10/M7 权重，**M7 盈利质量归 L4**；`get_m7_fundamentals` 退出 L3 主运行时；History of Market 不再作为独立 L4 函数重复计票。原因：第一性原理审计发现层职责错配与重复计票。（L1186、L1232）

### 2026-07-14：证据菜单对称性

10. 【次要】**多空证据源对称性审计落地**：裁决"证据菜单不再先天偏空"（31/46 双向、0 个只准乐观、8 个只准风险），3 个真缺口当场补齐（%Above MA 获削弱风险许可、HYG 修复+OAS 收窄可作确认证据、实际利率高位回落可作估值承受力改善证据）。改的是各层供给内容/发言权边界，不是站点编排。（L1101）

### 2026-07-17（记录在 07-19 节）：对照 07-03 文档的总诊断 + R1-R8 立案

11. **四问诊断直接对照 2026-07-03 两份设计文档：认识论架构报告实施率约六成，未实施的恰是灵魂件**——第二层 LLM 卡、第三层真裁决、受控追问执行器、非单调触发。实证：受控追问只有骨架（`is_deterministic_stub` 恒 True，"本轮未执行真实调查"）；非单调降级机制 17 个 run 从未触发（触发源被 stub 堵死）；新闻-数据"综合"是两句模板文案二选一。用户拍板立 R1-R8 工单包（R1 模板处置 / R2 脊柱重排 / R3 判决正文双轨 / R4 受控调查执行器 / R5 新闻官方日历底账 / R6 LLM 事件卡 / R7 Thesis 强制回应竞争假说 / R8 第三层真裁决范围锁定）。（L1045-L1050）

### 2026-07-18（记录在 07-19 节）：三层架构对辩终审 + R8 第三层真裁决 + R4 调查执行器

12. **三层架构第一性原理对辩终审结论：三层划分不动**。唯一骨架级缺陷 = 层间时点契约与 run 生命周期（F8：7-14 数据 + 7-18 事件卡仍判"可发布"）；优先序 F8 > F7（缺 3-5 年层与决策政策分离）> F2 > F6 > F1。（L1007-L1012）

13. **R8 第三层真裁决落地——integrated_adjudicator（IA）正式成为真 LLM 裁决站**：`IntegratedAdjudication` 合约族（姿态锚 + 证据联动 validator）；builder 裁决级改造（effective_date / ref_authority / 调查白名单入载荷、伪引用硬闸门、confirmed 无证据自动降级）；报告新增"第三层·综合裁决"章节——**"新闻出题、数据回答"闭环首次可见**。**门脸署名切换暂缓**（红队 C1：形状校验防不住正文实质改判），第一层 reasoned_verdict 继续坐镇门脸。（L1014-L1020）

14. **R4 受控调查执行器验收通过**：`downgrade_or_split_events` 机制建成后**首次开火**（17 run 恒零 → 1）——求真回路从骨架变成真机制。（L1032）

### 2026-07-19：W 工单包（时点契约 / 缺口反馈 / 长期资产层）

15. **W1 层间时点契约硬闸门（对辩 F8 的施工）**：综合报告所有输入必须同一 as_of 日历日，缺日期/错配一律 fail-closed 为 audit_only，**时点不一致时 LLM 裁决直接跳过**。（L997）

16. **W2 缺口受控反馈**：run 落盘 `recollection_requests.json`，只从三处结构化缺口字段复制文本（代码结构保证够不到判决正文）。（L998）

17. **W3 长期资产评估层（对辩 F7 的施工）**：`LongTermAssessment` 合约上线，3-5 年以上判断与 6-12 月姿态分离。同日 live 事故后改为**字段级 fail-closed**（违规字段清空 + 留痕，run 存活），确立"可选辅助字段违规不得炸掉整次 run"的爆炸半径原则。（L999、L987-L992）

18. **R3 判决正文双轨定稿**：`reasoned_verdict` 机器校验带 300-1300 字（终审判决书从此有成文论证字段，治"报告太碎"）。 （L1024、L1047）

### 2026-07-20：事件链接 LLM + "外部世界"章节定名 + 脊柱定稿

19. **Q3 governed 事件总结——event_section_summary（ESS）独立站点诞生**：新合约 `EventSectionSummary` + 独立提示词 + orchestrator 独立阶段（五重校验：引用声明与正文逐一对应、卡片白名单、2-5 张引用带、固定边界句、禁 L1-L5 ref）+ 失败宁缺毋滥留痕 + **渲染进外部世界章节顶部**。原因：外部世界章节缺合规 LLM 总结（07-19 遗留问题），此前模板句冒充章节总结被 R1 测试拦下。（L928、L946）

20. **brief 脊柱定稿，"外部世界"正式成为章节名**：01 正方主论证 → 02 反方压力测试 → **03 外部世界** → **04 综合裁决** → 05 改判条件 → 06 L1-L5 五层底稿 → 07 审计。（L948；前序版本 L967 写"04 第三层综合裁决"）**三层架构的"第二层外部世界材料层/第三层综合裁决层"在报告层的落地措辞由此固定。**

21. 【次要】Q6 补采清单质量：integrated_adjudicator 提示词强制写明具体缺什么数据/字段/窗口、禁笼统套话；补采条目带 quality 结构化标记。（L931）

### 2026-07-24：第三层职责边界裁决

22. **【关闭 T02】取消第三层接管首页**：第三层只负责事件与数据对质解释，继续由经过 Critic/Reviser/Final 治理的第一层判断坐首页；不为署名切换新建难以可靠证明的自然语言语义检测器。（L882）——R8 红队 C1"署名暂缓"在此转为正式裁决。

### 2026-07-25：治理链连环修复（reviser / counter_thesis / 输入瘦身）

23. **【关闭 T19】reviser 合约架构性修复**：说明书补齐 `hypothesis_responses` 与证据引用纪律（A 类规格漂移）；`_carry_forward_reviser_thesis_fields` 字段继承（C 类）；reviser 失败退回未修订原稿 + `degraded_fallback` 标记进终审质量闸门（D 类爆炸半径）；新增 `STAGE_CONTRACT_PROMPT_REQUIREMENTS` 登记表 + 防漂移闸门（F 类）。原因：reviser 挂的合约最多、说明书写的最少，连崩两跑。（L809-L831）

24. **【关闭 T21】counter_thesis 合约—说明书错位修复 + 反方降级可见度对齐**：反方降级从只留痕在自身文件，补齐为 `counter_thesis_degraded_deterministic_fallback` 进终审判决书质量闸门（与 reviser 对称）。原因：反方两次真实事故烧掉约 28 万 token 退回确定性兜底稿，而终审判书完全看不出反方是模板凑数。（L789-L791）

25. **T24 thesis / counter_thesis 输入瘦身**（07-26 验收关单）：只在"喂给 LLM 的 prompt 文本"层压缩 `evidence_index` 审计级明细，thesis 读入降 75.8%、counter 降 74.7%；契约 payload 不动。（L761-L773、L732-L741）

26. 【次要】T25 事件链两站（event_card_interpreter、event_section_summary）模型路由改 flash 优先。理由记录：事件卡产出隔离在 `event_index`，不进主证据链，换模型不会让弱判断升格为强证据。（L783）

27. 【只读调查，未改接线】T22 critic / risk_sentinel 冗余调查：实证"两个独立视角互相校验"目前架构上未兑现（critic 证伪的数字，risk_sentinel 同跑一次当事实引用）；合并与否留用户裁决。（L785）

### 2026-07-26：终审判决书引用硬校验 + 严格模式试点 + 字数上限重构

28. **终审 `reasoned_verdict` 零引用漏洞修复**：新增 `_validate_reasoned_verdict_refs` 接进 final 阶段 validator 链——零方括号引用或引用越界即拒绝重试。原因：说明书写了硬要求但代码从未接成会拦截的合约（"规则没有被强制"型规格漂移）。（L743-L755）

29. **T26 DeepSeek 严格函数调用试点落地（bridge 单点）**：`sanitize_json_schema_for_strict_tool_calling` + `_STRICT_TOOL_CALLING_ELIGIBLE_STAGES={"bridge"}` + 环境变量逐站点名启用；`contracts.py` 一行未动。（L648-L665；同日发现并修复两个真实 API bug：anyOf 形态与思考模式 tool_choice 不兼容，L623-L644）

30. **数字规则重构**：`Critique.overall_assessment` 等 15 处字数上限移除/放宽（含 `reasoned_verdict` 上限 1300→3000）；仅 `final_stance`（200）与 `local_conclusion`（500）因喂进固定宽度展示位而保留。原因：全部 max_length 均无文档化依据。（L669-L707）

### 2026-07-27：瘦身复核回滚一半 + reasoned_verdict 引用收紧

31. **输入瘦身受控复核：撤销"丢弃 hypothesis_competition_summary / adjudication_history"那一半（仅省 0.40%），保留证据索引压缩（省 72.71%）**。原因（日志原文逻辑）：未解决争议清单是改判条件的推导来源，从 thesis 眼前拿走等于把确认偏误制度化，还删掉"HY OAS 走阔 >3.5% 即转空"这类单信号独立触发通道。同时 `_validate_reasoned_verdict_refs` 从"至少一条引用"收紧为"至少三条不同引用"。（L589-L617）

32. 【次要】T27 `price_reflection_map` 调查关闭：未改代码；"类别齐全由代码兜底 ≠ 五类都被分析过"等判读边界写入 RESEARCH_CANON。（L558-L583）

### 2026-07-28：竞争假说触发集合倒置修复（T28）+ 合约治理机制改造

33. **【关闭 T28】竞争假说回应触发集合倒置修复**：`_validate_thesis_hypothesis_responses` 触发集合从 `status == "candidate"` 扩大为 `status != "downgraded"`（kept_unresolved / leading / split 全要求回应）。原因：只要受控调查提出挑战，全部假说被统一改判 kept_unresolved，candidate 集合归零、合约整体空转——**越有争议、越需要被正面回应的假说，越不会被要求回应，逻辑正好反了**（四次真实 run 三次 candidate 数为 0）。（L440-L468）

34. **同一倒置在报告层的第三处修复**：`vnext_reporter._hypothesis_competition_block` 同样用 candidate 过滤，导致真实 run 里该节长期渲染"本轮没有结构化竞争假说"（明明有两条）。（L527-L542）

35. **counter_thesis 接入断点续跑检查点机制**（此前从未接进，续跑必重跑反方并级联使 thesis/reviser/final 检查点全部失配、静默覆盖已验证产物）；走确定性兜底的反方稿登记为不可复用。（L490-L503）

36. **合约治理机制改造（甲+丙，乙被用户否决）**：甲——`_compose_prompt` 注入由 pydantic 契约自动生成的输出字段规格，从结构上根除形状漂移类事故；丙——登记表改由 AST 反射闸门守（挂 validator 的站必须登记），上线即查出 3 个漏登记站，其中 **bridge 是真缺口**（`resonance_chains` 的 confirming_indicators/falsifiers 非空要求说明书里 0 次提及，白烧一次 11 万 token 调用），已在 `cross_layer_bridge.md` 补写。（L505-L525）

37. **终审站两处规格漂移修复**：`claim_ledger` 形状（契约要求对象、说明书 0 次提及）补 model_validator + 说明书章节；`reasoned_verdict` 逗号合并引用——**07-27 收紧为"至少三条引用"的第一次真实 run 回归**，修法为按组拆分（不放松任何 ref 合法性）。（L544-L556）

### 2026-07-30（节内多条目内部引用 07-29 工作，见文首日期异常说明）：严格模式扩围 + 措辞闸门拆除

38. **【关闭 T26】【关闭 T31】严格模式试点通过 + 冲突编号误报根除**（run 20260729_175306）：bridge 严格模式一次通过；`retained_conflicts[].conflict_id` 与桥的高严重度冲突逐字命中，闸门谎报"冲突被抹平"根除。（L387-L398）

39. **【关闭 T30】降级归因从"扫词表猜意思"改为"自报出处 + 对身份" + 严格模式扩围四站**：`_STRICT_TOOL_CALLING_ELIGIBLE_STAGES` 从 {bridge} 扩到 **{bridge, thesis, event_card_interpreter, event_section_summary}** 并接线四处 `_run_stage` 调用点。遵循"闸门不判意思"原则。（L366-L383）

40. **【关闭 T32】严格模式扩围验收 + 重要证伪**：四站零 API 拒绝；但**"模型会逐字复制一个字符串"的假设三处全败**（attribution_quote、citation_caveats.quote、conflict_id 前缀自改写）——确立修正判据：结构化槽位只能做"与非散文事物核对"的身份比对，让模型摘散文再逐字比对是听写不是校验。（L334-L362）

41. **【关闭 T33】删掉三条"必须怎么说"的措辞闸门**（机制假设开头句、非官方来源措辞、仅标题限制语）——保证移交给报告渲染层（代码渲染弱来源标注，一条不漏）；删除为此引入的 `attribution_quote` / `citation_caveats` 字段。依据：28 条重试/失败中措辞类占 50%，全部集中在这三条规则。（L301-L330）

42. 【次要】【关闭 T29】严格模式离线 schema 体检：8 个契约 7 处不合规全是"无属性 object"；BridgeMemo/ThesisDraft/EventInterpretationCard/EventSectionSummary 零问题。（L402-L412）

43. **冲突编号通道补建**：`Conflict` 契约补 `conflict_id`（此前闸门只能拿被正方改写过的类型名认亲）；事件引用前缀陷阱修复（payload 直接给现成引用串）。自动字段规格必须标注嵌套子字段类型。（L416-L434，关键见 L421-L422）

### 2026-07-31：T36 事件总结永不失败 + T34 选单化 + T35 判据拆分

44. **【关闭 T36】事件总结（ESS）永不失败**：`cited_event_ids` 改为**代码从正文 `[card:<id>]` 内联标记导出**（提示词与契约里的该要求删除）；`_run_stage` 新增 `raw_text_fallback`——重试耗尽且 parse_error 时收下原始文本标 `degraded_fallback`，配 `_salvage_text_field_from_broken_json` 只捞正文不渲染 JSON 外壳；降级写 `index_degraded` 并推进 `final.quality_gate.notes`（不新造发布闸门）。同时**两把锁从二选一改成叠加**（strict tools 与 json_object 并存，因 tool_choice="auto" 下模型可不走工具通道）。**用户同日在 CLAUDE.md/ARCHITECTURE.md 确立"轨道 > 导出 > 规矩"原则**：接口能强制的进 schema；代码能导出的别让模型填；剩下的才进提示词。压舱石："结构用来定位，不许用来拒收"。（L246-L264；同节 L289-L293 记录 ESS 第七次连挂实测——严格模式是概率性保证）

45. **【关闭 T34】冲突编号从"要求照抄"改为"只能从清单里选"**：`retained_conflicts[].conflict_id` 在严格 schema 里限定为本轮实际 conflict_id ∪ null 的动态 enum（接替被证伪的 T31 修法）；可选容器字段可空化卸掉"每个数组必须填满"的配额压力（8 契约 197 处站点）。（L198-L219）

46. **【关闭 T35】证据编号合法性判据拆分**：合法 ref 名单从"权限分级表键名"改为"分级表键 ∪ payload 真实顶层键"——把"这个字段存不存在"和"够不够格支撑强结论"拆开；权限分级一个字不动（不给未登记字段自动补 audit_only，防 `mixed_field_authority` 连带降级七个好函数）。原因：L4 的 17 个 get_* 里 10 个从未登记，其真实存在的官方数据被永久判 invalid。离线回放合法 ref 104 → 459，零丢失。（L266-L287；T20 只修好一半，thesis/final/reviser 三站的 `_validate_stage_evidence_refs` 白名单问题留待扩围，L285）

47. 【次要】【关闭 T20】登记表键名对齐真实字段名（用户裁决选 B：改名字，不放开菜单——放开会从 104 条涨到 459 条且把下载链接等元数据摆上菜单）。（L221-L244）

### 2026-08-04：T37 并入 T38——"给少了"问题正式立案

48. **【关闭 T37】卷宗审计并入 T38**：实测发现 `integrated_adjudicator` 的 26 个 `L#.get_*` 允许引用里 **20 个只有名字和许可、没有数值**（结构化数值供给实为 0/26）。根因定性：**声明"你可以用这些"的地方与决定"实际给你什么"的地方是两处独立代码，中间没有闸门**。（L178-L192）

### 2026-08-05：T42① 止血（IA 许可从实发导出）+ T44④ 留痕 + T46 立案 + T47 立案

49. **T42① 止血——IA 许可改为从实发导出（构造上恒真）**：`integrated_synthesis_report.py` 先拼真正要发的 payload（新增 `evidence_refs` 与压缩版 `key_support_chains` 两个正式证据字段），再 `allowed_refs = _allowed_data_refs(payload)` 回填——**"许可 ⊆ 实发"物理上不可分叉**。捎带 `canonical_question` / `current_reading`（模型此前声称拿不到的 VXN 数值因此真的进入供给）。**这是求真回路 IA 站供给方式的一次实质改动。**（L91-L103，关键见 L95）

50. **T44④ 运行留痕**：`orchestrator.py:5189` 逐站落 `strict_tool_schema_enabled`（取自真实每站结果）；`llm_engine.py` 五处记 `prompt_cache_hit_tokens`（null/0 语义分离）。（L97）

51. **新立 T46（治理链供给偏科，未修）**：`GovernanceInputPacket` 41 字段 thesis 相关 20 个、counter 相关 0 个；`key_evidence_refs` = 高严重度冲突 ∪ thesis 引用，counter_thesis 零命中——**治理四站的证据集合由正方引用单方圈定**。（L83）

52. **T47 立案（上下文体检扩展到"缺料"维度）**：实测 13/25 站零检查规则（含 integrated_adjudicator、controlled_investigation、event_section_summary、10 个 event_card_interpreter）；原型扫 35 次跑检出 41 条"有数值、也许可、却没发过去"，**全部在 integrated_adjudicator，跨连续 8 次跑、最早 2026-07-19**——即 IA 的缺料从 R8 上线起就存在。（L126-L148）

53. 【主方向换挡】T38 → T47：T47 升格为主方向（上下文根本性体检），T38（未来供给设计）退居其后；新增《体检方法.md》长期方法论。（L107-L124）

### 2026-08-06 / 08-08（文档层，不计为架构改动）

- 08-06：T47 重定义为"一次性根本架构审查"（纯文档，L57-L67）。
- 08-08：文档体系大整理——新增《系统说明书.md》总纲，退役 ARCHITECTURE.md 等四份旧文档，新增"代码模块必须在文档有概念提及"的机器对照测试（防"架构长新器官文档零提及"）。（L36-L51）**对校准报告的意义：07-03 认识论架构文档的权威继任者是《系统说明书.md》。**

### 2026-08-09：T47 独立重做 + ESS 供给小修 + 五项架构级发现（未动，待拍板）

54. **ESS 输入卡补 `needs_data_confirmation` 字段**（`orchestrator.py:1640`）：上游契约已有、IA 同批卡 10/10 带、ESS 0/10——事件链两站供给对齐的一处小修。（L19）

55. **T47 全量通读的架构级发现（全部带证据、一律未动、待所有者拍板）**：① 治理链四站（critic/risk/…）吃同一份逐字相同的证据供给（critic==risk 输入 sha256 一致）；② 终审裁原稿不裁修订稿；③ 第三份调查失败静默缺席；④ L1-L5 输入预置本层结论；⑤ **事件链对 bridge/治理断供**。另有两条阴性反证保留：L1-L5 隔离红线在材料层成立、正反材料层对称。（L28、L11-L31）

---

## 三、按主题归并（对账索引）

### 主题 1：三层架构措辞的演变
- 07-01（基线）："第一层/第二层/第三层"，第三层=综合报告（L1511-L1571）。
- 07-17 诊断：07-03 认识论报告实施率约六成，灵魂件未实施（L1045-L1050）。
- 07-18 对辩终审：**三层划分不动**，唯一骨架级缺陷是层间时点契约（L1012）。
- 07-18 R8：报告新增"第三层·综合裁决"章节（L1017）。
- 07-20 脊柱定稿：**"外部世界"正式成为章节名**（03 外部世界 → 04 综合裁决）（L948）。
- 07-24 T02：第三层不接管首页，只做事件与数据对质解释（L882）。
- **日志中未找到**任何把"L1-L5 正式观测层/外部世界材料层/综合裁决层"作为系统级正式命名的条目——这套措辞只存在于 07-03 文档本身；代码与报告层的落地措辞是"外部世界章节"与"（第三层）综合裁决"。

### 主题 2：事件链（五段流水线 → LLM 化）
- 五段流水线定义见背景基线（L1566）：采集与时间闸门 → 事件聚类与 claim 拆解 → 事件研究包 → 市场验证 → 账本/报告/综合层交付（2026-07-01，全程无 LLM）。
- LLM 化接线：R6 LLM 事件卡（07-17 立案 L1048、07-18 派工 L1026——**日志未找到 R6 独立关单条目**）；Q3 ESS 独立站（07-20，L928）；T25 两站换 flash（07-25，L783）；严格模式扩围进事件两站（07-30，L379）；T36 ESS 永不失败（07-31，L246-L264）；ESS 输入补 needs_data_confirmation（08-09，L19）。
- 事件链对 bridge/治理断供：仅作为 T47 发现记录（08-09，L28），**未找到修复条目**。

### 主题 3：求真回路（controlled_investigation / IA）
- 受控调查：07-06 已有骨架 + stub 诚实标注（L1429）；07-17 实证 stub 恒 True（L1045）；R4 执行器 07-18 验收、降级机制首次开火（L1032）；W2 缺口反馈（07-19，L998）。
- IA：R8 真裁决落地（07-18，L1014-L1020）；W1 时点契约（07-19，L997）；Q6 补采清单质量（07-20，L931）；T37/T38 发现 20/26 许可引用无数值（08-04，L182）；**T42① 许可从实发导出（08-05，L91-L103）**；T47 发现其缺料跨连续 8 次跑、最早 07-19（08-05，L142）。

### 主题 4：竞争假说治理链
- 建链：07-06 阶段 3（L1454-L1481）+ 同日更正为 LLM 优先（L1430）。
- 接线变化时间线：别名吸收（07-08，L1388）→ 阶段模型路由 + 引用源头校验（07-08，L1308-L1309）→ R7 Thesis 强制回应竞争假说（07-17 立案 L1048，验收 L1034）→ T19 reviser（07-25）→ T21 counter_thesis + 降级可见度对称（07-25）→ T24 输入瘦身（07-25）→ 瘦身复核回滚 disputes 字段（07-27，L589-L617）→ **T28 触发集合倒置修复（07-28，L440-L468）** → counter_thesis 接入检查点（07-28，L490-L503）→ T31/T34 冲突编号通道→选单（07-30、07-31）→ T46 供给偏科立案（08-05，L83）→ T47 发现治理四站同一份证据供给（08-09，L28）。

### 主题 5：T36 / T42 / T44
- T36（事件总结永不失败）：07-31 关单，见条目 44（L246-L264）。
- T42（幽灵引用/许可-实发分叉）：08-04 立案于 T37 关闭条目（L182）；08-05 ①止血落地、②③④未做（L91-L103）。
- T44（严格模式扩围）：T26 试点 bridge（07-26）→ T30 扩四站（07-30）→ T32 验收（07-30）→ T44④ 留痕（08-05，L97）；**final/critic 开严格模式与 T36 兜底链路真实验证在日志中仍标注"尚余"（L103），未找到完成记录**。

### 主题 6：bridge 职责/输入变化
- 07-06：Bridge V2 对 Thesis 的输入贡献收窄为反馈摘要+未解决问题（L1432）——**日志中找到的唯一一次 bridge 数据流向改动**。
- 其余均为工程质量项：严格模式试点（07-26）、说明书补登记（07-28，L519）、T27 判读边界（07-27，未改代码）。
- **未找到** bridge 站点职责（跨层对质的定位）本身被重新定义的条目。

### 主题 7：新增站点 / 站点顺序变化
- 新增站：counter_thesis + hypothesis_competition（07-06，L1459）；integrated_adjudicator 真裁决化（07-18，L1017）；event_section_summary（07-20，L928）；controlled_investigation 执行器化（07-18，L1032）。
- 顺序变化：counter_thesis 插在 Bridge V2 之后、Thesis 之前（07-06，L1459）；报告脊柱定稿（07-20，L948）。
- 职责变化：L3/L4 重划（07-10，L1186）；第三层不接管首页（07-24，L882）。

---

## 四、统计与"未找到"清单

**时间线条目总数**：55 条编号（含【次要】7 条、纯发现/治理决定类 5 条：第 7、27、32、51、55 条；背景基线 2 条不计入）。

**日志中明确未找到记录的主题**（如实报告）：
1. 三层架构措辞的**正式重命名**条目（"正式观测层/外部世界材料层/综合裁决层"这套命名在日志中零出现；落地措辞是"外部世界章节/第三层综合裁决"）。
2. R6 LLM 事件卡（event_card_interpreter）的**独立关单/验收条目**——只有立案（L1048）与派工（L1026），之后事件卡在 07-20 条目中作为已存在事物出现。
3. T42 ②③④（编造比对、冲突回应校验、reviser 选单）与 T44 剩余项（final/critic 严格模式）的**完成记录**——日志明确写"两项均不关单"（L103）。
4. T46（治理四站证据正方单方圈定）与 T47 五项架构级发现（含事件链对 bridge/治理断供、终审裁原稿不裁修订稿）的**任何修复条目**——均标注"待所有者拍板，供给设计类一律未动"（L28）。
5. bridge 站点**职责重新定义**的条目（唯一一次是 07-06 的输入贡献收窄，属流向调整非职责重定义）。
