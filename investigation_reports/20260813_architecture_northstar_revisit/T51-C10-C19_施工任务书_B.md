# T51-C10-C19 施工任务书 B 包（C11 前端 + C19 文档卫生）

你是执行者，唯一任务来源=本文件。仓库 `/Users/aidianchi/Desktop/ndx_mac`，分支 `docs/consolidation`。不 commit、不 push。

## 范围与文件边界

- 允许改：`src/agent_analysis/vnext_reporter.py`、`src/agent_analysis/llm_engine.py`、`系统说明书.md`、`investigation_reports/20260813_architecture_northstar_revisit/P4_任务书.md`、`investigation_reports/20260813_architecture_northstar_revisit/05_六问综合整理_供所有者辩论.md`、对应测试文件；根目录 `ai_response_debug_*.txt` 的**移动**（不删）。
- 禁止碰：orchestrator.py、contracts.py、integrated_synthesis_report.py、现在.md、WORK_LOG.md、人话进度报告.md（根线程在写）；不得重构。

## 依据（先读）

1. `investigation_reports/20260814_total_reorg/02_总待办建议稿.md` 的 C11/C19 行与 01 稿第五节伴随建议。
2. `investigation_reports/20260804_seven_way_audit/WORK_ORDERS.md` T40。
3. `investigation_reports/20260813_architecture_northstar_revisit/P4_BLOCKED.md`（P4 指路纠错出处）。

## 施工清单

### C11（T40 判断书阅读体验两件）
- 在 `vnext_reporter.py` 的 brief 模板实现：①术语悬浮解释（把现有术语表/词条以 title/tooltip 形式挂到关键术语上；范围：风险边界与证据权限术语，不铺满全文）；②时间戳仅在数据陈旧时显示（数据新鲜时不显示时间戳；陈旧判定沿用现有 data_quality/日期字段口径）。
- 配 `tests/test_vnext_reporter.py` 断言（悬浮解释存在、陈旧才显示时间戳、新鲜时无时间戳）。

### C19 文档卫生（你负责的部分；WORK_LOG/信/现在.md 由根线程写）

1. **P4 任务书指路纠错（B24）**：把 P4_任务书.md 中 `packet_builder.py` 的指路改成实际位置 `orchestrator.py`（约 975 行 `_build_layer_input_policy`，以实读为准）。任务书是活文档，直接改，历史由 git 保留。
2. **llm_engine 调试文件改落 run 目录（B25）**：`llm_engine.py` 两处写根目录 `ai_response_debug_*.txt` 改为写 `self.output_dir`（或 run 目录）下；若函数没有 output_dir 上下文，写环境变量 NDX_DEBUG_DIR 或明确参数，报告你的选择。把根目录现存 72 个 `ai_response_debug_*.txt` **移动**到 `output/debug_archive/`（mkdir，不删）；确认未来全量测试不再在根目录产生新文件。
3. **R13 悬空引用**：`系统说明书.md` 2.11 的"（R10/R12/R13）"改为"（R10/R12/N2）"或在对照稿补 R13——**选前者**（对照稿是历史案卷，系统说明书是活地图）；并保证 docs 测试仍绿。
4. **05 综合整理"均无阻塞项"改如实**：`05_六问综合整理_供所有者辩论.md` 中"P1～P6_BLOCKED.md（均无阻塞项，仅有非阻塞备注）"改为"P1～P6_BLOCKED.md（P6 列有三条未解决的验证缺口：Sharpe 1975 全文未获取、SSRN/Wiley/Oxford 反爬未读全文、工作区基线不干净；均自标非阻塞）"。
5. 不做的 C19 项（已由根线程/前批完成）：WORK_LOG 日期节、第 25 封信、§8 第 7 项、P1 标题。

## 验收

```bash
.venv/bin/python -m pytest tests/test_vnext_reporter.py tests/test_wo_r1_reporter.py tests/test_vnext_llm_engine.py tests/test_docs_consistency.py -q
```
`git status` 根目录不再有 ai_response_debug_*.txt；`git diff --check` 干净。

## 交付报告

- 每项改了什么/文件/行；测试最后一行；调试文件移动数量；偏离处；BLOCKED。
