# T51-C10-C19 施工任务书 A 包（C10/C12/C14/C15 代码零活）

你是执行者，唯一任务来源=本文件。仓库 `/Users/aidianchi/Desktop/ndx_mac`，分支 `docs/consolidation`。不 commit、不 push。**供给代码区只有你一个执行者在动。**

## 范围与文件边界

- 允许改：`src/agent_analysis/orchestrator.py`、`src/agent_analysis/contracts.py`、T39/T43 工单点名的实际文件（先读工单定位，改动前把文件名记进报告）、对应测试文件。
- 禁止碰：`src/agent_analysis/vnext_reporter.py`、`src/agent_analysis/llm_engine.py`、`src/integrated_synthesis_report.py`、所有 `*.md` 文档（另一执行者在改）；不得重构、不得顺手改无关代码。

## 依据（先读）

1. `investigation_reports/20260814_total_reorg/02_总待办建议稿.md` 的 C10/C12/C14/C15 行。
2. 各工单原文：`investigation_reports/20260804_seven_way_audit/WORK_ORDERS.md` 的 T39/T42/T43/T44；`investigation_reports/20260804_seven_way_audit/HANDOFF.md` 相关节。
3. 现场代码：orchestrator 的 schema guard / strict tool schema / evidence 校验；`src/core/collector.py` 与数据工具里的回测日期守卫（grep `end_date` / `backtest`）。

## 施工清单

### C10（T39 回测时点泄漏，两处一行级守卫）
- 按工单 T39 找到两处：兜底路径绕过 end_date、覆盖率计数失真。各加一行级守卫（最小改动），不关下游硬拦。
- 配真实实现测试（不是 mock 被测守卫本身）；反例先红后绿。
- 注明：只有要跑历史回测时才急，本次施工不触发任何真实回测。

### C12（T42 余三件：三道机器核对）
- ①防编引用（存在性/身份比对，不判意思）；②冲突回应核对（高严重度冲突是否被回应/保留，身份比对）；③反方选单（counter 候选/选单存在性核对）。
- 落在现有 schema guard / validator 体系内，每道配反例测试；不新增散文判定规则。

### C14（T43 余两件）
- ①缺一行导入（按工单定位并补）；②pandas 版本越界（按工单定位声明/约束文件，把版本范围修正；误入 git 的生成文件已处理，无需再做）。
- 改完全量相关测试仍绿。

### C15（T44 余：final/critic 严格模式 + T36 兜底链路验证）
- 按 T44 工单给 final/critic 开严格模式（沿用 `_strict_tool_schema_for_stage` 现有机制，不另起炉灶）。
- T36 兜底链路验证：读 `output/analysis/vnext/c6_baseline_20260815/` 的 event_section_summary 与 llm_stage_diagnostics，确认本轮 ESS 失败后是否走了既有兜底/降级路径并如实留痕（只读验证，不跑新 LLM）。把结论写进交付报告。

## 验收

```bash
.venv/bin/python -m pytest tests/test_vnext_orchestrator.py tests/test_governance_input.py tests/test_vnext_llm_engine.py tests/test_docs_consistency.py -q
```
（按改动实际涉及的测试文件补齐；全绿为准。）`git diff --check` 干净。

## 交付报告

- 每项：改哪个文件/行、红灯测试名、验证结果一句话；
- T36 兜底验证结论（c6_baseline run 证据）；
- 偏离处及原因；BLOCKED。
