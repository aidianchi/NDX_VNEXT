# T50-2/3 施工任务书：C2 口径锁定 + C3 分料（2026-08-15）

你是执行者，这份文档是唯一任务来源。中途没人可问；拿不准写进本任务书末尾"执行者 BLOCKED 清单"，跳过继续做别的，不要臆断。断了或换新会话：先读本任务书接着做。仓库：`/Users/aidianchi/Desktop/ndx_mac`，分支 `docs/consolidation`。

## 背景（已裁决，不许重新讨论）

- C2 所有者选 **A 档**：保留 evidence_index 里 field_value 的聚合统计与发言权元数据，只压缩审计级明细（07-25 已实现）。本次 C2 的施工 = 用测试把 A 档口径锁死，防止未来被"顺手整删"。
- C3 已批准按合成稿施工：critic/risk 分料 + risk 论证盲 + risk 提示词同批改写。对照实验的 runner 不在本任务书（另批做），你只做"分料上线"部分。
- 设计依据：`investigation_reports/20260811_layer2_research/配餐单_v0_四站条目.md` 第二节与第四节（先读）。

## 允许改动的文件（白名单，一个都不能多）

1. `src/agent_analysis/orchestrator.py`
2. `src/agent_analysis/contracts.py`
3. `src/agent_analysis/prompts/risk_sentinel.md`
4. `tests/test_governance_input.py`
5. `tests/test_vnext_orchestrator.py`

其他文件一律只读；不 commit；不 push；不得改 `.codex`、不得改总纲、不得改现在.md/WORK_LOG（根线程收口）。

## 任务一：C2-A 口径锁定（只加测试，不改生产逻辑）

现有 `_slim_evidence_index_for_prompt` / `_slim_long_list_for_prompt`（orchestrator.py 约 L6729-6785）已经实现"只压超长嵌套列表、保留聚合字段"。在 `tests/test_vnext_orchestrator.py` 找一个最贴近的既有瘦身测试，**最小增量**加断言：

1. 构造 mini payload：`synthesis_packet.evidence_index` 含一条子 ref，其 `field_value` 同时有聚合字段（如 `value`/`unit`/`coverage`）和一个超长列表（>8 条且序列化 >800 字符）。
2. 断言 `_slim_evidence_index_for_prompt(payload, "synthesis_packet")` 之后：
   - 该子 ref 的 `field_value` 键仍然存在（A 档：键不得整删）；
   - `field_value` 的聚合字段 `value`/`unit`/`coverage` 逐字节不变；
   - 超长列表被压成 `{"_prompt_summary": True, "count": ..., "sample": ..., "note": ...}` 形态。
3. 断言 ref key 集合（含 `parent#field` 子 ref）压缩前后完全一致。

若既有测试已覆盖上述大部分，只补缺失的断言，不许重写整个测试文件。

## 任务二：C3 分料（按合成稿第四节）

### 1) 合约（contracts.py）

`GovernanceInputPacket` 增加可选字段：

```python
layer_summaries: List[Dict[str, Any]] = Field(
    default_factory=list,
    description="五层摘要（L1-L5 LayerSynthesisItem 序列化）；仅 risk 论证盲分料版填充，critic/reviser/final 恒空"
)
```

### 2) 打包函数（orchestrator.py `_build_governance_input_packet`，约 L4854 起）

- 签名加 `consumer: str = "critic"`（合法值 critic / risk；其他调用不传 = 现行为，保证 reviser/final 不变）。
- **critic 版 = 现行为**：现有收集逻辑与全部字段一字不改（默认参数保证）。
- **risk 版（consumer="risk"）**：
  - 全部 thesis_* 字段用默认空值（空字符串/空列表/空 dict，不得携带任何 thesis 散文或结构），包括 `thesis_hypothesis_responses`、`retained_conflict_types`、`thesis_principal_contradiction` 等 17 个字段。
  - 新增 `layer_summaries` = `[_model_dump(item) for item in synthesis_packet.layer_summaries]`（现成字段，构造处约 L4628-4636）。
  - `key_evidence_refs` 的收集来源改为：高严重度冲突（typed 与普通 conflict）的 `evidence_refs` + `principal_contradictions` 的 `evidence_refs`（如有）+ `synthesis_packet.layer_summaries[].indicator_refs`（最多 12 个/层）。**不得**从 thesis 支撑链、假说回应、仓位、时间尺度、读者结论里收集（论证盲）。
  - `key_event_refs` 维持恒空（事件不进第一层）。
  - `synthesis_guidance` 置空列表；`evidence_registry_summary` 置空 dict；`pricing_expectation_ledger` 置空 dict（配餐单：risk 提示词未列这些，不给）。
  - `high_severity_typed_conflicts`、`principal_contradictions`、`objective_firewall_summary`、`known_data_gaps`、`unresolved_questions`、schema_* 照旧保留（schema 反馈 retry 时照旧注入）。
- 实现建议：在函数内先把两个证据集合分别算好（critic 用现有 `all_evidence_refs`；risk 另算 `risk_evidence_refs`），返回前按 consumer 分支组装 kwargs，避免复制一大段逻辑。保持函数可读、注释写清"risk = 论证盲"。

### 3) 调用点（orchestrator.py 约 L551-568 与 retry 约 L582-599）

- critic 调用不变。
- risk 调用改为先构造 `gov_input_risk = self._build_governance_input_packet(synthesis_packet=synthesis_packet, thesis=thesis, layer_cards=layer_cards, consumer="risk")`，再喂给 risk。
- schema_guard_retry 分支同样构造 `gov_input_risk_retry`（consumer="risk" + schema_report），critic retry 不变。
- reviser/final 的调用一字不动。

### 4) risk_sentinel.md 同批改写（绑定手术，不许只改代码）

先通读现文件，做三处最小改写，其余（角色边界、输出格式、13 种冲突矩阵、统计禁令、质量检查、示例）不动：

- **「## 输入」节**：删除全部 thesis_* 字段列表，改为：`layer_summaries`（五层摘要，你的事实面）、`high_severity_typed_conflicts`（必须保留的高严重度冲突清单）、`principal_contradictions`（Bridge 主要矛盾候选）、`key_evidence_refs`（与冲突和层摘要相关的证据读数卡片）、`known_data_gaps`、`objective_firewall_summary`、`unresolved_questions`（Bridge 未解决问题）、retry 时附加 `schema_*` 反馈。明确写一句：**你拿不到论点论证（thesis 的任何段落与结构），这是刻意设计（论证盲），不得脑补一个论点出来攻击。**
- **「### 1. 失效条件检查」**：把"基于 Thesis Draft 的 dependencies 列失效条件"改为"基于冲突清单 + 各层摘要列失效条件：每条高严重度冲突和每层摘要中的风险信号，分别写出它在什么可观察变化下变成现实损失；数据缺口与 Bridge 未解决问题同样要列"。保留双向要求（上行/下行失效）。
- **「### 3.6 主要矛盾风险检查」**：从"检查 Thesis 是否正确处理主要矛盾（thesis_principal_contradiction 是否为空）"改为"检查 Bridge 候选主要矛盾（principal_contradictions）与高严重度冲突：每条候选矛盾在冲突清单中是否有对应物、哪条风险边界没有被它覆盖、被忽略后最坏走到哪"。
- 保留所有机器闸门要求原句："不得编造历史胜率、回测收益、样本区间或概率数字""除非输入 evidence_refs 明确提供这类统计""不得编造点位、跌幅、估值倍数、盈利增速阈值或其他定量影响幅度"（tests/test_prompt_guardrails.py 逐字断言这些句子，一个字都不能少）。

### 5) 测试

- `tests/test_governance_input.py` 现有测试全部照旧绿（默认 consumer=critic 必须保持现行为）。
- 新增（同文件）：
  1. risk 版不携带任何 thesis 内容：构造一个含非空 thesis 字段的调用，断言 risk 包的 `thesis_main == ""`、`thesis_environment == ""`、`thesis_key_support_chains == []`、`thesis_hypothesis_responses == []`、`thesis_principal_contradiction is None`（至少查这五个，越多越好）。
  2. risk 版 `layer_summaries` 非空且与 synthesis_packet.layer_summaries 对应；critic 版（默认）`layer_summaries == []`。
  3. risk 版 `key_evidence_refs` 包含"冲突引用的 ref"与"layer_summaries.indicator_refs 里的 ref"，且**不包含**只被 thesis 支撑链引用、冲突与层摘要都没引用的 ref（构造数据时造出这样一条 ref 验证）。
  4. critic 版行为不回退：现有测试已锁，无需新增。

## 验收命令（必须全部跑给你自己看）

```bash
.venv/bin/python -m pytest tests/test_governance_input.py tests/test_vnext_orchestrator.py tests/test_vnext_llm_engine.py tests/test_prompt_guardrails.py tests/test_docs_consistency.py -q
```

要求全绿（0 failed）。跑完 `git status --short` 只允许出现白名单 5 个文件。

## 交付报告格式（回复根线程）

- 改动的文件清单 + 每个文件改了什么（一句话）；
- 测试结果原文（最后一行）；
- 有没有偏离本任务书的地方及原因；
- BLOCKED 清单（没有写"无"）。
