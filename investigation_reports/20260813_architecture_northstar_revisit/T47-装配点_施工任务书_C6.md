# T47 装配点施工任务书（C6 装配点②-⑤ + 两处已批供给改动 + 常设检查接线）

你是执行者，这份文档是唯一任务来源。仓库 `/Users/aidianchi/Desktop/ndx_mac`，分支 `docs/consolidation`。**不 commit、不 push。** 供给代码区只许你这一个执行者动。

## 允许改动的文件（白名单，一个都不能多）

1. `src/agent_analysis/orchestrator.py`
2. `src/agent_analysis/contracts.py`
3. `src/agent_analysis/prompts/critic.md`
4. `src/agent_analysis/prompts/reviser.md`
5. `src/agent_analysis/prompts/final_adjudicator.md`
6. `tests/test_governance_input.py`
7. `tests/test_vnext_orchestrator.py`
8. `tests/test_persistent_checks_wiring.py`（新建）
9. `系统说明书.md`（只加两处：§1 收尾工序提一句常设检查自动落盘；第五节模块地图加 `persistent_checks_a.py`/`persistent_checks_b.py` 两行）

其他文件一律只读。特别地：`persistent_checks_a.py`/`persistent_checks_b.py`（若已存在）只读调用，不许改；四份历史案卷由另一执行者在改，你不许碰。

## 前置与依据（先读）

- 配餐单 v0：`investigation_reports/20260811_layer2_research/配餐单_v0_四站条目.md`、`配餐单_v0_余站条目.md`（第三节两项已批供给改动）。
- 8.10 拍板②-⑤：`8.10 拍板记录.md`（若该文件正被重写，以对照稿与本节任务书为准）。
- C3 现状代码：`_build_governance_input_packet` 已有 `consumer` 参数（critic/risk），你在其上扩展。

## 施工清单（逐项，最小改动）

### ① counter 反方原文与反证引用进 critic/reviser/final（拍板③ + T46）

- `GovernanceInputPacket` 新增字段：
  `counter_thesis_hypotheses: List[Dict[str, Any]] = Field(default_factory=list, description="来源为 counter_thesis 的竞争假说原文（critic/reviser/final 填充；risk 恒空）")`。
- `_build_governance_input_packet`：从 `synthesis_packet.competing_hypotheses` 过滤 `source == "counter_thesis"` 的假说，序列化为 dict 列表（hypothesis_id/hypothesis_text/status/support_evidence_refs/counter_evidence_refs/diagnostic_evidence_refs）。critic/reviser/final 分支填该字段；risk 分支恒空。
- 同一过滤结果中的三类 evidence_refs 全部并入 critic/reviser/final 的 `all_evidence_refs`（risk 不并）。
- 提示词：critic.md / reviser.md / final_adjudicator.md 的输入清单各加一行 `- **counter_thesis_hypotheses**: 反方假说原文（来源 counter_thesis），用于检查论点是否选择性回应反方证据。`（措辞可微调，禁止引入测试禁用的历史概率/点位片段）。
- 测试：在 test_governance_input 加反例——counter 独有的 ref 必须进 critic 包；risk 包 counter_thesis_hypotheses == []。

### ② 终审收原稿 + 修订稿 + 修订说明（拍板②）

- `GovernanceInputPacket` 新增 `thesis_original: Optional[Dict[str, Any]] = Field(None, description="修订前 Thesis 原稿，仅 final 填充")`。
- `_build_governance_input_packet` 加参数 `thesis_original: Optional[ThesisDraft] = None`；consumer=="final" 时填 `_model_dump(thesis_original)`，其余分支 None。
- final 调用点（orchestrator.py 约 L681-689）传 `thesis=analysis_revised.revised_thesis, thesis_original=thesis`。
- final_adjudicator.md 输入清单加一行：`- **thesis_original**: 修订前原稿；修订稿与原稿必须都能对照，修订说明要写清改了什么。`
- 测试：final 包 thesis_original 与原稿 main_thesis 一致；critic 包 thesis_original is None。

### ③ reviser/final 去噪音字段 + 证据索引瘦身（08-15 已批）

- `_build_governance_input_packet` 的 consumer 增加 `"reviser"` 与 `"final"`：
  - 基础同 critic（含 counter refs、thesis 全套、critique/risk/schema 按现有参数传入）。
  - `synthesis_guidance=[]`、`pricing_expectation_ledger={}`、`evidence_registry_summary={}`。
  - `key_evidence_refs` 的每条 entry 用现有 `_slim_long_list_for_prompt` 递归压 `field_value` 里的超长明细（**A 档：field_value 键保留、聚合字段逐字节不变、只压 >8 条且 >800 字符的列表**），ref key 集合不动。可加小 helper `_slim_governance_key_evidence_refs`。
- reviser 调用点传 `consumer="reviser"`；final 调用点传 `consumer="final"`；retry 不涉及。
- 测试：reviser/final 包 synthesis_guidance==[]、pricing_expectation_ledger=={}、evidence_registry_summary=={}；长列表被压成 `_prompt_summary` 且 `value` 聚合字段不变；critic 默认行为仍保留这些字段（现有测试锁住）。

### ④ 去预置 state / 预置跨层结论（拍板⑤）

- `_build_layer_stage_payload`（约 L971-986）：发给 L1-L5 的 `layer_facts` 改为净化副本——`pop("state")`；`summary` 去掉开头的"`<层>状态: <值>。`"段（用正则 `^.*?状态: [^。]*。` 替换为空），只留"关键事实: …"与"缺口=…"部分。落盘的 analysis_packet 与 packet_model 不动。
- `_run_bridge`（约 L4293-4310）：payload 里的 `context_brief` 换净化副本——`apparent_cross_layer_signals=[]`、`layer_highlights={}`、`special_attention=["检查高严重度冲突是否被完整保留。"]`（保留 data_summary/task_description）。全局 context_brief.json 落盘不动。
- 测试：层 payload 的 layer_facts 无 "state" 键、summary 不以"状态:"开头；bridge payload 的 context_brief.apparent_cross_layer_signals == []；落盘 analysis_packet 仍含 state（审计本体不动）。

### ⑤ bridge 事件恒空 + evidence_refs 不得含 event 前缀（08-15 已批）

- `_run_bridge` 删除 `if packet.event_refs: bridge_payload["event_refs"] = packet.event_refs` 注入块（4.7 口径：事件永不进第一层，恒空）。
- `_validate_bridge_memo_v2`：遍历 typed_conflicts/resonance_chains/transmission_paths 的 evidence_refs，凡以 `event:` 前缀开头的条目 → errors.append 明确文案（"事件不得作为 evidence_ref"）。
- 测试：packet.event_refs 非空时 bridge payload 仍无 event_refs 键；validator 对含 `event:xxx` 的 evidence_refs 报错、对普通 ref 不报。

### ⑥ 受控调查材料：截断标注 + 立场字段剥离（配餐单余站条目第 4 类）

- 材料打包/截断处（约 L2220-2291）：截断发生时，在块尾追加 `\n[已截断：本材料 JSON 可能不闭合，仅部分内容可见，禁止据此补全未显示内容。]`；序列化前递归删除材料 dict 中的 `dominant_side`、`action_implication` 及值含"仓位"的立场字段（删除处留一行说明键已剥离）。材料是给调查员看的内容，允许显示"已剥离立场字段"。
- 测试：合成含 dominant_side 的材料，打包产物不含该键且带截断/剥离标注。

### ⑦ 常设检查接线

- orchestrator 新增方法 `_run_persistent_checks(self)`：懒导入 `agent_analysis.persistent_checks_a.run_checks_a` 与 `agent_analysis.persistent_checks_b.run_checks_b`，以 `self.output_dir` 为 run_dir 执行，把两条结果合并写成 `persistent_checks_report.json`（顶层 schema_version/passed_count/failed_count/checks）。任何异常只写 failed 汇总，不得让主链崩溃。若两个模块尚不存在（检查包未交付），方法返回并写 `status: "modules_missing"`。
- 在 `run()` 尾部（run_review 生成之后、返回之前）调用一次并 `_save_json`。
- `系统说明书.md`：§1 收尾工序（第 12 步纯代码收尾段）补一句"并生成 20 项常设机械检查报告 `persistent_checks_report.json`"；第五节模块地图加 `persistent_checks_a.py` / `persistent_checks_b.py` 两行（文档闸门会要求承认新模块）。
- 测试：tests/test_persistent_checks_wiring.py——用 monkeypatch 向 sys.modules 注入假的 run_checks_a/b（返回可控结果），实例化 orchestrator（FakeLLMEngine 或直接构造，参考 tests/test_governance_input.py 的 `_orchestrator`），调 `_run_persistent_checks`，断言输出 json 存在且 passed/failed 计数正确；再测 modules_missing 分支。

## 验收命令（全绿才算完）

```bash
.venv/bin/python -m pytest tests/test_governance_input.py tests/test_vnext_orchestrator.py tests/test_persistent_checks_wiring.py tests/test_persistent_checks_a.py tests/test_persistent_checks_b.py tests/test_prompt_guardrails.py tests/test_docs_consistency.py -q
```

（A/B 两个检查测试文件若尚未存在，跳过它们但要在报告里说明；等它们交付后必须补跑。）
`git diff --check` 干净；`git status --short` 的已跟踪修改只允许白名单文件。

## 交付报告

- 每个文件改了什么（一句话）；测试最后一行；偏离任务书处及原因；BLOCKED 清单。
