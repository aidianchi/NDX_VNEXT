# T47 常设检查施工任务书 B 包（PC-11 ~ PC-20）

你是执行者，这份文档是唯一任务来源。仓库 `/Users/aidianchi/Desktop/ndx_mac`，分支 `docs/consolidation`。只许新建/修改两个文件：
- `src/agent_analysis/persistent_checks_b.py`（新建）
- `tests/test_persistent_checks_b.py`（新建）

其他文件一律只读；不 commit；不 push。输入 = run_dir，只读机器检查，不调 LLM、不联网、不写 run_dir。

## 背景与口径

- 原始发现依据：`investigation_reports/20260806_t47_context_review/03_根本审查总报告.md` §3 的 B4/B5/B7/B8/B11/B12/B13/C3/C5/C12 与 §8 第 20 项（先读，按"病出现 = failed"反写）。
- C3/C2 已上线的事实：critic/risk 分料；thesis/counter 的 evidence_index 在 prompt 层已瘦身；governance 包暂未瘦身（C6 装配点将改 reviser/final，检查要能兼容两种形态，以实际 payload 为准）。
- 参考 run（只读开发用）：`output/analysis/vnext/20260731_002156/`。

## API 契约（与 A 包统一）

`persistent_checks_b.py` 导出 `run_checks_b(run_dir: Path) -> List[Dict[str, Any]]`，每条：
`{"check_id": "PC-11", "name": "...", "passed": bool, "detail": "...", "evidence": "..."}`。
缺失 artifact → failed 并写明；单条异常 try/except → failed + check_error；import 无副作用。

## 检查清单

**PC-11 输出示例指标本层存在性（B4）**：各层 prompt 正文中"正确示例/输出示例"点名的指标 function_id，必须在本层 payload 的 layer_raw_data 或当前层指标清单中存在；不存在 → failed。

**PC-12 manual_overrides 陈旧占位日期（B5）**：各层 payload 的 manual_overrides 中日期为 2022 年或明显早于 run 数据日期一年以上的占位 → failed，列出层与日期。

**PC-13 percentile 取值域混用（B7）**：扫 L1-L5 payload 与 layer_cards 中所有 percentile 类字段，同时出现 0-1 与 0-100 两种口径且未带 unit/scale 声明 → failed。

**PC-14 context_brief 日期 vs 指标日期（B8）**：context_brief.data_summary 的日期与各层 payload 中指标 data_date/visible_date 出现跨日不一致 → failed（允许同一日不同时区表示）。

**PC-15 high_severity 容器名与 severity 字段一致性（B11）**：synthesis_packet 中放进 high_severity_* 容器的冲突，其 severity 字段必须为 high/medium（按 03 报告口径）；出现 low 或缺失 → failed。

**PC-16 他站产物 null 占位/禁用标记进输入（B12）**：扫各站 payload，出现下游站产物键（thesis_draft/critique/risk_boundary_report/analysis_revised/final_adjudication 等）或 `_disabled`/`forbidden_as_core_ref` 标记以非空值进入不该进的站 → failed（已知 reviser/final 的 pricing_expectation_ledger 属此类，装配点修前允许 failed 并注明"待 C6"）。

**PC-17 bridge 冲突矩阵行完整性（B13）**：bridge prompt 正文的冲突矩阵定义必须包含 A-M 13 行（03 报告 §8 第 17 项；现只有 A/B/C/K 四行 → 装配点补全前 failed 并注明）。

**PC-18 L4 数据陈旧（C3）+ 回购逐字重复行（C5）**：L4 payload 中 available 指标的数据日期晚于 run 数据日期（陈旧）→ failed；L4 材料中出现逐字相同的回购明细行 → failed。

**PC-19 canon 名 vs 输入 metric_name（C12）**：各层 payload 中 metric_name 与 IndicatorCanon 注册名不一致（同义不同词）→ failed，列出对照。

**PC-20 已落地三件复核（FIX-1/FIX-2/摊开器）**：①ESS 输入卡含 `needs_data_confirmation` 字段；②重试反馈含真实错误定位（llm_stage_diagnostics 中 retry_feedback 非空且含行/列或 JSONDecodeError 定位）；③`tests/test_context_spread.py` 存在且其测试收集数 ≥ 15。三件任一不满足 → failed。

## 测试要求

- 每条检查至少一对合成用例（该过/该红，反例先红后绿）；一个缺失 artifact 用例。
- `.venv/bin/python -m pytest tests/test_persistent_checks_b.py -q` 全绿；`tests/test_docs_consistency.py` 仍绿。
- 真实 run 20260731_002156 只读调试，不写死断言。

## 交付报告

- 两个文件改了什么；测试最后一行；每条检查在真实 run 上的 passed/failed 清单（允许历史病 failed，如实报）；BLOCKED 清单。
