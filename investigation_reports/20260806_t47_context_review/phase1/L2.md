# L2 通读报告

站：L2（stage_key `l2_analyst`，模型 deepseek-v4-flash，run 20260731_002156，effective_date 2026-07-30）。
审查对象：该站两次发送的提示词材料结构本身（不评价产出质量）。

## 通读范围

读的文件与读法：

- `projected/L2/attempt_1.projection.md`（文件 122,361 字节 / 2,738 行；投影字符 102,622）：**逐行全读**（4 次 Read 覆盖 1–2738 行）。
- `projected/L2/attempt_2.projection.md`（123,421 字节）：读头部段落目录；随后对两个 projection 文件做全文 diff、对两个 full prompt 做全文 diff，以 diff 方式覆盖 attempt_2 的差异部分（两 prompt 仅末尾重试反馈块不同，见发现 #13）。
- `full/L2/attempt_1.prompt.txt`（534,273 字符）：**程序化全量解析**（python 解析其中全部 JSON 块：当前层指标清单 13 条、Runtime Input 全树），并对所有关键事实用 grep -n 取原文行号。未逐行目视通读 53 万字符全文；目视覆盖以投影为准，投影三处截断标记（`«…省略 N 字符…»`、`__omitted__`、`__table_projection__`）中，唯一的大块是 `get_vix_term_structure` 的 raw_series 表（2,507 行），该表用 python 做统计核验（行数、日期 min/max、列名），未逐行目视。
- `full/L2/attempt_2.prompt.txt`（534,823 字符）：以 diff 覆盖（与 attempt_1 仅差末尾 20 行重试反馈）。
- `full/L2/meta.json`、`context_spread/manifest.json`：全读（meta.json 用 cat，manifest 用 python 提取 L2 条目）。

抽查口径说明：凡投影中逐字保留的散文与 JSON 键路径，均以投影目视 + 原文行号抽查双重确认；凡投影截断处，以 python 在原文上解析核验，不凭印象。

## 材料结构总览

两次发送的 prompt 均为约 53.4 万字符（attempt_1: 534,273；attempt_2: 534,823，差异仅在末尾重试反馈块）。按投影段落目录（attempt_1）与实测字符量，材料分布如下：

| 区块 | 字符 | 占全 prompt |
|---|---|---|
| 规则/任务书正文（系统约束 333 + Canon 四节 10,554 + 4C 示例 2,369 + 契约条文 2,542 + 角色段 3,225 + 输出规格与 Response Rules 1,800） | ≈20,823 | ≈3.9% |
| `### 当前层指标清单`（嵌在契约段内的数据质量清单，13 指标，无当前数值） | 30,533 | 5.7% |
| `## Runtime Input` 整块 | 482,865（实测） | 90.4% |
| ├─ context_brief（任务描述+3 条 highlights） | ≈715 | 0.1% |
| ├─ layer_facts（预填 state/core_signals/summary） | ≈3,546 | 0.7% |
| ├─ layer_raw_data（13 指标完整数据块） | ≈478,507 | 89.6% |
| │　└─ 其中 get_vix_term_structure 的 raw_series 逐日表（2,507 行，2016-07-29→2026-07-29） | ≈432,891 | **81.0%** |
| └─ manual_overrides（active=false） | ≈93 | 0.0% |

实测命令与输出（在 `full/L2/` 下）：
- `python3` 解析 Runtime Input：`Runtime Input 总长: 482865`；`layer_raw_data 指标数: 13`；各键偏移 `context_brief:4 / layer_facts:719 / layer_raw_data:4265 / manual_overrides:482772`。
- raw_series 跨度：原文行 2393（`"raw_series": [`）至 17437（`raw_series_window_note`），约 15,043 行，占全文 17,677 行的 85.1%；字符跨度实测 432,891。
- `raw_series 行数: 2507 日期范围: 2016-07-29 -> 2026-07-29`。

目录解读：这是一份"薄规则 + 厚数据"的 prompt——规则与任务书不到 4%，而数据占 90%+；数据内部又极度不均：单个指标（get_vix_term_structure）的 10 年逐日原始序列一张表占去全 prompt 的 81%，其余 12 个指标的完整数据块合计只占约 8.5%。同一指标集合在材料中以五种形态重复出现：①IndicatorCanon（语义法典，13 条）②Indicator Semantics（角色段一句话语义，11 条）③当前层指标清单（数据质量元数据，13 条，无数值）④layer_facts.core_signals/layer_highlights（摘要数值，8 条/3 条）⑤layer_raw_data（完整数值+重复一次的 data_quality，13 条）。

## 发现清单

### 1. 一张逐日原始序列表占全 prompt 81%，且无任何指令要求消费它
事实：`get_vix_term_structure` 的 `value.percentile_context.raw_series` 是 2,507 行逐日表（列：data_date/vix/vix3m/ratio，2016-07-29→2026-07-29），在 prompt 中占约 432,891 字符 = 全文的 81.0%（行 2393–17436，占 85.1% 的行数）。任务书、契约、输出纪律、Response Rules 全文均未出现要求消费或重算该表的指令；`raw_series` 一词全文出现 4 次，全部在数据块内部（键名 1 次、window_note 1 次、coverage 统计 2 次），散文指令区 0 次。表自带的 window_note 称其存在目的是"足以独立重算 windows.5y / windows.10y 两个分位"，但 5y/10y 分位（16.7/16.9）已作为成品预填在同一 value 块里（投影行 2420-2449）。
证据：
- `grep -n '"raw_series": \[' attempt_1.prompt.txt` → `2393:`；`grep -n 'raw_series_window_note'` → `17437:`
- `wc -l attempt_1.prompt.txt` → `17677`
- python 实测：`raw_series 表在 prompt 中跨度: 约 432,891 字符（占 prompt 81.0%）`、`raw_series 行数: 2507 日期范围: 2016-07-29 -> 2026-07-29`
- `grep -c 'raw_series' attempt_1.prompt.txt` → `4`（均在 JSON 数据行内）
- 投影 `projected/L2/attempt_1.projection.md` 行 2498：`"raw_series_window_note": "raw_series 覆盖至多10年…足以独立重算 windows.5y / windows.10y 两个分位。"`

### 2. 材料深度偏科：13 个指标只有 1 个带原始序列，其余 12 个只有单点快照
事实：layer_raw_data 13 个指标中，仅 get_vix_term_structure 附 10 年逐日 raw_series；其余 12 个指标只有当前截面（level/MA/分位/少量子项），历史只有分位数字，无任何序列。站的职责（Cognitive Transform）是对"每个指标"做同等的三问分析，但材料给单个指标的原料厚度是其余指标的数千倍。
证据：python 解析 layer_raw_data：`raw_series 行数: 2507` 仅存在于 vts；投影行 1539-2710 逐块可见其余 12 个指标 value 均为单点对象（如 get_vix value 仅 level/historical_stats/spot_over_ma20_ratio/ma20，投影行 1543-1554）。

### 3. data_quality 双份：同一对象在「指标清单」与「layer_raw_data」各出现一次，12/13 完全相同
事实：13 个指标的 data_quality 块在 `### 当前层指标清单`（契约段内，30,533 字符）出现一次，又在 `## Runtime Input` 的 layer_raw_data 内逐指标重复一次。python 语义比对：12/13 完全相同（紧凑序列化合计 20,415 字符/份）；`"contract_version": "data_evidence_v1"` 全文出现 26 次（13×2）。唯一例外是 get_cftc_nq_positioning：清单版 metric_authority 被折叠成 `{"keys":[…], "summary":"large object omitted from indicator manifest; full detail remains in analysis_packet artifact"}`（原文行 920），layer_raw_data 版是全文（7 个字段全展开，投影行 2136-2186）——折叠说明指向的 analysis_packet artifact 并不在 prompt 里，但同一份信息其实在 prompt 后段的 layer_raw_data 里有全文。
证据：
- python 比对输出：`get_vix: 相同=True 清单dq字符=1439 rawdq字符=1439`（12 个全 True）；`get_cftc_nq_positioning: 相同=False … 差异键: metric_authority`
- `grep -n 'large object omitted' attempt_1.prompt.txt` → `920:        "summary": "large object omitted from indicator manifest; full detail remains in analysis_packet artifact"`
- `txt.count('"contract_version": "data_evidence_v1"')` → `26`

### 4. methodology / formula / notes 三胞胎：同一段说明文字单指标最多重复 6 处
事实：layer_raw_data 内 8/13 个指标满足 `data_quality.methodology == data_quality.formula == notes`（逐字相同）；再叠加上游指标清单里的 methodology/formula/notes 三处，同一段文字单指标共出现 6 次。例：VIX 的"VIX 恐慌指数；分层降噪：现值 + 趋势比(Spot/MA20)。"出现 6 次；HYG 的长说明"Successfully fetched data as of 2026-07-29. Uses dividend-adjusted prices…"也出现 6 次。
证据：
- python 三胞胎检查输出：`get_vix: methodology==formula==notes -> True`（共 8 个 True；False 的 5 个是因 notes 与 methodology 文案不同：hy_quality_spread/cftc/finra/vts/cnn 边界情况）
- `grep -o 'VIX 恐慌指数；分层降噪：现值 + 趋势比(Spot/MA20)。' attempt_1.prompt.txt | wc -l` → `6`
- `grep -o 'Successfully fetched data as of 2026-07-29. Uses dividend-adjusted prices' attempt_1.prompt.txt | wc -l` → `6`

### 5. layer_facts 预填了本站要自己判断的结论："state": "neutral"
事实：站的任务（`## Layer Synthesis` 段）要求自己回答"L2 是 risk_on、neutral、risk_off、extreme_greed 还是 extreme_fear"；但 Runtime Input 的 layer_facts 已预填 `"state": "neutral"`（原文行 1471）和 `"summary": "风险偏好状态: neutral。关键事实: …"`（行 1479）。同时 core_signals 只列 8/13 个指标、key_metrics 只列 5 个（hy_oas/hy_quality/hyg/ig_oas/vix），layer_highlights 只列 3 条（全为信用类）；cnn/crowdedness/finra/vxn_vix_ratio 四个 analysis_required=true 的指标不进任何摘要层。
证据：
- `grep -n '"state": "neutral"' attempt_1.prompt.txt` → `1471:    "state": "neutral",`
- `grep -n '风险偏好状态' attempt_1.prompt.txt` → `1479:    "summary": "风险偏好状态: neutral。关键事实: High Yield OAS | 值=2.87 | …缺口=get_cftc_nq_positioning。"`
- python：`core_signals 指标: ['get_hy_oas_bp','get_hy_quality_spread_bp','get_hyg_momentum','get_ig_oas_bp','get_vix','get_vix_term_structure','get_vxn','get_xly_xlp_ratio']`（8 个）
- 投影行 1423-1428：layer_highlights 仅 3 条（HY OAS / HY CCC&Lower-BB / HYG）

### 6. 同名分位键两种刻度并存：percentile_10y 在 6 个指标是 0-1 小数、2 个指标是 0-100
事实：layer_raw_data 中 `percentile_10y` 同名键，get_vix=0.7155926809864758、get_vxn=0.8722…、get_hy_oas_bp=0.3100…、get_ig_oas_bp=0.3372…、get_hy_quality_spread_bp=0.9987…、get_xly_xlp_ratio=0.7919…（0-1 刻度）；而 get_hyg_momentum=98.2、get_vix_term_structure=16.9（0-100 刻度）。材料内没有任何字段说明刻度差异；摘要层 core_signals 则统一按 0-100 给（如 VIX 71.55926809864758）。同一材料里同键名不同量纲，且摘要与原文并存两种写法。
证据：
- python 输出：`get_vix: percentile_10y=0.7155926809864758 刻度=0-1` … `get_hyg_momentum: percentile_10y=98.2 刻度=0-100`、`get_vix_term_structure: percentile_10y=16.9 刻度=0-100`
- `grep -n '"percentile_10y": 0.7155926809864758\|"percentile_10y": 98.2\|"percentile_10y": 16.9' attempt_1.prompt.txt` → 行 1489 / 1821 / 2363
- core_signals 对照（投影行 1485-1493）：`"metric": "get_vix" … "historical_percentile": 71.55926809864758`

### 7. 数据日期三套并存且互相矛盾：07-29 / 07-30 / 07-31，其中 07-31 晚于 run 的 effective_date、甚至晚于采集时刻
事实：run 级 effective_date=2026-07-30（meta.json）。材料内：多数指标 data_date=2026-07-29（94 处）；CNN 恐贪=2026-07-30（50 处含时间戳）；而 get_crowdedness_dashboard、get_cftc_nq_positioning、get_finra_margin_debt 的 as_of/effective 及 get_vix_term_structure 的 effective_date 共 21 处标 2026-07-31。具体矛盾：
- context_brief 宣称"数据日期 2026-07-30"（行 1364），与多数指标 07-29 不一致；
- crowdedness 的 data_date=2026-07-31（行 1984），但其 collected_at_utc=2026-07-30T16:18:15Z（行 1985 区域）——日期标签晚于采集时刻本身；
- crowdedness 子项内部日期也不一致：skew_index date=2026-07-29（行 1951），qqq_put_call_ratio_oi date=2026-07-31 但 notes 写"基于到期日: 2026-07-30 的期权持仓量"（行 1957-1959），qqq_short_interest_percent value=null 但 date=2026-07-31（行 1963-1964）；
- get_vix_term_structure 在清单内相邻两行 as_of_date=2026-07-29 / effective_date=2026-07-31（行 1097-1098），同一块内自相矛盾；raw 块内同样 as_of 07-29 / effective 07-31（投影行 2531-2532）；
- cftc 的 no_data sentinel 里 effective_date=2026-07-31（行 2159）。
证据：
- `grep -o '2026-07-29' attempt_1.prompt.txt | wc -l` → `94`；`2026-07-30` → `50`；`2026-07-31` → `21`
- `grep -n '2026-07-31' attempt_1.prompt.txt` → 行 810/817-819（清单 crowdedness）、886-887（清单 cftc）、955-956（清单 finra）、1098（清单 vts effective）、1957/1964/1970/1982-1984（raw crowdedness）、2054-2055（raw cftc）、2159（cftc no_data）、2194-2195（raw finra）、17471（raw vts effective）
- `sed -n '1090,1100p'` 输出照抄：`"as_of_date": "2026-07-29",` 紧接 `"effective_date": "2026-07-31",`
- `sed -n '1949,1970p'` 输出照抄：skew `"date": "2026-07-29"`；put/call `"date": "2026-07-31", "notes": "基于到期日: 2026-07-30 的期权持仓量"`
- meta.json：`"effective_date": "2026-07-30"`；`"collected_at_utc": "2026-07-30T16:18:15.998007+00:00"`（投影行 2044）

### 8. 输出「结构示例」用的是 L1 指标，示例 evidence_refs 指向本层输入里不存在的数据
事实：契约段 `### 结构示例`（原文行 1231-1256）的示范 JSON 使用 L1 宏观指标 get_10y_real_rate，示例 `"evidence_refs": ["L1.get_10y_real_rate"]`（行 1242）、`"covered_function_ids": ["get_10y_real_rate"]`（行 1250）。而系统级约束第 4 条（行 9）规定"所有 evidence_refs 必须来自本次输入的 raw_data，不得凭记忆添加"——L1.get_10y_real_rate 不在本层 raw_data 中。示例与约束在 evidence_refs 写法上互相打架；模型若模仿示例形状即违反约束。
证据：
- `grep -n '### 结构示例\|L1.get_10y_real_rate' attempt_1.prompt.txt` → `1231:### 结构示例`、`1235: "function_id": "get_10y_real_rate",`、`1242: "evidence_refs": ["L1.get_10y_real_rate"],`、`1250: "covered_function_ids": ["get_10y_real_rate"],`
- `grep -n 'evidence_refs 必须来自' attempt_1.prompt.txt` → `9:4. 所有 evidence_refs 必须来自本次输入的 raw_data，不得凭记忆添加。`

### 9. "HYG Momentum" 名实不符：摘要层把价格水平当动量指标的值和分位
事实：get_hyg_momentum 的指标名叫 "Adjusted-Price Momentum"，但 core_signals/layer_highlights 给出的 value=79.24 是调整后价格水平本身（raw level=79.23999786376953），historical_percentile=98.2 是价格的 10 年分位（raw 同时给出 percentile_1y=80.2）；真正的动量信息（velocity_1d=-0.18、acceleration_1d=-0.33、direction=falling）只在 raw 深层，不进任何摘要。摘要层读到的"动量指标"实为价格点位与价格分位。
证据：
- 投影行 1462-1471：`"metric": "get_hyg_momentum" … "value": 79.24, "historical_percentile": 98.2, "trend": "falling"`
- `grep -n '"velocity_1d"\|"percentile_1y": 80.2\|"percentile_10y": 98.2' attempt_1.prompt.txt` → 行 1812（velocity_1d -0.18）/ 1817 / 1821
- 投影行 1866-1882：value.level=79.239997… 与 momentum{} 子块并列

### 10. 摘要层 trend 字段大面积丢失：core_signals 8 个指标中 4 个 trend=null，但 raw 里有趋势信息
事实：core_signals 中 get_vix / get_vix_term_structure / get_vxn / get_xly_xlp_ratio 四个指标 `"trend": null`（原文行 1431/1442/1453/1464）；但这些指标的 raw 块里带着趋势代理字段——如 get_vix 有 `spot_over_ma20_ratio: 1.2005`、`ma20: 17.21`（现值高于 MA20 约 20%），get_xly_xlp_ratio 有 `position_vs_ma20: "below"`。摘要层（模型最先读到的 0.7% 材料）与 raw 层的趋势可见性不对称。
证据：
- `grep -n '"trend": null' attempt_1.prompt.txt` → 行 1431/1442/1453/1464（四处，均在 layer_facts.core_signals）
- 投影行 1543-1553：get_vix value 含 `"spot_over_ma20_ratio": 1.2005, "ma20": 17.21`
- 投影行 1939-1949：get_xly_xlp_ratio value 含 `"position_vs_ma20": "below", "ma20": 1.3588`

### 11. 角色段 Indicator Semantics 只定义 11/13 个指标，缺 cftc 与 finra
事实：`## Indicator Semantics`（角色职责段）逐指标给一句话语义，共 11 条；缺 get_cftc_nq_positioning 与 get_finra_margin_debt。而同一份 prompt 的 IndicatorCanon 为 13 条、当前层指标清单为 13 条、layer_raw_data 为 13 块。同一材料的不同章节对本层指标集合的覆盖不一致。
证据：
- `awk '/^## Indicator Semantics/,/^## Mechanism Grammar/' attempt_1.prompt.txt | grep -c '^- \`get_'` → `11`；列出的 11 个 id 中无 get_cftc_nq_positioning、get_finra_margin_debt
- `awk '/^### IndicatorCanon for L2/,/^### Canon Output Hints/' attempt_1.prompt.txt | grep -c '"function_id"'` → `13`

### 12. canon 与 metric_authority 的交叉验证目标指向输入中不存在的数据（静态引用，但数量不小）
事实：canon 的 cross_validation_targets 与 data_quality 的 metric_authority.requires_confirmation 大量引用非本层指标：get_advance_decline_line（全文 16 次）、get_net_liquidity_momentum（5 次）、get_atr_qqq（2 次）、get_percent_above_ma（2 次）、get_copper_gold_ratio、get_qqq_technical_indicators、get_ndx_ndxe_ratio（各 1 次）。这些数据不在本层输入中；canon 段头已声明"本段只提供静态规则"，这些引用本身合规，但材料里"被要求交叉验证的对象"与"手里实际有的数据"之间的缺口没有任何集中说明，模型需自行分辨哪些验证目标本层不可达。
证据：
- python 输出：`prompt 全文出现但不属于本层 13 指标的 function_id: ['get_10y_real_rate','get_advance_decline_line','get_atr_qqq','get_copper_gold_ratio','get_layer','get_ndx_ndxe_ratio','get_net_liquidity_momentum','get_percent_above_ma','get_qqq_technical_indicators']`，其中 `get_advance_decline_line: 出现 16 次`
- 投影行 58（canon 段头）：`本段只提供静态规则，不提供其他层的运行时数据、结论或候选跨层判断。`

### 13. attempt_2 的材料 = attempt_1 逐字节相同 + 末尾 20 行重试反馈，反馈里带有第一次答案的分析结论
事实：两 prompt 全文 diff 仅 22 行差异：attempt_2 在原文件末尾（行 17677 之后）追加一段"上一次返回未通过结构校验"的重试反馈。该反馈除报错（parse_error，原始响应 20,541 字符）外，还原文引用了 attempt_1 失败响应的结尾片段，其中包含第一次的分析结论性内容（"整体信用利差低 vs 尾部信用利差极端""恐惧情绪 vs 杠杆继续扩张""信用尾部压力（HY质量利差）是本层最突出的风险信号…情绪已进入恐惧但非极端，注意反向机会条件"）。即第二次发送的材料里混入了第一次答案的结论片段，而非纯粹的错误信息。投影侧表现为 `## Response Rules` 段从 365 字符涨到 915 字符，其余各段字符量两次完全相同。
证据：
- `diff attempt_1.prompt.txt attempt_2.prompt.txt | wc -l` → `22`；diff 体：`17677a17678,17697` 追加，末行 `请仅输出修正后的 JSON 对象，不要附加任何解释。`
- `diff projected/L2/attempt_1.projection.md projected/L2/attempt_2.projection.md`：除头部统计行外仅末尾新增 20 行（投影行 2739-2758），含 `"unresolved_internal_tensions": ["整体信用利差低 vs 尾部信用利差极端","恐惧情绪 vs 杠杆继续扩张"]` 等
- 两个投影的段落目录逐项字符量完全相同（仅 Runtime Input 占比 90.4%→90.3%、Response Rules 365→915）

### 14. manual_overrides 残留陈旧日期：active=false 但 date="2022-01-04"
事实：Runtime Input 末尾 `"manual_overrides": {"active": false, "date": "2022-01-04", "metrics": {}}`（原文行 17651-17655）。功能未启用，但一个与本次运行（2026-07-30）无关的 2022 年日期字段留在材料里，没有任何说明文字。
证据：`grep -n '"date": "2022-01-04"' attempt_1.prompt.txt` → `17653:    "date": "2022-01-04",`；python：`manual_overrides: {"active": false, "date": "2022-01-04", "metrics": {}}`

### 15. get_vxn_vix_ratio 来源字段标 missing/null，但 methodology 写明它是站内现算的派生值
事实：清单中该指标 `"source_name": null`（行 1035）、`data_quality.provider: "missing"`（行 1039）、source_url "missing"，anomalies 含 missing_source_url；但同一对象 methodology 写 "Calculated from latest levels: VXN=30.84, VIX=20.66"——即它由本层另外两个指标现算而来，来源字段却按"缺失"标记。对比：其他派生指标如 get_hy_quality_spread_bp 的 source 标注为 "FRED / ICE BofA"（有明确上游）。
证据：
- `grep -n '"source_name": null\|"provider": "missing"' attempt_1.prompt.txt` → `1035:` / `1039:`（清单）与 `2286:`（raw）
- 投影行 1096-1097：`"methodology": "Calculated from latest levels: VXN=30.84, VIX=20.66"`

## 核验为一致（检查过、未构成发现）

- core_signals 8 个指标的分位与 raw 数据完全吻合（0-1 刻度 ×100 换算后逐一相等，python 输出 8 行`一致=True`）。
- 派生数值算术全部吻合：VXN/VIX=30.84/20.66=1.4927 ✓；VIX3M/VIX=21.5/20.66=1.0407 ✓；VIX6M/VIX=23.06/20.66=1.1162 ✓；CCC−BB=10.13−1.76=8.37 ✓。
- raw_series 2,507 行日期范围 2016-07-29→2026-07-29，晚于 run effective_date（2026-07-30）的行数=0；该表本身无未来数据。
- 隔离边界：layer_raw_data 13 个键全部为本层指标；`"apparent_cross_layer_signals": []`（行 1372）；输入中未发现其他层的运行时数据或结论（发现 5 的 state 与本发现 12 的静态引用另计）。
- "L2 本层 12/13 个指标成功"（行 1364）与清单一致：13 条中仅 get_cftc_nq_positioning 有 error 且 analysis_required=false。
- NO_DATA_AVAILABLE 哨兵（cftc）与系统约束第 5 条的处置约定相互对应，材料里该指标 value=null、未填估计值。

## 盲区

- **raw_series 2,507 行未逐行目视**：投影只保留 head 3 行 + tail 1 行 + min/max 统计（`__table_projection__`），我在原文上用 python 核验了行数（2,507）、列名、日期范围（2016-07-29→2026-07-29，无晚于 effective_date 的行）与投影给出的 min/max 一致；中间 2,503 行的逐日数值正确性未抽查（无法判断该表内部是否有坏行）。
- **两处投影截断未回原文补读全句**：①get_vix_term_structure 的 methodology 长串投影省略 88 字符（投影行 2571，标记 `«…省略 88 字符…»`）——该句前后文完整、仅中段截断，我判断与结构审查无关未补读；②layer_facts.summary 投影省略 105 字符（投影行 1537）——已用 python 读全，内容无新信息（为 highlights 拼接+缺口标注）。
- **attempt_2 正文未逐行独立通读**：覆盖方式是全文 diff（prompt diff 22 行、projection diff 38 行，均只涉及头部统计与末尾重试块），据此推定其余部分与 attempt_1 逐字节相同；未对 attempt_2 单独跑 python 结构解析。若 diff 工具本身有漏报风险，则 attempt_2 存在理论上的漏检面。
- **full prompt 53 万字符未逐行目视**：散文部分以投影逐字保留为准（投影规则承诺散文不截断），JSON 部分以 python 解析为准；投影若存在未声明的渲染失真，我无从发现。
- **IndicatorCanon 13 条目视了投影全文，但未与项目法典 RESEARCH_CANON.md 原文对照**：法典内容本身的正确性不在本站"材料结构"审查边界内。
- **meta.json 中 attempt_1 失败响应的完整原文（response.raw.txt，30,361 字节）未读**：它与"发送材料结构"无关，仅在发现 13 中通过 meta.json 引用片段和 diff 确认了重试反馈的内容来源。
- **payload.json（约 51.8 万/51.9 万字节）未解析**：prompt.txt 是实际发送文本，已全量解析；payload.json 若与 prompt.txt 有结构差异，本报告覆盖不到。
