# event_card_interpreter.event_4a41774d6a5c953b 通读报告

## 通读范围

- 读了哪些文件：
  - `projected/event_card_interpreter.event_4a41774d6a5c953b/attempt_1.projection.md`（145 行，全文逐行读完）
  - `full/event_card_interpreter.event_4a41774d6a5c953b/attempt_1.prompt.txt`（129 行，6,903 字符，全文逐行读完）
  - `context_spread/manifest.json` 中本站条目（用 python 提取）
- 读了多少：投影 5,118 字符 + 原文 6,903 字符，全部覆盖；投影对原文覆盖率 74.1%。
- 怎么读的：**全读**（材料量小，非抽查）。投影里两处截断（`raw_text_excerpt` 中段省略 1,712 字符、`cth_01` 假说文中段省略 100 字符）均已到原文对应位置逐字核对，见下。
- manifest 事实：本站 `inspector_has_rules: false`（此前无机械检查规则），`verification_ok: true`，`projection_ratio: 0.7414`，prompt sha256 `f02ca330…59089`。

## 材料结构总览

段落目录（投影头部目录 + 用 python 对原文按标题切分复核，两者一致）：

| 段落 | 字符量 | 占比 |
|---|---|---|
| `## System Message` | 17 | 0.2% |
| `# System-Level Constraints (不可违反)` | 332 | 4.8% |
| `## User Message`（任务书+五条铁律） | 604 | 8.7% |
| `## Runtime Input`（JSON：event_material / allowed_financial_links / competing_hypotheses / output_contract / boundary） | 4,669 | 67.6% |
| `## 输出字段规格`（EventInterpretationCard 契约） | 905 | 13.1% |
| `## Response Rules` | 371 | 5.4% |

复核命令与输出：

```
$ python3 -c "import re; txt=open('attempt_1.prompt.txt').read();
  parts=re.split(r'\n(?=#{1,2} )',txt); [print(repr(p.split(chr(10),1)[0][:60]),len(p)) for p in parts]; print('TOTAL',len(txt))"
'## System Message' 17
'# System-Level Constraints (不可违反)' 332
'## User Message' 604
'## Runtime Input' 4669
'## 输出字段规格（由 EventInterpretationCard 契约自动生成，形状以此为准）' 905
'## Response Rules' 371
TOTAL 6903
```

解读：这是一个"单事件解读"站——给它一条已采集事件（美联储对两名银行前雇员的执法行动新闻稿），要求产出结构化解读卡（fact_summary / interpretation / mechanism_hypothesis / supports/refutes 假说等 12 个字段）。材料分布上：任务规则（System+User+Response Rules 合计约 1,324 字符，19.2%）远小于数据与契约（Runtime Input + 输出字段规格合计 5,574 字符，80.8%）。Runtime Input 内部的大头是 `event_material.raw_text_excerpt`（约 1,900 字符级长字符串）和 `output_contract`（约 800 字符级 JSON 示例）。

（发现清单逐条追加于下）

## 发现清单

### 发现 1：措辞规定自相矛盾——User Message 明文"没有固定说法要套"，output_contract 示例却规定弱来源开头措辞

证据：

- `full/.../attempt_1.prompt.txt:20`（User Message 铁律第 4 条）：
  ```
  $ sed -n '20p' attempt_1.prompt.txt
  - 来源不是官方披露的、或材料只有标题没有正文的，解读措辞该降一档就降一档，`limitations` 里如实写清限制。**措辞完全由你决定，没有固定说法要套**——报告会由代码在每条事件旁自动标出来源等级与"仅标题与片段·降级阅读"，读者不会误判，所以这里不设措辞检查。你要做的是别把只有标题的材料写得像读过全文一样确定。
  ```
- `full/.../attempt_1.prompt.txt:73`（Runtime Input 内 output_contract 的 interpretation 示例）：
  ```
  $ sed -n '73p' attempt_1.prompt.txt
      "interpretation": "模型解读；弱来源以据报道或该媒体称开头",
  ```
- 对照 `attempt_1.prompt.txt:113`（输出字段规格的 interpretation 条目）：`- `interpretation`（必填）：字符串 —— 与事实分开的模型解读`——规格本身不含措辞规定。

事实描述：同一份提示词里，User Message 用加粗明示"措辞完全由你决定，没有固定说法要套……不设措辞检查"；而 Runtime Input 内 output_contract 的 `interpretation` 示例值却写着一条固定措辞要求（"弱来源以据报道或该媒体称开头"）。两处指令方向相反。Response Rules（第 128 行）另有"正文里的示例只解释语义，形状冲突时以规格为准"的优先级条款，但该条款字面上只覆盖"形状"冲突，未覆盖这类语义/措辞层面的矛盾。本事件 tier 为 `official_macro`（官方来源），弱来源条款对本事件未必触发，但材料内矛盾作为结构事实存在。

### 发现 2：输出形状双份描述——output_contract 与"输出字段规格"两处各约 900 字符，合计占全文 26.2%

证据：

- output_contract 位于 `full/.../attempt_1.prompt.txt:70-102`（Runtime Input 内的 JSON 示例），"输出字段规格"位于第 110-122 行（契约自动生成的逐字段条目）。
  ```
  $ python3 -c "..."
  output_contract 行范围: 70-102, 字符数: 907
  输出字段规格 行范围: 110-122, 字符数: 905
  两份合计: 1812 占全文 6903 的 26.2 %
  ```

事实描述：同一份输出契约（12 个顶层字段：event_id … passport）在站内以两种形态各写了一遍：一遍是 Runtime Input 里的 JSON 示例（带语义注释如"只写材料事实""照抄 event_material.source"），一遍是文末"输出字段规格"的逐字段条目（带必填/可选与类型）。两份合计 1,812 字符，占提示词全文 26.2%。Response Rules 第 128 行"形状冲突时以规格为准"一句本身也表明系统预期两处可能不一致。逐条比对后，两份的字段集合一致（12 个顶层字段相同），发现的差异是措辞层面的（见发现 1 的 interpretation 示例）和详略层面的（output_contract 带"照抄"类填写指令，规格只写语义）。

### 发现 3：九个金融传导渠道清单在站内重复出现两次

证据：

- `full/.../attempt_1.prompt.txt:42-52`：Runtime Input 的 `allowed_financial_links` 数组，9 项（earnings_path … technical_flow）。
- `full/.../attempt_1.prompt.txt:116`：输出字段规格 mechanism_hypothesis 条目内嵌同一枚举：
  ```
  $ grep -n 'earnings_path' attempt_1.prompt.txt
  43:    "earnings_path",
  116:- `mechanism_hypothesis`（必填）：对象 EventMechanismHypothesis{financial_link:"earnings_path"|"valuation_multiple"|"discount_rate"|"risk_premium"|"liquidity_condition"|"credit_condition"|"index_structure"|"market_breadth"|"technical_flow", hypothesis} —— 金融链路与一句机制假设
  ```
- User Message 第 18 行另有一处文字引用："并从给定的九个金融传导渠道里选择"，与数组实有 9 项一致。

事实描述：同一份九项枚举以数组形态（第 42-52 行）和规格内联枚举形态（第 116 行）各出现一次；两处内容一致，无冲突。这是发现 2 双份契约结构的子项。

### 发现 4：raw_text_excerpt 约四分之三是网页导航样板，开头带 BOM 乱码；执法细节实际在未抓取的 PDF 附件里

证据：

- `full/.../attempt_1.prompt.txt:37`，用 python 提取该字段值逐段测量：
  ```
  摘录总字符: 1892
  开头20字符 repr: 'ï»¿ Federal Reserve '
  实质正文(For release at)起始下标: 1226
  实质正文(核心通告句)结束下标(Additional enforcement actions): 1696
  即: 样板/导航类字符约 1226 + 尾部 196 ; 核心通告内容约 470 字符, 占 24.8 %
  含附件引用: True | True
  ```
- 摘录前 1,226 字符原文照抄（同第 37 行）：`ï»¿ Federal Reserve Board - … Skip to main content An official website of the United States Government Here's how you know Official websites use .gov … Federal Reserve Facebook Page Federal Reserve Instagram Page Federal Reserve YouTube Page … Toggle Dropdown Menu Main Menu Toggle Button Sections Search Toggle Button Home News & Events Press Releases Press Release July 30, 2026`——为政府网站横幅、社交媒体链接列表、菜单折叠控件等页面框架文字。
- 核心通告内容（下标 1226-1696，约 470 字符）：`For release at 11:00 a.m. EDT Share The Federal Reserve Board on Thursday announced the execution of the following enforcement actions listed below: Consent prohibition order against Simon Alberto Gonzalez … Misappropriation of customer funds Consent prohibition order against Ralph A. Mojica … Misappropriation of customer funds and embezzlement of Bank funds`。
- 摘录末尾含 `Attachment 1 (PDF) Attachment 2 (PDF)`，但两份附件内容不在材料中（材料无其他附件字段）。

事实描述：这站唯一的"正文材料"（raw_text_excerpt，1,892 字符）中约 75% 是抓取页面的导航/样板文字，开头还带一处编码残留的 BOM 乱码（`ï»¿`）；真正的事件内容只有约 470 字符，且命令细节（ prohibition order 的具体条款、金额等）在材料以 `Attachment 1/2 (PDF)` 形式被引用但附件本身未进入材料。任务书第 17 行要求"fact_summary 里只许出现材料里逐字有的东西"，第 20 行的降级条款针对的是"只有标题没有正文"，而本材料的实际形态是"有正文但四分之三为页面样板"。

### 发现 5：输入 entities 为空数组，但摘录内含多个具名实体

证据：

- `full/.../attempt_1.prompt.txt:35`：`"entities": [],`（event_material 内）。
- 同文件第 37 行摘录内出现的具名实体：`Simon Alberto Gonzalez`（Regions Bank 前雇员，Birmingham, Alabama）、`Ralph A. Mojica`（First Interstate Bank 前雇员，Billings, Montana）、两家银行名（第 28 行标题与第 37 行正文均出现）。
  ```
  $ grep -n '"entities"' attempt_1.prompt.txt
  35:    "entities": [],
  74:    "entities": [
  ```
  （第 74 行为 output_contract 中要求模型输出的 entities 示例 `"材料中的实体"`。）

事实描述：上游采集给出的 `event_material.entities` 是空数组；材料正文实际包含两名人名、两家银行名、两个地名。输出契约要求模型自己从材料提取 entities 填入输出，故空输入不等于缺失功能，但"输入实体清单为空 vs 正文实体实际存在"是材料内的事实状态。

### 发现 6：系统级约束引用的字段/标记（evidence_refs、raw_data、NO_DATA_AVAILABLE）在本站输入与输出契约中均不存在

证据：

- `full/.../attempt_1.prompt.txt:9`：`4. 所有 evidence_refs 必须来自本次输入的 raw_data，不得凭记忆添加。`
- 第 10 行：`5. 若输入出现 `NO_DATA_AVAILABLE`，只能把它当作数据边界或置信度限制；…`
  ```
  $ grep -n 'evidence_refs\|raw_data\|NO_DATA_AVAILABLE' attempt_1.prompt.txt
  9:4. 所有 evidence_refs 必须来自本次输入的 raw_data，不得凭记忆添加。
  10:5. 若输入出现 `NO_DATA_AVAILABLE`，只能把它当作数据边界或置信度限制；不得围绕空数据补写数值、趋势、分位或原因。
  ```
  全文中 `evidence_refs`、`raw_data`、`NO_DATA_AVAILABLE` 只出现在这两行约束文本里。
- 本站 Runtime Input 的顶层键为 `event_material / allowed_financial_links / competing_hypotheses / output_contract / boundary`（第 26、42、53、70、103 行），无 `raw_data`；输出字段规格（第 110-122 行）12 个字段中无 `evidence_refs`；输入中亦无 `NO_DATA_AVAILABLE` 标记。

事实描述：系统级约束第 4、5 条以"本次输入的 raw_data"和"evidence_refs"为约束对象，但本站的输入键名是 `event_material`、输出契约里没有 `evidence_refs` 字段；这两条约束在本站没有可作用的对象。约束是跨站通用模板直接落到本站提示词中，与本站契约的字段命名不一致。

### 发现 7：事件内容（对两名个人前雇员的执法）与纳指 100 无可说明关联，但经 trigger_reasons=["mainline"] 进入本站

证据：

- `full/.../attempt_1.prompt.txt:28`（标题）：`Federal Reserve Board issues enforcement actions with former employee of Regions Bank and former employee of First Interstate Bank`。
- 第 37 行核心内容：两份 consent prohibition order，对象是个人（挪用客户资金/侵占银行资金），非银行机构本身，更非指数成分或宏观政策。
- 第 38-40 行：
  ```
  $ grep -n 'trigger_reasons' -A2 attempt_1.prompt.txt
  38:    "trigger_reasons": [
  39-      "mainline"
  40-    ]
  ```
- 任务书第 21 行备有逃生门：`与纳指 100 没有可说明关联的事件，诚实输出 `interpretation: 与判断对象关联不足`，不要硬找联系。`
- 输入 `event_type`（第 34 行）为 `policy_or_financial_conditions`；输出契约（第 77 行 `"event_type": "事件类型"`）与输出字段规格（第 115 行"事件类型"）均未说明该字段是照抄输入分类还是由模型重分类。

事实描述：这条材料由"mainline"触发进入解读站，但事件实质是美联储对两名个人前雇员（挪用/侵占资金）的禁止从业命令，与 NDX 判断对象之间材料本身不提供任何可说明的关联路径。任务书已预设"关联不足"的诚实出口，结构上有覆盖；同时输入分类 `policy_or_financial_conditions`（政策或金融条件）与事件实质（针对个人的执法行动）之间的语义差距、以及输出 event_type 字段"照抄还是重分类"无指令，是材料内的两处事实状态。

### 发现 8（一致性核对·未见矛盾）：时间戳、假说数字、假说引用三项核对结果

证据与事实：

- **时间戳链一致**。`full/.../attempt_1.prompt.txt:31-33`：`published_at: "Thu, 30 Jul 2026 15:00:00 GMT"`、`event_date: "2026-07-30"`、`effective_date: "2026-07-30"`；摘录正文（第 37 行）`For release at 11:00 a.m. EDT`、`Press Release July 30, 2026`、`Last Update: July 30, 2026`。11:00 EDT 折合 15:00 UTC，与 published_at 一致；三处日期一致，未见时间矛盾。passport 要求照抄的五个字段在输入中全部存在（第 29-33 行 + source/tier 第 29-30 行），无缺。
- **假说数字无同指标异值**。用 python 抽取三条假说中的数字（命令输出）：
  ```
  hyp_base_88ece56ecb -> ['99.4%分位'（实际利率分位）]
  cth_01 -> ['19.46倍'(Forward PE), '30日+4.2%', '90日+10.7%'(盈利修正斜率), 'M7资本开支+75%', 'RSI 32.4', 'MA200', 'HY OAS 31%分位']
  cth_02 -> ['30d flagged权重41%'(supplier_lookback), 'M7回购-68.74%']
  ```
  同一指标未在不同假说中取出不同数值；三条假说立场相反（看多/看空）但各用各的数字，属竞争性陈述而非数值矛盾。
- **假说引用闭环**。competing_hypotheses 提供 3 个 id（`hyp_base_88ece56ecb`、`cth_01`、`cth_02`，第 55/60/65 行）；output_contract（第 83、86 行）与输出字段规格（第 117-118 行）均要求 supports/refutes 只能引用这些 id。指令要求消费的对象在材料中全部存在。
- **投影两处截断已核原文**：
  - `raw_text_excerpt` 省略的 1,712 字符中段：已核，即发现 4 测量的样板段+核心通告段，投影未见内容不影响结构判断。
  - `cth_01` 省略的 100 字符中段（投影第 77 行 `价«…省略 100 字符…»视`）：已核原文为 `格在MA200上方并接近Donchian下轨，显示抛压可能已近尾声。若实际利率不再上行，盈利兑现将触发估值修复，市场可能已过度悲观。净流动性边际改善和期限利差转正也提供宽松背景。主线对信用尾部风险的重`（python 实测中段恰为 100 字符），为技术面与流动性论据，无结构性新信息。

## 盲区

- **投影截断**：两处截断（excerpt 中段 1,712 字符、cth_01 中段 100 字符）均已到 `full/attempt_1.prompt.txt` 逐字核对，投影侧无残留盲区。
- **同目录非提示词文件未逐字读**：`full/event_card_interpreter.event_4a41774d6a5c953b/` 下另有 `attempt_1.parsed.normalized.json`、`attempt_1.payload.json`、`attempt_1.response.raw.txt`、`meta.json`、`output.validated.json`。本任务审查对象是"站收到的提示词材料结构"，提示词全文（attempt_1.prompt.txt）已 100% 覆盖；这些产出/元数据文件不属于"站收到的材料"，故未逐字读。若需核"站实际产出与契约的吻合度"，需另开范围。
- **材料成因不可见**：为何这条执法新闻被标 `trigger_reasons: ["mainline"]`、为何 `entities` 采集为空、为何摘录未剥离网页样板——成因在上游采集/选料代码与运行日志里，不在本站语料内，本报告只记录现象不定原因。
- **附件内容不可得**：摘录引用的 `Attachment 1 (PDF)`、`Attachment 2 (PDF)`（执法命令全文）不在材料中，其内容对本通读不可见。
- **九渠道与三假说的语义适配**（例如"个人执法事件能否走 discount_rate 渠道"）属投研判断，超出"材料结构对不对"的审查边界，未展开。
