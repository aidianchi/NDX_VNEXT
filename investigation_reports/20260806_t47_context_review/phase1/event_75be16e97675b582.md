# event_card_interpreter.event_75be16e97675b582 通读报告

## 通读范围

- **投影**：`context_spread/projected/event_card_interpreter.event_75be16e97675b582/attempt_1.projection.md`，148 行 / 4,906 字符，**全文逐字读完**（Read 工具一次读完）。
- **原文**：`context_spread/full/event_card_interpreter.event_75be16e97675b582/attempt_1.prompt.txt`，132 行 / 4,993 字符，**全文逐字读完**（Read 工具一次读完）。投影覆盖率 98.3%，唯一截断处（cth_01 假说文本中段 «…省略 100 字符…»，投影 L80）已对照原文 L64 查全。
- **同目录辅助文件**：`meta.json` 全读（1,430 字符：模型 deepseek-v4-flash、status ok、effective_date 2026-07-30、attempts=1、无 validation_errors）；`attempt_1.parsed.normalized.json` 全读（用于核实"指令实际被怎样消费"，manifest 显示它与 output.validated.json 同 sha256）；`attempt_1.payload.json` 只读开头 600 字符（确认 payload.event_material 与 prompt 输入一致）。
- **manifest.json** 中本站条目已查：prompt_chars=4,993、projection_chars=4,906、inspector_has_rules=false（本站此前无机械检查规则）、verification_ok=true。
- 读法：小站（约 5K 字符），全文精读，无抽查。所有发现均用 sed/grep/python 复核，命令原文见各条证据。

**投影保真度附注**：投影标记 «…省略 100 字符…»，实测被省略段为 101 字符。复核命令与输出：
```
$ python3 -c "import re; full=open('attempt_1.prompt.txt',encoding='utf-8').read(); m=re.search(r'接近超卖，(.*?)视可能高估', full); print(len(m.group(1)))"
101
```
省略段原文（原文 L64 中段）：`价格在MA200上方并接近Donchian下轨，显示抛压可能已近尾声。若实际利率不再上行，盈利兑现将触发估值修复，市场可能已过度悲观。净流动性边际改善和期限利差转正也提供宽松背景。主线对信用尾部风险的重`

## 材料结构总览

原文 4,993 字符，六段。段落目录标注的字符量与实测完全一致（实测命令：python 按标题切分，输出见下）。

| 段落 | 字符 | 占比 | 内容性质 |
|---|---|---|---|
| `## System Message` | 18 | 0.4% | 仅段标题（System 正文被并入下一段统计） |
| `# System-Level Constraints (不可违反)` | 333 | 6.7% | 6 条通用纪律（原文 L4-11） |
| `## User Message` | 605 | 12.1% | 任务书：角色 + 5 条铁律（原文 L14-21） |
| `## Runtime Input` | 2,760 | 55.3% | 数据 JSON：event_material + allowed_financial_links + competing_hypotheses + output_contract 示例 + boundary（原文 L24-111） |
| `## 输出字段规格` | 906 | 18.1% | EventInterpretationCard 契约字段清单（原文 L113-125） |
| `## Response Rules` | 371 | 7.4% | 5 条输出规则（原文 L127-132） |

实测输出：
```
total chars: 4993
## System Message: 18 chars
# System-Level Constraints: 333 chars
## User Message: 605 chars
## Runtime Input: 2760 chars
## 输出字段规格: 906 chars
## Response Rules: 371 chars
```

**Runtime Input 内部细分**（2,760 字符）：event_material 约 480 字符（其中真正的事件内容只有标题 67 字符，raw_text_excerpt 为空）；allowed_financial_links 约 200 字符（9 项）；competing_hypotheses 约 1,050 字符（3 条假说长文本，本站最大的信息块）；output_contract 示例 907 字符；boundary 约 120 字符。

**消费方式实测**（指令要求 vs 实际）：12 个输出字段中，passport 5 字段契约明确要求"照抄 event_material.xxx"（原文 L99-103），event_id 对应底账 id——即至少 6/12 字段是机械搬运；生成性字段为 fact_summary / interpretation / mechanism_hypothesis / supports / refutes / limitations / needs_data_confirmation。响应（parsed.normalized.json）顶层 12 字段齐全，形状合规，status ok，无 validation_errors。

**数值核对记录（未见矛盾）**：hyp_base / cth_01 / cth_02 三条假说中共出现 9 个数值——99.4%分位、Forward PE 19.46、+4.2%、+10.7%、+75%、RSI 32.4、HY OAS 31%分位、41%、-68.74%。逐一 grep 计数，每个数值在全 prompt 中各只出现 1 次：
```
$ for v in '99.4%' '19.46' '+4.2%' '+10.7%' '+75%' '32.4' '31%分位' '41%'; do printf '%s: ' "$v"; grep -oF "$v" attempt_1.prompt.txt | wc -l; done
99.4%: 1 / 19.46: 1 / +4.2%: 1 / +10.7%: 1 / +75%: 1 / 32.4: 1 / 31%分位: 1 / 41%: 1
$ grep -oF -- '-68.74%' attempt_1.prompt.txt | wc -l
1
```
即站内无"同一指标不同位置不同值"型数值矛盾。这些数值全部来自假说文本（被评估对象），本站没有任何独立数据可对其交叉验证。

## 发现清单

> 行号均指 `context_spread/full/event_card_interpreter.event_75be16e97675b582/attempt_1.prompt.txt`（下文简称 prompt.txt）。只列事实，不定级、不开方。

### 发现 1：同一份输出契约以三种形态重复出现，合计 2,019 字符 ≈ 全文 40.4%

**事实**：EventInterpretationCard 的 12 字段契约在站内出现三次：(a) Runtime Input 内嵌的 `output_contract` JSON 示例（带语义批注，L73-105）；(b) 独立段落 `## 输出字段规格`（字段名+必填/可选+类型，L113-125）；(c) `## Response Rules` 里的字段名清单行（L130）。三处合计 2,019 字符，占全文 4,993 字符的 40.4%。指令自己规定了冲突裁决顺序："形状……以上面「输出字段规格」为准；正文里的示例只解释语义，形状冲突时以规格为准"（L131）——即系统预期两处可能冲突。

**证据**：
```
$ python3（按标题切分并量三段长度）
output_contract block chars: 907
输出字段规格 section chars: 906
Response Rules 字段清单行 chars: 206
三处契约枚举合计: 2019 = 40.4 %
$ sed -n '130,131p' attempt_1.prompt.txt
- JSON 顶层字段必须匹配: event_id, fact_summary, interpretation, entities, event_type, mechanism_hypothesis, supports_hypotheses, refutes_hypotheses, limitations, needs_data_confirmation, upgrade_candidate, passport。
- 字段的**形状**（对象 / 数组 / 标量、是否可为 null）以上面「输出字段规格」为准；正文里的示例只解释语义，形状冲突时以规格为准。
```

### 发现 2：interpretation 措辞要求在三处互相不一致，裁决规则不覆盖措辞

**事实**：(a) User Message 铁律："**措辞完全由你决定，没有固定说法要套**……所以这里不设措辞检查"（L20）；(b) Runtime Input 的 output_contract 示例却给出固定开头说法：`"interpretation": "模型解读；弱来源以据报道或该媒体称开头"`（L76）；(c) 输出字段规格对 interpretation 只写"与事实分开的模型解读"（L116），无措辞要求。三处口径互不相同。L131 的裁决规则只管"形状"（对象/数组/标量），不管措辞；且 output_contract 嵌在 Runtime Input 的 JSON 数据里，是否算 L131 所说的"正文里的示例"，材料本身未界定。响应实际表现：interpretation 以"据该媒体报道"开头——跟随了 (b) 的固定开头，而非 (a) 的自由措辞。

**证据**：
```
$ sed -n '20p' attempt_1.prompt.txt
- 来源不是官方披露的、或材料只有标题没有正文的，解读措辞该降一档就降一档，`limitations` 里如实写清限制。**措辞完全由你决定，没有固定说法要套**——……所以这里不设措辞检查。……
$ sed -n '76p' attempt_1.prompt.txt
    "interpretation": "模型解读；弱来源以据报道或该媒体称开头",
$ sed -n '116p' attempt_1.prompt.txt
- `interpretation`（必填）：字符串 —— 与事实分开的模型解读
$ python3（读 attempt_1.parsed.normalized.json）
== interpretation 开头 60 字 ==
'据该媒体报道，Meta在最近一次财报电话会议后可能面临股价持续下跌压力。……'
```

### 发现 3：任务书声明材料为"标题、来源、日期、正文摘录"四件套，本实例正文摘录为空

**事实**：User Message 开篇声明"给你一条已经采集好的事件材料（标题、来源、日期、正文摘录）"（L14）；本实例 event_material 实际为 `"raw_text_available": false, "raw_text_excerpt": ""`（L38-39）——正文摘录是空字符串，四件套缺一件。铁律同时规定"fact_summary 里只许出现材料里逐字有的东西"（L17），即本实例 fact_summary 可逐字使用的材料只有标题一句。L20 预见了"材料只有标题没有正文"的情形（要求降档+limitations 写明），所以这是被预见的情形，但任务书开篇的材料组成声明与本实例现实不符。另注：唯一可逐字引用的"事实"是标题本身 `Meta stock poised for a new losing streak: Earnings call takeaways`（L28），其中 "poised for a new losing streak" 是媒体的前瞻判断而非中立事实陈述。

**证据**：
```
$ sed -n '14p' attempt_1.prompt.txt
你是外部世界材料层的解读员。给你一条已经采集好的事件材料（标题、来源、日期、正文摘录），你的任务是把它变成一张对投研有用的结构化卡片。
$ sed -n '38,39p' attempt_1.prompt.txt
    "raw_text_available": false,
    "raw_text_excerpt": "",
```

### 发现 4：System 级约束引用本站不存在之物——evidence_refs、raw_data、NO_DATA_AVAILABLE

**事实**：System 约束 4："所有 evidence_refs 必须来自本次输入的 raw_data"（L9）。但 (a) 本站输出契约 12 个字段里没有 evidence_refs 字段（L130 字段清单无此项）；(b) 本站输入 JSON 里没有 raw_data 键（输入顶层键为 event_material / allowed_financial_links / competing_hypotheses / output_contract / boundary）。约束 5 提到的 `NO_DATA_AVAILABLE`（L10）在本实例输入中也未出现。grep 全文：evidence_refs、raw_data、NO_DATA_AVAILABLE 各只出现 1 次，且都在约束条文自身里。即这三处约束在本站没有被引对象，属于通用模板条文。

**证据**：
```
$ grep -n 'evidence_refs\|raw_data\|NO_DATA_AVAILABLE' attempt_1.prompt.txt
9:4. 所有 evidence_refs 必须来自本次输入的 raw_data，不得凭记忆添加。
10:5. 若输入出现 `NO_DATA_AVAILABLE`，只能把它当作数据边界或置信度限制；不得围绕空数据补写数值、趋势、分位或原因。
$ python3（计数）
evidence_refs 出现在 prompt 中次数: 1
raw_data 出现在 prompt 中次数: 1
NO_DATA_AVAILABLE 出现在 prompt 中次数: 1
```

### 发现 5："弱来源"与 tier 判定标准在材料内无定义

**事实**：L20 的降档触发条件是"来源不是官方披露的"，本实例 tier=`reliable_mainstream_report`（L30），非官方披露，触发降档。但 L76 的固定开头针对的是"弱来源"——材料中没有任何位置定义 tier 的取值全集，也没有定义哪些 tier 算"弱来源"。reliable_mainstream_report 是否属于 L76 所说的"弱来源"，从本材料内无从判定。grep 显示 tier 在全文出现 4 处（L30 输入值、L76 示例批注、L100 passport 照抄、L125 规格），无任何取值集合或分级定义。

**证据**：
```
$ grep -n 'tier\|弱来源\|官方披露' attempt_1.prompt.txt
20:- 来源不是官方披露的、或材料只有标题没有正文的，解读措辞该降一档就降一档，……
30:    "tier": "reliable_mainstream_report",
76:    "interpretation": "模型解读；弱来源以据报道或该媒体称开头",
100:      "tier": "照抄 event_material.tier",
125:- `passport`（必填）：对象 EventInterpretationPassport{source, tier, published_at, event_date, effective_date} —— 来源与时间护照
```

### 发现 6：trigger_reasons 无消费方；event_type 是否照抄无指令

**事实**：输入含 `"trigger_reasons": ["mainline", "inquiry_reference"]`（L40-43），12 个输出字段无一消费它，任务书与规则也未提及该字段——它是只进不出的信息。另外，输入含 `"event_type": "mega_cap_market_news"`（L34），输出契约同名字段只写"事件类型"（L80、L118）——passport 的 5 个字段都明确写了"照抄 event_material.xxx"（L99-103），而 event_type 没有照抄指令，是照抄输入值还是自由生成，材料未规定。

**证据**：
```
$ grep -n 'trigger_reasons\|event_type' attempt_1.prompt.txt
34:    "event_type": "mega_cap_market_news",
40:    "trigger_reasons": [
80:    "event_type": "事件类型",
118:- `event_type`（必填）：字符串 —— 事件类型
130:- JSON 顶层字段必须匹配: event_id, fact_summary, interpretation, entities, event_type, ……
```
（trigger_reasons 仅在输入出现 1 次；输出字段清单 L130 无 trigger_reasons。）

### 发现 7："关联不足"退路只覆盖 interpretation，必填的 mechanism_hypothesis 无退路

**事实**：User Message 给了诚实退路："与纳指 100 没有可说明关联的事件，诚实输出 `interpretation: 与判断对象关联不足`，不要硬找联系"（L21）。但输出字段规格中 `mechanism_hypothesis` 为**必填**（L119），必须从九个渠道选一并写一句机制假设；supports/refutes/limitations/needs_data_confirmation 均为可选。即：即便判定关联不足，结构上仍必须产出一条金融传导机制假设——退路只覆盖 interpretation 一个字段。本实例输入恰是"仅标题的单股（META）事件"，而三条 competing_hypotheses 全是宏观层面假说（实际利率/估值/盈利修正/回购，L56-72），尺度不匹配。响应实际表现：mechanism_hypothesis 填了 earnings_path 并写了机制句，supports=['hyp_base_88ece56ecb','cth_02']、refutes=['cth_01']——即把一条仅标题单股事件挂上了宏观假说。

**证据**：
```
$ grep -n '关联不足' attempt_1.prompt.txt
21:- 与纳指 100 没有可说明关联的事件，诚实输出 `interpretation: 与判断对象关联不足`，不要硬找联系。
$ sed -n '119p' attempt_1.prompt.txt
- `mechanism_hypothesis`（必填）：对象 EventMechanismHypothesis{financial_link:"earnings_path"|"valuation_multiple"|……, hypothesis} —— 金融链路与一句机制假设
$ python3（读 attempt_1.parsed.normalized.json）
== mechanism_hypothesis ==
{"financial_link": "earnings_path", "hypothesis": "该报道可能表明Meta财报电话会议释放的信号被市场解读为偏负面，若Meta盈利预期因此下修，可能通过盈利预期下行路径拖累其股价，并因其在纳指100中的较大权重对指数产生压力。"}
== supports/refutes == ['hyp_base_88ece56ecb', 'cth_02'] ['cth_01']
```

## 盲区

- **payload.json 未全读**：只读了开头 600 字符（确认其 event_material 与 prompt 内 Runtime Input 一致），未逐字节比对 payload 全文与 prompt 的完全一致性。若 payload 中段有 prompt 里看不到的额外字段，我没有发现。
- **response.raw.txt 未逐字读**：消费方式取证走的是 `attempt_1.parsed.normalized.json`（解析归一化后的响应，已全读）。原始响应文本（1,210 字符）未逐字读，raw 与 parsed 之间若有归一化改动，我看不到。manifest 显示 output.validated.json 与 parsed.normalized.json 同 sha256。
- **projection_verification.md 未读**：context_spread 根目录下的投影验证总文件（4,999 字符）未读；本站投影保真度是我自行对照原文验证的（唯一截断处已回查，实测省略 101 字符 vs 标记 100 字符，差 1）。
- **其他站未看**：按纪律只看本站。event_card_interpreter 家族另有 9 个同类站，发现 2/3/4/5/6 是否为家族性/全系统性模式，本站视角无法判断。
- **外部事实未核验**：标题所指的 Meta 财报电话会议是否真实发生、tier 分级体系在代码里的实际定义（如 src/ 中的 tier 枚举与"弱来源"判定），按只读语料、不扩范围的纪律未去核 src/ 代码。发现 5 只陈述"材料内无定义"，不涉及代码里是否有定义。
- **"正文里的示例"指代不明是我的判读**：发现 1/2 中，L131"正文里的示例只解释语义"是否涵盖嵌在 Runtime Input JSON 里的 output_contract，材料未界定；我把两种读法都列为事实，未做选择。
- ** Runtime Input 内部细分的字符量为约数**：总览中 event_material≈480、competing_hypotheses≈1,050 等是按键位置估算的约数（±几十字符），未做精确切分；精确数只有六段切分（与段落目录完全一致）和契约三处（2,019 字符）。
