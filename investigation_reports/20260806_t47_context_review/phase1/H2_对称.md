# T47 阶段一 · H2 对称片：正反对称与审查独立性

- 语料根：`/Users/aidianchi/Desktop/ndx_mac/output/analysis/vnext/20260731_002156/context_spread/`（下称 `$CS`）
- 比对对象：`full/{thesis,counter_thesis,critic,risk,reviser,final_adjudicator}/attempt_1.*`
- 口径说明：
  - **逐字节相同**：对 JSON 块先 `json.dumps(..., ensure_ascii=False, sort_keys=True)` 规范化再取 sha256，或直接对原文区间取 sha256。两者均给出可复跑命令。
  - **字符数**：均指 `json.dumps(value, ensure_ascii=False)` 的字符长度（近似等于 prompt 中展开长度，键序差异不影响内容比对）。
  - 每个站 prompt.txt 结构为：系统/任务散文（头部）+ `## Runtime Input`（内嵌 JSON）+ 输出契约尾部。材料差异主要以 payload.json 为准，prompt.txt 用于定位散文层差异。

## 1. critic ↔ risk 输入同一性核验

**结论：两站的 Runtime Input（governance_input）逐字节相同；payload.json 仅 stage_key/stage_name 两个字段值不同；prompt.txt 的差异只在头部任务散文（角色/输出格式/检查清单）和尾部输出契约，材料区零差异。**

证据 1.1 — 全文哈希（不同）：

```
$ cd $CS/full && shasum -a 256 critic/attempt_1.prompt.txt risk/attempt_1.prompt.txt critic/attempt_1.payload.json risk/attempt_1.payload.json
40ab99a874ffcf5c...  critic/attempt_1.prompt.txt
98694cd352e777a9...  risk/attempt_1.prompt.txt
0b73fe7debc987be...  critic/attempt_1.payload.json
9140669cf0313424...  risk/attempt_1.payload.json
```

证据 1.2 — payload.json 逐键比对（仅 stage 名不同）：

```
$ python3 - <<'EOF'   # 在 $CS/full 下
import json
cp=json.load(open('critic/attempt_1.payload.json')); rp=json.load(open('risk/attempt_1.payload.json'))
for k in set(cp)|set(rp):
    a=json.dumps(cp.get(k),ensure_ascii=False,sort_keys=True); b=json.dumps(rp.get(k),ensure_ascii=False,sort_keys=True)
    print(('DIFF key: ' if a!=b else 'same: ')+k, ('| critic='+a[:60]+' | risk='+b[:60]) if a!=b else '')
EOF
DIFF key: stage_key | critic= "critic" | risk= "risk"
DIFF key: stage_name | critic= "critic" | risk= "risk"
same: attempt / same: payload / same: retry_feedback
```

证据 1.3 — governance_input 规范化 sha256（相同）：

```
$ python3 -c "
import json,hashlib
ci=json.load(open('critic/attempt_1.payload.json'))['payload']['governance_input']
ri=json.load(open('risk/attempt_1.payload.json'))['payload']['governance_input']
h=lambda o: hashlib.sha256(json.dumps(o,ensure_ascii=False,sort_keys=True).encode()).hexdigest()
print(h(ci)); print(h(ri)); print('identical:', h(ci)==h(ri))"
de78947d080ebeec100f29c37a8e296f98a9bc2a7bcd548c2c7056c9c0fc6dba
de78947d080ebeec100f29c37a8e296f98a9bc2a7bcd548c2c7056c9c0fc6dba
identical: True
```

证据 1.4 — prompt.txt 差异定位（diff 只在头部散文与尾部契约）：

```
$ cd $CS/full && diff critic/attempt_1.prompt.txt risk/attempt_1.prompt.txt | grep -E '^[0-9]'
14c14 / 18c18 / 20c20 / 23c23 / 25c25,28 / 27,29d29 / 35,37c35,37 / 40c40,41 / 42,45c43,46
51,52c52 ... 222,225c237,251        ← critic 行 14–225：角色定义/输入说明/输出格式/检查清单散文
6512c6538 / 6514,6517c6540,6546 / 6522c6551   ← 尾部输出契约（Critique vs RiskSentinel 字段规格）
$ wc -l critic/attempt_1.prompt.txt risk/attempt_1.prompt.txt
6524 critic / 6553 risk
```

头部差异的典型行（照抄）：
- critic 行18：`你是 **Critic**，负责攻击 Thesis Draft 的逻辑漏洞。`
- risk 行18：`你是 **Risk Sentinel**，负责监控失效条件、风险边界和双向 trade-off。`
- risk 独有统计约束段（行25-28）：`【统计约束】不得编造历史胜率、回测收益、样本区间或概率数字...`
- critic 独有证据纪律段（行27-29）：`【证据纪律】攻击论点时引用具体 evidence_refs 中的数据...`

证据 1.5 — Runtime Input 段原文区间逐字节相同（从 `## Runtime Input` 行起各取 6252 行，覆盖整个内嵌 JSON）：

```
$ c=230; r=256   # 两站 "## Runtime Input" 起始行（grep -n 所得）
$ tail -n +$c critic/attempt_1.prompt.txt | head -n 6252 > /tmp/t47_c.txt
$ tail -n +$r risk/attempt_1.prompt.txt   | head -n 6252 > /tmp/t47_r.txt
$ shasum -a 256 /tmp/t47_c.txt /tmp/t47_r.txt
b4b8f493f032a8410e97a0be97fb3f189f3aab7afad9287f70b4647123ef8f0d  /tmp/t47_c.txt
b4b8f493f032a8410e97a0be97fb3f189f3aab7afad9287f70b4647123ef8f0d  /tmp/t47_r.txt
```

即：critic 行 230–6481 == risk 行 256–6507，逐字节相同；critic 行 6482 起为各自的输出字段规格（diff 佐证：critic 尾 `JSON 顶层字段必须匹配: generated_at, overall_assessment, issues, cross_layer_issues, revision_direction。` vs risk 尾 `... failure_conditions, boundary_status, must_preserve_risks, conflict_matrix_check, opportunity_costs, confirmation_costs, false_safety_risks。`）。

**相同的部分**：全部输入材料（thesis 摘要字段 22 项 + high_severity_typed_conflicts/principal_contradictions/objective_firewall_summary + key_evidence_refs(30键,116150字符) + evidence_registry_summary + pricing_expectation_ledger + known_data_gaps + unresolved_questions + synthesis_guidance 等，governance_input 共 40 键全部相同）。
**不同的部分**：角色定义与任务散文（审查视角：挑刺 vs 风险预警）、输出契约（Critique vs RiskSentinel schema）、以及散文中各自的纪律段（critic 有【证据纪律】，risk 有【统计约束】）。

## 2. thesis ↔ counter_thesis 证据索引逐字节比对

**结论：两站的 evidence_index 在 prompt 原文区间逐字节相同（174050 字节，sha256 一致，diff 零输出）。**

```
$ cd $CS/full
$ sed -n '1527,5341p' thesis/attempt_1.prompt.txt > /tmp/t47_t_ei.txt     # thesis: `"evidence_index": {` 起、下一顶层键前止
$ sed -n '1092,4906p' counter_thesis/attempt_1.prompt.txt > /tmp/t47_c_ei.txt
$ wc -c /tmp/t47_t_ei.txt /tmp/t47_c_ei.txt
174050 /tmp/t47_t_ei.txt
174050 /tmp/t47_c_ei.txt
$ shasum -a 256 /tmp/t47_t_ei.txt /tmp/t47_c_ei.txt
03b06408623ce955de825e6740e7bb1abe00bdf4afc9dc8510df9cd903203585  (两文件相同)
$ diff /tmp/t47_t_ei.txt /tmp/t47_c_ei.txt ; echo exit=$?
exit=0
```

载荷层复核（canonical JSON sha，两站 evidence_index 均为 `d01d5b6576cc2748`）与键数复核（各 102 键，见第 4 节）一致。

## 3. thesis ↔ counter_thesis 键级对账（payload 层）

**结论：共享 11 个块中 10 个逐字节相同；唯一内容差异块是 packet_meta（object_run_gate 子键不同）；thesis 独有 3 个块（competing_hypotheses / hypothesis_competition_summary / adjudication_history，合计约 5331 字符）；counter_thesis 在材料包之外另有 6 个顶层材料键（合计约 22856 字符）。**

命令（在 `$CS/full` 下）：

```
$ python3 - <<'EOF'
import json, hashlib
t=json.load(open('thesis/attempt_1.payload.json'))['payload']
c=json.load(open('counter_thesis/attempt_1.payload.json'))['payload']
ts=t['synthesis_packet']; cs=c['synthesis_packet_without_self_reference']
h=lambda v: hashlib.sha256(json.dumps(v,ensure_ascii=False,sort_keys=True).encode()).hexdigest()[:16]
for k in sorted(set(ts)|set(cs)): ...   # 逐键 len + sha 比对
EOF
```

输出（照抄）：

```
key                                        thesis  counter byte_same
adjudication_history                            2        0 thesis_only
bridge_summaries                            17335    17335 True
competing_hypotheses                         4326        0 thesis_only
context_summary                                99       99 True
evidence_index                             103322   103322 True
generated_at                                   29       29 True
high_severity_conflicts                      1516     1516 True
high_severity_typed_conflicts                2095    2095 True
hypothesis_competition_summary               1003        0 thesis_only
layer_summaries                              9704    9704 True
objective_firewall_summary                    683     683 True
packet_meta                                   801    1385 False
principal_contradictions                     1845    1845 True
synthesis_guidance                            827     827 True

counter payload extra top-level keys:
  allowed_evidence_refs                        3814
  bridge_v1_structure                         15518
  bridge_v2_feedback_summary                    833
  forbidden_context_refs                        73
  non_stub_investigation_reports               2598
  output_contract                                20
```

packet_meta 差异定位：`object_run_gate` 子键不同——thesis 版含 `prompt_note`（"Full object boundary is stored in analysis_packet meta; use this only as object scope."），counter 版含 `object_scope` / `methodology_boundary` / `data_boundary` / `evidence_boundary` / `backtest_date` 五个展开字段；两版其余 packet_meta 字段（version/generated_at/data_date/collector_timestamp/indicator 计数等）逐字节相同。

thesis 独有块的任务书理由（thesis prompt 行101，照抄关键句）：
> "`synthesis_packet.competing_hypotheses` 里除 `status` 为 `downgraded`（已被裁决出局）之外的每一个假说……你都必须在 `hypothesis_responses` 里逐一回应……"

即 competing_hypotheses / hypothesis_competition_summary 是 thesis 必须逐一回应的输入；counter_thesis 的材料包键名本身就标注为 `synthesis_packet_without_self_reference`。adjudication_history 为空数组（`[]`，2 字符），属名义差异。

counter 独有顶层键的任务书理由（counter_thesis prompt 行17-19 与"输入边界"段，照抄）：
> "你是 Counter-Thesis Builder。你的任务是在 Thesis 生成之前，基于允许输入提出 1-2 个真正有区分力的反方假说。"
> "你只能读取：`synthesis_packet_without_self_reference`、`bridge_v1_structure`、`bridge_v2_feedback_summary`、`non_stub_investigation_reports`、`allowed_evidence_refs`。你禁止读取或引用：`thesis_draft.json`、`analysis_revised.json`、`final_adjudication.json`。"

载荷层复核：`forbidden_context_refs` 实际值 = `['thesis_draft.json', 'analysis_revised.json', 'final_adjudication.json']`（73 字符，与任务书一致）；`allowed_evidence_refs` 为 102 条白名单，与 evidence_index 键集合完全相等（见第 4 节）。

## 4. 证据窗口集合关系（critic/risk vs thesis 引用集）

**结论：critic/risk 的证据窗口 key_evidence_refs 共 30 键，是 thesis evidence_index（102 键）的真子集，也是 thesis 产出中实际引用过的 L-ref 集合（33 条）的真子集；thesis 引用过但未进入审查窗口的有 3 条，全部位于 price_reflection_map 的 counterevidence_refs（反证位）。**

命令与输出（`$F=$CS/full`）：

```
$ python3 - <<'EOF'
import json,re
F='.../context_spread/full'
ci=json.load(open(F+'/critic/attempt_1.payload.json'))['payload']['governance_input']
ker=set(ci['key_evidence_refs'].keys())
ei=set(json.load(open(F+'/thesis/attempt_1.payload.json'))['payload']['synthesis_packet']['evidence_index'].keys())
out=open(F+'/thesis/output.validated.json').read()
refs=set(re.findall(r'L[1-5]\.[A-Za-z0-9_]+(?:#[A-Za-z0-9_]+)?', out))
print(len(ker), len(ei), len(refs), ker<=ei, ker<=refs)
EOF
critic key_evidence_refs keys: 30 | risk == critic: True
evidence_index keys: 102
ker ⊆ ei: True | ker-ei: [] | ei-ker: 72
thesis output L-refs: 33
refs - ei (引了但索引没有): []            ← thesis 引用全部合法（⊆ evidence_index）
ker ⊆ thesis refs: True
ker - thesis refs: []                     ← 审查窗口 30 条 thesis 全部引用过
thesis refs - ker: 3 ['L1.get_fed_funds_rate_path', 'L2.get_ig_oas_bp', 'L2.get_vix_term_structure']
```

3 条未进审查窗口的引用定位（照抄）：

```
.price_reflection_map[2].counterevidence_refs[1] -> L2.get_ig_oas_bp
.price_reflection_map[3].counterevidence_refs[2] -> L2.get_vix_term_structure
.price_reflection_map[4].counterevidence_refs[0] -> L1.get_fed_funds_rate_path
```

口径声明：thesis 引用集是用正则 `L[1-5]\.[A-Za-z0-9_]+(#...)?` 从 thesis 产出 JSON 全文提取的（全量，非抽样）；key_support_chains 内 evidence_refs 字段单独核验为 16 条，全部 ⊆ 30 键窗口。critic 与 risk 的 key_evidence_refs 键集合完全相等（第 1 节 governance_input 整体相同的推论）。

另：counter_thesis 的 allowed_evidence_refs（102 条）与 evidence_index 键集合**完全相等**（全量比对，`allowed - evidence_index = []`，`evidence_index - allowed = []`），即反方证据白名单 = 正方证据索引全集，无子集锚定。

## 5. 修订稿下游可见性（reviser → final_adjudicator）

**结论：final_adjudicator 未收到修订稿全文。它收到的是 19 个 thesis_* 平铺字段 + 一段 126 字符的 revision_summary + critic/risk 的部分产出字段；修订稿中唯一真正改动的 retained_conflicts（原稿 4 条 1565 字符 → 修订稿 3 条 946 字符）全文未进入终审材料，终审只能看到 3 个 conflict_type 名（93 字符）。**

证据 5.1 — 修订稿 vs 原稿逐字段比对（reviser.revised_thesis 的 19 个业务字段与原稿逐字节相同，唯一差异字段是 retained_conflicts）：

```
$ python3 比对 reviser/output.validated.json.revised_thesis vs thesis/output.validated.json（逐字段 canonical json）
environment_assessment  rev==th   ...（19 字段全部 rev==th，fa==rev）
retained_conflicts: rev==th? False
  thesis.retained_conflicts chars: 1565   (4 条)
  reviser.retained_conflicts chars: 946   (3 条)
thesis retained_conflicts types:   ['rate_vs_valuation','credit_tail_vs_earnings_momentum','technical_bounce_vs_weak_breadth','growth_assumption_vs_rate_headwind']
reviser retained_conflicts types:  ['rate_vs_valuation','credit_tail_vs_earnings_momentum','technical_vs_fundamental_breadth']
```

证据 5.2 — 终审材料中修订痕迹的可见范围：

```
fa.revision_summary == reviser.revision_summary: True（126 字符，全文照抄见下）
fa.retained_conflict_types: ['rate_vs_valuation','credit_tail_vs_earnings_momentum','technical_vs_fundamental_breadth']
fa.retained_conflict_types == [reviser retained types]: True   ← 类型名单反映的是修订后的 3 条
reviser retained_conflicts(946字) 出现在 fa governance_input: False
reviser remaining_conflicts / accepted_critiques / rejected_critiques 出现在 fa governance_input: 均 False
fa prompt 全文含 'retained_conflicts': False
fa prompt 全文含 'why_retained': True —— 仅出现在 revision_summary 字符串内部（fa prompt 行6689）
```

revision_summary 全文（照抄）："根据Critic反馈，原分析主体框架严谨、跨层一致，未发现重大逻辑断裂。本次修订整合Risk Sentinel的全部风险警示，确保所有high severity冲突在最终报告中显式保留并添加why_retained解释。未对主体论点做出方向性调整。"

附带事实（只报线索）：revision_summary 声称"添加why_retained解释"，但 reviser 的 revised_thesis.retained_conflicts 三个对象均只有 `conflict_id/conflict_type/severity/description/implication/involved_layers` 六个键，**没有 why_retained 字段**；reviser 输出全文中 "why_retained" 仅出现于 revision_summary 字符串自身。

证据 5.3 — critic/risk 产出进入终审的范围（fa governance_input 字段与两站 output.validated.json 比对）：

```
critic.overall_assessment   == fa.critique_overall:            True
critic.cross_layer_issues   == fa.critique_cross_layer_issues: True（均为 []）
critic.issues（3 条具体攻击，含 target/severity/suggestion）出现在 fa: False
  —— fa prompt 全文搜索 critic.issues[0].issue 前40字: False
risk.must_preserve_risks    == fa.must_preserve_risks:    True
risk.opportunity_costs      == fa.opportunity_costs:      True
risk.confirmation_costs     == fa.confirmation_costs:     True（同结构比对，见 reviser 输入 453/497/318 字符键）
risk.false_safety_risks     == fa.false_safety_risks:     True
risk.failure_conditions（968字符）出现在 fa: False
risk.boundary_status        出现在 fa: False
risk.conflict_matrix_check  出现在 fa: False
```

即：critic 的 3 条具体 issues 和 risk 的 failure_conditions / boundary_status / conflict_matrix_check 均未进入终审材料；终审拿到的是 critic 的总体评估一句话 + risk 的四个风险清单字段。

注：reviser 与 final_adjudicator 的 governance_input 结构完全相同（40 键），差异仅在 must_preserve_risks/opportunity_costs/confirmation_costs/false_safety_risks（reviser 站已填入 risk 产出）、critique_overall（填入 critic 产出）、retained_conflict_types（4 类型 → 3 类型）与 revision_summary（null → 126 字符）。

## 6. 反方原文下游可见性（counter_thesis → thesis/critic/risk/reviser/final_adjudicator）

**结论：counter_thesis 的 2 条反方假说原文（cth_01 280 字符、cth_02 206 字符）全文只进入 thesis 一站（经 competing_hypotheses 块注入，逐字节相同）；critic/risk/reviser/final_adjudicator 均拿不到反方原文，只能通过 thesis_hypothesis_responses 看到 thesis 对 cth_01/cth_02 的回应（二手）。**

证据 6.1 — counter 产出注入 thesis：

```
$ python3 比对 counter_thesis/output.validated.json vs thesis payload competing_hypotheses
thesis competing_hypotheses 条目数: 3
  hyp_base_88ece56ecb | status: leading    ← 主线假说
  cth_01 | status: candidate
  cth_02 | status: candidate
counter h1 全文出现在 thesis competing_hypotheses: True
counter h2 全文出现在 thesis competing_hypotheses: True
thesis hypothesis_responses: [('hyp_base_88ece56ecb',...), ('cth_01',...), ('cth_02',...)]  ← thesis 逐一回应了 3 条
```

（与第 3 节互证：competing_hypotheses / hypothesis_competition_summary 是 thesis 独有块，counter 自己不回收这些键。）

证据 6.2 — 下游四站全文检索（prompt + payload 双检索）：

```
$ python3 对 critic/risk/reviser/final_adjudicator/thesis 的 attempt_1.prompt.txt 与 attempt_1.payload.json 检索 cth_01 及 h1 原文前40字
critic/risk/reviser/final_adjudicator:
  prompt 含 cth_01（假说ID）: True
  prompt/payload 含 h1 原文前40字: False / False
thesis:
  prompt/payload 含 h1 原文前40字: True / True
cth_01 在 critic governance_input 中出现位置: 仅 thesis_hypothesis_responses 键
```

定位照抄：critic prompt 行310 为 `"hypothesis_id": "cth_01",`，位于内嵌 governance_input 的 thesis_hypothesis_responses 数组内。

即：反方假说对下游审查/裁决站不可见一手原文；它们能看到的是"thesis 回应了 cth_01/cth_02"这件事及 thesis 的回应理由。

## 7. thesis ↔ counter_thesis 对称性对账总表

| 键/块 | 正方字符 | 反方字符 | 逐字节相同 | 证据 |
|---|---:|---:|---|---|
| evidence_index | 103322 | 103322 | 是（prompt 原文区间 174050B 同 sha） | §2 |
| layer_summaries | 9704 | 9704 | 是 | §3 |
| bridge_summaries | 17335 | 17335 | 是 | §3 |
| high_severity_conflicts | 1516 | 1516 | 是 | §3 |
| high_severity_typed_conflicts | 2095 | 2095 | 是 | §3 |
| principal_contradictions | 1845 | 1845 | 是 | §3 |
| objective_firewall_summary | 683 | 683 | 是 | §3 |
| context_summary | 99 | 99 | 是 | §3 |
| synthesis_guidance | 827 | 827 | 是 | §3 |
| generated_at | 29 | 29 | 是 | §3 |
| packet_meta | 801 | 1385 | 否（object_run_gate 子键不同） | §3 |
| competing_hypotheses | 4326 | 0（缺席） | 正方独有（含 counter 产出的 cth_01/cth_02 全文回注） | §3、§6 |
| hypothesis_competition_summary | 1003 | 0（缺席） | 正方独有 | §3 |
| adjudication_history | 2 | 0（缺席） | 正方独有（空数组，名义差异） | §3 |
| bridge_v1_structure | 0 | 15518 | 反方独有 | §3 |
| bridge_v2_feedback_summary | 0 | 833 | 反方独有 | §3 |
| non_stub_investigation_reports | 0 | 2598 | 反方独有 | §3 |
| allowed_evidence_refs | 0 | 3814 | 反方独有（102 条 = evidence_index 键全集） | §3、§4 |
| forbidden_context_refs | 0 | 73 | 反方独有（禁读 thesis_draft 等 3 件） | §3 |
| output_contract | 0 | 20 | 反方独有（"CounterThesisDraft"） | §3 |

材料包共享部分合计约 137156 字符（不含 packet_meta），其中逐字节相同的约 136355 字符；正方独有约 5331 字符；反方包外独有约 22856 字符。

## 8. 盲区与未覆盖

1. **比对均在 full/ 原文上做**（payload.json 逐键 + prompt.txt 原文区间），未依赖投影；投影截断对本片结论无影响。未核对 projected/ 与 full/ 的一致性本身（超出本片范围）。
2. **集合关系为全量核验，非抽样**：evidence_index 键集（102）、allowed_evidence_refs（102）、key_evidence_refs 键集（30）均为精确集合运算。唯一例外是"thesis 实际引用集"——用正则 `L[1-5]\.[A-Za-z0-9_]+(#...)?` 从产出 JSON 全文提取，可能漏掉不以 `L数字.` 开头的引用形态（如 bridge memo 路径、event_refs），故"审查窗口 ⊆ thesis 引用集"对 L 系证据是全量结论，对非 L 系引用未覆盖。
3. packet_meta 差异定位为人工逐键查看（两版各 800/1385 字符，全文已目检：差异集中在 object_run_gate 五个子键），未做字符级 diff 输出存档。
4. "critic.issues / risk.failure_conditions 未进终审"的核验方式：issues 三条各取前 40 字 + 数组整体 JSON 在 fa prompt/payload 全文检索（均 False）；failure_conditions/boundary_status/conflict_matrix_check 以整体 JSON 检索（均 False）。理论上存在"改写后部分内容进入"的可能，未做语义级比对。
5. 修订稿可见性结论基于字段级 canonical JSON 比对（19 个业务字段 + retained_conflicts）；reviser 的 remaining_conflicts（3 条，与 retained_conflicts 同型）是否以改写形式进入 fa 未做语义级核验（原文检索为 False）。
6. 单 run（20260731_002156）、单 attempt_1；无跨 run/跨 attempt 结论。
7. 本片只覆盖任务书指定的 6 站；bridge、controlled_investigation、integrated_adjudicator 与这 6 站之间的材料流动未核验。
