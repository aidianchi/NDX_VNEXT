# L4 通读报告

## 通读范围

- 语料根目录：`/Users/aidianchi/Desktop/ndx_mac/output/analysis/vnext/20260731_002156/context_spread/`
- 通读主材料：`projected/L4/attempt_1.projection.md`（559,123 字节 / 13,978 行；manifest 记投影字符 532,819，占原文 75.0%）
- 原文备查：`full/L4/attempt_1.prompt.txt`（733,887 字节 / 19,459 行；manifest 记 710,404 字符）
- 辅助结构分析：`full/L4/attempt_1.payload.json`（682,805 字节，运行时输入的结构化形态）、`full/L4/meta.json`
- manifest 记录：`inspector_has_rules: true`、`inspector_can_read: true`、`verification_ok: true`（命令：`python3 -c` 读 `manifest.json` 取 `stage=="L4"` 条目）
- 读法（按任务拆分规则执行，已全部完成）：
  - 散文/规则区（投影行 44–1401，约 1,360 行，含 System Message / Canon / 契约 / 指标清单 / 人设与纪律）：**逐字全读**。
  - Runtime Input 数据区（投影行 1402–13958，约 12,557 行 / 645,323 字符，占 90.8%）：**结构 + 边界 + 抽查**。做法：把原文行 1344–19439 的内嵌 JSON 完整 `json.loads`（可解析），逐键统计体量；11 个指标块的投影边界全部定位（投影行 1650/1853/1927/2239/2306/2553/8931/9031/11096/11530/13703 起）；聚合标量、data_quality、coverage、MetricAuthority 等键经 python 全量比对；逐成分/逐季度行级明细按"首末行+聚合复算"抽查（抽查口径见各发现与第 23 条）。
  - 输出规格与 Response Rules（投影行 13959–13978）：**逐字全读**。
  - 投影截断核对：24 处散文省略标记抽查 3 处、12 处表投影抽查 1 处，均与原文精确吻合（细节见发现 23）。
- 本报告边查边写，各节完成后立即落盘。

## 材料结构总览

投影头部段落目录（投影行 6–41，命令 `sed -n '6,41p'` 已核对）给出的分布：

- 任务书/人设/规则类（散文为主，逐字保留）：
  - `# System-Level Constraints` 333 字符 + `## Deep Research Canon` 下 `### ObjectCanon` 767 字符、`### IndicatorCanon for L4` 11,451 字符（1.6%）、`### Canon Output Hints` 231 字符
  - `## Layer-Local 4C Few-Shot Examples` 三个例子合计约 3,594 字符
  - `## vNext v2 Context-Bounded Professional Layer Contract` 本体/字段/隔离/单位纪律合计约 1,479 字符 + `### 当前层指标清单` 33,797 字符（4.8%）+ `### 结构示例` 904 字符
  - `# L4 Context-Bounded Valuation Analyst` 人设区：`Context Boundary` 227、`Professional Lens` 188、`Cognitive Transform` 237、`Indicator Semantics` 1,737、`L4 数据源优先级与新鲜度` 619、`Data Authority Discipline` 4,643（0.7%）、`Mechanism Grammar` 1,381、`Layer Synthesis` 234、`Internal Conflict Analysis` 285、`Cross-Layer Hooks` 310、`UI Quality Requirements` 218、`Output Discipline` 223
  - `## 输出字段规格` 1,435 字符 + `## Response Rules` 365 字符
- 数据类：`## Runtime Input` 645,323 字符（90.8%）——一站独大。

规则/任务书合计约 6.2 万字符（≈8.8%），数据区 64.5 万字符（90.8%）。结构上的问题高度可能集中在 Runtime Input 的构成与规则区对它的引用方式是否匹配。

（以下发现清单随通读推进逐条追加。）

## 发现清单

> 约定：行号 `投影:NNNN` 指 `projected/L4/attempt_1.projection.md`，`原文:NNNN` 指 `full/L4/attempt_1.prompt.txt`。所有 python 核对命令均以 `full/L4/attempt_1.prompt.txt` 行 1344–19439 的 Runtime Input JSON 为对象（`json.loads` 可完整解析，已验证）。

### 1. Runtime Input 里同一份"核心信号摘要"以三种形态重复出现

- 证据：
  - `投影:1407-1413`（`context_brief.layer_highlights.L4`，3 条摘要字符串）与 `投影:1437/1452/1518`（`layer_facts.core_signals[].summary`）逐字相同；`原文:1588` 的 `layer_facts.summary` 又把同样内容第三次拼接（"估值状态: expensive。关键事实: Damodaran US Implied ERP Reference | 值=…"）。
  - 命令：`python3` 比较 `[s['summary'] for s in layer_facts.core_signals]` 与 `context_brief.layer_highlights.L4` → 输出 `highlights verbatim == core_signals.summary: 3 of 3`。
- 事实：3 条 highlights 与 core_signals 里对应 summary 字符串完全逐字重复；`layer_facts.summary`（原文:1588）是同一批字符串的再次拼接。同一信息在 Runtime Input 内出现三次。

### 2. 规则区"当前层指标清单"（33,797 字符）与 Runtime Input 内各指标 `data_quality` 大块逐字重复

- 证据：
  - 清单位于 `投影:455-1201`；每个指标块内含完整 `data_quality`（如 `投影:465-549` 的 Wind snapshot data_quality）；同一 data_quality 又出现在 `layer_raw_data.<fid>.data_quality`（如 Wind snapshot 数据块 `投影:1650-1852` 内）。
  - 命令：`python3` 逐指标比较清单条 `data_quality` 与 `layer_raw_data[fid]['data_quality']`（sort_keys 后比 JSON）→ 输出 11 行，其中 9 行 `data_quality identical=True`；仅 `get_ndx_earnings_revision_metrics`、`get_m7_earnings_blackout_calendar` 两条 `False`，差异为清单把大对象换成占位（见下条）。
- 事实：11 个指标中 9 个的 `data_quality`（含 source_tier、日期、coverage、formula、anomalies、metric_authority 等）在提示词里出现两次、逐字一致；清单一节 33,797 字符（4.8%）的主体是这次重复的载体。

### 3. 清单与原始块不一致的两处差异形态：清单侧是"已省略"占位，数据侧是全量

- 证据：
  - 命令同上的 diff 输出：`get_ndx_earnings_revision_metrics` 差异键为 `metric_authority`，清单值=`{"keys": [...], "summary": "large object omitted from indicator manifest; full detail remains in analysis_packet artifact"}`，raw 值为完整 5 键对象；`get_m7_earnings_blackout_calendar` 差异键为 `endpoint_metadata`，形态相同。占位串亦见 `投影:820,1029`。
- 事实：清单对这两个大对象给了"完整细节在 analysis_packet artifact"的占位说明，而完整细节实际就在同一提示词的 Runtime Input 里（不是外部 artifact）。同一字段两种详略形态并存。

### 4. `get_ndx_earnings_revision_metrics` 一个指标占 Runtime Input 一半，主体是逐成分股明细

- 证据：
  - 命令：`python3` 对 `layer_raw_data` 逐键 `len(json.dumps(...))` → `get_ndx_earnings_revision_metrics: 198,592 (51.6%)`（紧凑序列化口径；同口径 layer_raw_data 合计 384,548 字符）。
  - 细分输出：`value.slope_30d.constituents` 97 条 49,436 字符（`原文:2973` 起）、`value.slope_90d.constituents` 87 条 44,499（`原文:5501` 起）、`value.divergence.7d.constituents` 99 条 16,305、`value.breadth_30d.periods.0y/+1y.constituents` 各 103 条（`原文:7469/8208` 起）、`value.dispersion_ntm.constituents` 103 条 16,948、`value.analyst_coverage.constituents` 103 条 6,955。
  - 单条成分记录形态（命令输出照抄）：`{"ticker": "NVDA", "weight_pct": 8.288274, "slope": 0.0070594609, "slope_raw": ..., "winsorized": false, "fy1_revision": ..., "fy2_revision": ..., "fy1_weight": ..., "fiscal_weight_method": "fiscal_calendar", "fiscal_year_end": "2027-01-25", "material": "supplier_lookback", "target_anchor_date": ..., "actual_anchor_date": ..., "anchor_offset_days": 0, "flagged": false, "flag_reasons": [], "earnings_dates_in_flag_window": []}`。
- 事实：该指标在投影中占行 2553–8930（6,378 行，约为投影全文 45%）；其中逐成分明细合计约 15.5 万字符。canon 对该指标的消费要求是读"修正方向/广度/覆盖率/两轨分歧"等聚合量（`投影:222-251`），逐成分明细是否预期被逐条消费，指令无明示。

### 5. 同一指标内 `flagged` 列表逐字双份

- 证据：
  - 命令：`erm['flagged']['30d'] == erm['value']['slope_30d']['flagged']` → `True`；`90d` → `True`。值各 6,976 / 6,934 字符。`flagged_weight_pct` 顶层 `{'30d': 40.980741, '90d': 51.609783'}` 与 value 内两窗各自数值一致。
  - 位置：`value.slope_30d.flagged` 于 `原文:2518`、`value.slope_90d.flagged` 于 `原文:5051`、顶层 `"flagged": {` 于 `原文:10360`。
- 事实：42 条 flagged 明细在 30d、90d 两处各出现一次后，又在指标顶层原样再出一次；同一提示词内每个 flagged 成分记录实际出现两次（value 内 + 顶层）。

### 6. `get_m7_buyback_flow` 同一季度序列以两种形态重复

- 证据：
  - `value.per_company.<T>.quarters`（每公司 12 季，含 period_start/filed_date/derivation/yoy_pct 等，AAPL 5,048 字符）与 `value.raw_quarterly_series.<T>`（每公司 12 季，字段子集，AAPL 2,580 字符；`原文:18403` 起）。
  - 命令：对比 AAPL 两列表 → 季度集合相同（`same set of quarters: True`），同季度 `value_usd` 一致（两形态首条均为 `17478000000.0` / `2023Q3`）。raw_quarterly_series 全 7  ticker 合计 15,157 字符。
- 事实：同一批 SEC XBRL 回购季度数在同一 payload 内以"丰富形态 + 子集形态"各存一份。

### 7. `get_m7_earnings_blackout_calendar` 附 24 年历史财报日期全表，可消费产物只有 1,388 字符

- 证据：
  - `value.raw_earnings_dates`（`原文:14456` 起）7 个 ticker 共 35,207 字符，每 ticker 58–100 条；AAPL 首条 `{"date": "2002-04-17", "endpoint": "get_earnings_dates"}`（`原文:14459`）。
  - 同块 `per_ticker` 仅 7 条 1,388 字符（形态：`{"ticker": "AAPL", ..., "next_earnings_date": "2026-07-31", "days_to_earnings": 0, "in_estimated_blackout": true, "window_start": "2026-07-10", "window_end": "2026-08-02"}`）。
  - canon 消费方式（`投影:310-337`）：窗口固定为 [财报日前21个自然日, 财报日后2个自然日]，"先看逐公司日期和覆盖率，再看7家公司等权占比"。
- 事实：该指标 42,758 字符（占 raw data 11.1%），其中 82% 是 2002 年起的历史财报日期清单；canon 描述的计算只需要 next_earnings_date 与窗口。

### 8. 材料体量偏科：supporting_only 类指标占 raw data 近九成，核心估值锚不到 2%

- 证据：
  - 命令同第 4 条的逐键统计：`get_m7_buyback_flow` 54,116（14.1%）+ `get_m7_capex_cycle` 49,301（12.8%）+ `get_m7_earnings_blackout_calendar` 42,758（11.1%）+ `get_ndx_earnings_revision_metrics` 198,592（51.6%）≈ 344.8K / 384.5K ≈ 89.6%；主锚 `get_ndx_wind_valuation_snapshot` 6,487（1.7%）、`get_damodaran_us_implied_erp` 6,368（1.7%）、`get_equity_risk_premium` 3,238（0.8%）。
  - 权限标注：buyback/blackout 全部字段 `supporting_only`（`投影:1032-1040,1137-1152`）；capex 的 `yoy_acceleration` 亦 `supporting_only`（`投影:966-972`）。
- 事实：按 canon 的权限语义，占材料约九成的块都只能"旁证/补位/时间上下文"，而 `core_allowed` 的主锚数据合计不到 3%。

### 9. 输入里带着本层预判结论：`layer_facts.state = "expensive"`

- 证据：
  - `原文:1580` `"state": "expensive"`（投影对应 `投影:1639`）；`原文:1588`/`投影:1647` `"summary": "估值状态: expensive。关键事实: …"`；`key_metrics` 于 `投影:1640-1646`。
  - 规则侧：`投影:1352-1357` Layer Synthesis 要求"必须回答：L4 是 cheap、fair、expensive 还是 expensive-but-supported"——即该判断本是本站产出物。
- 事实：上游预聚合层把"估值状态=expensive"作为既定事实写进输入；同时 `layer_facts.key_metrics` 只列 5 个指标（`原文:` layer_facts 块内），pre-选了什么算"关键"。

### 10. `get_ndx_forward_pe_full_constituent` 是 analysis_required=true 且数据可用，但没进 `layer_facts.core_signals`

- 证据：
  - 清单：`投影:711-757`，`"analysis_required": true`（`投影:713`）、`"error": null`；canon 称其为"L4 全成分估值核心/确认水平证据"（`投影:218`）。
  - 命令：`[s['metric'] for s in layer_facts.core_signals]` 输出 8 条，不含 `get_ndx_forward_pe_full_constituent`；缺失集合 = 该指标 + 两个 unavailable 指标。
  - 该指标 raw 值存在：`value=19.4623`、`forward_earnings_yield=0.0513814586`、`effective_date=2026-07-22`（命令输出）。
- 事实：预聚合摘要对一个必答且可用的核心指标保持沉默，却收录了 supporting_only 的 buyback/capex/blackout 三条。

### 11. `layer_facts` 把 DanjuanFunds 的第三方分位挂到 HoM 指标头上，且不标来源

- 证据：
  - `投影:1447` `"historical_percentile": 43.48` 与 `投影:1452` summary "NDX P/E and Earnings Yield | 值={'PE': 30.31, ...} | 分位=43.48 | 状态=valid | 含非分位相对位置"——值是 HoM trailing PE 30.31，分位未署名。
  - 该指标 raw 中 `TrailingPEHistoricalPercentile: null`（命令输出 PE/percentile 字段一览）。
  - 43.48 的真实出处：`原文:2077-2078`（ThirdPartyChecks 内 DanjuanFunds 条目 `"percentile_10y": 43.48, "historical_percentile": 43.48`），`原文:2101` `"pe_percentile_raw": 0.4348`，数据日 `原文:2079` `2026-07-29`，样本起点 2016-04-15。
  - 规则侧：`投影:1308` "当 Wind 可用时，不要让蛋卷十年分位覆盖 Wind"；Wind 自己的 10y PE 分位为 41.66（`投影:1661,1683`）。
- 事实：预聚合层把 Danjuan 序列（Danjuan PE 29.4 的 10y 分位）填进了以 HoM 为 source_name 的信号的 `historical_percentile` 字段；摘要文本同时并列 Wind 41.66 与该 43.48 两个"分位"，均未在摘要串内注明各自来源与窗口。

### 12. Damodaran `expected_return` 同月两个值：8.62 vs 8.65

- 证据：
  - `原文:19197` `"expected_return": 8.62`（value 顶层，data_date 2026-07-01）；`原文:19213` `"expected_return": 8.65`（`monthly_series.latest_record`，同一 data_date 2026-07-01）；`原文:19338` `"expected_return": 8.65`（`monthly_series.numeric_summary.expected_return.latest`）。
  - 命令：打印三处 → `top-level expected_return: 8.62` / `latest_record expected_return: 8.65` / `numeric_summary ... latest: 8.65`；其余字段（sp500_level、us_10y、各 ERP 口径、adjusted_riskfree_rate）两处一致。
- 事实：同一 payload、同一数据日、同一指标字段，两个相邻位置给出不同数值，差 0.03；材料内无一处说明差异原因。

### 13. 规则说简式收益差距"只作 fallback/diagnostic"，payload 却给它 `core_allowed`

- 证据：
  - 规则：`投影:1320` "当 `get_ndx_wind_valuation_snapshot` 可用时，`get_equity_risk_premium` 的简式收益差距只作为 fallback/diagnostic，不再作为 L4 风险补偿主锚"；同向表述见 payload 内 `authority_boundary`（`投影:1721` "simple yield gap is fallback/diagnostic only"）。
  - 同一块的 `MetricAuthority.level`：`usage: "core_allowed"`，authority `derived_simple_yield_gap_official_inputs`（`原文:12213-12217`）。
  - 另：canon 对该指标的 `core_vs_tactical_boundary` 是"核心估值-利率桥梁指标"（`投影:277`），与"fallback/diagnostic"表述并存。
- 事实：Wind snapshot 本次可用（availability=available）。在此前提下，规则文本、payload 内 boundary 文本、payload 内 MetricAuthority 三处对该指标发言权给出两种不一致标注；同时 `yield_type` 字段被标 `supporting_only` 并要求"引用 #level 获取实际差距数值"。

### 14. 输出结构示例用 L1 指标做示范，evidence_refs 写的是 `L1.get_10y_real_rate`

- 证据：
  - `投影:1203-1243`（`### 结构示例`）：示例 `function_id: "get_10y_real_rate"`、`"evidence_refs": ["L1.get_10y_real_rate"]`（`投影:1220`）、`cross_layer_implications: ["需要 L4 验证估值是否已反映高实际利率"]`。
  - 系统约束：`投影:53` "所有 evidence_refs 必须来自本次输入的 raw_data"；L4 的 raw_data 里没有 `get_10y_real_rate`（11 个 function_id 清单见第 4 条命令输出）。
- 事实：全提示词唯一的完整输出示范是一个 L1 指标的分析条目，其 evidence_refs 指向 L4 输入中不存在的指标；示例视角（"需要 L4 验证…"）也是上游层向下说话的口吻。

### 15. 同一指标两个"日期"字段值不同；context_brief 以单一"数据日期 2026-07-30"概括全层

- 证据：
  - `get_ndx_pe_and_earnings_yield` 清单条：顶层 `"date": "2026-07-24"`（`投影:612`），其 `data_quality.data_date/as_of_date/effective_date` 均为 `"2026-05-18"`（`投影:619-621`）；`coverage.trailing_data_date=2026-07-24`、`forward_data_date=2026-05-18`（`投影:635-636`）。
  - `context_brief.data_summary`："数据日期 2026-07-30，L4 本层 9/11 个指标成功。"（`投影:1406`）。
  - 命令：逐指标打印日期 → 实际 effective_date 分布：2026-07-30（Wind snapshot）、2026-07-31（yield gap/capex/blackout/buyback）、2026-07-24+2026-05-18（HoM 双日期）、2026-07-22（forward PE、revision）、2026-07-01（Damodaran）、not_available（2 个不可用）。
- 事实：9 个可用指标的数据日横跨 2026-05-18 至 2026-07-31；"数据日期 2026-07-30"的概括只与 Wind 主锚一致。HoM 指标顶层 date 与其 data_quality.data_date 相差 67 天，规则未说明顶层 date 字段语义。

### 16. 提示词内 10 年期美债收益率出现两个值：4.61% 与 4.45%

- 证据：
  - `get_equity_risk_premium.value.components`：`"10Y Treasury Yield (Risk-Free)": "4.61%"`（命令输出，该块全文已打印）；`level: -1.21` 与 `3.4013605442176873% - 4.61% ≈ -1.21` 自洽。
  - `get_damodaran_us_implied_erp.value.us_10y_treasury_rate: 4.45`（data_date 2026-07-01，月度表）。
- 事实：两处数值口径不同（日度组件值 vs Damodaran 月度记录），材料内未并置说明；yield gap 的 10Y 组件只给字符串百分比，未附来源与日期字段。

### 17. 30d 盈利修正主斜率全部来自待验证供应商补位，自产档案贡献为零

- 证据：
  - 命令输出：`slope_30d.coverage = {"included_constituents": 97, ..., "self_archive_constituents": 0, "self_archive_official_weight_pct": 0.0, "supplier_lookback_constituents": 97, "supplier_lookback_official_weight_pct": 95.093082}`；`material: "supplier_lookback"`、`verification_status: "pending_validation"`；`divergence.30d/90d.status = "not_comparable"`，reason `no_ticker_has_both_a_valid_self_archive_anchor_and_supplier_lookback`。
  - canon 预期（`投影:232`）："supplier_lookback 只在自产档案缺口时补位；pending_validation 表示同窗自产档案尚不足以核验"。
- 事实：本次 30d/90d 主斜率 100% 权重来自 supplier_lookback 且 pending_validation，两轨对撞 30d/90d 均不可比（仅 7d 窗有 99 条可比成分）。canon 对此情形有文字预期，属"指令预见的降级形态"，记录为事实供上游判断材料与职责匹配度。

### 18. 输出字段规格把契约宣称"必须认真填写"的字段标为"可选"

- 证据：
  - 契约散文（`投影:432-444`）："必须新增并认真填写的字段"列出 local_conclusion、indicator_analyses、layer_synthesis、internal_conflict_analysis、quality_self_check；并写 local_conclusion "缺失会被结构校验直接拒绝"。
  - 输出字段规格（`投影:13967-13970`）：`indicator_analyses`（可选）、`layer_synthesis`（可选，字符串 或 null）、`internal_conflict_analysis`（可选…或 null）、`quality_self_check`（可选…或 null）。
  - Response Rules（`投影:13977`）："字段的形状…以上面「输出字段规格」为准；正文里的示例只解释语义，形状冲突时以规格为准"——只仲裁"形状"，未仲裁"必填性"。
- 事实：同一提示词内，5 个字段在散文契约里是"必须"，在自动生成的规格表里是"可选/可 null"；两处表述并存且没有必填性冲突的仲裁规则。

### 19. 简式收益差距指标的 `data_quality.coverage` 内容是另一个指标（HoM）的覆盖率

- 证据：
  - `get_equity_risk_premium` 块 `投影:8995-9012`：coverage 为 `trailing: "97.71% of NDX mcap"`、`forward_data_date: "2026-05-18"`、decision_rule `"any reported percentage outside [0,100] forces all HoM fields to audit_only"`——与 `get_ndx_pe_and_earnings_yield` 清单条的 coverage（`投影:632-650`）逐字相同。
  - 该指标自身 method 为 `wind_trailing_earnings_yield_minus_10y`，组件为 Wind PE 派生收益率 3.4014% 与 10Y 4.61%（`投影:8937-8942`）。
- 事实：按 payload 自述，该指标由 Wind PE 与 10Y 计算，但其 coverage 块描述的是 HoM trailing/forward 覆盖率与 HoM 决策规则；coverage 与计算输入对不上。

### 20. 回购原始序列含逐字重复行，且重复行在双形态中各出现两次

- 证据：
  - `grep -n '"value_usd": 4627000000' 原文` → 4 处：`原文:17421,17438`（`per_company.MSFT.quarters` 内两行）、`原文:18599,18608`（`raw_quarterly_series.MSFT` 内两行）。
  - 命令打印 2026Q1 全部行：MSFT 2026Q1 两行 `4.627 / filed 2026-04-29 / form 10-Q / fp Q3` 完全相同；AAPL 2023Q3 亦有两行但值不同（17.478 与 21.003）。
  - 聚合核对：`m7_quarterly_total=20.73` = 12.288+4.627+3.815+0+0（去重后）；若按行直接求和为 25.357。payload 内无字段说明聚合做了去重。
- 事实：原始行级数据里同一申报事实被原样重复（MSFT 2026Q1 ×2，再乘双形态 ×2，共 4 份）；聚合值与行级求和不一致，差异只能靠自己复算发现。

### 21. AMZN 回购序列标 available，但最新季是 2024Q4（申报日 2025-02-07）

- 证据：
  - 命令：`per_company.AMZN.quarters` 范围 `2023Q1 ... 2024Q4`，末行 `filed_date: 2025-02-07`、`period_end: 2024-12-31`；`per_company.AMZN.latest_calendar_quarter = 2024Q4`。
  - 同块 `availability: "available"`（各公司均有），`data_quality.coverage.companies_available: 6`、`companies_missing: ["TSLA"]`、`companies_via_sec_xbrl` 含 AMZN（`投影:1075-1089`）；指标级 `as_of_date/effective_date: 2026-07-31`。
  - `aggregate_context.excluded_for_fiscal_calendar_misalignment` 含 AMZN（命令输出）。
- 事实：AMZN 被计入 6 家"可用"，但其最新回购数据距 effective_date 约 17 个月；聚合层通过错位排除机制把它挡在 2026Q1 合计之外，行级数据本身仍原样在材料里。

### 22. `layer_facts.core_signals` 的 M7 摘要值剥掉了季度与单位上下文

- 证据：
  - `投影:1534-1554`：buyback 摘要 value 仅保留 `{"as_of_date": "2026-07-31", "per_company": {...3 家 availability...}, "m7_quarterly_total": 20.73}`；无季度标签、无单位。原始块里该合计对应 2026Q1、单位 USD billions（命令：`lrd['get_m7_buyback_flow']['unit']` → `"USD billions, quarterly/TTM"`；`aggregate_context.latest_calendar_quarter = "2026Q1"`）。
  - 摘要 per_company 只列 AAPL/MSFT/GOOGL 3 家（`投影:1536-1551`），原始块有 7 家。
  - 数值单位纪律（`投影:452-453`）："引用金额/规模数值时必须带上 payload 中的 unit 单位"。
- 事实：预聚合摘要层对 M7 三指标做了字段裁剪；裁剪后的 m7_quarterly_total 数值本身不带单位与季度，纪律要求的单位信息只在原始块里。

### 23. 投影保真度抽查记录（方法学，非发现）

- 24 处 `«…省略 N 字符…»` 抽查 3 处（`投影:756` 省 348、`投影:826` 省 771、`投影:10997` 省 1080），用原文重算省略中段长度，三处 N 均精确吻合。
- 12 处 `__table_projection__` 抽查 1 处（`投影:7528-7581`，breadth_30d.periods.0y.constituents）：`__total__=103` 与原文列表长度一致；head[0]（NVDA）与 tail[0]（CPRT）与原文首末行逐字段相等；`min/max:weight_pct`（0.111973/8.288274）、`upLast30days`（0/47）、`downLast30days`（0/27）均与原文重算一致。
- 结论：抽查范围内投影未引入数值失真；下文"盲区"交代未抽查的其余标记。

## 盲区

1. **截断标记未全量核对**：24 处 `«…省略 N 字符…»` 只重算了 3 处（结论均为 N 精确）；其余 21 处（多在 methodology/formula/notes/source_boundary 长文）未逐一核对中段原值。12 处 `__table_projection__` 只核了 1 处（head/tail/min/max/total 全对）；其余 11 张表的中段行未逐行读。
2. **行级明细未逐行通读**：earnings_revision 约 15.5 万字符逐成分记录（6 张表、97/87/99/103/103/103 行）、capex/buyback 各 84 行季度记录、blackout 616 行历史财报日期，均以"结构+聚合复算+首末行抽查"覆盖，未逐行审数值合理性。已做的复算：slope_30d 覆盖率字段自洽、flagged 双份相等、blackout 4/7 计数与 per_ticker 一致、回购 2026Q1 合计 20.73=去重行和。
3. **未核对算术**：Wind 分位 rank/sample_count 与 percentile 的换算（如 1048/2514 vs 41.66）、WorldPERatio 各窗均值±标准差区间自洽性、breadth/dispersion 聚合值由成分行重算——这些都没做。
4. **Damodaran `monthly_series.recent_records`** 只读了前 2 条（共 5 条，note 称完整序列在外部 artifact chart_time_series.json，该 artifact 不在语料内）。
5. **未审本站产出**：`output.validated.json`/`response.raw.txt` 未读——任务边界是材料结构，不评价站产出好坏。
6. **payload.json 与 prompt 内嵌 JSON 的关系**：确认 `payload` 键（紧凑 392,678 字符）即 Runtime Input 主体，未做逐字节 diff；分析均基于 prompt.txt 内嵌 JSON（可完整 `json.loads`）。
7. **清单 data_quality 一致性**用 sort_keys 后的 JSON 比较（9/11 恒等、2/11 占位差异），未逐字肉眼比对两处文本渲染。
8. **两个 NO_DATA 块**（`投影:1853-1926`、`2239-2305`）已通读，结构/哨兵字段一致；其中 `no_data_text` 的省略中段（`投影:1913`，省 61 字符）未核对原值。
9. **`get_ndx_earnings_revision_metrics.divergence.7d` 的 99 条成分对撞明细**只读了聚合标量（self_archive_slope/supplier_lookback_slope/supplier_minus_archive 等键名），未逐行读。
10. **meta.json 已读**（effective_date 2026-07-30、mode latest、model deepseek-v4-flash、input_artifacts=layer_context_briefs/L4.json + analysis_packet.raw_data.L4）；这两个输入 artifact 原文不在语料内，无法核对提示词是否与它们逐字一致。
