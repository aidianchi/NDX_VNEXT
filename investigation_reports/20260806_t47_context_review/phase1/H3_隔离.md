# H3 隔离片：L1-L5 上下文隔离红线横向通读

- 语料：`output/analysis/vnext/20260731_002156/context_spread/`（full/ 原文为准，projected/ 投影仅定位）
- 审查日期：2026-08-08 / 2026-08-09（会话跨日续作）
- 纪律执行：只读语料与代码；每条发现带 `文件:行号` + 命令 + 关键行照抄；区分"声明文本"与"实物材料"；只报事实，不报严重度、不开处方。

---

## 1. 扫描口径

### 1.1 关键词表（第一组：跨层产物指纹）

```
thesis | bridge | counter | adjudicat | synthesis | judgment | 立场 | 仓位 |
action_implication | 跨层 | conflict | apparent_cross_layer_signals |
verdict | ruling | 裁决 | 反方 | 综合判断   （大小写不敏感）
```

选词理由：覆盖任务书指定词（thesis/bridge/counter/adjudicat/synthesis/judgment/立场/仓位/action_implication/跨层/conflict/thesis_draft），另补 verdict/ruling/裁决/反方/综合判断（下游裁决站的常见中英文产物名）。`thesis_draft` 作为独立词未单独出现（被 thesis 前缀覆盖）。

### 1.2 关键词表（第二组：评价性跨层文本与立场声明）

```
L1 结论|L2 结论|L3 结论|L4 结论|L5 结论 | L1..L5 认为 | L1..L5 显示 |
其他层的 | 系统当前 | 整体结论 | 最终结论 | 当前判断 | 系统立场 |
目标仓位 | 建议仓位 | 增持 | 减持 | 加仓 | 减仓
```

选词理由：第一组抓"命名产物"，第二组抓"不挂名的其他层结论形态文本"（任务书第 4 条）和立场声明实物矛盾（第 3 条）。

### 1.3 扫描范围

- 主目标：`full/L{1..5}/attempt_*.prompt.txt`（模型实际收到的提示词；L1/L3/L4/L5 各 1 个 attempt，L2 有 attempt_1 与 attempt_2 两份，共 6 份）。
- 旁证：`full/L{1..5}/attempt_*.payload.json`（请求载荷，核 apparent_cross_layer_signals）。
- 任务书第 3 条要求的全站扫描：`full/` 下全部 25 站目录（含 controlled_investigation 3 实例、event_card_interpreter 10 实例）。
- 响应类文件（`*.response*.txt`、`output.validated.json`、`*.parsed.normalized.json`）是站的产出而非输入，命中不计入隔离违规，仅在计数节注明。

### 1.4 已知盲区（初版，末节补全）

- 英文指纹词未全扫：stance / position sizing / overweight / underweight / bullish / bearish / recommendation 等（见第 5 节补扫结果）。
- 截断留痕处（投影三处截断）需要时在 full/ 核对；本轮 L1-L5 判读全部基于 full/ 原文，未依赖投影。

---

## 2. L1-L5 逐站命中判读表

判读类别：**违规实物**（其他层运行时数据/结论/候选跨层关系的实体内容）/ **声明文本**（规则、隔离纪律、静态本体描述）/ **同名巧合**（词形命中但语义无关，如"仓位"作为本层指标语义）。

扫描命令（以 L1 为例，各站同式）：

```bash
grep -inE 'thesis|bridge|counter|adjudicat|synthesis|judgment|立场|仓位|action_implication|跨层|conflict|apparent_cross_layer_signals|verdict|ruling|裁决|反方|综合判断' \
  output/analysis/vnext/20260731_002156/context_spread/full/L1/attempt_1.prompt.txt
```

命中量（prompt.txt，模型输入面）：L1=26、L2 attempt_1=35、L2 attempt_2=35、L3=24、L4=27、L5=26。

### 2.1 L1（full/L1/attempt_1.prompt.txt，2612 行）

| 行号 | 原文照抄（节选） | 判读 |
|---|---|---|
| 15 | `本段只提供静态规则，不提供其他层的运行时数据、结论或候选跨层判断。` | 声明文本（隔离声明本身） |
| 319 | `你在一个隔离的本层上下文中工作：角色是专业认知镜头……可被 Bridge 消费的结构化产物。` | 声明文本（角色契约，提及 Bridge 仅为接口说明） |
| 323 | `- L2: 风险偏好、信用利差、波动率、情绪、仓位和拥挤度。` | 声明文本（静态五层本体，该节标题见 321 行"静态五层本体（只用于路由，不代表当前状态）"） |
| 327 | `- Bridge: 读取各层结构化产物，验证跨层共振、冲突和传导机制。` | 声明文本（同上，静态本体） |
| 340-341 | `layer_synthesis 必须由 indicator_analyses 归纳……` / `internal_conflict_analysis 必须讨论本层内部指标之间的共振、背离……` | 声明文本（本层输出字段规则） |
| 346 | `跨层内容只能写成待 Bridge 验证的问题，不能写成已经成立的跨层结论。` | 声明文本（禁止性规则） |
| 822、826、860-898 | Cognitive Transform / layer_synthesis / internal_conflict_analysis / cross_layer_hooks 等写作要求 | 声明文本（规则） |
| 922 | `"apparent_cross_layer_signals": [],` | 实物（运行时输入字段），值为空数组，详见第 4 节 |
| 923 | `"task_description": "只完成 L1 的本层分析……需要 Bridge 后续验证的问题。"` | 声明文本（任务说明） |
| 2602-2610 | 输出字段规格（layer_synthesis、internal_conflict_analysis 等字段名列举） | 声明文本（schema） |

违规实物：**0 条**。

### 2.2 L2（full/L2/attempt_1.prompt.txt 17677 行；attempt_2.prompt.txt 17697 行，两版命中同构）

| 行号（attempt_1） | 原文照抄（节选） | 判读 |
|---|---|---|
| 15 | `本段只提供静态规则，不提供其他层的运行时数据、结论或候选跨层判断。` | 声明文本 |
| 230、245、255、257、269、350 | `"canonical_question": "风险资产仓位或交易是否已经拥挤？"` 等 | 同名巧合（"仓位"是本层拥挤度指标的领域语义，出自 IndicatorCanon 静态法典，非系统仓位指令） |
| 413、417、421、434-435、440 | 同 L1 对应节（隔离契约、静态本体、字段规则、跨层禁止性规则） | 声明文本 |
| 422 | `以上只是职责边界和接口协议，不是其他层的当前数据、状态或结论。你……不得据此推断其他层现在是 bullish、bearish、expensive、healthy 或 uptrend。` | 声明文本（禁止性声明） |
| 1263 | `你只接收 L2 风险偏好、情绪、信用和仓位上下文。……运行时不会向你提供其他层的当前数据、结论或状态。不得从 L2 数据反推出其他层当前结论。` | 声明文本（隔离声明） |
| 1281、1294、1307、1314 | 本层指标语义中的"仓位"（拥挤度、put/call 等） | 同名巧合 |
| 1372 | `"apparent_cross_layer_signals": [],` | 实物（空数组），见第 4 节 |
| 17443 | `"reason": "期限结构倒挂（VIX3M<VIX）是恐慌/风险信号，可作为 L2 风险确认或预警证据之一，仍须与 HY OAS/A-D/ATR 等交叉验证……"` | 声明文本（本层指标数据内嵌的静态法典 usage boundary；提到 A-D/ATR 是跨层验证路由建议，不含其他层数值或结论） |
| 17445-17456（contango/flat/source_boundary） | 同上区域 | 声明文本 |
| 17667-17675 | 输出字段规格 | 声明文本（schema） |

attempt_2 命中与 attempt_1 逐项同构（行号基本一致：15、230-350、413-440、1263、1372、17443、17667-17675），判读相同。
违规实物：**0 条（两版均 0）**。

### 2.3 L3（full/L3/attempt_1.prompt.txt，1434 行）

| 行号 | 原文照抄（节选） | 判读 |
|---|---|---|
| 15 | `本段只提供静态规则……` | 声明文本 |
| 229、233、237、250-251、256 | 隔离契约、静态本体、字段规则、跨层禁止性规则（同 L1 结构） | 声明文本 |
| 238 | `以上只是职责边界和接口协议，不是其他层的当前数据、状态或结论。……` | 声明文本 |
| 646 | `你可以使用静态五层本体理解 L1、L2、L4、L5 分别负责什么……但运行时不会向你提供其他层的当前数据、结论或状态。L3 只回答：指数运动的内部结构是否健康……` | 声明文本 |
| 656、667、688-726 | 本层流程与字段写作规则 | 声明文本 |
| 750 | `"apparent_cross_layer_signals": [],` | 实物（空数组） |
| 1424-1432 | 输出字段规格 | 声明文本（schema） |

违规实物：**0 条**。

### 2.4 L4（full/L4/attempt_1.prompt.txt，19459 行）

| 行号 | 原文照抄（节选） | 判读 |
|---|---|---|
| 15 | `本段只提供静态规则……` | 声明文本 |
| 262 | `"core_vs_tactical_boundary": "结构性多头证据的直接补位；核心仓可作产业周期支撑证据之一，不能单独据此扩大仓位……"` | 同名巧合（法典静态词条，仓位语义为本层指标使用边界） |
| 368 | `CORRECT OUTPUT: NDX简式收益差距为-0.5%……只能作为估值脆弱性和跨层验证需求的锚点。` | 声明文本（few-shot 示例，含"跨层验证需求"措辞但无他层实物） |
| 377、381、385、398-399、404 | 隔离契约、静态本体、字段规则、跨层禁止性规则 | 声明文本 |
| 1203、1257、1287、1291-1331 | 本层流程、数据优先级、字段写作规则 | 声明文本 |
| 1355 | `"apparent_cross_layer_signals": [],` | 实物（空数组） |
| 19449-19457 | 输出字段规格 | 声明文本（schema） |

违规实物：**0 条**。

### 2.5 L5（full/L5/attempt_1.prompt.txt，1939 行）

| 行号 | 原文照抄（节选） | 判读 |
|---|---|---|
| 15 | `本段只提供静态规则……` | 声明文本 |
| 129、348 | `"canonical_question": "波动幅度是否正在扩大，仓位和止损距离是否需要调整？"` / ATR few-shot 中的仓位语义 | 同名巧合（法典/示例内领域语义） |
| 358、362、366、379-380、385 | 隔离契约、静态本体、字段规则、跨层禁止性规则 | 声明文本 |
| 367 | `以上只是职责边界和接口协议，不是其他层的当前数据、状态或结论。……` | 声明文本 |
| 860 | `你只接收 L5 价格趋势、动量、波动和成交量上下文。……运行时不会向你提供其他层的当前数据、结论或状态。不得从 L5 数据反推出其他层当前结论。` | 声明文本 |
| 872、910-949 | 本层流程与字段写作规则 | 声明文本 |
| 957 | `不得因为趋势强给出买入建议；最终裁决不在本层。` | 声明文本（"裁决"为禁止性声明，非裁决实物） |
| 974 | `"apparent_cross_layer_signals": [],` | 实物（空数组） |
| 1929-1937 | 输出字段规格 | 声明文本（schema） |

违规实物：**0 条**。

### 2.6 L1-L5 运行时输入结构核验（`## Runtime Input` 节）

逐站核对了 `context_brief` 的构成（命令：`Read` 相应行区间）：

| 站 | Runtime Input 起始行 | data_summary 照抄 | layer_highlights 键 | apparent_cross_layer_signals |
|---|---|---|---|---|
| L1 | 910 | `数据日期 2026-07-30，L1 本层 9/9 个指标成功。` | 仅 `"L1"`（916-920 行，3 条本层指标） | `[]`（922 行） |
| L2 attempt_1 | 1360 | `数据日期 2026-07-30，L2 本层 12/13 个指标成功。` | 仅 `"L2"`（1366-1370 行） | `[]`（1372 行） |
| L2 attempt_2 | ~1360（同行区） | 同上 | 仅 `"L2"`（1366-1370 行） | `[]`（1372 行） |
| L3 | 738 | `数据日期 2026-07-30，L3 本层 6/6 个指标成功。` | 仅 `"L3"`（744-748 行） | `[]`（750 行） |
| L4 | 1343 | `数据日期 2026-07-30，L4 本层 9/11 个指标成功。` | 仅 `"L4"`（1349-1353 行） | `[]`（1355 行） |
| L5 | 962 | `数据日期 2026-07-30，L5 本层 11/11 个指标成功。` | 仅 `"L5"`（968-972 行） | `[]`（974 行） |

五站 `special_attention` 均为同两条声明（L1 924-927 行照抄）：

```
"可以使用静态五层职责边界来路由验证问题，但不要读取或推断其他层当前状态。",
"不得因为最终报告需要完整结论而提前吸收其他层叙事。"
```

第二组关键词（评价性跨层文本）在 L1-L5 prompt.txt 的命中：`最终结论`（各站 32 行，法典 ObjectCanon 静态条文：`"最终结论必须说明判断对象是 NDX、QQQ 暴露，还是等权 Nasdaq-100 口径。"`）、`其他层的`（各站静态本体节禁止性声明）。**无"LX 结论/LX 认为/LX 显示/系统当前/整体结论/目标仓位/建议仓位/增持/减持/加仓/减仓"命中。**

### 2.7 L1-L5 小结

- 6 份 prompt.txt 中，跨层产物指纹命中全部是：隔离声明、静态五层本体（明确标注"只用于路由，不代表当前状态"）、本层输出字段规则、schema 列举、法典/示例内的领域词同名巧合。
- 运行时输入（Runtime Input）只含本层 `layer_facts` 与本层 `layer_highlights`，`apparent_cross_layer_signals` 均为空数组。
- 未在 L1-L5 材料中发现其他层运行时数据、摘要、结论、候选跨层关系或 Bridge/Thesis 判断的实物。

---

## 3. 立场声明 vs 实物矛盾核验

### 3.1 controlled_investigation 三实例（重点核验项）

三实例共用同一段"铁律"声明（各文件第 5 行，逐字相同）：

```bash
grep -n '你不知道' full/controlled_investigation/*/inv_*.prompt.txt
```

照抄（三实例四份 prompt 均为第 5 行）：

```
- 你不知道、也不需要知道系统当前的判断是什么。不要试图讨好任何立场。
```

逐实例核对"允许材料"实物（全文通读，文件均 ≤414 行）：

#### (a) inv_ae806095c18a/inv_ae806095c18a.attempt_1.prompt.txt（128 行）

- 声明：第 5 行（见上）。
- 实物：第 25 行 `[M1] artifact=bridge_memos/bridge_0.json`，材料为 Bridge 站运行时产物 bridge memo，内含：
  - 第 36 行 `"summary": "极端实际利率（99.4%分位）压制估值倍数 vs 盈利上修能否吸收折现率压力的核心矛盾"` —— 系统当前核心矛盾判断。
  - 第 38 行 `"dominant_side": "估值压缩风险（实际利率极端、简式收益差距-1.21%、L5已确认下跌趋势、L3广度恶化）"` —— 系统当前主导方向判断。
  - 第 41 行 `"action_implication": "核心仓：保持防御性配置，估值压缩风险大于盈利支撑力度，不宜重仓。战术仓：RSI 32.4接近超卖可轻仓博弈反弹，但须在VWAP（702.73）和MA20（704.33）设置止盈，跌破Donchian下轨（661.14）止损。等待现金：……当前不追加核心仓位"` —— 仓位指令实物。
  - 第 58 行 `"implication": "触发核心仓进一步减仓；估值将从'中等估值但脆弱'转向'高估值压缩中'"` —— 仓位指令实物。
  - 第 67 行 `"implication": "可逐步增加核心仓……"`、第 76 行 `"implication": "……可增加风险暴露"` —— 条件式仓位指令。
  - 第 90-113 行 `cross_layer_claims`（含 `"confidence": "high"/"medium"` 的跨层主张及机制叙述）—— 候选/已立跨层关系实物。
- 材料在第 125-126 行中段截断（`"claim": "M7资本开支加速（同比+75%）提供结构` 后接 `[/M1]`）。
- 判读：声明（第 5 行"你不知道系统当前的判断"）与实物（第 36/38/41/58 行系统核心矛盾、主导方向、仓位指令）**同处一份提示词**，矛盾成立。实物来源标注为 bridge_memos/bridge_0.json（Bridge 站产物）。

#### (b) inv_c4c2b3b067ff/inv_c4c2b3b067ff.attempt_1.prompt.txt（129 行）

- 声明：第 5 行（同上）。
- 实物：第 25 行 `[M1] artifact=bridge_memos/bridge_0.json`，与 (a) 同一份 bridge memo，关键行：
  - 第 36/38/41/58/67/76 行：与 (a) 逐字相同（summary、dominant_side、action_implication、三条 transformation implication）。
  - 第 90-96 行 `"key_uncertainties"`（系统级不确定性清单）。
  - 第 97-123 行 `typed_conflicts`，含第 99 行 `"conflict_id": "L1_L4_valuation_compression"`、第 105 行 `"implication": "核心持仓需考虑估值压缩风险；……若斜率转负应进一步减仓"`、第 122 行 `"status": "confirmed"` —— 已确认跨层冲突实物。
- 材料在第 126 行中段截断（`"conflict_t` 后接 `[/M1]`）。
- 判读：声明 vs 实物矛盾成立，同 (a)。

#### (c) inv_e75efe82e7c0（attempt_1 414 行；attempt_2 416 行，与 attempt_1 仅差末尾重试附言）

- 声明：第 5 行（两 attempt 相同）。
- 实物：第 25 行 `[M1] artifact=cross_layer_questions.json`（event→data 开放问题清单，含 `"status": "open"`）；第 158 行 `[M2] artifact=event_layer_summary.json`（事件簇工作产物，含 `"materiality": "medium"`、`"market_has_likely_priced": "insufficient_data"`、`"research_status": "ready_for_market_validation"` 等评价性状态）；第 281 行 `[M3] artifact=event_mechanism_report.json`（与 M1 同源的 cross_layer_questions 复述）。
- 仓位类词命中：第 175/204/233/262 行 `"价格变化可能来自既有仓位、估值再定价或市场广度变化，而不是该事件本身。"` —— 同名巧合（alternative_explanations 里的领域语义，非仓位指令）。
- attempt_2 末尾附注重试原因：`校验错误：ValueError: claims_supported[0] missing [M#] material citation`（两 attempt 的 error.json 亦存档：attempt_1 `claims_supported[0] missing [M#] material citation`、attempt_2 `finding missing [M#] material citation`）。
- 判读：本实例材料为事件层开放问题与待验证状态，**未见系统市场结论或仓位指令**；但 M2 含事件层运行时的评价性判断（materiality、readiness 状态），属于"其他环节运行时产物"实物，与第 5 行"系统当前的判断"字面范围（系统整体市场判断）不直接冲突。记录为"声明-实物同处但实物为待验证事件层工作产物"，不与 (a)(b) 同类。

#### (d) 溯源交叉验证

`触发核心仓进一步减仓；估值将从'中等估值但脆弱'转向'高估值压缩中'` 同一字符串在 full/ 全部 prompt.txt 中的分布（命令：`grep -rn '触发核心仓进一步减仓' full/ --include='*.prompt.txt'`）：controlled_investigation/inv_ae806095c18a:58、controlled_investigation/inv_c4c2b3b067ff:58、thesis:879 与 1317、counter_thesis:610、1048 与 5187、critic:876、risk:902、reviser:964、final_adjudicator:1016。另有一条近义串 `核心仓须进一步减仓；Forward PE将从19.46向Trailing PE 30.31靠拢，跌幅可能显著加速` 分布于 risk:608、critic:582、reviser:670、final_adjudicator:722、integrated_adjudicator/20260730T165426Z:77（integrated_adjudicator 只含此条，不含上一条）。即同一份 bridge memo 文本被多发到受控调查员与多个上/下游站。这些站本身均不含"你不知道系统判断"式声明（见 3.2 全站扫描），故声明-实物矛盾点集中在 controlled_investigation (a)(b) 两实例。

### 3.2 全站立场声明扫描（25 站 prompt.txt）

命令：

```bash
grep -rnE '你不知道|不需要知道|不要试图讨好|不会向你提供|不会收到|未向你提供|不掌握|无法看到|尚未形成|不持有立场|没有立场|保持中立|you do not know|no access to|系统的判断|系统的立场|系统当前' full/ --include='*.prompt.txt'
grep -rnE '不知道|讨好|未告知|尚未向你|独立于.*判断|既定立场|结论尚未|do not know|not been given|not aware' full/ --include='*.prompt.txt'
```

结果（两组词合计去重）：

| 站 | 声明行号 | 原文照抄 | 同类实物是否同处 |
|---|---|---|---|
| L1 | 814 | `你可以使用静态五层本体理解 L2-L5 分别负责什么……但运行时不会向你提供 L2-L5 的当前数据、结论或状态。不得从 L1 数据反推出其他层当前结论。` | 否（第 2 节零实物） |
| L2 attempt_1 / attempt_2 | 1263 / 1263 | `你只接收 L2 风险偏好、情绪、信用和仓位上下文。……运行时不会向你提供其他层的当前数据、结论或状态。不得从 L2 数据反推出其他层当前结论。` | 否 |
| L3 | 646 | `你可以使用静态五层本体理解 L1、L2、L4、L5 分别负责什么……但运行时不会向你提供其他层的当前数据、结论或状态。L3 只回答：指数运动的内部结构是否健康……` | 否 |
| L4 | 1191 | `你只接收 L4 估值、盈利收益率、风险补偿和基本面估值上下文。……运行时不会向你提供其他层的当前数据、结论或状态。不得从 L4 数据反推出其他层当前结论。` | 否 |
| L5 | 860 | `你只接收 L5 价格趋势、动量、波动和成交量上下文。……运行时不会向你提供其他层的当前数据、结论或状态。不得从 L5 数据反推出其他层当前结论。` | 否 |
| controlled_investigation ×3（4 份 prompt） | 均为 5 | `- 你不知道、也不需要知道系统当前的判断是什么。不要试图讨好任何立场。` | **是（inv_ae806095c18a、inv_c4c2b3b067ff 两实例，见 3.1）；inv_e75efe82e7c0 未见系统市场结论/仓位实物** |
| bridge / thesis / counter_thesis / critic / risk / reviser / final_adjudicator / integrated_adjudicator / event_card_interpreter ×10 / event_section_summary | — | 本词表下未命中"你不知道系统判断/立场"类声明 | 无从构成此型矛盾（这些站按设计接收上游产物，本扫描不展开其合法性） |

补充记录（非矛盾，但属"立场"词实物命中，供其他片参考）：integrated_adjudicator/20260730T165426Z/attempt_1.prompt.txt:11 有反向声明 `数据判决是锚，你无权改判……stance_echo 字段必须逐字复制输入里的 final_stance`，其输入第 49 行 `"final_stance": "在当前极端实际利率压制和盈利上修力度削弱的证据下，风险补偿偏薄，赔率不利，宜防守等待……"` —— 该站被明确要求复读系统立场，不属于"声称不知道"型矛盾。

---

## 4. apparent_cross_layer_signals 现状

命令：

```bash
grep -rn 'apparent_cross_layer_signals' full/
```

结果：

| 文件 | 行号 | 值 |
|---|---|---|
| L1/attempt_1.prompt.txt | 922 | `[]` |
| L1/attempt_1.payload.json | 16 | `[]` |
| L2/attempt_1.prompt.txt / attempt_2.prompt.txt | 1372 / 1372 | `[]` |
| L2/attempt_1.payload.json / attempt_2.payload.json | 16 / 16 | `[]` |
| L3/attempt_1.prompt.txt | 750 | `[]` |
| L3/attempt_1.payload.json | 16 | `[]` |
| L4/attempt_1.prompt.txt | 1355 | `[]` |
| L4/attempt_1.payload.json | 16 | `[]` |
| L5/attempt_1.prompt.txt | 974 | `[]` |
| L5/attempt_1.payload.json | 16 | `[]` |
| bridge/attempt_1.prompt.txt | 401 | **非空**，3 条叙事串（对照） |
| bridge/attempt_1.payload.json | 36 | 非空（同上） |

L1-L5 六份 prompt + 六份 payload 该键全部为空数组 `[]`，位于 `## Runtime Input` → `context_brief` 内（实物字段）。上一轮"声称为空数组"本轮取证属实。

对照（非 L1-L5）：bridge/attempt_1.prompt.txt:401-405 照抄：

```json
"apparent_cross_layer_signals": [
  "流动性收紧与高估值并存，估值压缩风险需要在桥接阶段被显式检验。",
  "广度走弱但价格趋势仍强，趋势质量与集中度风险需要被单独讨论。",
  "环境偏紧但趋势仍强，需确认价格是否只是延迟反应。"
],
```

同站 413 行起还有 `candidate_cross_layer_links`（含 `link_type: L1_L4 / L3_L5` 与 relevant_metrics）。说明该键在下游 Bridge 站确实被填充使用，在 L1-L5 侧保持空置。

---

## 5. 补充扫描（针对第一轮词表盲区）

### 5.1 英文指纹词（L1-L5，prompt.txt）

命令：`grep -inE 'stance|overweight|underweight|recommend|bullish|bearish|position_size|长仓|空仓|做多|做空|买入建议|卖出建议' full/L{1..5}/…prompt.txt`

- `stance` 裸词会误命中 `distance_from_ma20_pct`（如 L3:856），已用 `\bstance\b` 复扫，L1-L5 全目录（含 payload/response）**零命中**。
- `bullish|bearish`：L1:328、L2:422、L3:238、L4:386、L5:367 均为同一句禁止性声明 `不得据此推断其他层现在是 bullish、bearish、expensive、healthy 或 uptrend`；L3:206 `"divergence": "bearish"` 为本层指标 get_advance_decline_line 的 few-shot INPUT 示例原始数据（本层语义）；L4:16900 `state_usage_boundary` 内 `not bullish proof` 为本层指标（财报静默期）法典使用边界静态文本。
- `做空/空仓`：L2:235 `拥挤不是做空理由，需要价格、波动和信用触发。`（法典静态词条）、L2:827 methodology 文本含"空仓率"（本层指标方法描述）、L4:106 `高估值不是自动做空信号`（法典静态条文）。同名巧合/规则文本。
- `final_stance|reader_final|payoff_assessment|portfolio_actions`：L1-L5 全目录零命中。

### 5.2 下游产物指纹溯源扫描（L1-L5 全目录，含 payload 与 response 文件）

命令：`grep -rinE 'dominant_side|principal_contradiction|typed_conflicts|cross_layer_claims|candidate_cross_layer_links|key_uncertainties|unresolved_questions|bridge_memo|bridge_0|transformation_signals|\bstance\b|final_stance|reader_final|payoff_assessment|portfolio_actions' full/L{1..5}/`

结果：**五站目录全部零命中**（Bridge/Thesis/Risk/Final 产物的键名与文本指纹均未渗入 L1-L5 任何文件）。

### 5.3 payload.json 抽查

逐站 grep payload.json 同第一组词表：L1/L3/L4/L5 各 2 处命中（`apparent_cross_layer_signals: []` 与 `task_description`，均在 context_brief，与 prompt 一致）；L2 两版 payload 各 3 处（多出的第 3 处为 layer_facts 内法典 `state_usage_boundary.reason` 静态文本，与 prompt 17443 行同文）。payload 相对 prompt 未见额外跨层内容。

### 5.4 响应侧文件说明（非输入，仅备查）

首轮计数中 `*.response.raw.txt` / `output.validated.json` / `*.parsed.normalized.json` 的命中（L1=3、L2=8-11、L3=3、L4=3-4、L5=6）均为各站自身输出中的 `layer_synthesis`、`internal_conflict_analysis`、`cross_layer_hooks` 字段及"仓位"领域语义。这些文件是站的产出，不构成隔离输入；`cross_layer_hooks` 按契约为"待 Bridge 验证的问题"形态（规则节如 L1:878 `## Cross-Layer Hooks`，各站行号略有差异），本轮未逐条审读输出内容（属输出侧，不在本片输入隔离口径内）。

---

## 6. 盲区与未覆盖

1. **词表盲区（已部分补救）**：第一轮未扫 stance/overweight/underweight/recommend/bullish/bearish/做多/做空 等英文与口语仓位词，5.1 已补扫 L1-L5；但未对全部 25 站逐站补扫英文词（任务 3 的声明扫描用了中文为主的词表 + `you do not know|no access to|do not know|not been given|not aware`，其他英文声明措辞如 `blind to`、`unaware of`、`without knowledge of` 未覆盖）。
2. **未扫描的词**：`memo`、`brief`、`echo`、`复读`、`继承`、`上游`、`下游`、`governance` 未扫。`thesis` 单词本身在 L1-L5 prompt.txt 零命中（第一组命中来自 synthesis/Bridge/conflict 等词形），`thesis_draft` 随之零命中；`verdict` 在 L1-L5 亦零命中。
3. **投影截断处**：L1-L5 判读全部基于 full/ 原文，未依赖 projected/；controlled_investigation 材料内部（[M1]/[M2]/[M3] 包）存在原始 JSON 中段截断（inv_ae806095c18a:125-126、inv_c4c2b3b067ff:126、inv_e75efe82e7c0:155/278/411），截断点之后的内容本次运行即不可见，无法核验被截掉的部分是否含更多系统结论。这是语料自身的截断，非投影截断。
4. **输出侧未审读**：各站 response/output 文件只在计数与指纹扫描中路过，未逐条审读 L1-L5 输出文本是否写出越权结论（属"输出是否守隔离"问题，不在"输入是否被污染"口径内）。
5. **event_card_interpreter ×10 与 event_section_summary**：只做了声明词扫描（零命中），未通读其材料；它们不在 L1-L5 隔离红线直接范围，但若任务 3 要覆盖"所有站"的声明-实物矛盾，此 11 站的材料通读未做。
6. **meta.json / error.json**：未纳入关键词扫描（体积小的运行元数据）。
7. **日期口径**：所有判断仅针对 run 20260731_002156 这一次运行摊开语料，未外推到其他 run。

---

## 7. 事实清单（一句话/条）

1. L1-L5 六份 prompt.txt 中未发现其他层运行时数据/结论/候选跨层关系的实物；命中全部为隔离声明、静态五层本体、本层字段规则、schema 列举、法典同名巧合（第 2 节）。
2. L1-L5 `layer_highlights` 只含本层键；`apparent_cross_layer_signals` 全部为空数组（prompt 与 payload 双侧取证）（第 2.6、4 节）。
3. Bridge/Thesis/Risk/Final 产物指纹（dominant_side、principal_contradiction 等 15 键/词）在 L1-L5 全目录零命中（5.2 节）。
4. L1-L5 的隔离声明（"运行时不会向你提供其他层的当前数据、结论或状态"，L1:814/L2:1263×2/L3:646/L4:1191/L5:860）与其实际材料一致，未见矛盾（3.2 节）。
5. controlled_investigation inv_ae806095c18a 与 inv_c4c2b3b067ff：声明"你不知道系统当前的判断"（第 5 行）与 Bridge memo 实物同处一份提示词——含系统核心矛盾（36 行）、主导方向 dominant_side（38 行）、仓位指令 action_implication（41 行）、"触发核心仓进一步减仓"（58 行）等；矛盾成立（3.1 节 a/b）。
6. controlled_investigation inv_e75efe82e7c0（两 attempt）：同声明，但材料为事件层开放问题与待验证状态，未见系统市场结论/仓位指令实物（3.1 节 c）。
7. 同一 Bridge memo 文本"触发核心仓进一步减仓；估值将从'中等估值但脆弱'转向'高估值压缩中'"另见于 thesis:879/1317、counter_thesis:610/1048/5187、critic:876、risk:902、reviser:964、final_adjudicator:1016；近义串"核心仓须进一步减仓；Forward PE将从19.46向Trailing PE 30.31靠拢……"另见于 risk:608、critic:582、reviser:670、final_adjudicator:722、integrated_adjudicator:77；这些站均无"你不知道系统判断"式声明（3.1 节 d、3.2 节）。
8. "你不知道系统当前的判断"类声明在全 25 站中仅出现于 controlled_investigation（3.2 节）。
