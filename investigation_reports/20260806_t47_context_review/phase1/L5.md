# L5 通读报告

站：L5（l5_analyst，价格趋势/动量/波动/成交量分析层）；run：20260731_002156；模型：deepseek-v4-flash；attempt 1（共 1 次）。

## 通读范围

- 主材料：`output/analysis/vnext/20260731_002156/context_spread/projected/L5/attempt_1.projection.md`，1997 行 / 82,691 字节，**全文逐行通读**（分 7 段连续读完，未抽查、未跳段）。
- 原文：`context_spread/full/L5/attempt_1.prompt.txt`，1939 行 / 80,187 字节（64,613 字符）。投影覆盖率 100.1%（manifest.json：prompt_chars=64,613，projection_chars=64,668），全投影只有 **1 处截断标记**（投影行 1139 的 summary 字符串中段 «…省略 69 字符…»），该处已回原文核对（见发现 F-09 证据）。
- 辅助文件：`context_spread/full/L5/meta.json`（39 行，全读）；`context_spread/manifest.json`（用 python 提取 L5 实例条目）。
- 读法：先读投影头部"段落目录"建立材料分布图，再按行序通读；发现候选问题后用 grep/python 在 `full/L5/attempt_1.prompt.txt` 原文复核，所有数值矛盾均以原文行号为准。
- 未读文件（与"提示词材料结构"无关，仅产出侧）：`attempt_1.response.raw.txt`、`attempt_1.parsed.normalized.json`、`attempt_1.payload.json`、`output.validated.json` —— 本任务审查的是输入材料结构，产出文件只在个别核对时参考（实际未用到）。

## 材料结构总览

依据投影头部"段落目录"（投影行 6-40，字符数以目录标注为准，合计 64,613 与原文字符数一致）：

- **运行时数据 `## Runtime Input`：31,423 字符（48.6%）**。python 解析原文该 JSON 块实测 31,404 字符，内部构成：
  - `layer_raw_data`：24,405 字符，11 个指标完整记录。其中每个指标内嵌一份 `data_quality` 元数据（合计 9,673 字符），真正的指标数值 `value` 合计仅 6,744 字符；snapshot 一条的 value 占 3,940 字符（含 30 日收盘价序列 recent_closes_30）。
  - `layer_facts`：4,335 字符，其中 `core_signals` 8 条（3,658 字符）——是 raw_data 数值的"摘要字符串+value 复写"版。
  - `context_brief`：599 字符，含 `layer_highlights` 3 条摘要字符串（同一批数值的第三种形态）。
- **规则/任务书类：约 18,472 字符（28.6%）**，含：System 约束 333；Deep Research Canon 8,706（其中 IndicatorCanon for L5 单段 7,608，11 个指标的发言权规则）；4C few-shot 范例 1,914（4 个例子）；Layer Contract 散文部分 2,542；L5 角色指令各节 3,143；输出字段规格 1,435；Response Rules 365。
- **`### 当前层指标清单`：14,718 字符（22.8%）**。实测 JSON 14,704 字符，11 个指标条目，**不含任何指标数值**（带 `value` 键的条目数=0），其中 9,673 字符是 `data_quality` 块——与 `layer_raw_data` 里的 11 份 `data_quality` **逐字节完全相同**（11/11 条 json 排序后相等）。

材料分布的一句话概括：近一半篇幅是运行时数据；约 23% 是一份没有数值、只有采集元数据的"指标清单"；规则约束占约 29%。同一批采集元数据（data_quality）在全文出现 22 份（11 指标 × 2 处），合计约 19,346 字符，约占全文 30%。

## 发现清单

以下行号均为 `context_spread/full/L5/attempt_1.prompt.txt`（共 1939 行）的行号；`proj:` 指投影文件行号。所有命令在 `context_spread/full/L5/` 下执行，可复跑。

### F-01 同一指标 OBV 在材料内有两个不同的值，且规则指定以"值不同"的那一份为准

- 事实：`get_l5_deterministic_snapshot` 快照里的 OBV = **670,737,927**；而 `get_obv_qqq` 分项指标、`get_qqq_technical_indicators` 内嵌 OBV、`layer_facts.core_signals` 摘要三处均为 **353,656,127**。两者相差约 1.9 倍。这是 14 项跨块数值核对中唯一不一致的一项（其余 13 项——RSI/MACD×3/ATR/ADX×3/volume_ma_ratio/VWAP/MFI/CMF/Donchian 上轨——三处全部逐字节相同）。
- 规则冲突：canon 明确说 OBV 数值"优先引用本快照"（行 52：`"凡是精确价格、均线、RSI、MACD、ATR、ADX、OBV、VWAP/MFI/CMF 或 Donchian 数值，优先引用本快照。"`），即规则要求以 670,737,927 为准；但另外两个分项指标（本身也是 analysis_required=true、需要逐一分析的指标）携带的是 353,656,127，且 `get_obv_qqq` 的 20 日变化率 -45.32% 挂在 353,656,127 这一份上。材料没有给出任何调和说明。
- 证据：命令 `grep -n '670737927\|353656127' attempt_1.prompt.txt`，输出：
  ```
  1086:        "value": 353656127,
  1092:        "summary": "QQQ OBV (On-Balance Volume) | 值=353656127 | 趋势=distribution"
  1192:          "obv": 670737927,
  1393:        "obv": 353656127,
  1649:        "level": 353656127,
  ```
  （1192 行为 snapshot `exact_technical_values.obv`；1086/1092 为 core_signals；1393 为 get_qqq_technical_indicators；1649 为 get_obv_qqq。）
- 交叉核对命令：`python3` 解析 Runtime Input JSON 逐字段比对（脚本见 F-04 证据），输出 `obv: snapshot=670737927 subindicator=353656127 tech_indicators=353656127 *** DIFF ***`，其余 13 项均 `SAME`。

### F-02 材料内并存四套日期口径：数据日 07-29、简报日 07-30、快照 effective_date 07-31、站元数据 effective_date 07-30

- 事实：
  - 全部 11 个指标的实际数据日期为 2026-07-29（`data_date`/`as_of_date`/各 `value.date`）。
  - `context_brief.data_summary` 写"**数据日期 2026-07-30**，L5 本层 11/11 个指标成功"（行 966），与材料里每一处 data_date=2026-07-29 不一致。
  - `get_l5_deterministic_snapshot` 声明 `effective_date: 2026-07-31`，出现 5 次（行 409、1031、1127、1137、1336）；其余 10 个指标的 `data_quality.effective_date` 均为 2026-07-29（出现 20 次）。同一份提示词里"effective_date"这个键有两种值。
  - 站级元数据 `full/L5/meta.json:13` 记 `"effective_date": "2026-07-30"`——第三个值，但该值不在提示词正文里。
- 附带张力：canon 给快照定的 falsifier 是"快照数据日期晚于 effective_date……"（行 66），用于识别快照异常；本材料里实际是快照自带的 effective_date（07-31）晚于站级 effective_date（07-30）和数据日（07-29），方向相反，材料未解释。
- 证据：命令 `grep -n '"effective_date"' attempt_1.prompt.txt`（输出 25 行，07-31×5、07-29×20，见上方 grep 输出列表）；命令 `grep -n '数据日期' attempt_1.prompt.txt`，输出 `966:    "data_summary": "数据日期 2026-07-30，L5 本层 11/11 个指标成功。",`；`full/L5/meta.json:13`：`"effective_date": "2026-07-30",`。

### F-03 上游预计算标签 `state: "uptrend"` 与材料内几乎全部方向性数值相矛盾

- 事实：`layer_facts` 携带预计算的 `"state": "uptrend"`（行 1110）和 `"summary": "趋势与波动状态: uptrend。关键事实: …"`（行 1118）。但同一份材料里的方向性证据几乎全指向下跌：
  - ADX：`"trend": "down"`（行 993，core_signals）、`"direction": "down"`（行 1556，raw_data），mdi=37.18 远高于 pdi=12.49；
  - MACD：`"status": "bearish"`（行 1050、1604），三线全负；
  - Donchian：`"status"/"signal": "near_lower"`（行 1020、1404、1796），close 661.73 距下轨 661.14 仅 0.59；
  - OBV：`"trend": "distribution"`（行 1088、1651），20 日 -45.32%；
  - 均线：快照 `moving_average_positions` 中价格低于 sma_5/20/50/60/100（偏离 -2.56%/-6.05%/-7.55%/-7.26%/-1.77%），仅高于 sma_200（+2.73%）（行 1150-1175 区间，proj:1208-1233 逐字一致）；
  - 30 日收盘序列：2026-06-16 收 729.86 → 2026-07-29 收 661.73，区间 -9.33%（python 实测：`recent_closes_30 n= 30 first= {'date': '2026-06-16', 'close': 729.86} last= {'date': '2026-07-29', 'close': 661.73} pct= -9.33`）；
  - `layer_highlights` 选出的 3 条亮点（ADX 趋势=down、ATR、Donchian near_lower，行 967-972）本身也偏空。
- 任务关联：L5 的任务书（Layer Synthesis 节，行 914）要求它回答"L5 是 uptrend、strong_uptrend、sideways、downtrend 还是 overextended_uptrend"，而输入里已预置了一个与数值相左的 `uptrend` 标签。
- 证据：命令 `grep -n '"state"\|uptrend' attempt_1.prompt.txt`，输出含 `1110:    "state": "uptrend",`、`1118:    "summary": "趋势与波动状态: uptrend。…"`；方向性证据行号见上列 grep 结果（行 993/1020/1050/1088/1556/1604/1651/1796）。

### F-04 结构性冗余（一）：同一份 data_quality 元数据出现 22 份、约 1.9 万字符，且每条指标的方法论同一句话写三遍

- 事实：
  - `data_quality` 块在全文出现 **22 次**（11 个指标 × 2 处：`### 当前层指标清单` 一份、`layer_raw_data` 一份）。实测清单侧 11 份合计 9,673 字符，与 raw_data 侧 11 份**逐字节完全相同**（11/11 条 JSON 排序后相等）。即同一段采集元数据纯复制一遍，约 9,673 字符 ≈ 全文的 15.0%；两侧合计约 19,346 字符 ≈ 全文的 29.9%。
  - 每个指标内 `"methodology"`、`"formula"`、`"notes"` 三个键的值**完全相等**（11/11 条为 True），同一句话在清单处写 3 遍、在 raw_data 处再写 3 遍，单条指标最多 6 份。`methodology`/`formula`/`notes` 各出现 22 次。
  - `anomalies` 四项（missing_source_url、license_note_defaulted、vintage_date_not_available、coverage_missing_or_unspecified）在 22 份块中完全相同；`missing_source_url`、`public_endpoint_review_required`、`"source_url": "missing"` 各出现 22 次。
  - `### 当前层指标清单`（14,718 字符，占 22.8%）整条目不含任何指标数值（带 `value` 键的条目数=0），正文指令中未见要求模型消费该清单的字段（指令只要求"对每一个 analysis_required=true 的指标输出一条原生分析"）。
- 证据：命令 `python3 -c "raw=open('attempt_1.prompt.txt').read(); ..."` 计数，输出：`"data_quality" -> 22`、`"methodology" -> 22`、`"formula" -> 22`、`"notes" -> 22`、`missing_source_url -> 22`；解析比对输出：`清单 data_quality total: 9673`、`raw_data data_quality total: 9673`、`清单/raw_data data_quality 完全相同条数: 11`、`清单 entries with value key: 0`、每指标 `method==formula==notes:True`（11 行）。

### F-05 结构性错配：契约里的"结构示例"和 local_conclusion 示例都是其他层的范例（L1 利率指标、L4 估值口吻）

- 事实：
  - `### 结构示例`（行 826-851 区间）用的完整示例指标是 `get_10y_real_rate`（L1 宏观利率），示例内含 `"evidence_refs": ["L1.get_10y_real_rate"]`、`"cross_layer_implications": ["需要 L4 验证估值是否已反映高实际利率"]`、`"risk_flags": ["valuation_compression"]`、`"covered_function_ids": ["get_10y_real_rate"]`——全部是 L1/L4 语境。
  - `local_conclusion` 的填写示例（行 370）是"估值处于历史高位但盈利韧性提供部分支撑"——L4 估值口吻。
  - 而 L5 自身职责（行 862-864，Context Boundary 节）明确"L5 不判断估值是否合理"，Output Discipline（proj:1017）要求"不得因趋势强合理化估值；估值属于 L4"。示例的语域与 L5 被允许的发言范围不一致。
- 证据：命令 `grep -n 'get_10y_real_rate\|估值处于历史高位' attempt_1.prompt.txt`，输出：
  ```
  370:- local_conclusion: 必填字段，最多500字符，本层最核心的一句结论（例如"估值处于历史高位但盈利韧性提供部分支撑"）；…
  832:      "function_id": "get_10y_real_rate",
  839:      "evidence_refs": ["L1.get_10y_real_rate"],
  847:    "covered_function_ids": ["get_10y_real_rate"],
  ```

### F-06 同一指标在两处形态不同：`core_signals` 版 multi_scale_ma_position 只有 3 个均线尺度，raw_data 版有 4 个，自述方法也是 4 个

- 事实：`layer_facts.core_signals` 里 `get_multi_scale_ma_position` 的 `value.ma_positions` 只有 ma5/ma20/ma60 三个尺度（行 1058-1075），其 `summary` 字符串同样只列 3 个尺度（行 1081）；而 `layer_raw_data` 同一指标的 `ma_positions` 有 ma5/ma20/**ma60/ma200** 四个尺度并附 `ma_order`、`cross_scale_divergence`（行 1839-1873，ma200 见行 1856、1862）。该指标自述 methodology 为"**4尺度**MA分析：MA5(1周)/MA20(1月)/MA60(1季)/MA200(1年)"（行 810 等处）。ma200（当前唯一偏离为正的均线，+2.73%）在 core_signals 形态里静默缺失。
- 证据：命令 `sed -n '1055,1080p' attempt_1.prompt.txt`（core_signals 版，ma_positions 只到 ma60）；命令 `grep -n 'ma200' attempt_1.prompt.txt`，输出 `1856:          "ma200": {`、`1862:          "ma200",`（均在 raw_data 版）。

### F-07 `当前层指标清单` 里 9/11 个指标的顶层 `date` 为 null，而同指标在 raw_data 里的 `value.date` 都是 2026-07-29

- 事实：清单 11 条中仅 `get_l5_deterministic_snapshot`、`get_qqq_technical_indicators` 顶层有 `"date": "2026-07-29"`，其余 9 条 `"date": null`（行 482、521、560、599、638、677、716、755、794）。同 9 个指标在 `layer_raw_data` 里的 `value.date` 均填了 `"2026-07-29"`（如行 1520 get_rsi_qqq、1565 get_atr_qqq、1616 get_adx_qqq 等）。同一份提示词内，同一指标在一个块里"无日期"、在另一个块里"有日期"。
- 证据：命令 `grep -n '"date": null' attempt_1.prompt.txt`（9 行输出，行号如上）；对照 `sed -n '1515,1521p'`：`"value": {"level": 32.4, "status": "neutral", "date": "2026-07-29"}`。

### F-08 结构性冗余（二）：同一批指标数值以三~四种形态重复，且"重点指标"有四个口径不一的子集

- 事实：同一批指标数值在材料里以如下形态重复出现：
  1. `context_brief.layer_highlights`：3 条摘要字符串（行 967-972）；
  2. `layer_facts.core_signals`：8 条 `value` 复写 + `summary` 字符串（3,658 字符）；
  3. `layer_facts.summary`：把 highlights 的 3 条摘要再拼接一遍（行 1118）；
  4. `layer_raw_data`：11 条完整记录（24,405 字符），其中 snapshot 的 `exact_technical_values` 又汇总了其余 10 个分项指标的数值——14 项可比对字段中 13 项逐字节相同（唯一例外即 F-01 的 OBV）。
- 重点子集口径：`analysis_required=true` 11 个指标 → `core_signals` 收 8 个 → `key_metrics` 列 5 个（行 1111-1117）→ `layer_highlights` 选 3 个。四个子集的筛选标准在材料中无说明。
- 附带事实：`core_signals` 里的 snapshot 条目（行 1026-1032）`value` 只有信封字段 `{schema_version, symbol, effective_date}`，不带任何技术数值——"关键信号"集合里放了一条空信封。
- 证据：`python3` 解析输出：`highlights n= 3`、`key_metrics= [adx, atr, donchian, snapshot, macd]`、`core_signals metrics= [adx, atr, donchian, snapshot, macd, multi_scale, obv, pvq]`、`core_signals chars: 3658 n= 8`；sed/grep 行号如上。

### F-09 `manual_overrides` 块携带一个 2022 年的陈旧日期占位

- 事实：Runtime Input 末尾 `"manual_overrides": {"active": false, "date": "2022-01-04", "metrics": {}}`（行 1913-1916）。在一份 effective_date 为 2026-07 的材料里出现一个 2022-01-04 的日期字段（虽然 active=false、metrics 为空）。材料中无任何说明。
- 证据：命令 `grep -n '2022-01-04' attempt_1.prompt.txt`，输出 `1915:    "date": "2022-01-04",`。

### F-10 canon 与 Indicator Semantics 对"必须优先以快照为准"的指标列举不一致（canon 含 OBV，semantics 不含）

- 事实：canon（行 52）列举"精确价格、均线、RSI、MACD、ATR、ADX、**OBV**、VWAP/MFI/CMF 或 Donchian"；Indicator Semantics（行 882）列举"最新收盘价、均线、RSI、MACD、ATR、ADX、VWAP/MFI/CMF、Donchian"——无 OBV。两处规则文字对同一优先权的覆盖范围不一致，且恰与 F-01 中值不一致的指标（OBV）重合。
- 证据：命令 `grep -n '凡是精确价格\|必须优先以它为准' attempt_1.prompt.txt`，输出：
  ```
  52:      "凡是精确价格、均线、RSI、MACD、ATR、ADX、OBV、VWAP/MFI/CMF 或 Donchian 数值，优先引用本快照。",
  882:- `get_l5_deterministic_snapshot`: …凡是最新收盘价、均线、RSI、MACD、ATR、ADX、VWAP/MFI/CMF、Donchian 这类精确数值，必须优先以它为准；…
  ```

### F-11 正面事实：L1-L5 隔离纪律在本站材料层面成立，未发现跨层运行时数据混入

- 事实（不是缺陷，如实记录）：整份提示词不含其他层的运行时数值、结论或状态；`context_brief.apparent_cross_layer_signals` 为空数组（行 974）；其他层指标 id（get_vxn、get_hy_oas_bp、get_ndx_pe_and_earnings_yield、get_advance_decline_line、get_percent_above_ma）只出现在 canon 的静态 `cross_validation_targets` 路由字段里（行 62-265 区间）；`静态五层本体`段（proj:403-410）自带"不代表当前状态"声明。结构示例虽是 L1 语境（见 F-05），但它是格式示例，不含其他层当前数据。
- 证据：命令 `grep -n 'get_vix\|get_vxn\|get_hy_oas\|get_ndx_pe\|get_advance_decline\|get_percent_above_ma\|get_10y' attempt_1.prompt.txt`（命中行均在 canon 路由字段与结构示例内）；`grep -n 'apparent_cross_layer_signals' attempt_1.prompt.txt` 输出 `974:    "apparent_cross_layer_signals": [],`。

### F-12 输入侧 trend 词表（"down"/"distribution"）与输出规格 trend 枚举（rising/falling/stable/volatile）不一致，无映射说明

- 事实：输入 `core_signals` 的 `trend` 字段取值为 `"down"`（行 993）、`"distribution"`（行 1088、1651）或 null；而 `## 输出字段规格` 要求 `core_facts[].trend` 只能取 `"rising"|"falling"|"stable"|"volatile"`（行 1923）。材料没有说明输入标签到输出枚举的映射关系（如 down→falling、distribution→?）。
- 证据：命令 `grep -n '"trend":\|"rising"' attempt_1.prompt.txt`，输出含 `993:        "trend": "down",`、`1088:        "trend": "distribution",`、`1923:- \`core_facts\`（必填）：… trend:"rising"|"falling"|"stable"|"volatile" …`。


## 盲区

- **投影截断核对情况**：全投影只有 1 处截断标记（proj:1139，core_signals 里 multi_scale_ma_position 的 summary 字符串中段 «…省略 69 字符…»）。已回原文核对（行 1081）：省略内容是 `'deviation_pct': -2.56}, 'ma20': {'value': 704.33, 'deviation_pct':` 片段，这些数值在同条目 value（行 1058-1075）和 raw_data（行 1839-1873）中完整存在，无独有信息损失。除该处外无其他截断，投影覆盖率 100.1%（manifest.json 实测 prompt_chars=64,613 / projection_chars=64,668）。
- **未读的产出侧文件**：`attempt_1.response.raw.txt`、`attempt_1.parsed.normalized.json`、`attempt_1.payload.json`、`output.validated.json` 未通读。本任务边界是"输入材料结构"，模型实际如何应对这些矛盾（例如 OBV 最终引用了哪个值、state=uptrend 是否被照单全收）不在本阶段范围，故未核对产出与输入的对应关系。
- **未做的核对**：没有核对 Twelve Data 的真实 OBV 数值（两个值哪个对需要站外数据，超出本任务）；没有追溯 `state="uptrend"`、`data_summary="数据日期 2026-07-30"`、snapshot `effective_date="2026-07-31"` 在上游代码里的生成逻辑（src/ 只读且不属于本阶段）；没有跨站比对（其他站是否也有相同的 22 份 data_quality 冗余、L1 结构示例是否通用模板，需等其他通读片结果）。
- **理解不了/未深究**：`manual_overrides.date="2022-01-04"` 的来源与含义无法从材料内推断；`layer_highlights`/`key_metrics`/`core_signals` 三个子集的筛选标准在材料内无说明，无法判断是否有意为之。
- **行号精度说明**：发现清单中的行号以 `full/L5/attempt_1.prompt.txt` 为准；区间型行号（如"行 1150-1175 区间"）是 grep 多点定位后的范围估计，未逐行展开该区间全部内容，但投影对应区段（proj:1208-1233）已逐字读过并与原文一致。
