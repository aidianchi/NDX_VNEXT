# L1 通读报告

## 通读范围

- 站：L1（attempt_1），run 20260731_002156。语料根：`/Users/aidianchi/Desktop/ndx_mac/output/analysis/vnext/20260731_002156/context_spread/`
- 主材料：`projected/L1/attempt_1.projection.md`，2,670 行 / 106,841 字节。文件头自述：原文 87,691 字符，投影 87,569 字符（99.9%）。
- 读法：投影**逐行全读**（2670/2670 行，分 4 段）。投影内仅 1 处省略标记（`«…省略 130 字符…»`，出现于同一字符串的两份拷贝），已回查 `full/L1/attempt_1.prompt.txt` 补齐原值（见盲区节）。
- 辅助材料：`manifest.json`（L1 条目：`inspector_has_rules: true`、`inspector_can_read: true`、`verification_ok: true`，即该站此前**已有**机械检查规则）、`full/L1/meta.json`（model=deepseek-v4-flash，effective_date=2026-07-30，status=ok）、`full/L1/attempt_1.payload.json`（用于精确测量 Runtime Input 各块字符量）。
- 取证方式：所有量化结论均由可复跑命令得出，命令与关键输出照抄于各发现条目内。工作目录均为语料根，下文不再重复。

## 材料结构总览

按投影头部"段落目录"（`projected/L1/attempt_1.projection.md:6-40`）：

- **纯规则/任务书散文** ≈ 18,400 字符（约 21%）：System 约束 333、Canon 引导 100、ObjectCanon 767、Canon Output Hints 231、4C few-shot 引导 90、v2 契约散文（159+381+892+124+82+904）、L1 角色提示词十节（46+237+190+227+643+488+226+216+326+240+251）、输出字段规格 1,435、Response Rules 365。
- **指标语义规则（JSON 形态）**：IndicatorCanon for L1 6,893 字符（7.9%）+ 4C few-shot 四例 2,513 字符。
- **随规则段携带的数据**：`### 当前层指标清单` 14,996 字符（17.1%）——名为"清单"，实为 9 个指标各带一整块 data_quality。
- **Runtime Input** 54,302 字符（61.9%）：context_brief、layer_facts（预消化摘要层）、layer_raw_data（9 指标原始载荷）、manual_overrides。

即数据类内容合计约 79%，其中 Runtime Input 占绝对大头；而 Runtime Input 内部又严重偏向单一指标：`get_fed_funds_rate_path` 一个指标占 layer_raw_data 的 60%（测量见发现 2）。

补充事实（meta/manifest）：该站由 `layer_context_briefs/L1.json` + `analysis_packet.raw_data.L1` 两个 artifact 组装而成（`full/L1/meta.json` 的 `input_artifacts`），提示词内可见的四段（canon、few-shot、契约、角色词）+ Runtime Input 即组装结果。

## 发现清单

### 发现 1：每个指标的 data_quality 块在站内出现两次，合计约 1.9 万字符（约 22%）

**事实**：9 个指标的 `data_quality` 块以近乎逐字相同的形态出现两份：一份在契约段 `### 当前层指标清单`（projected/L1/attempt_1.projection.md:396-821），一份在 Runtime Input 的 `layer_raw_data.<function_id>.data_quality`（同文件 1112-2642 各处）。指令中没有任何一处说明两份的关系或该消费哪一份。

**证据**：

```
$ grep -c 'data_evidence_v1' full/L1/attempt_1.prompt.txt
18          # = 9 指标 × 2 份
```

```
$ python3（解析 payload.json 与提示词清单段）
当前层指标清单: 9 项, 整段=12332 字符, 其中 data_quality=9549 (77%)
layer_raw_data 合计=31443, data_quality 合计=9549 (30%)
```

同一块内容 9,549 字符 × 2 ≈ 19,098 字符，占全提示词（87,691）约 21.8%。此外每个指标的方法学字符串以 `notes`（清单）、`data_quality.methodology`、`data_quality.formula`（清单）+ `notes`（raw）、`data_quality.methodology`、`data_quality.formula`（raw）六种形态重复：

```
$ grep -c '10Y-2Y 利差；分层降噪' full/L1/attempt_1.prompt.txt
6
```

### 发现 2：`get_fed_funds_rate_path` 一个指标占 layer_raw_data 的 60%，其中 10,828 字符的逐日 raw_series 没有任何指令要求消费

**事实**：Runtime Input 的 layer_raw_data 共 31,443 字符（JSON 紧凑口径），`get_fed_funds_rate_path` 单块 18,921 字符（60%）。其内部 `value.raw_series`（13 个月度合约 × 每个 10 个交易日的 close/volume，projected/L1/attempt_1.projection.md:1384-2178）占 10,828 字符；而指令（Indicator Semantics、Mechanism Grammar、canon 的 interpretation_rules）只引用 `path`/`slope_12m`/`cuts_priced_bps`/`state`/`horizon_used` 这些已聚合字段（`path` 数组本身仅 2,777 字符）。没有任何指令要求模型读取或重算逐日 close/volume。且每个合约的 `avg_volume_10d` 已在 `path[]` 与 `raw_series[]` 中各出现一次。

**证据**：

```
$ python3（解析 full/L1/attempt_1.payload.json）
get_fed_funds_rate_path: 整块=18921, 其中 data_quality=2853
（其余 8 个指标整块均≤1820 字符）
  path: 2777 字符
  raw_series: 10828 字符
```

```
$ python3 -c "...txt.count('\"avg_volume_10d\"')..."
avg_volume_10d 出现次数: 27     # = path[13] + raw_series[13] + front_month[1]
```

### 发现 3：同一键 `percentile_10y` 在不同指标间使用两种量纲（0-1 与 0-100），无标注

**事实**：`layer_raw_data` 内，6 个指标的 `value.relativity.percentile_10y`（net_liquidity/copper_gold 为 `value.historical_stats.percentile_10y`）是 0-1 小数，而 `get_fed_funds_rate` 与 `get_m2_yoy` 同键是 0-100 百分数。预消化层 `layer_facts.core_signals[].historical_percentile` 全部折算成 0-100（52.90…=0.529×100 等），few-shot 示例的 INPUT 也用 0-100（5.2、94.0、8.0）。同一键名、两种量纲、无单位或口径说明。

**证据**：

```
$ grep -n '"percentile_10y"' full/L1/attempt_1.prompt.txt
290:...few-shot INPUT..."percentile_10y": 5.2
296:...few-shot INPUT..."percentile_10y": 94.0
302:...few-shot INPUT..."percentile_10y": 8.0
1043:          "percentile_10y": 0.5290116046418567,      # get_10y2y_spread_bp
1102:          "percentile_10y": 75.4,                  # get_fed_funds_rate
2248:          "percentile_10y": 42.7,                  # get_m2_yoy
2312:          "percentile_10y": 0.5795236791678072,    # net_liquidity
2362:          "percentile_10y": 0.17827298050139276,   # copper_gold
2434:          "percentile_10y": 0.9779911964785915,    # 10y_treasury
2488:          "percentile_10y": 0.9935974389755903,    # 10y_real_rate
2542:          "percentile_10y": 0.5850340136054422,    # 10y_breakeven
```

### 发现 4：context_brief 声称"数据日期 2026-07-30"，但 9 个指标的 data_date 无一为 2026-07-30（最近 07-29，最旧 2026-06-01）

**事实**：`context_brief.data_summary` 写"数据日期 2026-07-30，L1 本层 9/9 个指标成功。"（projection:972）。而逐指标 data_quality 的 data_date/as_of_date：5 个指标为 2026-07-29，2 个为 2026-07-28（10Y 名义、实际利率），2 个为 2026-06-01（联邦基金利率、M2——比声明日期旧约两个月）。2026-07-30 对应的是 `collected_at_utc`（采集时刻）与 meta.json 的 run 级 `effective_date`，不是任何指标的数据日期。此外 `get_fed_funds_rate_path` 的 `effective_date` 为 2026-07-31（value 与两份 data_quality 共 3 处），晚于 context_brief 声明的"数据日期 2026-07-30"，也晚于其自身 data_date 2026-07-29——同一提示词内存在 4 种 effective_date 取值。

**证据**：

```
$ grep 统计（full/L1/attempt_1.prompt.txt，剔除 raw_series 观测行）
data_date/as_of_date 分布：2026-07-29 ×5 指标、2026-07-28 ×2、2026-06-01 ×2（各 ×2 份拷贝）
"data_summary": "数据日期 2026-07-30，L1 本层 9/9 个指标成功。"  ×1

$ python3 统计 effective_date 取值
{'2026-07-29': 8, '2026-06-01': 4, '2026-07-31': 3, '2026-07-28': 4}
```

### 发现 5：预消化摘要层（layer_facts/core_signals/layer_highlights）既有损又预先给出了模型被要求自己得出的答案

**事实**，四点：

1. `layer_facts.core_signals` 只覆盖 8/9 个指标，缺的恰是 `get_fed_funds_rate_path`——该指标在清单中 `analysis_required: true`，契约要求"每个 analysis_required=true 的指标必须有一条 indicator_analyses"。摘要层静默漏掉了一个必分析指标，只有 layer_raw_data 里才有它。
2. 摘要层 3 个指标 `trend: null`（copper_gold、m2、net_liquidity，projection:1039/1061/1072），而原始载荷里有方向信息（如 copper_gold `position_vs_ma50: "above"`，projection:2425）。
3. `layer_facts.state = "restrictive"`、`summary = "宏观流动性状态: restrictive。关键事实: …"`（projection:1079/1087）——而角色词 `## Layer Synthesis` 要求模型自己回答"L1 总体是 restrictive、neutral、expansionary 还是混合状态"（projection:918-925）。输入材料已把这道题的答案词（restrictive）连同成稿句式一并给出。`key_metrics` 也预选了 5 个"关键"指标（projection:1080-1086）。
4. 同一条摘要串在站内出现 3 次（`context_brief.layer_highlights.L1`、`core_signals[].summary`、`layer_facts.summary`）：

```
$ grep -c '10Y-2Y Treasury Spread | 值=45.0' full/L1/attempt_1.prompt.txt
3

$ python3（core_signals 覆盖核验）
core_signals 指标数: 8
缺失: {'get_fed_funds_rate_path'}
```

### 发现 6：3/9 个指标在 canon 与清单/raw 中各有一个不同的 `metric_name`，契约却要求"metric 必须优先等于输入 metric_name"

**事实**：契约写"indicator_analyses[].metric 必须优先等于输入 metric_name"（projection:377），但提示词内有两个都自称 `metric_name` 的来源，且 3 个指标取值不同：

- IndicatorCanon（projection:84-321）：`10Y-2Y Spread`、`M2 YoY`、`Net Liquidity Momentum`
- 当前层指标清单 / layer_facts / layer_raw_data：`10Y-2Y Treasury Spread`、`M2 YoY Growth`、`Net Liquidity (Fed - TGA - RRP)`

其余 6 个指标两来源一致。

**证据**：

```
$ python3 统计全提示词 "metric_name" 取值
  1x 10Y-2Y Spread            （仅 canon）
  3x 10Y-2Y Treasury Spread   （清单 + layer_facts + raw）
  1x M2 YoY / 3x M2 YoY Growth
  1x Net Liquidity Momentum / 3x Net Liquidity (Fed - TGA - RRP)
```

### 发现 7：9 个指标的语义/机制在三个不同段落各讲一遍，内容重叠但不逐字相同

**事实**：同一组 9 个指标的"它是什么、能证明什么"在站内有三份描述：① `### IndicatorCanon for L1`（6,893 字符，含 interpretation_rules/misread_guards/b_prompt，projection:84-321）；② 角色词 `## Indicator Semantics`（643 字符，每指标一句话，projection:892-902）；③ 角色词 `## Mechanism Grammar`（488 字符，每指标一条机制链，projection:904-916）。三者口径一致但措辞不同，均未声明以谁为准。另：canon 各条的 `cross_validation_targets` 指向其他层指标（如 `get_hy_oas_bp`、`get_vix`、`get_qqq_technical_indicators`、`get_ndx_pe_and_earnings_yield` 等，projection:100-104、154-159、284-288），即"需要谁验证"指向的是本层运行时看不到的数据——与隔离纪律配合时只能落成 cross_layer_hooks，提示词未明说这一对应关系。

### 发现 8：站内有两套示例系统，虚构数值的警示只挂在一套上

**事实**：① `## Layer-Local 4C Few-Shot Examples`（四例，合计约 2,513 字符，projection:328-359）开头明确写"不要复制具体数值，只学习推理结构与叙事密度"（projection:329），示例 INPUT 用虚构值（如 -55.2bp、5.25%、-1.8%、5200B）。② 契约段 `### 结构示例`（904 字符，projection:823-863）无此警示，其 `current_reading` 写"实际利率 1.95%，处于高位并上行"（projection:829）——恰好对应真实在跑的 `get_10y_real_rate`，而该指标本次真实值为 2.41%（projection:1016/2539），分位 99.4。虚构示例与真实指标同名同义、数值不同，且无"不要复制"标注。

### 发现 9：净流动性的单位字符串有两种写法

**事实**：`layer_raw_data.get_net_liquidity_momentum` 顶层 `"unit": "USD Billions"`（projection:2376），其 `value` 内部却是 `"level_unit": "billion_usd"`、`"components_unit": "billion_usd"`、`component_units` 逐分量 `billion_usd`（projection:2353-2367）。同一数量两种单位拼写并存；`### 数值单位纪律` 要求"引用金额/规模数值时必须带上 payload 中的 unit 单位；payload 无单位标注时不得猜测单位"（projection:393-394），未说明两级 unit 不一致时以哪个为准。

### 发现 10：Runtime Input 尾部带一个 2022 年的占位日期

**事实**：Runtime Input 末尾 `"manual_overrides": {"active": false, "date": "2022-01-04", "metrics": {}}`（projection:2644-2648）。块处于停用状态，但携带一个形如数据日期的 "2022-01-04" 字符串，与本批数据日期（2026 年）相差 4 年半，提示词未说明该字段含义。

## 盲区

1. **投影唯一省略处已回查补齐**：`get_fed_funds_rate_path` 的 `data_quality.methodology` 中段 130 字符（该串在清单与 raw 两处各省略一次，projection:519/2236）。已在原文取回完整 310 字符：
   ```
   For each of 13 monthly ZQ contracts from the effective-date month: select the latest close on/before effective_date, implied_rate=100-close, average the available last 10 daily volumes, exclude avg_volume<5, retain 5<=volume<100 as thin, then slope=farthest qualified implied rate-front qualified implied rate.
   ```
   补齐后不改变任何发现。除此之外投影无其他截断（`__omitted__`/`__table_projection__` 标记 0 处）。
2. **raw_series 的算术未核算**：13 合约 × 10 条逐日 close/volume 我逐行过完，但没有重算 `avg_volume_10d` 是否等于 10 条 volume 的均值、`close` 与 `path[]` 结算价是否逐日一致——这属于数据内容核算，本次只审"材料结构"。如需可补跑。
3. **字符量口径**：发现 1/2 的字符数用 JSON 紧凑序列化（`json.dumps`）测得，与投影目录的"段落字符"（含缩进）口径不同；两种口径在文中均已注明，不可混用相加。
4. **未读响应侧文件**：`attempt_1.response.raw.txt`、`output.validated.json`、`parsed.normalized.json` 未读——本任务只审输入材料结构，不评价站产出。
5. **manifest 其他站条目未看**（边界纪律），仅提取了 L1 条目。
