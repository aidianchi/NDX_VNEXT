# event_card_interpreter.event_7392b9f2aa06f502 通读报告

> T47 上下文根本性审查（阶段一·逐站通读）· 单站报告
> run：20260731_002156 ｜ 站族：event_card_interpreter ｜ manifest 标注：`inspector_has_rules=false`（两个 attempt 此前均无机械检查规则），`verification_ok=true`

## 通读范围

语料根目录：`/Users/aidianchi/Desktop/ndx_mac/output/analysis/vnext/20260731_002156/context_spread/`

| 文件 | 行数 | 字节 | 读法 |
|---|---|---|---|
| `projected/event_card_interpreter.event_7392b9f2aa06f502/attempt_1.projection.md` | 146 | 8,748 | 全读 |
| `projected/event_card_interpreter.event_7392b9f2aa06f502/attempt_2.projection.md` | 166 | 9,639 | 全读 |
| `full/event_card_interpreter.event_7392b9f2aa06f502/attempt_1.prompt.txt` | 130 | 8,006 | 全读（核对投影截断处+逐行证据） |
| `full/event_card_interpreter.event_7392b9f2aa06f502/attempt_2.prompt.txt` | 150 | 8,896 | 全读（同上） |
| `manifest.json`（本站两条 instance 记录） | — | — | 全读相关条目 |

两个 attempt 全部覆盖，并对两者做了 diff 比较（差异见"材料结构总览"末尾与发现清单）。投影占原文比例 98.3% / 98.5%（manifest），截断面积极小；所有截断标记处均已回查原文。

（本报告边查边写，后续小节陆续追加。）

## 材料结构总览

字符量取自投影头部"段落目录"（其分母为原文字符数），子块尺寸用 python 实测（命令见发现清单 F1）。

**attempt_1（原文 5,017 字符）六段构成：**

| 段落 | 字符 | 占比 | 内容性质 |
|---|---|---|---|
| `## System Message` 容器 | 18 | 0.4% | 容器标题 |
| `# System-Level Constraints` | 333 | 6.6% | 系统级纪律 6 条（通用模板） |
| `## User Message` | 605 | 12.1% | 任务书：职责 1 句 + 铁律 5 条 |
| `## Runtime Input` | 2,784 | 55.5% | 数据材料（JSON） |
| `## 输出字段规格` | 906 | 18.1% | 输出契约（规格形态） |
| `## Response Rules` | 371 | 7.4% | 输出形式规则 5 条 |

**Runtime Input（2,784 字符）内部构成（python 实测，含键名与缩进开销）：**

- `event_material` 547 字符 —— 待解读事件本体；其中实质事件内容只有 108 字符的标题（`raw_text_available=false`、`raw_text_excerpt=""`）
- `competing_hypotheses` 826 字符 —— 3 条假说（1 leading + 2 candidate），hypothesis_text 合计 534 字符
- `output_contract` 820 字符 —— 输出契约的"示例形态"（带语义占位值）
- `allowed_financial_links` 190 字符 —— 九项金融传导渠道枚举
- `boundary` 104 字符 —— 三个机器布尔旗标（`event_ref_only` / `must_not_become_l1_l5_evidence_ref` / `must_not_feed_back`）

**按功能归并：**

- 输出契约/规格/形式规则类：`output_contract`(820) + `输出字段规格`(906) + `Response Rules`(371) = **2,097 字符（41.8%）**
- 事件本体：`event_material` 547 字符（10.9%），其中事件实质内容（标题）**108 字符（2.2%）**
- 链接目标：`competing_hypotheses` 826 字符（16.5%），假说正文 534 字符（10.6%）
- 任务书+系统纪律：956 字符（19.1%）

**段落目录解读：** 全文没有一段是"事件正文数据"；最大单一信息块是 Runtime Input，但它的一半是契约示例与假说文本，事件本体只有标题。契约类文本（约 42%）远超事件内容（约 2%）。

**attempt_1 vs attempt_2 差异：** `diff` 显示 attempt_2 与 attempt_1 逐字节相同，仅末尾追加 21 行（原文 5,609 − 5,017 = 592 字符）的重试说明块（"上一次返回未通过结构校验……请仅输出修正后的 JSON 对象"）。payload.json 侧对应 `retry_feedback` 字段（attempt_1 为空串、attempt_2 为 595 字符）。投影段落目录把该追加块并入 `## Response Rules` 段（371→963 字符，7.4%→17.2%），因为追加文本没有自己的标题——这是投影分区口径的结果，原文的 Response Rules 本身未变。

**完整性核验：** 每个投影只有 1 处截断标记（cth_01 的 hypothesis_text 中段省略 100 字符），已回查原文补全；`payload.payload` 与 prompt 内 Runtime Input 深比较相等（两个 attempt 均 True），即 prompt.txt 就是发送给模型的全部材料，无 prompt 外隐藏输入。

## 发现清单

> 行号凡未注明 attempt_2 者均指 `full/event_card_interpreter.event_7392b9f2aa06f502/attempt_1.prompt.txt`（两 attempt 前 130 行逐字节相同，行号通用）。

### F1 事件本体近乎无字，材料重心在"要链接的假说"一侧（结构性偏科）

事实：站的职责是解读这条事件材料并链接到竞争假说。但事件本体 `event_material`（547 字符）里 `raw_text_available=false`、`raw_text_excerpt=""`（第 36–37 行），实质事件内容只有 108 字符标题；而链接对象 `competing_hypotheses`（826 字符）的三条 hypothesis_text 合计 534 字符，携带本次运行的主要定量叙事，至少含 9 个具体数字：实际利率 99.4%分位、Forward PE 19.46、盈利修正 30 日 +4.2% / 90 日 +10.7%、M7 资本开支同比 +75%、RSI 32.4、HY OAS 31%分位、supplier_lookback 30d flagged 权重 41%、M7 回购同比 -68.74%。系统纪律第 1 条（第 6 行）"不得编造……除非输入数据明确提供"使这些假说内数字成为"输入明确提供"的数据，模型在 interpretation 中引用它们在规则内。

证据：
```
$ sed -n '35,37p' attempt_1.prompt.txt
    "entities": [],
    "raw_text_available": false,
    "raw_text_excerpt": "",
$ python3 - <<'…'  （按 Runtime Input 子块 json.dumps 测长）
  event_material: 547 字符 / competing_hypotheses: 826 字符 / output_contract: 820 字符
  allowed_financial_links: 190 字符 / boundary: 104 字符
  hypotheses 字数: 534
$ python3 -c "print(len('Stock Market Today: …(Live Coverage)'))"
标题字符数: 108
```

### F2 职责描述称材料含"正文摘录"，实际为空（声明与现实矛盾）

事实：User Message 首句（第 14 行）"给你一条已经采集好的事件材料（标题、来源、日期、**正文摘录**）"，按有正文摘录描述材料；实际输入 `raw_text_excerpt` 为空串、`raw_text_available` 为 false（第 36–37 行）。铁律第 4 条（第 20 行）确实预期了"材料只有标题没有正文"的情形并要求措辞降档，即规则层覆盖了该情形，但职责句的描述与手头材料不一致。

证据：
```
$ sed -n '14p' attempt_1.prompt.txt
你是外部世界材料层的解读员。给你一条已经采集好的事件材料（标题、来源、日期、正文摘录）…
```

### F3 系统级纪律引用本站不存在的字段与 token（声明与现实矛盾）

事实：System Message 第 4 条（第 9 行）"所有 `evidence_refs` 必须来自本次输入的 `raw_data`"——本站输出契约的 12 个顶层字段（第 128 行清单）中没有 `evidence_refs`，输入 JSON 中也没有 `raw_data` 键（输入里对应物叫 `event_material.raw_text_excerpt`）；`evidence_refs` 与 `raw_data` 在全文的唯一出现就是这条规则本身。第 5 条（第 10 行）"若输入出现 `NO_DATA_AVAILABLE`……"——该 token 在全文唯一出现也是这条规则本身；输入中的空数据实际以 `""` + `raw_text_available:false` 表达，这种表达在六条系统纪律里没有对应条款。

证据：
```
$ grep -n "evidence_refs" attempt_1.prompt.txt
9:4. 所有 evidence_refs 必须来自本次输入的 raw_data，不得凭记忆添加。
$ grep -n "raw_data" attempt_1.prompt.txt
9:（同上，唯一命中）
$ grep -c "NO_DATA_AVAILABLE" attempt_1.prompt.txt attempt_2.prompt.txt
attempt_1.prompt.txt:1
attempt_2.prompt.txt:1
$ sed -n '10p' attempt_1.prompt.txt
5. 若输入出现 `NO_DATA_AVAILABLE`，只能把它当作数据边界或置信度限制；…
```

### F4 输出契约以三种形态重复，契约类文本约占全文 42%（结构性冗余）

事实：同一份 12 字段输出契约出现三次——(a) Runtime Input 内 `output_contract` 示例块（第 71–110 行，820 字符，带"只写材料事实"等语义占位值）；(b) 独立"输出字段规格"块（第 111–124 行，906 字符，标注必填/可选与类型）；(c) Response Rules 的字段清单行（第 128 行）。九项 `allowed_financial_links` 枚举出现两次（第 43–53 行数组 + 第 117 行 `EventMechanismHypothesis{financial_link:"earnings_path"|…}` 内嵌）。三者合计 2,097 字符（41.8%）。指令的消费方式：第 129 行规定"形状冲突时以规格为准"、示例只解释语义；本次两处规格形状一致、无实际冲突。

证据：
```
$ grep -n -e "\"output_contract\"" -e "输出字段规格" -e "JSON 顶层字段必须匹配" -e "EventMechanismHypothesis" attempt_1.prompt.txt
71:  "output_contract": {
111:## 输出字段规格（由 EventInterpretationCard 契约自动生成，形状以此为准）
117:- `mechanism_hypothesis`（必填）：对象 EventMechanismHypothesis{financial_link:"earnings_path"|"valuation_multiple"|…|…
128:- JSON 顶层字段必须匹配: event_id, fact_summary, interpretation, …, passport。
```

### F5 attempt_2 的重试诊断材料指向"响应末尾未闭合"，实际语法错误在响应第 3 行的未转义引号（诊断与错误位置、错误类型均不符）

事实：attempt_1 的响应 JSON 解析失败点在开头（line 3 col 76，全文 1,382 字符中的 pos 117）：`fact_summary` 第一个字符串内出现未转义的英文双引号（`标题为"Stock Market Today: …`）。而 attempt_2 末尾追加的诊断材料（attempt_2.prompt.txt 第 131–151 行）称"响应末尾片段（用于定位 JSON 语法错误，请检查最后未闭合的数组、对象或字符串）"，所附片段确为响应末尾——其中 `needs_data_confirmation` 数组、`passport` 对象、顶层 `}` 全部正常闭合，片段内不包含错误位置；"未闭合"的提示类型与实际错误类型（字符串内引号未转义）也不同。事实补充：尽管诊断错位，attempt_2 产出通过了校验（`full/` 下存在 `attempt_2.parsed.normalized.json`，且 manifest 中其 sha256 与 `output.validated.json` 相同：376c3619…）。

证据：
```
$ diff attempt_1.prompt.txt attempt_2.prompt.txt
130a131,151
> 上一次返回未通过结构校验，错误如下：
> event_card_interpreter.event_7392b9f2aa06f502 did not return a parseable JSON object. 原始响应字符数: 1382. 响应末尾片段（用于定位 JSON 语法错误，请检查最后未闭合的数组、对象或字符串）：
> 确，无法进一步验证其财报预期"
…（片段止于完整闭合的 "passport": {…} 与顶层 }）
$ python3 -c "import json; json.loads(open('attempt_1.response.raw.txt').read())"
JSONDecodeError: Expecting ',' delimiter | line 3 col 76 | pos 117 of 1382
context around error: '{…"fact_summary": "据 Yahoo Finance M7 Headlines 于2026年7月30日发布的实时盘面跟踪报道，标题为"Stock Market Today: Nasdaq Rebounds As Chips Jump; …'
```

### F6 `trigger_reasons` 含 `inquiry_reference`，但材料内无任何 inquiry 相关内容（引用无对应物）

事实：`event_material.trigger_reasons`（第 38–41 行）为 `["mainline", "inquiry_reference"]`；`inquiry` 在全文仅此处出现一次。材料未解释该标记含义，也未提供对应的 inquiry 文本或上下文。

证据：
```
$ grep -n "inquiry" attempt_1.prompt.txt
40:      "inquiry_reference"
```

### F7 数值一致性核查：未发现矛盾（核查记录）

事实：对假说文本中 9 个关键数值逐一 `grep -o` 计数（attempt_2 全文），每个数值均只出现 1 次，不存在"同一指标多处不同值"。命令与输出：

```
$ for v in "19.46" "32.4" "99.4" "+4.2%" "+10.7%" "+75%" "31%分位" "41%"; do printf "%s: " "$v"; grep -o "$v" attempt_2.prompt.txt | wc -l; done
19.46: 1 / 32.4: 1 / 99.4: 1 / +4.2%: 1 / +10.7%: 1 / +75%: 1 / 31%分位: 1 / 41%: 1
$ grep -c -e "-68.74%" attempt_2.prompt.txt   → 1（首次 grep 因前导 dash 报用法错，加 -e 后确认 1 次）
```

### F8 投影层观察：attempt_2 重试块被并入"Response Rules"段（投影分区口径事实，非原文问题）

事实：attempt_2 末尾追加的 592 字符重试块没有自己的标题，投影段落目录将其计入 `## Response Rules`（371→963 字符，占比 7.4%→17.2%）；原文的 Response Rules 本身与 attempt_1 逐字节相同（diff 仅显示 130 行后追加）。阅读投影占比时需注意该段被重试块撑大。

## 盲区

- **响应文件未逐字全读**：`attempt_1.response.raw.txt`（2,380 字节）只读了开头约 400 字符 + 用 `json.loads` 定位了解析错误（F5 需要）；`attempt_2.response.raw.txt` 完全未读。响应不是本次"材料结构"审查对象，仅为核实重试诊断而查。
- **`meta.json` 未逐字全读**：只取了前 600 字符（确认 model=deepseek-v4-flash、attempts=2、status=ok、effective_date=2026-07-30、mode=latest）；`data_boundary`、`attempt_files` 等键未展开。
- **`output.validated.json` 未读**：站产出内容质量不在本任务范围（任务书明确"不是评价站产出的好坏"）。
- **payload.json 未逐字读**：用深比较（`json.loads` 后 `==`）确认 `payload.payload` 与 prompt 内 Runtime Input 完全相等（两个 attempt 均 True）、`retry_feedback` 与 attempt_2 末尾追加块对应，据此认定 prompt.txt 即全部发送材料；未做字节级 diff。
- **cth_01 省略段已补全，但假说数字未做外部核验**：投影唯一截断（cth_01 hypothesis_text 中段 100 字符）已回查原文；假说文本内的数字（19.46、32.4 等）是否在本次运行的其他正式数据源中真实存在，超出本站单站通读范围，未核验。
