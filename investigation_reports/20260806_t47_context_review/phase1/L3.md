# L3 通读报告

## 通读范围

- 主读材料：`projected/L3/attempt_1.projection.md`，全文 1,492 行 / 55,519 投影字符，**逐行全读**（三次顺序读取：1–120、120–620、620–1120、1120–1492）。
- 原文核对：`full/L3/attempt_1.prompt.txt`，全文 55,527 字符。投影比 99.99%（manifest.json：`projection_ratio: 0.9999`），投影中唯一的截断是 notes 字段里一处 72 字符省略（Invesco 报错 URL 中段），已到原文 481、1237 行核对原值，内容为报错消息 `406 Client Error: Not Acceptable for url: https://dng-api.invesco.com/...`，无分析价值。除此之外投影无 `__omitted__` / `__table_projection__` 标记（命令：`grep -n '省略\|__omitted__\|__total__\|__table_projection__' projected/L3/attempt_1.projection.md`，仅命中 284 行"不允许省略"正文与 524/1295 行该 URL 省略）。
- 辅助读取：`manifest.json`（L3.attempt_1 条目：`inspector_has_rules: true`，即该站此前已有机械检查规则；prompt_chars 55,527）、`full/L3/attempt_1.payload.json`（用于比对 Runtime Input 与 payload 的关系）。
- 未读（与"材料结构"无关，见盲区节）：`attempt_1.response.raw.txt`、`attempt_1.parsed.normalized.json`、`output.validated.json`、`meta.json`。
- 量化方式：用 Python 解析 prompt 全文，按标题切分量字符、逐键对比 JSON 块（命令见各发现条目，均可复跑）。

## 材料结构总览

prompt 总字符 55,527。按标题切分的实测字符量（区间起讫用 `str.find` 定位，命令输出见发现 0 附注）：

| 段落 | 字符量（实测） | 占比 | 性质 |
|---|---|---|---|
| System Message（系统级约束 6 条） | 351 | 0.6% | 规则 |
| Deep Research Canon（ObjectCanon + IndicatorCanon×6 + Output Hints） | 5,363 | 9.7% | 静态规则 |
| Layer-Local 4C Few-Shot Examples（4 个范例） | 1,974 | 3.6% | 范例 |
| vNext v2 Contract（隔离纪律/字段要求/五层本体等通用部分） | 1,638 | 2.9% | 规则 |
| └ 当前层指标清单（6 指标元数据+data_quality） | 14,106 | 25.4% | 数据 |
| └ 结构示例（get_10y_real_rate 样例） | 904 | 1.6% | 范例 |
| L3 Context-Bounded Market Internals Analyst（任务书：Boundary/Lens/Semantics/Grammar/Synthesis/Hooks/Output Discipline） | 3,213 | 5.8% | 任务书 |
| Runtime Input（context_brief + layer_facts + layer_raw_data + manual_overrides） | 26,162 | 47.1% | 数据 |
| 输出字段规格 | 1,435 | 2.6% | 规则 |
| Response Rules | 365 | 0.7% | 规则 |

段落目录解读：

- 规则/任务书/范例合计约 8,700 字符（约 16%），数据材料合计约 40,300 字符（约 73%，指标清单 14,106 + Runtime Input 26,162），其余为标题与规格。
- 数据材料中最大的单一成分是每个指标的 `data_quality` 证据块：6 个指标各一份，且在「当前层指标清单」和 `Runtime Input.layer_raw_data` 中**逐字出现两次**（实测见发现 1）。
- 指标数值本身（level/percentile/Top10 明细等）只占很小一部分；data_quality 双份合计 18,910 字符，占全 prompt 34.1%。

## 发现清单

以下行号均指 `full/L3/attempt_1.prompt.txt`（原文），投影行号与之接近（投影仅多 42 行头部/目录）。所有命令在 `context_spread/` 目录下可复跑。

### 发现 1：6 份 data_quality 证据块逐字重复两次，双份合计占全 prompt 34.1%

证据：
- 第一处：「### 当前层指标清单」（263–611 行）内 6 个 `data_quality` 块（273、336、399、444、492、555 行起）。
- 第二处：`## Runtime Input` → `layer_raw_data`（738 行起）内同一组 `data_quality` 块（864、942、1026、1200、1269、1347 行起）。
- 命令：`python3` 逐键对比（见下），输出：

```
get_advance_decline_line: data_quality 一致=True, 单份字符=1814
get_percent_above_ma: data_quality 一致=True, 单份字符=1770
get_ndx_ndxe_ratio: data_quality 一致=True, 单份字符=918
get_qqq_top10_concentration: data_quality 一致=True, 单份字符=1370
get_new_highs_lows: data_quality 一致=True, 单份字符=1792
get_mcclellan_oscillator_nasdaq_or_nyse: data_quality 一致=True, 单份字符=1791
data_quality 重复总量(单份): 9455 字符; 双份合计: 18910
```

- `notes` 字段同样逐字重复两份（命令输出：`notes 一致=True` ×6，单份合计 672 字符；例：325 行与 916 行的"上涨38只，下跌65只"完全相同）。
- 指标清单内 data_quality 占该节 67.1%（9,455/14,092 字符）；指标清单**不含任何 value 键**（命令输出：`指标清单是否含 value 键: False`），即这 1.4 万字符里没有指标数值，只有元数据与质量信息。

事实：同一份数据质量证据以同一 JSON 形态在站内出现两次，合计 18,910 字符（占 prompt 34.1%），加上 notes 重复 1,344 字符，两处之外还有 source_tier/source_name/error/function_id/metric_name/manual_override_used 等字段的小额重复。任务书与合约均未说明两处各自应如何被消费（系统约束第 4 条只说 evidence_refs 必须来自"本次输入的 raw_data"）。

### 发现 2：同 3 条指标摘要以三种形态重复（layer_highlights / core_signals[].summary / layer_facts.summary）

证据：
- 743–749 行 `context_brief.layer_highlights.L3` 三条摘要串：`"NDX/NDXE Ratio | 值=2.7861 | 分位=90.0517309988062 | 趋势=below"` 等。
- 762–837 行 `layer_facts.core_signals[]` 每条各带一个内容相同的 `summary` 字段。
- 846 行 `layer_facts.summary` 把同样三条再复述一遍：`"内部健康度状态: deteriorating。关键事实: NDX/NDXE Ratio | 值=2.7861 | ... McClellan Oscillator | 值=-1.74。"`
- 命令输出：`core_signals[0].summary == layer_highlights[0]? True`。

事实：三条摘要串在 Runtime Input 内出现三次；且三处覆盖的都不是全部 6 个指标（见发现 5）。

### 发现 3：context_brief 声称"数据日期 2026-07-30"，与全部指标的 effective_date 不一致；材料内并存四个日期角色

证据：
- 742 行：`"data_summary": "数据日期 2026-07-30，L3 本层 6/6 个指标成功。"`
- 但 5 个指标的 `as_of_date`/`effective_date`/`data_date` 均为 `2026-07-29`（279/280、342/343、405/406、498/499、561/562、870/871、948/949、1032/1033 行等），`get_qqq_top10_concentration` 为 `2026-07-15`（450/451、826、1072 行）。
- `2026-07-30` 是 `collected_at_utc` 的日期（采集时间），`universe_provenance.as_of` 又是 `2026-07-31`（317、380、536、599 行）。
- 命令输出（Python 遍历 layer_raw_data）：每个指标 `as_of: 2026-07-29 | effective: 2026-07-29 | universe_as_of: 2026-07-31 | collected: 2026-07-30`（top10 为 2026-07-15）。

事实：材料头部摘要里的"数据日期"不等于任何指标的 effective_date；同一材料里并存 7-15（top10 快照）、7-29（数据生效日）、7-30（采集日）、7-31（成分宇宙 as_of）四个日期。任务书（672 行）要求 top10"必须读取 effective_date"，系统红线要求尊重 effective_date，但摘要行给了一个与之不同的"数据日期"。

### 发现 4：输入预置本层结论 state="deteriorating"，而任务书要求本站自行判定 L3 状态

证据：
- 838 行：`"state": "deteriorating"`；846 行 `layer_facts.summary` 开头即 `"内部健康度状态: deteriorating。"`。
- 任务书 692 行：`"- L3 是 healthy、neutral、deteriorating，还是 extreme_concentration。"`（Layer Synthesis 要求本站回答该问题）。

事实：站被要求判断本层健康状态，但输入材料已经携带一个预制的本层状态判断（deteriorating）及其文字摘要。隔离纪律（297–301 行附近）禁止的是"其他层"的当前判断；本层预制 state 不在禁止清单内，但事实上站手里攥着一份现成答案。

### 发现 5：预制 key_metrics 只列 5/6 指标、layer_highlights 只列 3/6，与"6/6 个指标成功"并存

证据：
- 742 行：`"L3 本层 6/6 个指标成功"`（6 个指标 error 均为 null，属实）。
- 839–845 行 `key_metrics` 只有 5 个：`get_ndx_ndxe_ratio, get_advance_decline_line, get_mcclellan_oscillator_nasdaq_or_nyse, get_new_highs_lows, get_percent_above_ma`——缺 `get_qqq_top10_concentration`。
- 743–749 行 `layer_highlights.L3` 只列 3 个指标。

事实：三处预制的"重点指标"清单互相覆盖不一致（6 / 5 / 3），且被省略的 top10 恰是任务书称为"集中度硬锚"（672 行）的指标。

### 发现 6：趋势词表三套并存且无映射——输入用 "declining"/"below"/null，范例用 "rising"/"falling"，输出枚举为 "rising"|"falling"|"stable"|"volatile"

证据：
- 输入 core_signals：765 行 `"trend": "below"`（NDX/NDXE，该值实为比值相对 MA20 的位置，不是趋势方向）、776/854 行 `"trend": "declining"`、787/802/816/831 行 `"trend": null`（4/6 无趋势）。命令输出：`core_signals: trend=null 4/6, status=null 6/6, historical_percentile=null 5/6`，`trend 非空值: ['below', 'declining']`。
- few-shot 范例（206、212 行）用的是 `"trend": "falling"`、`"index_trend": "rising"`。
- 输出规格 1418 行：`CoreFact{... trend:"rising"|"falling"|"stable"|"volatile" ...}`。

事实：输入的 "declining" 不在输出枚举内（需站自行映射为 falling），"below" 是位置词被装进 trend 字段；4/6 指标输入不给趋势、5/6 不给分位、6/6 不给 status，而输出 CoreFact 形状里 trend/magnitude 是规格内字段。prompt 中没有任何输入词表→输出枚举的映射说明（`grep -n 'falling\|"rising"'` 仅命中范例与规格两处）。

### 发现 7：输出形状示例（结构示例）整段使用 L1 指标 get_10y_real_rate，并示范引用 "L1.get_10y_real_rate"

证据：
- 612–631 行「### 结构示例」：`"function_id": "get_10y_real_rate"`（616 行）、`"evidence_refs": ["L1.get_10y_real_rate"]`（623 行）、`"covered_function_ids": ["get_10y_real_rate"]`（631 行），示例叙事是实际利率→DCF→成长股估值（L1/L4 语境）。
- 同合约段 245 行：`evidence_refs 必须是字符串数组，例如 ["L2.get_vix"]`；246 行：`必须写成 L4.function_id#FieldName`。
- 系统约束第 4 条（prompt 开头）："所有 evidence_refs 必须来自本次输入的 raw_data"。

事实：L3 站内的输出形状示例及其证据引用全部取自其他层指标（L1 实际利率、L2 VIX、L4 混合字段），与本站输入的 6 个 L3 指标无关；L3 专属 few-shot（201–228 行）是另一套示例，站内两套示例并存。

### 发现 8：L3 专属 few-shot 只覆盖 4/6 指标，缺 top10 集中度与 McClellan

证据：
- 201 行「## Layer-Local 4C Few-Shot Examples」下仅 4 个范例（204、210、216、222 行起）：get_advance_decline_line、get_percent_above_ma、get_ndx_ndxe_ratio、get_new_highs_lows。
- 缺 `get_qqq_top10_concentration` 与 `get_mcclellan_oscillator_nasdaq_or_nyse`。
- 任务书对这两个缺失指标恰好有最重的话：672 行 top10"是集中度硬锚……必须读取 effective_date、Top10 权重、M7 权重、超额权重、NDX 相对 NDXE 表现差"；666 行 McClellan"必须依赖稳定的每日涨跌家数序列，不能替代前两类基础广度锚"。

事实：被要求读取字段最多、语义限定最细的两个指标没有认知校准范例；有范例的 4 个指标恰是语义较简单的。

### 发现 9：NDX/NDXE few-shot 范例的场景前提与本次运行数据相反

证据：
- 218 行范例 INPUT：`"cap_weight_price_vs_ma60": "above"`；220 行 CORRECT OUTPUT：`"尽管 NDX 价格仍在 60 日均线之上，但其与等权指数 NDXE 的比率已回落至 20 日均线之下……"`。
- 实际数据 1020 行：`"cap_weight_price_vs_ma60": "below"`，且 1016 行 `numerator_close: 27192.31` < 1021 行 `cap_weight_ma60: 29323.98`（Python 复核：`close<ma60? True`）。
- 范例开头 202 行声明："不要复制具体数值，只学习推理结构与叙事密度"。

事实：范例的"典范输出"叙事前提（价格在 MA60 上方、指数趋势尚可）在本次数据中不成立（价格与比值双双在均线下方）；范例声明只学结构不学数值，但其叙事密度示范文本内嵌了一个与本次事实相反的前提句。

### 发现 10：canon 的 cross_validation_targets 指向不在本层输入中的其他层指标

证据（IndicatorCanon for L3 内）：
- 59/60 行：`get_qqq_technical_indicators`（L5 语境）、`get_hy_oas_bp`（L2 语境）；86 行：`get_multi_scale_ma_position`；110/111 行：`get_qqq_technical_indicators`、`get_ndx_pe_and_earnings_yield`（L4 语境）；136 行：`get_ndx_forward_earnings_quality`（L4 语境）；161 行、186 行：`get_rsi_qqq`。
- 这些 function_id 均不在本站输入的 6 个指标中（本站输入：267–612 行清单与 738 行起 layer_raw_data 仅含 6 个 L3 指标）。
- Canon 段开头 15 行声明："本段只提供静态规则，不提供其他层的运行时数据、结论或候选跨层判断。"

事实：本层材料以静态 canon 形式携带了一份其他层指标名清单（仅名字与路由关系，无数值无状态）；输出规格（1431 行附近 IndicatorAnalysis 形状）又鼓励把 cross_validation_targets 抄进输出。

### 发现 11：canon metric_name 与输入 metric_name 有 3 处不一致

证据：
- canon 侧（45/70/146 行）：`"Advance Decline Line"`、`"Percent Above Moving Average"`、`"New Highs/Lows"`。
- 输入侧（267/330/486 行及 920/998/1325 行）：`"Advance/Decline Line (NDX100)"`、`"% Stocks Above MA (NDX100)"`、`"New Highs-Lows Index"`。
- 合约 244 行：`indicator_analyses[].metric 必须优先等于输入 metric_name`。

事实：同一指标的 canon 名与输入名不同（3/6），只能靠 function_id 对齐；合约已规定以输入名为准，但两份材料自身命名不统一。

### 发现 12：manual_overrides 占位块带陈旧日期 "2022-01-04"

证据：
- 1408–1411 行：`"manual_overrides": { "active": false, "date": "2022-01-04", "metrics": {} }`。

事实：一个 inactive、metrics 为空的占位块携带了一个 2022 年的日期，与本次运行所有日期（2026-07）无关；属于惰性占位信息。

### 发现 13（核验记录）：抽查的关键数值均自洽，未发现数值矛盾

命令：Python 解析 Runtime Input 逐项复算，输出：

```
NDX/NDXE: 27192.31 / 9760.03 = 2.7861 | level: 2.7861
percentile_10y: 0.9005173099880621 *100 = 90.0517309988062
layer_facts percentile: 90.0517309988062
ratio_ma20: 2.8946 level<ma20? True trend_vs_ma20: below
cap_weight_ma60: 29323.98 close<ma60? True price_vs_ma60: below
Top10 合计: 46.04 | top10_weight_pct: 46.04
Top5 合计: 29.29 | top5: 29.29
Top3 合计: 20.47 | top3: 20.47
等权基准 10/108 = 9.26 | equal_weight_top10_baseline_pct: 9.26
excess: 36.78 = 46.04-9.26 = 36.78
proxy 21 d: 1.72 calc: 1.72 ; proxy 63 d: 2.79 calc: 2.79
新高新低 percent: 1/101 = 0.99 vs 0.99 ; 2/101 = 1.98 vs 1.98
A/D: level 296 ma20 348 dist%: -15.0 calc: -14.94
```

- 同一指标在 layer_facts.core_signals 与 layer_raw_data 两处出现的值一致（命令输出逐项相等，见通读过程记录）。
- 仅 3m/6m 窗口的 market_cap_minus_equal_weight 与 ndx−ndxe 手算差 0.01 个百分点（-0.58 vs -0.57；1.37 vs 1.38），属舍入级别；ratio_change_pct（0.15/-0.5/1.22）与 diff 不完全相等（用的是比值口径，methodology 已注明两个口径）。
- `m7_weight_pct: 37.79` 与 Top10 明细中 M7 成分（NVDA+AAPL+MSFT+AMZN+GOOGL+GOOG+META+TSLA=37.7863）一致。
- Runtime Input 与 `full/L3/attempt_1.payload.json` 的内层 `payload` 完全相等（命令输出：`Runtime Input == payload.payload ? True`；wrapper 额外含 stage_key=l3_analyst、attempt=1、retry_feedback=""）。

### 发现 14（核验记录）：指令要求消费的 X 均在材料中存在——未发现结构性缺失

逐项核对指令引用的消费对象：

- 任务书 672 行要求 top10 必须读取的 5 项（effective_date / Top10 权重 / M7 权重 / 超额权重 / NDX 对 NDXE 表现差）在 1072、1064–1071（top10_weight_pct 46.04、m7_weight_pct 37.79、top10_excess 36.78）、market_cap_vs_equal_weight.windows 全部存在。
- IndicatorCanon 6 个指标齐全（41–194 行），与输入 6 个 indicator 一一对应（function_id 均匹配）。
- Canon Output Hints 鼓励填写的 permission_type/canonical_question/misread_guards/cross_validation_targets/falsifiers/core_vs_tactical_boundary 在每条 canon 中均非空（41–194 行逐条读）。
- 系统约束第 5 条的 `NO_DATA_AVAILABLE` 未出现（`grep -c 'NO_DATA_AVAILABLE'` = 1，仅命中规则本身），6 个指标 availability 均为 available。
- 结构性偏科未观察到极端形态：6 个指标中 4 个广度类（A/D、%AboveMA、新高新低、McClellan）+ 2 个集中度类（NDX/NDXE、Top10），与 L3 职责（广度+集中度+领导力质量）匹配；canon、任务书、输入三者覆盖同一指标集。
- canon 中 `source_hint`、`frequency_hint` 全部为空字符串（48/49、73/74、99/100、124/125 行等，6 条 canon 皆然）——属 canon 数据自身的空字段，记录备查。

## 盲区

- **投影与原文未逐字全文比对**：投影比 99.99%，投影内唯一的截断标记是 notes 字段一处 72 字符省略（Invesco 报错 URL 中段），已到原文 481/1237 行核对原值；其余部分我信任 manifest 的 verification_ok 与投影规则声明，逐字读的是投影，行号证据全部落到原文并用 grep 复核。
- **输出侧未读**：`attempt_1.response.raw.txt`、`attempt_1.parsed.normalized.json`、`output.validated.json`、`meta.json` 未读。本阶段边界是"输入材料结构"，因此不知道站实际产出是否被结构校验拒绝、是否真受上述结构问题影响（例如是否照抄了预制 state 或范例前提句）。
- **双份 data_quality 的成因未追**：「当前层指标清单」与 layer_raw_data 为何各装一份，属于 prompt 组装代码的行为，本阶段只读语料、未查 src/。
- **canon cross_validation_targets 的层归属是推断**：get_qqq_technical_indicators / get_rsi_qqq / get_multi_scale_ma_position 按五层本体语义推为 L5 语境，get_hy_oas_bp 推为 L2，两个 ndx_* 推为 L4；未去其他站语料实证（范围纪律不允许）。
- **数值核验是抽查而非穷举**：覆盖了全部 6 个指标的关键派生值（比值、分位、Top3/5/10 合计、M7 合计、proxy 差值、新高新低百分比、A/D 均线距离），但 prompt 内本就不含历史序列（如 2763 个观测），无从复算分位本身。
- **3m/6m 窗口 0.01 个百分点差异**只标记为舍入级，未追 yfinance 原始收益率口径。
- **机械检查规则内容未读**：manifest 显示 `inspector_has_rules: true`（该站此前已有机械检查规则），但我未读规则文本，发现清单中可能有与既有机械检查重复的条目。
- **layer_highlights 中分位浮点 `"分位=90.0517309988062"`**（13 位小数的原始浮点直接进摘要串）这类展示层瑕疵未单列发现，记录在盲区备查。
