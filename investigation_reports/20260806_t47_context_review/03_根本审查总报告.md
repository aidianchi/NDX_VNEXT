# T47 根本审查总报告（骨架重修 + 全量通读 + 并集收口）

> 唯一权威表述：`T47_上下文根本性审查.md`。本文件是本轮（2026-08-08/09 独立重做）的总报告。
> 与上一轮底稿（00/01/02）的关系：本报告每条发现均在修复后的语料上重新取证，不沿用旧结论；
> 旧底稿中与本轮一致的，标注"复现"；数字口径不同的，以本轮复核值为准并在 §7 对照。
> 复核与严重度评定由根线程在全链路上完成，未下放；子代理只交证据。
>
> ⚠️ **勘误（2026-08-11 三明治重决策，所有者拍板）**：A9"事件链断供三连"中，bridge 零事件供给与治理四站 key_event_refs 全空经重裁决为**正确隔离行为**（事件永不进第一层，含治理链），不是断供；A9 仅剩"ESS 缺 needs_data_confirmation"为真病（已由 FIX-2 修复）。§8 常设检查第 7 项方向写反：应改为"bridge/治理站事件字段**恒为空**，非空即报警（那才是污染事故）"。其余 30 类病的定性不受影响。依据：`investigation_reports/20260811_sandwich_redecision/三明治重决策对照稿.md`（R5/R6）。

## 0. 总览

- 基线 run：`output/analysis/vnext/20260731_002156`（全项目最新一次完整真实跑）。
- 语料：修复后的摊开器产物 `context_spread/{full,projected}`，25 站目录、32 实例、原文 3,650,375 字符。
- 阶段零：摊开器逐行重审，实锤并修复 **9 类骨架裂缝**（含任务点名的 2 条），红灯测试 15 项全绿，重跑校验 32/32 通过且全部改为落盘回查。
- 阶段一：25 站逐站片 + 4 横向片全部交卷（29 份报告在 `investigation_reports/20260806_t47_context_review/phase1/`），无一片申请拆分。
- 阶段二：根线程对全部发现按"病的类型"聚类复核（逐条机械验证命令见 §3 各条），确认 **31 类病 + 5 项阴性事实**；小修 2 项当场落地（配红灯测试）；T38 并集对照见 §5。

---

## 1. 阶段零留证：骨架重修

### 1.1 逐行重审发现的全部问题（除任务点名的两条外）

任务点名裂缝：(a) 键路径与承诺内数值两项校验自算自证、不回查落盘文件；(b) 数值正则不含 1–3 位整数。逐行重审另外实锤：

| # | 问题 | 证据（命令+输出要点） |
|---|---|---|
| S1 | **缩进 JSON 独立块验证盲区**：`verify_projection` 用 `re.finditer(r"^\s*(\{|\[)")` + 从行首 `raw_decode`，而 `raw_decode` 不吃前导空白——缩进块全部静默跳过。基线 run 103 个被投影块中 **37 个是缩进独立块**（36%），其键/数值覆盖从未被校验 | `python3 -c` 扫描全 run：被投影块 103、其中缩进 37；`raw_decode('  {}')` → `Expecting value: line 1 column 1` |
| S2 | **7–15 项非标量列表截断 bug**：旧切口 `头10+尾5` 在列表短于 16 项时不省略任何元素，却产出负 `__omitted__` 并重复元素 | 红灯实录：10 项混合列表投影后变 16 项，`__omitted__=-5`，元素 5 出现 2 次（本 run 投影中该 bug 0 次触发，属潜在病） |
| S3 | **extras 哈希算而未存**：`copied` 字典算了全部配套文件 sha256，manifest 只落 prompt 一个哈希 + 一串文件名（字段名 `extras` 还名不副实地包含 prompt 自身） | 旧 manifest `instances[].extras` 仅为文件名列表 |
| S4 | **调查布局 extras 跨 attempt 污染**：前缀过滤只到实例级，`inv_e75efe82e7c0.attempt_2` 把 attempt_1 的 prompt/response 收进自己 extras | 基线 run `prompt_audit/controlled_investigation/` 目录实测 |
| S5 | **嵌套布局 extras 完全不过滤**：同目录任何文件（含其他 attempt 的提示词）都会被收编（潜在，本 run 该目录仅 attempt_1） | 旧代码 `extras` 收集对 nested 无分支 |
| S6 | **文档/注释三处与实现不符**：模块 docstring、投影头部、校验报告均称"投影看不到的只有两处"，实际有三处（漏披露"同构标量表 >30 行的中段行"）；`project_prompt` 注释"只在该 JSON 以整行结尾时替换"无此检查；`_project_value` docstring 未披露非标量列表规则 | 代码与文档逐行对照 |
| S7 | **规则内丢弃计数口径偏弱**：只报唯一 token 数——表格中段 500 行同值计为 1；且 dropped 集合跨块累加，块 A 的丢弃会掩盖块 B 的同值缺失 | 旧代码 `len(dropped_by_rule & all_numbers)` |
| S8 | **全文数值校验用原文 raw 文本取 token**：JSON 块重序列化后表示形式变化（如 `19.460`→`19.46`）会被误报缺失；放宽正则后风险放大 | 旧代码 check 3 直接对 `original` 全文取 token |
| S9 | **重跑不清理旧产物**：陈旧 stage 目录会混入新一轮留证；`project_prompt` 还有死条件 `len(stripped) >= 1` | 旧 `spread_run` 仅 mkdir 不清理 |

### 1.2 红灯证据（修复前实录，证明裂缝真实咬人）

- 篡改 A：把落盘投影里 `"beta": 42` 改成 `43` → 旧校验 `ok=True`（短整数偷换放行，裂缝 b）。
- 篡改 B：从落盘投影删掉 `"gamma"` 键整行 → 旧校验键缺 = 0（键校验自算自证，裂缝 a；只因 19.46 碰巧被抓才整体判否）。
- 7–15 项列表：`_project_value(10 项混合列表)` → 16 项、`__omitted__=-5`（S2）。
- 正则：`"占比 42%" / "加息 75bp" / "共 7 条事件"` 旧正则提取全部为空（裂缝 b）。

### 1.3 修复内容（改动限于校验与投影逻辑）

1. **校验全部落盘回查**：`verify_instance_files(full/拷贝, projected/文件, 源文件)`——原文用 full/ 落盘拷贝并与 prompt_audit 源文件 sha256 逐字节比对；投影从落盘文件重新解析。新增**块配对**校验：原文与投影文件的 JSON 块按同一发现器 `_iter_json_blocks`（投影器与校验器共用，缩进块不再漏）逐块配对，块数不一致即失败。
2. **数值尺子加宽**：`(?<![\w.])-?\d+(?:\.\d+)?(?:[eE][+-]?\d+)?(?![\d.])`——覆盖 1–3 位整数、负号、科学计数法；左边界挡标识符（attempt_1/sha256/v1.2 不产生 token），右边界允许带单位数值（75bp）。散文区按原文取 token、JSON 块按 dumps 归一化取 token（S8 误报消除）。
3. **列表切口修正**：非标量列表 ≤15 全保留；>15 且中段全为标量才截断（中段含 dict/list 则整列保留——键路径承诺是全覆盖，中段结构化元素的键无法留痕）；投影器与丢失收集器共用 `_list_middle_omitted` 判定。
4. **extras 按 attempt 归账**（调查/嵌套布局均加 attempt 前缀过滤），manifest 逐文件落 sha256（`files` 映射 + `projection_sha256`）。
5. **三处丢失边界如实披露**（模块 docstring、投影头部、校验报告口径统一）；丢弃计数改报"唯一 token 数/出现次数"双口径；重跑前清理自身旧产物；删死条件、修正注释。

### 1.4 红灯测试与新校验结果

- `tests/test_context_spread.py` 15 项全绿（旧 5 项保留适配 + 新 10 项：篡改投影短整数/删键/删整块/篡改 full 拷贝、缩进块发现与校验、正则边界、7–15 列表、extras 归账与哈希、三边界披露、浮点归一化）。
- 全量套件（`.venv`，Python 3.12）：1077 passed, 5 failed——5 个失败全部与本次无关且为先前已存在（3 个 docs_consistency 因未提交的 `现在.md` 台账状态值"收口复核"非法；2 个 console_run_all 在 HEAD 上即红，已用 stash 对照确认）。
- 重跑基线 run：25 站目录、32 实例、原文 3,650,375 / 投影 2,409,841（66.0%，与修复前一致——投影逻辑修正对本 run 输出中性）。新校验 **32/32 通过**：逐字节一致、块配对一致（含此前漏验的 37 个缩进块）、承诺内数值零丢失。留证：`context_spread/projection_verification.md` + `manifest.json`（spread_version=2）。

### 1.5 基线评估（阶段零④——证据列此，换不换由所有者拍板）

- `output/analysis/vnext/` 下 **没有比 20260731_002156 更新的 run**；换基线 = 重跑一次真实跑（成本 + 改变审查对象）。
- 基线之后有 4 个触碰 src 的 commit 会影响提示词/供给：`47b5645`（T42① 许可改为从实发导出——**直接影响 §4-A5 综合裁决站许可形态**，该站材料反映的是旧导出逻辑）、`eb71a00`（证据编号合法性拆分 + orchestrator 223 行 + thesis_builder.md 提示词）、`ede0f2f`（L1/L4 字段名对齐 158 行）、`9c0a9e3`（模型路由，不改提示词结构）。
- 根线程意见（不代为拍板）：本报告全部发现以基线 run 为证据基座，可复现复核；其中 IA 许可类发现（A5）对应当前代码可能已部分变化，建议下次真实跑后对 A5 做单点复核，不必为此换全量基线。

---

## 2. 阶段一覆盖声明

- 逐站片 25/25 交卷，横向片 4/4 交卷；**无一片申请拆分**（L2/L4 两大站以"散文逐字 + 数据区结构/边界/抽查"策略吃完，抽查口径与未逐行读的区域已在各报告盲区节如实交代）。
- 报告清单位置：`investigation_reports/20260806_t47_context_review/phase1/`（25 站 + H1 冗余 / H2 对称 / H3 隔离 / H4 许可供给）。
- 中断事故与对策：执行中两次 403 额度中断；因第一轮"报告未落盘"的教训，本轮全部片强制"边查边写"，29 份报告全部落盘，无二手口述。
- 语料冻结：阶段一期间语料与代码零改动（全部片只读 + 新增报告文件）。

---

## 3. 确认发现总表（病的类型 · 证据 · 严重度 · 三态）

严重度由根线程在全链路上评定（子代理不报级）。复核方式三种：**R**=根线程在语料上独立复跑命令验证；**S**=类型级机械全量扫描（一条命令覆盖全部实例）；**P**=片内证据充分、根线程抽查同类属实。三态：A=落常设机械检查 / B=不能机械化（写明为什么）/ C=留所有者拍板。

### 3.1 架构级发现（详细影响面见 §4，此处只定级归态）

| # | 发现（类型） | 复核 | 严重度(全链路) | 三态 |
|---|---|---|---|---|
| A1 | 治理四站共享同一份 key_evidence_refs（30 条，序列化 116,150 字符，sha256 `b276d6482374` ×4 站）；critic 与 risk 的整个 Runtime Input（229,296 字符）**逐字节相同**（sha256 `eb319514e4c5`）——两个"不同视角"的审查站吃同一碗饭 | R（sha256 复算） | 高：审查独立性结构失效 + 4 倍重复供给 | A（critic/risk 输入 sha256 比对入常设检查）+ C（独立性设计） |
| A2 | 证据窗口子集锚定：30 条 key_evidence_refs 是 thesis 引用集（33 条）的真子集，缺的 3 条全部位于 price_reflection_map 的 **counterevidence_refs 反证位**——审查站看不到正方引用之外的反证 | R（H2 全量核验，我抽验集合包含关系） | 高：挑刺站的弹药库被正方圈定 | A（key_evidence_refs ⊇ thesis∪counter 引用集的集合检查）+ C（锚定设计） |
| A3 | 终审不接收修订稿：final 输入 19 个 thesis_* 字段与修订前逐字节相同；唯一真改动（retained_conflicts 4 条→3 条）全文未进终审；终审只见 3 个类型名 + 126 字符 revision_summary，且 summary 自称"添加 why_retained 解释"而 reviser 对象里**没有该字段** | R（H2 逐字节比对） | 高：终审裁决对象是被否过的原稿 | A（final 输入 revised 内容存在性检查）+ C（管线设计） |
| A4 | 反方假说原文对 critic/risk/reviser/final 四站不可见，只以 thesis 的 hypothesis_responses 二手形式出现 | R（H2 grep + 键集穷举） | 高：审查与裁决只能看到正方转述的反方 | C（可见性设计） |
| A5 | 综合裁决站许可半瘫：ref_authority 34 条中 23 条 `unknown`（67.6%）、规则定义的 audit_only **0 实例**、34 白名单 ref 中 11 个全文连数值都没有；且指令定义的标签体系与实发标签不匹配 | R（我复跑分布：unknown 23 / core_allowed 5 / supporting_only 6） | 高：许可闸门失效（注意：基线后 T42① 已改许可导出，需新 run 单点复核） | A（unknown 占比阈值告警） |
| A6 | 第三份调查静默缺席：3 次调查仅 2 份进 IA 材料；`inv_e75efe82e7c0` 两 attempt 均失败（error.json），allowed_investigation_ids 与 investigation_reports 里都**无占位、无失败状态**；对应 cross_layer_question 仍挂在材料里 | R（我复跑：ids=['ae…','c4…']，reports=2，e75 全文 0 次） | 高：失败被静默吞掉，裁决者不知道有调查没回来 | A（调查报告数 vs 委托数对账检查） |
| A7 | 受控调查站三重病：① 调查材料=提出问题的那份文档本身（对着问题查问题）；② ae/c4 两实例第 5 行声明"你不知道系统当前的判断"，同份材料 [M1] 内含核心矛盾/dominant_side/仓位指令（"不宜重仓""触发核心仓进一步减仓"）；③ 全部 4 份提示词材料区在 char 3953 处硬截断、JSON 不闭合（解析报 Invalid control character）；e7 实例与问题直接相关材料仅 838 字符（6.4%） | R（截断我复跑 4/4；声明-实物矛盾 H3 行号对行号核验） | 高：调查独立性与材料完整性双失效 | A（材料 JSON 闭合性 + 立场字段检测）+ C（调查站材料设计） |
| A8 | L1-L5 输入预置本层结论：`layer_facts.state` 五站全带答案（restrictive/neutral/deteriorating/expensive/uptrend），而"判定本层状态"正是各站任务书核心产出；L2/L4 报告另证摘要层缺指标、trend=null 等预消化损耗 | S（五站全量扫描） | 高：预置答案诱导全链判断同向 | C（供给设计：预置层该不该含结论） |
| A9 | 已由 08-11 三明治重决策推翻，现口径见 `investigation_reports/20260811_sandwich_redecision/三明治重决策对照稿.md`。 | | | |
| A10 | 巨型材料供给：L4 的 revision_metrics 一指标独占 Runtime Input 51.6%（约 15.5 万字符逐成分明细）；L2 的 vix_term_structure raw_series 2,507 行（序列化 232,419 字符 ≈ 全站 43.5%，原始区间约 43 万字符 ≈ 81%）；治理四站证据索引占各自输入 49-80%，其中 slope_30d 单条 58,859 字符 ×4 站逐字相同 | R（我复算 vts/slope_30d 体量；L4 占比取片内 python 解析值） | 高：T38 主战场，两站+治理链吞掉全程 token 大头 | C（T38 供给设计） |
| A11 | 数值矛盾沿共享供给逐字复制到多站：Trailing PE 29.4（Wind 卡）vs 30.31（散文 ×7 站终审材料）；简式收益差距 -1.21% 与同句构成算不齐（3.3%−4.61%=−1.31%）；"隐含约36%盈利增长"用材料内任何 PE 值都复算不出（应 51%-56%）；"高于MA20 3.26%"×5 处 vs 证据完整写法 MA20=2.334（可读作 MA20=3.26%，reviser 材料里已实际出现"2.41%高于3.26%"自相矛盾句）；Damodaran expected_return 8.62 vs 8.65 并存；L5 OBV 快照 670,737,927 ×1 vs 分项 353,656,127 ×4 | R（逐条 grep 复跑） | 高：一处错处处错，审查站拿着矛盾双方却无一处 reconcile | A（同名指标多值检测）+ C（口径裁决：以哪个为准是应然判断） |
| A12 | **阴性事实（反证，保留）**：正反材料层对称——thesis 与 counter_thesis 的 evidence_index 102 条 canonical 逐字节相同（103,322 字符）；T46 偏科不在材料层，若存在则在规则/输出层 | R（我复算 canonical sha256 相同） | —（健康项，作 T46 对照基线） | B（记录为对照） |

### 3.2 模板级/系统性疾病（多站同型，一次全量扫描定案）

| # | 发现 | 覆盖 | 复核 | 严重度 | 三态 |
|---|---|---|---|---|---|
| B1 | System-Level Constraints 第 4/5 条引用各站不存在的键（raw_data / evidence_refs / NO_DATA_AVAILABLE）——通用模板未按各站 schema 裁剪 | 事件链 11 站全中；critic/risk/reviser 同型 | S | 中：纪律文本空转，且让模型以为有别的材料 | A（约束引用键存在性检查） |
| B2 | 事件站措辞规则自相矛盾："弱来源以据报道/该媒体称开头"（契约示例）vs"措辞完全由你决定，没有固定说法"（任务书） | 10/10 事件解读站（ESS 只有后者） | S | 中：响应实际跟随了契约示例（有证据），规则内耗 | A（矛盾对共存检测）+ B（裁决哪个对是语义判断） |
| B3 | 输出契约同站三形态重复（output_contract 示例 + 字段规格 + Response Rules），合计约占各事件站提示词 26%-44%；九渠道枚举双份 | 10/10 事件站 + ESS | S/P | 中（结构性冗余） | C（模板瘦身→T38） |
| B4 | 输出结构示例用他层指标（L1 的 get_10y_real_rate 出现在 L2/L3/L4/L5 的输出示例，示例 evidence_refs 指向本层不存在的数据） | L2/L3/L4/L5（L4 达 7 处） | S | 中：示例教模型违例 | A（示例指标本层存在性检查） |
| B5 | manual_overrides 占位块带陈旧日期 "2022-01-04"（inactive 但无说明） | L1-L5 全中 | S | 低 | A（陈旧占位日期检测） |
| B6 | data_quality 块双份供给（指标清单 + layer_raw_data 各一份），L1-L5 块数≈2×指标数；L1 为语义相等键序不同（逐字扫描会漏）；L1 占全提示词 21.8%、L3 占 34.1% | L1-L5 全中 | S | 中（系统性冗余） | C（供给去重→T38） |
| B7 | `percentile_10y` 同键双刻度并存无标注：0-1 小数制与 0-100 百分制 | L1（6+5）、L2（6+6） | S | 中：同名不同义，模型最易读错的那类坑 | A（同键取值域检测） |
| B8 | context_brief 统一声称"数据日期 2026-07-30"，与各指标真实日期矛盾（L1 无一是 07-30；L3 effective_date 07-15×4 + 07-29×10 并存；L2 出现晚于 run 有效日的 07-31） | L1/L2/L3 | S/P | 中（时点纪律） | A（brief 日期 vs 指标日期一致性检查） |
| B9 | 重试机制病：① 反馈把错误定位写成"检查末尾未闭合"，实际错误在响应前段未转义引号（ESS + 2 事件站实锤）；② L2 attempt_2 反馈携带第一次答案的分析结论片段（锚定下 attempt）；③ ESS 两 attempt 同模式失败，meta status=failed | 事件链 + L2 | R | 中-高 | 小修一已修①；② C（重试带不带上一稿是设计权衡） |
| B10 | synthesis_guidance（827 字符，写给 Thesis 的行为指令）混入 critic/risk/reviser/final 输入，且引用这些站不存在的键（competing_hypotheses 等） | 治理四站 | S | 中 | A（跨站指令串味检测） |
| B11 | `high_severity_typed_conflicts` 名实不符：3 条中 2 条 severity=medium；thesis 侧两容器条目集不一致（4 条 vs 3 条） | 治理链 | R | 中：名称说谎，下游按名取信 | A（容器名与 severity 字段一致性检查） |
| B12 | 治理四站同收 41 键共享信封，含他站产物 null 占位与空数组（critic 22 / risk 18 / final 9 / reviser 6 个未声明键）；counter 暗供 forbidden_context_refs/output_contract；pricing_expectation_ledger 自标 forbidden_as_core_ref/audit_only 仍进输入且无消费指令 | 治理链 | P | 低-中 | A（null 占位与禁用标记进输入的检测） |
| B13 | bridge：冲突矩阵 A-M 只列 A/B/C/K 四行、D–J/L/M 以省略号代替；"cross_layer_claims ≥2"质检与被标"合法输出"的空表示例矛盾；候选关系示例与实际三条链接不重合；场景示例用输入中不存在的语义式 ref | bridge | R（矩阵省略我复核） | 中 | A（矩阵行完整性检查）+ C（示例与规则对齐） |
| B14 | 摘要层逐级缩水且取舍标准无定义（highlights 3 / core 8 / key 5 等），被省掉的恰是关键指标的实例存在（L3 缺 top10"硬锚"） | L1/L2/L3/L5 | P | 中 | B（取舍标准需人声明，声明后可机械化） |

### 3.3 单站个案（证据硬、影响局部）

| # | 站 | 发现 | 复核 | 严重度 | 三态 |
|---|---|---|---|---|---|
| C1 | L3 | 持仓锚 effective_date 07-15（距 run 07-30 滞后 15 天），holdings 105 与 108 两计数并存 | R（我复跑：07-15×4、07-29×10；105×2、108×5） | 高（时点纪律） | C（数据质量修复判断） |
| C2 | L4 | supplier_lookback 处 pending_validation 状态却 100% 权重撑起 30d/90d 盈利修正主斜率，自产档案贡献为零 | P（片内 223 处提及+9 处 pending_validation 我复核存在性） | 高（主斜率立在未验证数据上） | C（数据源裁决） |
| C3 | L4 | AMZN 回购标 available 但最新季 2024Q4（距 effective_date 约 17 个月），仍计入 companies_available=6 | R（2024Q4 ×36 处） | 中 | A（available 但数据陈旧的检测） |
| C4 | L4 | yield gap 三处身份矛盾：规则"只作 fallback/diagnostic" vs MetricAuthority `core_allowed` vs canon"核心桥梁指标"；其 data_quality.coverage 逐字抄自 HoM 指标 | P | 中 | C（身份裁决） |
| C5 | L4 | 回购序列 MSFT 2026Q1 逐字重复行（双形态=4 份），聚合值 20.73 是去重后和，材料无去重说明；blackout 指标附 616 行历史日期全表而 canon 只需 next date | P | 中 | A（逐字重复行检测）+ C（供给裁剪） |
| C6 | L5 | `state=uptrend` 与材料内几乎全部方向性数值矛盾（ADX down/MACD bearish/破均线/OBV distribution/30 日 -9.33%）；core_signals 版均线尺度缺 ma200 | P | 中-高（与 A8 合流） | C（state 生成逻辑） |
| C7 | counter_thesis | 站内 bridge_v1_structure 双份（15,516×2 逐字相同）；packet 自述"已完成 Layer→Final 完整链路"与本站"在 Thesis 生成之前运行"直接矛盾；bridge 材料含 evidence_index 查无出处的定量预测"跌幅可能加速至30%"；firewall 全绿与 3 项调查未决并存 | P | 中 | A（自洽性检查：packet 自述链路状态 vs 实际阶段）+ C |
| C8 | IA | invalidation_conditions 第 5 条标【转空】，内容却是"向上突破 VWAP+广度修复"的反转向上；调查报告 9 处引用输入中不存在的材料键 [M1]；MSFT 卡 interpretation 在**原文**中断词（"postin"，非投影伪影） | P | 中 | A（标签方向 vs 内容方向检测可半机械化） |
| C9 | 事件卡各站 | BOM 乱码 ï»¿ 进提示词（82d5）；raw_text_excerpt 恰 2,200 字符硬截断断词（b8f4）；同一摘录内 MSFT 角标 +15.09% 与正文 "up 13%" 并存（b8f4）；entities 空数组但摘录内含具名实体（多站）；trigger_reasons/boundary 块无消费方（多站）；AAPL 日历卡 published_at 与 run 启动秒级重合（ESS） | R（BOM/双涨幅我复核） | 中（事件数据质量） | A（BOM/截断断词检测）+ C（抓取清洗改进，与 BODY_FETCH 计划并流） |
| C10 | thesis | mixed_field_authority 强制子 ref 路径上 54 个子条目 25 个取不到值；L2 摘要正文三处用 FGI 38 但 indicator_refs 不含该指标；47/50 的 3 个失败指标无清单 | P | 中 | A（子 ref 可取值性检查） |
| C11 | ESS | `citation` 字段 10/10 是 event_id 的机械包装；同卡数组 published_at 三种时间格式并存；指令散文用 ASCII 双引号裹中文短语 22 处（与两次 parse_error 形态同构） | P | 低-中 | B（ASCII 引号是提示词文风问题，改提示词属供给侧） |
| C12 | L1/L2/L3 | canon metric_name 与输入 metric_name 多处不一致（如 "M2 YoY" vs "M2 YoY Growth"），契约却要求引用必须等于输入 metric_name | P | 低-中 | A（canon 名与输入名一致性检查） |

### 3.4 阴性事实（查过且干净，反证保留）

1. **L1-L5 隔离红线在材料层成立**：六份 prompt（含 payload/response）未见其他层运行时数据/结论/候选跨层关系实物；`apparent_cross_layer_signals` 全部 `[]`；15 个 Bridge/Thesis/Risk/Final 指纹键零命中（H3 全量扫描）。
2. 正反材料层对称（见 A12）。
3. counter 的 allowed_evidence_refs 102 条与证据索引 102 条完全对齐——VXN 型"许可>供给"病在本轮不存在于此站（H2/H4）。
4. 各站数值抽查自洽项：L3 关键比值/分位复算一致；事件站时间字段互洽；L2 core_signals 分位与 raw 吻合。
5. 投影校验 32/32 承诺内零丢失（新口径、落盘回查、含 37 缩进块）。

---

## 4. 可能撬动架构级变动的发现（只列问题与影响面，不开药方）

1. **审查站独立性（A1+A2+A3+A4 合流）**：critic 与 risk 同输入、证据窗口被正方引用圈定、终审裁原稿不裁修订稿、反方原文全程黑盒——治理链四个保护装置在材料层是同一份供给的四个读者。影响面：治理链全部结论的"被审查"成色；T46 已确诊偏科的同类土壤。
2. **调查与裁决的回路断裂（A6+A7）**：调查站对着问题查问题、立场声明与实物矛盾、材料 3953 字符硬截断、失败调查静默缺席。影响面：controlled_investigation→integrated_adjudicator 这条"主动求真"回路的可信度；任何依赖调查结论的裁决。
3. **预置答案（A8+C6）**：L1-L5 输入自带本层结论 state，且至少 L5/L3 的预置值与材料内原始信号方向相反。影响面：五层判断的独立性成色；后续所有以五层结论为原料的站。
4. > 已由 08-11 三明治重决策推翻，现口径见 `investigation_reports/20260811_sandwich_redecision/三明治重决策对照稿.md`。
5. **供给体量失衡（A10+B3+B6）**：全链 3.65M 字符里 72.2% 处于 ≥2,000 字符的逐字重复块；最大单站 L4 56 万字符、单条证据 slope_30d 5.9 万字符 ×4 站。影响面：token 成本、有效信息密度、T38 的全部设计前提。
6. **数值一致性无闸门（A11+C1-C4）**：同名指标多值并存横穿至少 6 站，且随共享供给逐字复制；现没有任何一站的材料内 reconcile。影响面：北极星"每个数字经得起独立重算"的当前缺口。

---

## 5. T38 并集对照表

| T38/七路审计已知（来源） | 本轮复核状态 | 关系 |
|---|---|---|
| 数值供给 0/26（VXN 病）；T42① 已止血：许可从实发导出（WORK_ORDERS 第三节①） | 本轮基线 run 仍是旧导出逻辑的材料：A5（23/34 unknown）为该病的同族形态；止血效果需新 run 单点复核 | 复现（旧形态）+ 待新 run 验证止血 |
| evidence_index 深度冗余：全量 478,178 字符，投影 2.1%-5.9%（T38 立论依据） | 本轮实测供给端同病加重：治理四站 key_evidence_refs 116,150 字符 ×4 逐字重复（A1/A10） | 复现并加重（量化更新） |
| "正反同菜单"只在 thesis/counter 一对成立，过了就不成立（七路审计定性） | 本轮精确化：材料层正反确实对称（A12 阴性）；不对称发生在治理链（A1/A2/A4） | 复现 + 边界精确化 |
| get_m7_fundamentals / HoM 连续 4 跑未被调用（建议并入 T38 实测） | 本轮 L4 片见 HoM 指标在材料中（date 错位 67 天）；未调用原因本轮未覆盖 | 部分相关，原因未查（列入未覆盖） |
| —（T38 未知） | 新增：终审不收修订稿（A3）、第三调查静默缺席（A6）、CI 材料截断+立场泄漏（A7）、state 预置（A8）、事件链（A9）、数值矛盾跨站复制（A11）、重试反馈错位（B9） | 本轮净增量 |

---

## 6. 小修记录（复核确认后当场修，配红灯测试）

| # | 修什么 | 证据 | 执行 | 状态 |
|---|---|---|---|---|
| FIX-1 | 重试反馈错误定位：解析失败时报告真实 JSONDecodeError 位置（行/列/上下文窗口），不再无条件指向"末尾未闭合" | B9①（ESS + 2 事件站，错位实锤） | 专门执行者（worker）改 `orchestrator.py` 解析失败分支 + 红灯测试 | 见 §6.1 执行回执 |
| FIX-2 | event_section_summary 输入卡补 `needs_data_confirmation` 字段（上游契约已有、IA 同批卡 10/10 带、ESS 0/10） | A9（我比对两站卡键集） | 专门执行者改 `_build_event_section_summary` 的 compact_cards 构造 + 红灯测试 | 见 §6.1 执行回执 |

边界声明：supports/refutes 等其余缺失字段、事件底账进 bridge/治理站，均属供给设计，未动，留所有者/T38。

### 6.1 执行回执

专门执行者（worker）2026-08-09 完成，根线程验收：

- **FIX-1**：`src/agent_analysis/llm_engine.py:716-747` 新增 `diagnose_json_error(text)`（纯增量 32 行，按 extract_json 同款顺序找块、`json.loads` 捕获 `JSONDecodeError`，返回"错误信息+块内行:列+前后约 200 字符窗口"）；`orchestrator.py:5286-5308` 解析失败分支改为真实定位为主、末尾片段降为次要参考，误导句"请检查最后未闭合的数组、对象或字符串"移除；拿不到定位时逐字回退旧行为（旧测试 `test_run_stage_parse_error_feedback_includes_response_excerpt` 仍在绿）。
- **FIX-2**：`orchestrator.py:1640` compact_cards 增加 `"needs_data_confirmation": list(card.needs_data_confirmation)[:3]`（截断风格照 limitations）。
- 红灯证据：实现前 3 个新用例全红（字段缺失 / 定位仍指末尾）；实现后 `test_vnext_orchestrator.py` 151 passed、`test_vnext_llm_engine.py` 24 passed、`test_integrated_adjudication.py + test_contracts.py` 51 passed、`test_context_spread.py` 15 passed。
- 遗留风险（worker 如实交代）：诊断行:列相对提取出的 JSON 块而非全文（文本已注明）；llm_engine.py 的 UTF-8 BOM 曾误丢已逐字节恢复。
- 未触碰：extract_json 行为与接口、供给设计类字段（supports/refutes 等）、任何索引文档。

工单：本节 + `WORK_LOG.md` 2026-08-09 条目。

---

## 7. 与上一轮底稿（00/01/02）的对照

- **复现**：上轮 34 条确认发现中，主干全部复现（critic==risk 同输入、终审不收修订稿、IA unknown 占比、CI 立场泄漏、L5 OBV 双值、L2/L4 巨型材料、事件仅标题率、ESS 缺字段、L3 持仓滞后等）。
- **关闭"未恢复"**：上轮片 2（桥/裁决/调查）9 条、片 4（事件链）7 条因报告未落盘而丢失——本轮两片已全量重跑并落盘（bridge 11 条、CI 10 条、IA 10 条、事件链 11 站 93 条），未恢复事项清零。
- **数字口径修正（以本轮复核值为准）**：key_evidence_refs 上轮"96,311 字符"→ 本轮实测序列化 116,150 字符；slope_30d 上轮"58,097"→ 本轮 58,859；治理四站 Runtime Input 体量上轮 H1 侧写与本轮 sha256 复核值略有出入（229,296 逐字节相同仅 critic==risk 成立，reviser/final 为 231,222/231,300 有增量）——以 sha256 为准。
- **方法修正继承**：上轮 P3-2"投影比≠数据占比"继续有效，00 文档的误读表述上轮已改。
- **上轮三态清单的常设检查 10 项**：本轮 §3 的 A 类吸收了其中全部有效项并按新证据更新；落地排期建议见 §8。

## 8. 常设机械检查落成清单（A 类汇总，供排期）

1. critic vs risk（及任意两审查站）Runtime Input sha256 比对（A1）
2. 治理站 key_evidence_refs ⊇ thesis∪counter 引用集（A2）
3. final 输入含修订后内容（revised 字段/文本存在性）（A3）
4. IA ref_authority unknown 占比阈值告警（A5）
5. 委托调查数 vs 进材料报告数对账（含失败占位）（A6）
6. CI 材料 JSON 闭合性 + 立场字段（action_implication/dominant_side）检测（A7）
7. bridge/治理站事件字段**恒为空**，非空即报警（那才是污染事故）（A9）
8. 同名指标多值检测（PE/OBV/expected_return 类）（A11）
9. 约束/指令引用键存在性（raw_data/NO_DATA_AVAILABLE 类悬空）（B1/B10）
10. 事件站措辞矛盾对共存检测（B2）
11. 输出示例指标本层存在性（B4）
12. manual_overrides 陈旧占位日期（B5）
13. percentile_10y 取值域（0-1 vs 0-100）混用检测（B7）
14. context_brief 日期 vs 指标日期一致性（B8）
15. high_severity 容器名与 severity 字段一致性（B11）
16. 他站产物 null 占位/禁用标记进输入检测（B12）
17. bridge 冲突矩阵行完整性（B13）
18. L4 available 但数据陈旧检测（C3）；回购逐字重复行检测（C5）
19. canon 名 vs 输入 metric_name 一致性（C12）
20. （本轮已落）ESS 卡 needs_data_confirmation 存在性（FIX-2 配套）；重试反馈真实错误定位（FIX-1 配套）；摊开器 15 项红灯（阶段零）

不能机械化（B 类）的理由（下轮方法线索）：
- "该给什么"从没人声明过的，机器无从检查——每站"必备核心材料"需人一次性声明（体检方法第六节原话）；B14 摘要取舍标准同此。
- 措辞/标签的语义对错（B2 裁决、C8 方向标签）机械能发现共存，不能裁决正误。
- 偏科的"度"（supporting_only 占九成是否病态）是设计判断不是事实判断。

## 9. 未覆盖 / 未恢复事项（如实交代）

1. 各站**产出侧**（response/解析产物）未在本轮通读范围（本轮审的是输入侧材料结构）。
2. 语义级重复（换键序/换形态的同内容）未系统枚举——H1 只做了逐字块 + 定向核查，全量需 canonical-JSON 哈希专项（H1 盲区节已记）。
3. L4 的 15.5 万字符逐成分明细、L2 的 2,507 行序列未逐行审值（结构+聚合复算+首末行抽查覆盖，口径见 L4/L2 报告）。
4. 英文立场声明措辞（blind to/unaware of 等）未全覆盖（H3 盲区）。
5. HoM/m7_fundamentals 连续未调用的原因未查（T38 工单遗留，本轮未覆盖）。
6. 基线 run 之后的系统改动（§1.5）使 IA 许可类发现需新 run 单点复核——本轮未跑新 run（换基线留所有者拍板）。
7. 阶段一全部片无拆分申请；若所有者对某片质量存疑，可按 phase1/ 报告的证据链抽查复跑。
