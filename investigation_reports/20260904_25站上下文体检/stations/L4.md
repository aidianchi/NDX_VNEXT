# L4 估值分析师 上下文体检报告

> 站：L4（Context-Bounded Valuation Analyst）· run：t70_glm_check_20260902 · 体检日 2026-09-04
> 材料：attempt_1（唯一 attempt）。原文 635,561 字符（全链最大单站），投影 434,968（68.4%）。

## 结论

L4 有明确病灶，但病灶全部在**体量端**而非质量端：94% 的字符是 Runtime Input，其中 4 个指标占 layer_raw_data 的 85%，最大单指标 `get_ndx_earnings_revision_metrics` 独占 282,789 字符（全 prompt 的 44.5%），其内 ~203k 是逐成分股 dump（含 `archive_path` 内部路径），而输出实际只消费 5-6 个标量值。无 P0：D1 越权扫描零命中、C1 无硬冲突、E1 复读机零命中、输出叙事质量在 25 站标准下属上乘——63.5 万字符是「可白嫖的浪费」，本轮尚未兑现为输出事故，但已出现两处可连线的输出端传染/失实。

## 体检明细

### A. 顺序

**A1 段落编排** — 判定：P2（轻微）
- 布局：System Constraints（offset 0 起）→ Canon（~2k）→ Few-Shot（~13k）→ Layer Contract + L4 分析师规则（~23k-36k）→ **Runtime Input 597,176 字符（offset 36542-633718）** → 输出字段规格（offset 633718）→ Response Rules。
- 合理处：任务描述在 `context_brief.task_description`（Runtime Input 开头）；输出规格压轴放在 597k 数据之后，占据 recency 位置，正确。
- 病灶：**结构示例**（投影 583-622 行）`evidence_refs: ["L4.get_ndx_wind_valuation_snapshot"]` 用函数级引用，而 Data Authority Discipline（投影 698 行）要求 mixed-field payload 必须用 `L4.function_id#FieldName` 子引用。示例与纪律不一致（条件性：仅 mixed-field 场景触发，示例未覆盖）。输出实际遵守了子引用格式，未造成事故。

**A2 数据时序** — 判定：合理
- capex/buyback 季度序列旧→新（AAPL 2023Q3→2026Q2，payload 实测首末值）；`raw_earnings_dates` 从 2002-04-17 排到 2026-10-30，旧→新。
- 过期段落无冒充当前状态：过期项全部带显式标记（见 C4）。

**A3 few-shot 打断** — 判定：合理
- 3 个示例共 ~3.5k 字符，位于 Canon 与 Contract 之间（投影 402-422 行），未切断「任务定义→数据→输出要求」主干。

**A4 重试差异** — 不适用（单 attempt）。

### B. 体量与冗余（本站主菜）

**B1 体量账本** — 判定：P1（全站最重）

| 段 | 字符 | 占全 prompt |
|---|---|---|
| 指令区（System→Response Rules 除 Runtime Input） | ~38,385 | 6.0% |
| `## Runtime Input` | 597,176 | 94.0% |
| ├ `layer_raw_data` | 554,032 | 87.2% |
| ├ `layer_facts` | 9,320 | 1.5% |
| └ `context_brief`+其余 | ~3,800 | 0.6% |

`layer_raw_data` 内部（payload 实测）：

| 指标 | 字符 | 占 lrd | 内容 |
|---|---|---|---|
| get_ndx_earnings_revision_metrics | **282,789** | 51.0% | 逐成分 dump |
| get_m7_earnings_blackout_calendar | 68,050 | 12.3% | 24 年财报日期史 |
| get_m7_capex_cycle | 64,604 | 11.7% | 7 家×12 季全字段 |
| get_m7_buyback_flow | 57,649 | 10.4% | 季度序列两份 |
| 其余 7 个指标合计 | ~81k | 14.6% | — |

逐段「删掉判断会变差吗」：

1. **earnings_revision_metrics 的逐成分 dump ~203k**：`slope_30d.constituents` 61,133（96 条×18 字段，含 `archive_path`/`target_anchor_date`/`anchor_offset_days` 纯采集元数据，原文 offset 92140 起）、`slope_90d.constituents` 46,203、`divergence.7d/30d.constituents` 37,546（96+96 条，第三份逐成分斜率）、`dispersion_ntm.constituents` 19,896、`analyst_coverage.constituents` 8,739。模型做本层判断只需要 `slope_30d=0.0393`、`slope_90d=0.0691`、`breadth_30d 0y=60.7%` 等 5-6 个标量 + flagged 汇总（~6.5k）。**删掉，判断不变差**——输出 9 条 indicator_analyses 无一消费逐成分行。
2. **blackout_calendar.raw_earnings_dates 48,685**（原文 offset 484184 起）：AAPL 一家就 99 行、从 2002-04-17 排起。模型需要的 `per_ticker`（1,634）与 `upcoming_28d_calendar` 已是独立字段。**删掉，判断不变差**——输出只用了「0 家在静默窗外/10 月上旬进入」。
3. **capex.companies.quarters ~35k / buyback per_company.quarters 31.9k + raw_quarterly_series 11.9k**：`m7_aggregate.by_calendar_quarter`（7,156）已是现成汇总；buyback 同一指标内季度序列存了两份。逐公司明细可压成每家「最新季值+同比+pit_safe」一行，~35k→~3k。

**合计：~280k 字符（全 prompt 44%）可压到 <8k 而无本层判断损失。**

**B2 站内重复** — 判定：P2
- WorldPERatio 四窗口估值表（1y/5y/10y/20y，~50 行）在 payload 出现 **2 次**：`layer_facts.core_signals[].relative_position_context` 与 `ThirdPartyChecks[0].relative_position`（`valuation_windows` 计数=2 实测）。
- `MetricAuthority` 块（1,792 字符）在 earnings_revision 与 wind snapshot 各存 2 份（value 内 + data_quality 内），共 4 份。
- 3 个指标的 `data_quality.methodology` 与 `data_quality.formula` 逐字相同（实测 `==` 为 True）。
- `context_brief.layer_highlights` 行与 `layer_facts.core_signals[].summary` 逐字重复（Damodaran 摘要行出现 3 次）。重复量 ~5k，不致命，但同构于「三层复读」模式。

**B3 粒度错配** — 判定：P1
- 见 B1 第 1、2 条：模型需要 min/max/最新/变化率，prompt 给的是 96-102 条同构记录 ×3 份副本 + 24 年×7 家历史财报日期。成分记录连 `archive_path`（内部文件路径，offset 92140）都带进来了。这是把预处理活推给模型注意力的典型样本。

### C. 质量

**C1 指令冲突** — 判定：未发现硬冲突
- 纪律之间衔接自洽（如「财报周和绝对斜率超 20% 只降置信并保留入算」投影 656 行定义，输出 reasoning 照做）。仅存 A1 所记示例/子引用轻微不一致，判 P2。
- `RejectedMetrics.PriceToBook` 条件指令（原文 offset 32549：「若 RejectedMetrics.PriceToBook 存在，必须写明 component-model PB 已被剔除」）：**本 run payload 中 `RejectedMetrics` 出现 0 次、`PriceToBook=null`**——指令引用的容器在本 run 不存在。作为条件指令不算死指令、不算冲突，但提示指令面与数据 schema 存在漂移（指令为「可能存在」的形状预留了规则，数据端从未产出该形状）。判 P2。

**C2 死指令** — 判定：未发现
抽查 5 项全部存活：① `#FieldName` 子引用要求（投影 698 行）→ 输出 evidence_refs 全部带 `#PE`/`#PB` 等子引用；② cross_layer_hooks ≥2 且必含 L1/L2（投影 751-754 行）→ 输出 4 条，L1/L2/L5/L3 全覆盖；③ quality_self_check 要求（投影 610-621 行）→ 输出如实记录 2 个不可用指标；④ 「不得输出买卖建议」→ 输出遵守；⑤ 「不得只说便宜/昂贵，必须说明相对于什么」→ layer_synthesis 逐项说明相对历史/债券/盈利增长。特殊关注点结论：**本 run 无 RejectedMetrics、无被剔除指标整段占位的问题**（PriceToBook 为 null，未产生 component-model PB 数据）。

**C3 黑话词典** — 判定：P2（2 个传染源）
- **「C3 口径」**：原文 offset 581766，AMZN buyback `stale_reason`「…按 C3 口径标 stale，不作当前回购事实」。全 prompt 仅此 1 处，**无任何解释**。新分析师模型无法知道 C3 指哪条纪律。数据侧生成的字符串，传染源。
- **「老板 2026-08-16 裁决（O13）」**：原文 offset 406573，`get_equity_risk_premium` MetricAuthority reason。人工治理记录进入数据 payload；「O13」编号无解释（"裁决"语义可猜，O13 不可）。已证实传染至输出（见 E2）。
- 合格项：**4C** 首次出现即解释（投影 403 行「语境化、精炼化、典范化、因果化」）；`supporting_only`/`self_archive`/`supplier_lookback`/`pit_safe`/`stale` 等系统词均在 Data Authority Discipline 与 metric_authority.reason 中有上下文解释。

**C4 数据新鲜度** — 判定：合理
- 全部核心 data_date 为 2026-09-01/02，运行时点 2026-09-02，无静默过期。
- 过期/降级项全部显式标注且输出端正确降级：forward PE 持仓 `holdings_stale:42d`（输出写「降级确认用」）、AMZN 回购 `latest_period_end=2024-12-31` 标 stale、META capex 走 yfinance fallback 标 `pit_safe=false`、Trendonify 403 按不可用处理。
- 未发现「字段名暗示当前但值是旧的」。

### D. 隔离

**D1 越权扫描** — 判定：未发现
- `final_adjudication`/`thesis_draft`/`user_decision_profile`/`golden_pit`/其他层 layer card：**全部 0 命中**。`apparent_cross_layer_signals: []`。
- 「老板 2026-08-16 裁决（O13）」是系统所有者的**权限裁决记录**（规定简式收益差距只能作诊断），属数据发言权元信息，不是其他层结论——不构成越权，但作为内部治理语言进入分析站上下文，归 C3 处理。
- `browser_sidecar` 在指令区 1 处（投影 668 行）属数据源纪律表述，非越权。

### E. 输出闭环

**E1 叙事字段体检** — 判定：总体优，3 处小瑕疵
- **复读机检测：0 命中**（程序化检查全部叙事字段按。；切句后，每句 ≥15 字符与 prompt 逐字比对，无一句命中——模型在作答，不是背题）。
- **簿记语言密度：极低**。全部叙事 ~5,000 字符中 `supporting_only` 1 次、`core_allowed` 1 次，其余枚举/ref 均留在结构字段；数字全部嵌在因果链（如「trailing 盈利收益率 3.33% 比 10Y 4.75% 低 1.42 个百分点」）。轻微违反自家文风约定（投影 775 行「内部簿记语言不进叙事字段」）2 处。
- **黑话传染：1 处**。get_equity_risk_premium reasoning 写「按**裁决**明确其不得独立支撑强结论」——「裁决」直接来自材料里「老板 2026-08-16 裁决（O13）」，报告读者无从知道这是什么。**长度纪律**：规格未设长度上限，实际 local_conclusion ~340 字、layer_synthesis ~500 字，密度高不注水。
- **事实瑕疵**：get_ndx_earnings_revision_metrics.narrative 写「近端约**八成**权重在上修」，而数据 `breadth_30d 0y=60.75%`，同输出的 local_conclusion 与 layer_synthesis 均正确写「约六成」——**八成与数据不符，且输出内部六成/八成不一致**。

**E2 因果对**
- **能连线 1 对**：材料「老板 2026-08-16 裁决（O13）」（原文 offset 406573，MetricAuthority.reason）→ 输出 get_equity_risk_premium.reasoning「按裁决明确其不得独立支撑强结论」。内部治理语言顺着数据 payload 传染进叙事字段。
- **能连线 2（弱）**：B1 逐成分 dump 283k → 输出**零消费**逐成分行。因果方向是「材料浪费未伤判断」而非「材料缺陷致输出毛病」——证明浪费是实锤，本轮模型注意力扛住了。
- **连不上（明说）**：「八成 vs 六成」失实无法归因到材料缺陷（breadth 数值在数据中清晰无歧义），属模型自身口误，需更多 run 样本才能定是否系统性。

## 病灶清单（按严重度排序）

| 严重度 | 病灶 | 证据位置 |
|---|---|---|
| P1 | 逐成分 dump ~203k 占全 prompt 32%，输出零消费，成分记录连 archive_path 内部路径都带进 prompt | payload `get_ndx_earnings_revision_metrics`；原文 offset 92140 起 |
| P1 | `raw_earnings_dates` 48.7k 存 24 年×7 家财报日期史，所需字段（per_ticker/upcoming_28d_calendar）已有独立轻量版 | 原文 offset 484184 起 |
| P1 | 输出「八成权重在上修」与数据 breadth 0y=60.7% 不符，且与同输出「约六成」自相矛盾 | output.validated.json `get_ndx_earnings_revision_metrics.narrative` |
| P2 | 同构季表/估值窗/authority 块多处重复（WorldPERatio 表×2、MetricAuthority×4、methodology==formula×3、摘要行×3） | payload 实测计数 |
| P2 | 「C3 口径」黑话无解释进入数据 payload | 原文 offset 581766 |
| P2 | 「老板 2026-08-16 裁决（O13）」内部治理记录入 payload 并传染至输出叙事 | 原文 offset 406573；输出 get_equity_risk_premium.reasoning |
| P2 | 结构示例 evidence_refs 函数级引用与子引用纪律不一致；RejectedMetrics 指令引用本 run 不存在的容器（schema 漂移） | 投影 599 行、698 行；原文 offset 32549 |
| P2 | 输出叙事字段 2 处枚举词（supporting_only/core_allowed）违反自家文风约定 | output.validated.json capex/buyback reasoning |

## 修复建议

1. **（对 P1-1）** 在 payload 装配层把 `slope_30d/90d.constituents`、`divergence.*.constituents`、`dispersion_ntm.constituentents`、`analyst_coverage.constituents` 替换为统计摘要（N、加权均值、分位、top5/bottom5 各一行）+ flagged/invalid 汇总；逐成分行落盘审计文件、按需引用。预计省 ~200k 字符。`archive_path` 一类采集元数据无论何种形态都不应进 prompt。
2. **（对 P1-2）** `raw_earnings_dates` 只保留每家「下一次财报日 + 上一次财报日」两行（或直接删掉，`per_ticker` + `upcoming_28d_calendar` 已够），预计省 ~47k。
3. **（对 P1-3）** 输出校验器加一条数值一致性检查：叙事字段中的权重/占比数字须能在 payload 标量中找到 ±5% 匹配（六成↔60.7% 通过，八成拦截）。
4. **（对 P2 重复）** 装配层去重：`layer_facts.summary` 引用 context_brief 行而不重抄；`MetricAuthority` 只存 data_quality 一份；`methodology`/`formula` 相同时只留一个键。
5. **（对黑话）** 数据侧生成 stale_reason 时把「按 C3 口径」展开为具体规则文本（如「按数据新鲜度纪律 C3：最新申报期早于有效日期 365 天以上」）；「老板 2026-08-16 裁决（O13）」改写为「系统裁决：简式收益差距仅作诊断辅助」——语义不变，去掉内部编号与口语。
6. **（对 schema 漂移）** RejectedMetrics 指令改为仅当容器存在时才注入；结构示例的 evidence_refs 补一个 mixed-field 子引用示范。
