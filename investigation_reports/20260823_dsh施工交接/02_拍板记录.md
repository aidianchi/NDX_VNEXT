# 拍板记录：T60 二档三档 dsh 施工线·一期 5 个细节点（2026-08-24）

老板 2026-08-24 对工单第四节 5 个待拍板点的裁决：

1. **搜索腿白名单**：按建议分档——官方 / 主流财经可进正文；卖方 / 社媒默认只进线索不进正文，档档带标签。
2. **预算默认数**：默认额度 **3000 万 token**（取代设计稿建议的二档 30 万 / 三档 300 万）。
3. **缺口类议程激活方式**：按建议——候选制，老板点头才激活（老板说"听不懂，先按建议来"，后续可再议）。
4. **调查员事件侧上交二档**：按建议认可，三期拆受控例外通道（同上，"先按建议来"）。
5. **一期开工范围**：全量开工，不搞试点。

环境事实补充（2026-08-23 开工当日实测）：

- `deepseek-harness-sdk==0.1.1rc1` + `deepseek-harness-runtime-bin==0.1.1rc1` 已装入项目 .venv，版本锁定不追新。
- 最小任务冒烟通过（`.t61_scratch/dsh_smoke.py`，finish=completed）。
- SDK 路线确认：hooks 桥（dsh-hooks-claude-code）、web_search/web_fetch、token-meter 都在 runtime 依赖闭包内；**schedule 不在**（持续追踪名单以后用外层 Python 定时投递）。自写 TS 插件能否被 exe 动态 import 未证实——一期卡口全部走 Python hooks 桥规避此风险。
