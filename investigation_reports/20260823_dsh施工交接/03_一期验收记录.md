# 一期验收记录：二档地基（2026-08-24）

> 对应工单 §六验收标准。两次真实 run 的落盘都在 `output/event_research/runs/`。

## 验收标准逐条对照

| 标准 | 结果 | 证据 |
|---|---|---|
| 用"AI capex 叙事"跑一次真二档巡逻 | ✅ 完成，finish=completed，花费 142 万 token（卡面 3000 万） | `EV-20260823-7e9c93_20260823T163200Z/run_summary.json` |
| 产出带对账通过率的材料卡 | ✅ 7 张卡，对账通过率 7/7 = 1.0（每条 fact 的引文逐字命中抓取原文） | 同目录 `material_cards.jsonl` 的 reconciliation 块 |
| 经费卡实测拦得住（故意小预算） | ✅ 卡 2000 token，第一笔请求花 4.8 万，第二笔被 pre-step 卡口拦死（finish=blocked），run_summary 如实标 budget_exhausted + publishable=false（半成品） | `EV-20260823-376912_20260823T163046Z/run_summary.json` |
| 全程材料带四条镣铐 | ✅ 镣铐机器校验生效：7 张卡中 2 张被抓（fact_summary 含 "would"/"could" 模糊词）→ 发布闸门标 shackles_failed、publishable=false。镣铐抓到了真违规，这正是闸门存在的意义 | 同上 validation_errors |
| 产出留在事件层，主链零污染 | ✅ 主链零改动；产出全部在 `output/event_research/`；全量测试含 PC 隔离检查不红 | `pytest` 全量 1329 绿 |

## 顺带实测到的卡口行为

- 白名单闸：同一次巡逻中 fool.com / cls.cn / qq.com / microsoft.com 的 web_fetch 被 PreToolUse 卡口 deny（session 日志 `hook/result` 逐条在案），reuters.com 放行。
- 经费卡闸：逐 step 查账（每次模型请求前 `UserPromptSubmit` → agent/pre-step 卡口触发，`budget_state.json` 逐步回写）。
- 来源打标器：每次抓取后给模型贴档位标签（PostToolUse additionalContext）。

## 施工中修掉的三个真 bug

1. **runner 相对路径**：cordis.yml 相对路径在 runtime 子进程（cwd=run 目录）里解析失败 → run_dir 一律 resolve 成绝对路径。
2. **对账器结果解析形状错误**：dsh 的 tool/result 文本在 `content[0].content`（tool-result 块嵌套）里、callId 在 `message.source.callId`——初版按扁平形状解析导致引文永远对不上（虚报 0/7），修正后真实 7/7。教训：对账器第一版按文档形状写，实测形状以 session 日志为准。
3. **材料卡域名口径冲突**：原型校验器写死 9 个官方域，与新分档白名单打架（cnbc.com 被误杀）→ card.py 滤掉原型这条错误码、以新白名单为准（原型文件不动）。

## 遗留问题（转老板定夺 / 二期）

- **公司官方 IR 域不在白名单**：microsoft.com 等 IR 页被抓取闸拦下，"巨头 capex 指引"只能经主流财经转述。巡逻的 charter_feedback 建议：白名单增设"公司官方披露"档（microsoft.com/investor 等 IR 域）。这属于白名单范围调整，按纪律归老板拍板。
- 主流财经正文多有付费墙/反爬（FT/Bloomberg 403），全文率受限于公开页——五项度量第 1 项的基线就此起步。
- 三档专题深挖（二期）、调查员事件侧上交与例外通道拆除（三期）未开工。

## 账本状态

- 议程账本 `output/state_ledger/event_agenda.jsonl` 已有两条记录：EV-20260823-376912（小预算实测，done）、EV-20260823-7e9c93（AI capex 巡逻，done）。
- 单测 43 条（tests/test_event_research.py）+ 全量 1329 绿。
