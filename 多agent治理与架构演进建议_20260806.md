# 多 agent 治理与架构演进建议（2026-08-06）

> 写给项目所有者。回答三个问题：① 非工程师如何掌控这个多 agent 项目；② "深度研究+金融分析"的最佳多 agent 范式；③ 对照 harness engineering / 渐进式上下文加载 / Codex 等顶级实现的分阶段演进路线。
> 依据：`系统说明_人话版.md`、`CLAUDE.md`（北极星与常驻边界）、`现在.md`（T09/T16/T38-T47 状态）、`体检方法.md`、2026 年初 Anthropic / OpenAI 关于 agent harness 的公开实践与《Agent Harness Engineering: A Survey》（2026-05，OpenReview）。
> 本文是建议，不是状态声明；事项状态以 `现在.md` 为准。

---

## 〇、先回答一句话

你的系统**范式选对了，不需要推倒重来**。真正要做的是三件事：把你的掌控机制从"看报告"升级成"看闸门"；把反方/论点两站的"整包灌入"改成"按需取用"（就是已立项的 T38）；把校准闭环从零次真实打分跑起来（T09）。其余都是优化，不是救命。

---

## 一、全局掌控路径：非工程师的所有者治理框架

### 1.1 心智模型：你管的不是代码，是"证据链的完整性"

软件项目的所有者如果不懂代码，最大的风险是只能问"做完了吗"，得到的永远是"做完了"。你这个项目有一个绝大多数软件项目没有的好处：**它的产出本身就是一份为自己辩护的证据链**。所以你不需要懂代码，你需要的是一套"质检动作"，让任何一环出问题都会在你能看见的地方留下痕迹。

把系统想象成一家报社：L1-L5 是五个互不串供的记者，桥接是编辑部主任，正反辩论是编前会，终审是总编，Schema Guard 是校对科的机器，发布闸门是付印前的最后一道章。你不需要会写稿，你只需要定期做四件事：**抽查一篇稿子、看校对科的拦截记录、看有没有记者偷偷串供、看上个月哪篇稿子被事实证明错了**。

### 1.2 理解系统结构：用"每站四问"当验收单位

你已经有的最佳工具是 `系统说明_人话版.md` 第二节的固定句式——每个站点回答四个问题：**在回答什么问题 / 凭什么有资格回答（能看到什么）/ 被禁止看到什么、为什么 / 失败了会怎样**。

建议把"每站四问"变成你的治理工具，而不只是阅读材料：

- 任何一次架构改动，要求执行方（AI 或工程师）**先回答"这改变了哪一站的哪一问"**。答不上来，或者答案是"改了某站能看到什么但没改禁止清单"，就是红灯。
- 每季度抽一站，让它重新"述职"：把该站最近一次真实运行的输入清单、输出、被禁项检查结果摆出来，你花 20 分钟按四问核对。一年下来 13 个站点轮一遍。

### 1.3 验证各 agent 工作质量：三层验证，每层你都有具体动作

| 层 | 谁在查 | 查什么 | 你的动作 |
|---|---|---|---|
| 机械层 | 代码（1055 项测试 + Schema Guard + 证据编号比对） | 形状、引用合法性、隔离是否被破 | 每次交付只问一句："全绿吗？有没有'合跑才绿、单跑会崩'这种水分项？"（T43 就是现成案例） |
| 判断层 | 你 + AI 互查 | 结论是否回答六问、冲突是否被抹平、降级是否暴露 | 每份发布报告花 15 分钟做"五问抽查"（见下） |
| 校准层 | 事后打分（T09） | 判断事后对不对、错在哪类 | 每个打分窗口看一次成绩单，追问"归因到了哪个方法改动" |

**五问抽查清单**（印在卡片上，每份报告照问）：
1. 这份报告的 conflicts 字段是空的吗？——空的本身就是事故（"冲突是资产"，不许空）。
2. 质量闸门写的是"批准"还是"批准但有保留"？保留的具体理由我读得懂吗？
3. 有没有降级标记（`deterministic_fallback` / `degraded_fallback`）？它出现在最终报告里，还是只在中间产物里？（T21 就是反方降级没暴露的真实事故）
4. 随便挑正文里一个数字，让 AI 当场演示从数字 → 证据编号 → 原始数据文件的完整追溯。一次追不通，整份报告的可信度打折。
5. 这次跑花了多少 token、比上次多了还是少了？花最多的那两站（反方、论点）的材料厚度有没有变化理由？

### 1.4 里程碑与验收标准：里程碑必须挂在北极星支柱上，不挂在功能上

错误示范："完成桥接重构"。正确示范："反方降级必须在最终判决书留痕——验收方式是人为制造一次反方失败，看判决书是否出现可见提示"。

建议的未来 12 个月四个里程碑（与 `现在.md` 现有工单对齐，不另起炉灶）：

| 里程碑 | 对应工单 | 北极星支柱 | 验收标准（人话版） |
|---|---|---|---|
| M1 第一张真实成绩单 | T09+T41 | 校准闭环 | 至少一轮"打分→归因→人工裁决→修方法"完整跑完；判断书里能看到"事后对错"或如实的"未到评分窗口" |
| M2 上下文按需取用 | T38（前置 T44④已完成） | 事实扎实（不降级前提下降本） | 同一批输入对照跑两次：新旧供给方式的结论、冲突清单、证据引用逐条一致；治理四站读入 token 降 40% 以上 |
| M3 正反证据对称 | T46+T21 修复 | 证据面均衡 | 反方引用有独立承载、不会随正方引用消失而消失；有一条专门测试守着 |
| M4 成批体检常态化 | T47+体检方法其余四式 | 全部三根 | 五种体检（上下文/溯源/时点/对称性/闸门有效性）每季度各跑一轮；每条确认发现要么落成机械检查、要么写明为何不能 |

每个里程碑的验收都要满足你已有的规矩：**可验证的成功标准 + 红灯测试摆在哪 + 否决过什么写进 WORK_LOG**。

### 1.5 不写代码如何驱动迭代：你的三个不可替代动作

1. **定优先级，而且敢于搁置。** `现在.md` 的"后排"和"明确不做"清单是你权力的痕迹——Finviz、Wind 一致预期、"历史上像今天"引擎都是被你判死的。这个清单本身就是最高效的成本控制：每一条"不做"都在替项目省钱。保持它。
2. **当最终读者验收。** 系统的"北极星六问"最后一问是"事后对错与方法修正"——只有真实读者说"这个结论对我没用/这个风险没提示我"，方法修正才有方向。这是 AI 自己给不了的信号。
3. **提供领域判断。** "当前哪类风险最不能漏"（比如集中度崩塌 vs 流动性冲击的相对权重）是投资决策者的输入，不是工程师的。每个季度给一次"我最怕什么"的排序，让它影响风险哨兵的检查权重。

节奏建议：**每周 10 分钟**读 `现在.md` 的"三十秒看懂全局"+"等你决定的"；**每份发布报告 15 分钟**做五问抽查；**每月 30 分钟**看一次 token 账单趋势和体检发现清单；**每季度**一次里程碑验收 + "我最怕什么"排序更新。

---

## 二、多 agent 最佳范式：从第一性原理看"深度研究+金融分析"

### 2.1 第一性原理：为什么需要多个 agent？

多 agent 存在只有三个正当理由：

1. **上下文隔离保护判断独立性。** 同一个模型在同一个上下文里做"五层交叉验证"，几乎是必然互相污染——它刚写完"趋势很强"，下一秒很难诚实地写"估值已透支"。这不是模型能力差，是注意力的物理性质。
2. **认知变换边界。** 同一份材料，"找支持证据"和"找反驳证据"是两种不可兼任的读取姿态。让一个上下文同时做两件事，结果通常是两件事都做一半。
3. **并行换成本与速度。** 互不依赖的分析可以同时跑。

**注意：三个理由里没有一个是"模仿人类公司的部门分工"。** 你项目里 "Context-first, role-second" 这条原则，和 2026 年初 Anthropic、OpenAI 公开实践中收敛出的共识完全一致——这是你的架构里最值钱的一条，不是巧合，是押对了。

### 2.2 当前顶级实践收敛出的范式

2026 年上半年，Anthropic（长任务 harness 两篇）、OpenAI（Codex harness 工程）、以及 5 月 OpenReview 的《Agent Harness Engineering: A Survey》学术综述，在"判断类/研究类多 agent"上收敛出的骨架是：

- **Agent = 模型 + Harness**（缰绳比马重要：LangChain 的对照实验里，只改 harness 不改模型，准确率 52.8%→66.5%）
- **独立分析者并行产出 → 对抗机制（debate / 反方）→ 裁决者 → 确定性校验器（非 AI 的机械闸门）→ 事后评估回环**
- **生成者与评估者分离**（GAN 式：写的人不能是判的人）
- **上下文防火墙**：子任务之间只传结构化结果，不传原始对话

### 2.3 对照结论：你的系统已经是这个范式的完整实例，而且有两处超前

逐条对：五层隔离分析 ✅（独立分析者）；反方在主论点之前先跑、物理上不可能"先看答案再假装反对" ✅（这是真对抗，多数系统的"反方"是看完结论后的表演式反对——**你这一点比绝大多数公开实现做得硬**）；正反共用同一份证据菜单 ✅（对称辩论）；Schema Guard 纯代码、不判断意思只判断形状 ✅（确定性校验器）；发布闸门+降级留痕 ✅；事后打分回环的设计 ✅（但还没真跑，见第三部分）。

**两处超前于常见实践**：① "隔离必须可事后证明"——每次跑落盘 prompt 全文，能 grep 实际发出的字节来证明五层互不知情；多数系统的隔离只是"设计如此"，无法举证。② "宁缺毋滥"——事件卡不足就留白，不用套话硬凑；多数系统把"有输出"当成功。

**三处与最佳实践的差距**（都可修，不需要重建）：

1. **批评者与风险哨兵拿到一字不差的同一份输入。** 视角多样性来自上下文差异，不是 prompt 差异——同一个模型读同一份材料，"一个挑逻辑、一个列风险"的分工会被上下文趋同吃掉大半。这是当前架构里性价比最高的一处修改：给两者不同的材料包（批评者看论证链结构，风险哨兵看风险边界清单+数据边界），比换更强的模型收益大。
2. **反方和论点建构两站读入 56 万 token，占全程 43%。** 这不是它们需要这么多材料，是"整包灌入"的供给方式造成的。顶级 harness 的共识是渐进式披露：先给地图，按引用编号取细节。这正是你已立项的 T38。
3. **评估回环零次真实运转。** 没有 eval 的多 agent 系统，所有"质量"都是体感。T09 的窗口已经开了，这是当前欠账里最影响"判断要准且越来越准"这条北极星的一项。

### 2.4 一句话回答"什么范式最适合"

> 对高 stakes 的金融判断任务，最佳范式是：**上下文隔离的独立分析 + 共享证据菜单的对抗辩论 + 不能给自己放行的终审 + 非 AI 的确定性闸门 + 事后校准回环**——也就是你现在这套。竞争范式（单强 agent 长链推理、辩论式无隔离、纯 ensemble 投票）分别输在污染、表演式对抗、和无解释力。你的问题不是范式选错，是范式里三个零件还没上紧。

---

## 三、架构演进路线：对照 harness engineering 与渐进式上下文加载

### 3.1 先定义差距：你在 harness 工程上的位置

Harness engineering 的六个共识组件（上下文工程 / 架构约束机械化 / 工具治理 / 子代理上下文防火墙 / 失败回路 / 评估体系），你的现状：

| 组件 | 你的现状 | 差距 |
|---|---|---|
| 上下文工程 | 有显式层级输入政策（白纸黑字的禁止清单）——比多数项目强 | 供给方式是"整包灌入"，不是渐进披露；13/25 站零检查规则（T47 已立案） |
| 架构约束机械化 | Schema Guard + 证据编号身份比对 + 1055 项测试 | 批评者/风险哨兵输入未分化；"发现新毛病"还靠撞（T47 正在补） |
| 工具/数据治理 | 弱来源隔离、数据源分级、effective_date 纪律 | 数据源权威性的长期悬案（T16 forward PE）未决 |
| 子代理防火墙 | 层间隔离可落盘证明——这是顶级水平 | 受控调查员是好雏形，但"按需取用"权限问题未解（T38 的 Q2 前置） |
| 失败回路 | 降级留痕机制有，但暴露不对等（T21：反方降级不进质量闸门） | "每次失败 → 结构性修复"的转化率靠人工，未成流水线 |
| 评估体系 | 打分器就位，零次真实打分 | 这是最大缺口：没有 eval 的 harness 无法证明自己在变好 |

**结论：你的 harness 在"约束与留痕"维度已是公开实践里的上游水平；差距集中在"上下文经济学"和"评估回环"两块。** 好消息：这两块都已经在 `现在.md` 里立项了（T38、T09/T41），不需要新方向，需要的是排序和验收标准。

### 3.2 分阶段方案（每阶段都标注与北极星三支柱的对齐）

**阶段 0：先量后裁（当前，1-2 周内）**
- 完成 T47 上下文体检 + T42 边界补牙余下三件。
- 理由：渐进披露要裁掉的是"整包灌入"里没人用的部分——**不把每个站实际拿到的材料摊开查一遍，就不知道什么能裁、什么裁了会出事**。这是你 08-05 拍板"先体检再改供给"的逻辑，外部实践完全支持这个顺序。
- 北极星对齐：事实扎实（防止裁掉该给的）。

**阶段 1：渐进式上下文供给落地（T38，1-2 个月）**
- 做法：治理四站（桥接→反方→论点→批评/风险）从"整包材料"改为"静态骨架 + 按证据编号按需取细节"；每次取用落盘留痕（哪一站、取了哪个编号、何时）。
- 验收：同一批输入新旧方式对照跑，结论、冲突清单、证据引用逐条一致；治理站读入 token 降 40%+（反方+论点 56 万 → 目标 30 万以下）。
- 红线：取用记录必须能像现在 grep prompt 全文一样**事后证明"这站当时看到了什么"**——`HANDOFF.md` 里已写明，任何重建不得牺牲可落盘证明的隔离。
- 北极星对齐：事实扎实不降级前提下降本；可审计性反而增强（从"给了它什么"细化到"它实际用了什么"）。

**阶段 2：对抗机制修偏（与阶段 1 可并行，小时级到天级改动）**
- 批评者与风险哨兵输入分化（不同材料包）；T21 反方降级进质量闸门；T46 证据集合正反对称。
- 北极星对齐：证据面均衡——这是三支柱里当前唯一有"今天就在跑的偏科"记录的支柱。

**阶段 3：校准闭环点火（T09+T41，打分窗口已到）**
- 跑完第一轮"打分→归因→人工裁决→修方法"；判断书给"事后对错"一个落点。
- 管理预期：独立观察点一年最多十来个，统计意义一两年后才谈得上——所以这阶段的目标不是"证明准"，而是**把"证明准的机器"先开起来**，让数据开始积累。
- 北极星对齐：校准闭环常开（第三支柱从零到一）。

**阶段 4：选择性引入 agentic 自取（T45 的终局问题，3 个月后再评估）**
- 顶级 harness（Codex 式）的"模型自己找材料"对你有一个不对称风险：审计链。**固定菜单保证证据面可枚举、可对称、可事后穷举检查；模型自取会引入"它为什么没看 X"这种无法证伪的缺口。**
- 建议：折中路线——主证据链保持固定菜单（不可谈判）；把"自取"限制在受控调查员的升级形态里（明确问题清单 + 白名单材料域 + 全程落盘），让它从"桥接的附属支线"逐步承担更多受控探索。
- 决策纪律：按 T45 已定的规矩——先落盘决策规则再做调研，事后不得改规则。
- 北极星对齐：用"可审计性不降级"作为任何自取机制的准入门槛。

### 3.3 什么不该学 Codex

- **不学无状态回放式编排**（Codex CLI 每次从头构建上下文）：你的工单系统里已明确 WorldState 式增量"不能照搬"——理由同样适用，"这一站当时看到了什么"必须能从该站 artifact 独立还原。
- **不追多模型混编的时髦**：在证据隔离纪律下，换模型带来的边际收益远小于修输入分化和上下文供给；且多模型会给"隔离可证明性"增加举证成本。真要试，等阶段 3 有了真实打分数据后用 eval 说话。

### 3.4 最终形态的一句话描述

> 五层隔离分析不变；治理链路从"背着整个档案袋开庭"变成"拿着证据目录开庭、随取随登记"；正反辩论物理对称；每次降级都在判决书留痕；每个判断 T+20 后自动收到成绩；成绩按类型归因回具体的 prompt / 权限 / 阈值修改。**架构的每一根梁都还是现在的梁，变的是材料供给方式和回环是否通电。**

---

## 附：本次阅读发现的一个文档问题

`系统说明_人话版.md` 正文四处引用"第四节"（T21 反方降级暴露不对等、批评者/风险哨兵同输入、叙述字段名 vs 合法引用名两套词汇等），但文档实际在第三节 token 结算单处结束——**第四节还没写**。这三处问题恰恰是所有者最该看的"没人替你验证过、需要自己盯着"的部分，建议补上。
