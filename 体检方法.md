# 体检方法

**这份文件回答一个问题：怎么在系统"出事"之前，成批地发现它有什么毛病。**

它不是待办清单（那在 `现在.md`），也不是某次调查的结论（那在 `investigation_reports/`）。它是长期方法论，和 `回测原则.md` 同级——回测原则讲"历史必须怎么验"，这份讲"系统本身必须怎么查"。

---

## 一、为什么需要它：撞见式排错的天花板

到 2026-08-05 为止，这个项目发现问题的方式几乎全是**撞见**：

- VXN 那条（终审站说"没数据"、而数据就在同一份报告里）——是模型恰好被追问到那个指标，不得不当场说实话才暴露的。
- 治理四站的证据被正方单方圈定——是一次人工方向审查偶然翻到的。
- 回测时点泄漏——是有人手工把回测日调用和实时调用摆在一起比，才发现返回值一模一样。

**撞见式排错有一个数学上的天花板：它只能发现"已经发作过的"问题。** 而这套系统的多数毛病是安静的——材料给少了、数值没传过去、反方的证据没进来，**模型不会报错，产物不会变红，报告照样生成**。

实证（2026-08-05）：写一个几十行的脚本扫全部 35 次跑，几秒钟检出 **41 条**"系统手里有数值、也许可了该站引用、就是没发过去"。而在此之前，同一个毛病已经**连续存在 8 次跑、最早 7 月 19 日**，无人察觉。

更要命的一条对照：七路审计当时扫过 7 次跑，查"模型有没有抱怨数据缺失"，0 次复现，于是写下"该症状本次为新发"。**那扫的是症状，不是病。** 症状只在模型恰好被追问时才出声；病一直在。

> **扫症状会把"一直在坏"读成"偶然出事"。**

---

## 二、配方：什么样的方法算"体检"

一个方法能成为体检，要同时满足三条：

1. **有完整语料**——不是抽样，是这一类东西的**全部**，而且系统运行时自动产生，不需要额外制造。
2. **能统一提问**——同一个问题可以机械地问遍语料里的每一个元素。
3. **不等症状**——问题不必发作过就能被问出来。

**这三条是生成器，不是描述。** 想找新的体检方法，就问：**这个系统还自动产生了哪些完整语料？对它能问出什么统一的问题？**

---

## 三、五种根本性体检

判"根本性"的标准：**它检验的是北极星的一根支柱，且当前没有任何常设检查在做这件事。**

### 1. 上下文体检——每个 agent 到底看到了什么

- **语料**：`prompt_audit/`，每次跑自动落盘每一站的完整提示词、材料、原始回答、解析结果。**原材料一直在，只是没人扫。**
- **统一提问**：这一站被要求回答的问题，它手上的材料够不够？不该它看的东西有没有混进来？
- **测哪根支柱**：事实扎实（材料齐全）+ 证据均衡（材料对称）。
- **能抓什么**：许可了却没给数值、系统手里有却没发、被预算截断砍掉的关键材料、跨层污染。
- **现状**：已有 `prompt_inspector.py` 但只查"污染"不查"缺料"，13/25 站零检查规则，出事的两站它连提示词都读不到。
- **T47 的唯一权威表述在 `T47_上下文根本性审查.md`**：主目标是 AI 通读找结构病，机械锁死只是确认后的防复发收尾——别读反（2026-08-06 已发生一次读反事故）。

### 2. 溯源体检——判断书里每个数字，能不能一路回到原始采集

- **语料**：一次跑的完整产物链（原始采集 → 层内事实 → 证据索引 → 主张台账 → 判断书）。
- **统一提问**：报告里出现的每一个数字，在上游能不能找到同一个数字？口径、单位、时点一致吗？
- **测哪根支柱**：事实扎实——"每个数字经得起独立重算和来源追溯"是北极星原话。
- **能抓什么**：模型自撰的数字、单位错位、陈旧值被当成当前值、经过多层转述后悄悄变形的数。
- **现状**：现有校验只查"引用编号存不存在"，**不查"引用旁边那个数字是真的吗"**。八条边界里"不得编造"是唯一没找到专门测试的一条。

### 3. 时点体检——历史里有没有混进未来

- **语料**：可以主动生成——每个数据工具用回测日调一次、用实时调一次。
- **统一提问**：两次返回一样吗？一样的要么是时不变量，要么是泄漏，必须逐个说清是哪种。
- **测哪根支柱**：事实扎实（时点纪律）——回测结论的可信度全押在这上面。
- **能抓什么**：绕过日期参数的兜底路径、被硬编码成"今天"的过滤条件、覆盖率计数器在回测下失真。
- **现状**：只有单点硬拦名单，而且**名单靠人手工维护、没有测试守住它的完整性**。

### 4. 对称性体检——正反双方拿到的东西是否真的对称

- **语料**：每一处"决定给谁看什么"的代码与其产物。
- **统一提问**：把多空两侧对调，这个决策还成立吗？任何一侧独有的材料，理由是什么？
- **测哪根支柱**：证据面均衡——"菜单不得先天偏科"是常驻边界。
- **能抓什么**：治理四站的证据被正方引用锚定（反方零承载）就是这么发现的；同类还可能藏在事件卡、冲突筛选、终审引用里。
- **现状**：只在"正方辩论 vs 反方辩论"这一对上被验证过；**过了这一对就没人查**。

### 5. 闸门有效性体检——保护装置本身可不可靠

- **语料**：全部测试与全部闸门。
- **统一提问**：故意把某处改坏，有没有测试尖叫？没有的话，那条闸门只是摆设。
- **测哪根支柱**：元层面——上面四条支柱都靠闸门守着，闸门本身没被验过，前面全是空头支票。
- **能抓什么**：已知两例——有测试单独跑会崩、跟别人一起跑才绿（说明它依赖巧合而非逻辑）；有测试把两条真实路径全 mock 掉了（说明它测的是替身不是本体）。**测试数量不等于保护强度。**
- **现状**：完全没有。1055 项全绿是"跑通了"，不是"拦得住"。

---

## 三·五、跑后复盘：每次真实运行后查什么

体检是"成批找病"，跑后复盘是"每跑完一次就过一遍验收标准"——两者是同一套检查精神在不同时机下的形态。本小节吸收原「RUN_REVIEW_CHECKLIST.md」（2026-08-08 并入）。

### 判断分三档

- **通过**：证据清楚，结果可追溯。
- **观察**：能用，但有明显瑕疵，下一轮要继续看。
- **未通过**：会误导读者，或破坏 vNext 的核心原则。

### 回测与快照专项检查（每次历史回测或采集机/快照模式运行后必查）

| 检查项 | 通过标准 |
| --- | --- |
| 回测有效日期 | 所有进入 agent 上下文的数据、新闻、图表行都不晚于 `backtest_date` / `effective_date`。 |
| 跳过项是否明示 | `backtest_data_boundaries` 列出被跳过的指标、原因和未来升级路径。 |
| 严格回测 invariant 是否入包 | `strict_backtest_invariants` 同时出现在数据包、DataIntegrity、AnalysisPacket / run summary 和 native brief 审计区。 |
| 强制项与限制项是否分清 | `hard_enforced` 只列已工程化强制的规则；ALFRED vintage、first-reported、point-in-time universe、LLM 后验知识应留在 `declared_limitations`，不能伪装成已解决。 |
| 当前快照是否误入回测 | 当前 Wind NDX 快照、bb browser、Trendonify、蛋卷、WorldPERatio 等当前数据没有在历史回测中默认充当当时证据。 |
| yfinance 成分股代理 | 回测模式未自动触发 yfinance 成分股基本面批量代理；如使用人工或可证明历史当时可见的 Wind 数据覆盖，必须有来源和数据日期。 |
| 新闻侧栏 | 新闻只作为辅助 sidecar，不进入 L1-L5，不成为 `evidence_ref`；发布日期不得晚于回测日。 |
| LLM 后验风险 | 报告承认新模型可能有后验知识；治理只要求证据不越界，不假装完全复原当时人脑。 |
| 数据快照不可变 | 分析阶段消费的是同一份数据包，不能一边采集一边切换网络导致半截数据、半截分析。 |
| 研究候选证据 | 联网 AI skill 找到的新来源只标记为 `research_candidate` / `manual_review_required`，未确认前不得进入核心证据。 |

### 五项复盘（每次真实 run 后必过）

1. **指标说明书是否减少误读**：每个指标都在自己的能力范围内说话（`layer_cards/L*.json`）——指标发言权是否填写、技术指标是否避免替估值/基本面下结论、代理指标是否被当成官方事实、反证条件是否具体、Wind 可用时实时 L4 是否优先使用 Wind 主锚、L4 旧替代项是否被正确降权、History of Market 低样本是否撤回分位、元信息是否薄而硬。
2. **跨层冲突是否稳定、具体、可追溯**：Bridge 是否生成 typed conflicts（`bridge_memos/*.json`），每个冲突是否含涉及层级、证据引用、冲突机制（为什么冲突），高严重度冲突是否被后续报告保留（`synthesis_packet.json` / `final_adjudication.json`）。
3. **客观性防火墙是否拦住过度自信**：报告下结论前是否检查对象、证据、时间、反证和未解决矛盾（`synthesis_packet.json` 的 `objective_firewall_summary`）；最终报告是否避免无证据强标签（`final_adjudication.json`）。
4. **第三层结构指标是否应升级为硬要求**：L3 是否能说明 NDX 内部结构（`layer_cards/L3.json`），是否区分指数上涨和市场广度、龙头贡献和整体健康；schema guard 是否只提示而非硬失败。每轮只回答三问：哪些软约束已稳定可转硬约束？哪些仍应保持提示？有没有会误导普通读者的展示或措辞？
5. **brief 页面是否适合普通读者连续阅读**：最终判断是否容易找到、主要证据链是否容易理解、跨层冲突是否可见、指标发言权和反证条件是否可见、证据跳转是否可用（`output/reports/*_brief.html`）。

### 评分复盘流程

成熟批次拿到 `claim_outcome_scores.json` 后，先按 `error_taxonomy` 聚类（当前评分器只把已有规则明确识别出的结果归为方向错误/正确/不可评分，不猜测"幅度错误"）。再由 Fable/用户主持人工裁决：某类错误是否真的指向提示词、合约、权限或阈值中的具体方法要素。只有人工确认后，才把文件路径、实际提交哈希、预期效果和复查日期写入 `method_revision_ledger.jsonl`；系统不得自动改方法，也不得虚构修订条目。下一批评分成熟后对照 `review_after` 检查预期效果是否出现，同时保留没有改善或出现副作用的结果。该台账是方法审计记录，不回流 L1-L5 或当前投资判断。

---

## 四、共同架构：AI 负责发现，代码负责锁死

**这两样不是替代关系，是接力关系。** 弄反了会同时失去两边的好处。

| | AI 通读审查 | 机械检查 |
|---|---|---|
| 能发现 | **全新的问题类型** | 已知类型的全部实例 |
| 单次成本 | 高 | 近零 |
| 能不能每跑都做 | 不能 | 能 |
| 防不防复发 | **不防** | 防 |

- **只做 AI 审查**：等于每隔几周花大钱重新撞一遍。VXN 就是活证据——被发现之前已经连续坏了 8 次跑；如果只靠人工审查，下次同类问题照样要等几周。
- **只做机械检查**：永远发现不了"本该有但从没人声明该有"的东西。治理四站的偏科，任何机械规则都推不出来——因为系统里从来没有一句话写着"挑刺站必须看到反方证据"。

**所以标准循环是**：

> AI 通读发现新类 → 当场落成一条机械检查 → 这一类此后不再复发 → AI 下次去找更深的东西。

这是「轨道 > 导出 > 规矩」用在检查上：**凡是这次搞明白了"该查什么"的，立刻变成代码，不许留给下一次的人靠自觉。**

**每条被确认的发现，必须当场回答一个问题：这条能不能变成机械检查？** 能就立刻落；不能就写下为什么不能——那个"为什么"本身就是下一轮体检方法的线索。

---

## 五、三条铁的约束（不遵守会重蹈覆辙）

这三条来自真实代价，不是谨慎起见。

1. **审查产出的是证据，不是结论。** 每条发现必须带"哪个文件哪一行、跑了什么命令、看到什么输出"。
   - 代价来源：七路审计中**三路把严重度报高了**，全部被汇总层改判——不是能力问题，是单路看不到下游还有闸门在拦。**严重度只能在全链路上评定。**
2. **动手前必须有人复核。**
   - 代价来源：2026-08-05 复核十条结论，**两条被证伪**（一次"破案"破错了人，一个"全系统没有任何一处"被反例推翻）、三条要改表述。**没有复核就照着做，会去修不存在的病。**
3. **分片必须按"失败方式"拆，不按代码模块拆。**
   - 代价来源：按模块拆会让多路重复读同一个巨型文件，读量爆炸而信息量低；按"这个系统会以哪几种方式害我做错判断"拆，每一路的边界天然清晰。
   - 附带纪律：每一路必须**如实交代自己看不到什么**。七路审计有一站掉在缝里，全靠两路各自老实说明边界，才被接上。

---

## 六、还没解决的

- **"本该给什么"需要人一次性声明。** 机械检查能查"许可了没给"，查不了"从没人说过该给"。每一站的"必备核心材料"需要人写一次，之后才能永久机械化。这是判断题，不是技术题。
- **通读的成本。** 25 站、单站二十几万字符，不能整包交给 AI。需要先做投影（实测：留住关键信息只需 2.6%-5.9% 的体积）。
- **这份清单不封闭。** 五种是按"完整语料 × 统一提问 × 不等症状"这个配方推出来的，配方还能推出更多。发现新的，就往这里加。

## 七、常见误判病例档案（2026-08-17 从总纲移入）

通则（留在总纲）：标 unavailable / 太年轻 / blocked 的，先查是不是设计如此，别当 bug 修。以下是带日期的具体病例：

- 打分器返回"判断太年轻、无法评分" → 诚实跳过，首批成熟样本在 2026-07-27 之后。
- CFTC 等数据源标 unavailable → 本机网络限制下的诚实降级，不得用别的源冒充补齐。
- 报告某节留空 → 查 `llm_stage_diagnostics.json` 分清是闸门挡劣质材料，还是模型被规则卡死（连续同因失败即后者）。
- 旧快照复核仍判 blocked → 正确行为，修兼容不等于放松闸门。
