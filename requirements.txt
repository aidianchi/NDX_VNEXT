# ndx_vnext bootstrap dependencies
# Phase 1 target: data collection + vNext main cognitive chain + legacy report bridge

pandas>=2.0.0,<3.0.0
numpy>=1.24.0
scipy>=1.11.0
requests>=2.31.0
python-dotenv>=1.0.0
cachetools>=5.3.0

yfinance>=0.2.28
ta>=0.11.0
pandas-ta>=0.3.14b
pandas-datareader>=0.10.0

# Nasdaq-100 historical constituents (GitHub-only package)
nasdaq-100-ticker-history @ git+https://github.com/jmccarrell/n100tickers.git

# Optional unified financial data platform used for provider coverage research
# and candidate replacement probes. These are not wired into the production
# L1-L5 data path by default.
openbb[all]==4.7.1
openbb-polygon==1.5.1

openai>=1.0.0
google-genai>=1.0.0

plotly>=5.18.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
xlrd>=2.0.1

pytest>=8.0.0
