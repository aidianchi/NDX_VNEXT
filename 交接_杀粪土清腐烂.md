# 交接：杀粪土小战役（T77）＋ 后续清腐烂跑道

> 这份文件给新对话的 AI 读。老板（项目所有者，没有软件工程背景）在旧对话里完成了"家底清单第一幕"并拍了板，你把剩下的活干完。**沟通纪律（2026-09-17 老板立"人话三问"，最高纪律）**：①每句话主语完整、是完整的句子，不许缩句缩到缺主语；②前因后果走完整长链，不许只给逻辑碎片；③每个术语第一次出现当场配半句人话。防止矫枉过正：反"缩"不反"简"（简是去掉杂质，缩是砍掉骨头），结论先行，论证走长链。每个机制讲到他能自己发现错误的程度；他给选项你执行，别替他拍板。

## 一、这是什么项目

`ndx_vnext`（本目录）：一条 25 站的多 agent 流水线，给纳指（NDX）做投资判断——五个分析员各看一层数据（L1 利率宏观/L2 信用波动/L3 资金行为/L4 盈利估值/L5 结构趋势）→ 桥接找跨层冲突 → 正反辩论 → 终审写判断书；另有新闻事件层与第三层综合裁决。先读 `AGENTS.md`（常驻边界与沟通纪律，绝对不可违反）。

## 二、旧对话留下的资产（别弄丢）

1. **`家底清单_第一批_主链.md`**（本目录）：76 张人话卡，每张写清"它哪来的、想干什么、怎么运作、坏在哪、初判建议"。**第 0 章最重要**：0.1 架构骨架图、0.2 宪法序言（草案 v1，老板还没逐句圈改完）、0.3 思想武装对照表、0.4 五条战术原则、0.5 三处真乱、0.6 证据等级口径。做任何事前先读第 0 章。
2. **老板 09-17 已盖完 6 张争议卡**（清单里各卡的"老板盖章"行已填）：C-31 杀｜C-35 改｜C-39 保持降级｜C-26 改｜C-10 留｜B-补 fact_card 杀·缓执行。
3. **哲学档案**：宪法序言草案 v1（清单 0.2）。老板的核心创见（09-16）："语境使数据活"——事件层给数据层四样（命名语境/机制假设/类比坐标/可证伪的钩子），数据层给事件层四样（筛选对账/定量锚/时点纪律/出题方向）；出题官该吃两路题（市场主线＋数据残局）。**三个开放问题仍等老板**：①Marks 第二层思维要不要进序言；②价值与趋势有无主从（政策书"价值买入、趋势卖出"暗示主从）；③宣言体语气可否＋序言是否升格进 AGENTS.md。
4. 工作日志：`.workbuddy/memory/2026-09-16.md`、`2026-09-17.md`（全过程裁决记录）。

## 三、本次任务：杀粪土小战役（立案号 T77，编号永不复用）

**只动三处代码**（其余三张已盖章的卡不动代码：C-39 保持降级、C-10 归类留、fact_card 缓执行）：

1. **C-31 杀**：`src/agent_analysis/orchestrator.py` 的 `_validate_investigation_limits_against_materials`（约 :2864）。它是语义代理类闸门——用正则读 AI 自由文本里的"否认数字"措辞判自相矛盾，会误伤合法表述。从校验链摘除，留退役注释（指向家底清单 C-31 卡＋老板 2026-09-17 盖章）。备选方案"收窄为 limits 点名的数字须在材料里"留档在卡，本批不做。
2. **C-35③ 降级**：`src/agent_analysis/orchestrator.py` 的 `_validate_reasoned_verdict_refs`（约 :6977）。它的第三项"数字逐字锚定"（判决正文里的百分数/小数必须逐字出现在输入 payload 里）把相减/比率/分位差三类合法派生数字全拦。把这一项从"报错触发重试"**降级为"留痕不拦"**——照 `src/agent_analysis/contracts.py:432` 的既有模式（logger.warning＋落盘侧记，不打回）。①②项（引用格式、引用在册）原样保留。
3. **C-26 收窄**：`src/agent_analysis/persistent_checks_b.py` 的 `_check_pc26`（约 :1147）。删除"reason 字段必须逐字含'诊断'或'辅助'二字"的子项，改为"reason 非空且说明用途"；`usage=="supporting_only"` 的枚举判定原样保留。

**施工纪律（每条都不可省）：**

- 每处改动**先写红灯测试**（证明：旧行为下合法派生数字/合规写法必被拦；新行为下不拦、编造仍被①②或留痕接住），看红再改，改绿。测试放 `tests/` 下对应文件。
- 全量测试必须绿：`cd /Users/aidianchi/Desktop/ndx_mac && PYTHONPATH=$PWD .venv/bin/python -m pytest -q -p no:cacheprovider`（基线 1576 绿）。**`PYTHONPATH=$PWD` 必须带**，否则 WorkBuddy 沙箱会把文件操作假失败；搜索用 Grep 工具，别用 shell grep（会返回假空）。
- 每处改动在代码注释里留"出生证"：指向家底清单卡号＋盖章日期。
- 外科手术式收敛：不顺手重构、不动无关代码、不改提示词。
- 收工同一批次内：①`WORK_LOG.md` 写【T77】决策记录（改了什么、为什么、红灯测试在哪）；②家底清单 C-31/C-35/C-26 三卡标注"已执行 2026-XX-XX"；③若 `系统说明书.md` 有对应描述（搜 fact_card、终审校验相关段），同批更新；④`pytest tests/test_docs_consistency.py` 全绿；⑤`现在.md` 若加了 T77 行，收工时删行并在 WORK_LOG 写【关闭 T77】。
- **fact_card（B-补卡）本批绝对不杀**——它与 C-35 的"可追溯+可重算"改造同批，另立工单；单杀会在"终审数字忠实"上留新洞。

## 四、杀完之后：清腐烂跑道（每件都要老板点头再动）

1. **出生证制度**：机制注册表＋一条"没户口不许出生"的文档测试。先给老板看一页纸设计稿，他批了再施工。
2. **orchestrator 按"一卡一模块"分片**：9,954 行的总编排器是最大形态病灶。注意《现在.md》"明确不做"里有老板旧裁决"单独拆分巨型代码文件：以后碰到相关区域就顺手拆"——**动手前必须请老板明着翻这条案**。分片用冻结输入做新旧 A/B 对照，每片老板验收再继续。
3. **事件层补哲学承诺**（语境使数据活：命名 regime、类比坐标）：功能建设，后排；注意"历史上像今天"引擎（analog_history_audit.py）目前是老板钦定冻结状态，解冻要他单独拍板。

## 五、老板手上并行的老口子（别搅进去，但要知道）

T72 人话主战役 paused 等他裁（立不立"标题取材改造"工单；**验收口径他 09-17 已拍板**：若立，验收 = 人复述 + 人话三问自检两道工序）；T70 等他盲读三段报告；T67 剩四件验收。杀粪土与它们不冲突——T72 正 paused，此刻动闸门不打扰任何实验；且 C-35③ 降级后，T72 的判决正文反而松绑。

## 六、验收口令

T77 完工 = 三处代码改完＋红灯测试在＋全量 1576+ 绿＋WORK_LOG/清单/说明书同批更新＋docs 测试绿。给老板的人话报告 ≤5 行：过没过／干成了什么／遗留什么／下一步。
